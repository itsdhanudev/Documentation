import ApiDoc from "@site/src/components/ApiDoc";

Complete API reference for `PerlinNoise`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Algorithms

**Component:** no

---

```java
public class PerlinNoise
```

<ApiDoc
  constructors={[
    { signature: "PerlinNoise()", description: <>Constructs a new instance of <code>PerlinNoise</code>.</> },
    {
      signature: "PerlinNoise(float)",
      description: <>Constructs a new instance of <code>PerlinNoise</code> with the specified <code>scale</code>.</>
    }
  ]}
  attributes={[
    {
      name: "scale",
      type: "float",
      description: <>Represents the <code>scale</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "scale",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScale</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "noise(double, double)",
      returns: "double",
      description: <>Executes <code>noise</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "noise(double, double, double)",
      returns: "double",
      description: <>Executes <code>noise</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "noise(double, double, double, double)",
      returns: "double",
      description: <>Executes <code>noise</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "noise(int, int)",
      returns: "float",
      description: <>Executes <code>noise</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "noise(int, int, int)",
      returns: "float",
      description: <>Executes <code>noise</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "noise(int, int, int, int)",
      returns: "float",
      description: <>Executes <code>noise</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "noise(float, float)",
      returns: "float",
      description: <>Executes <code>noise</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "noise(float, float, float)",
      returns: "float",
      description: <>Executes <code>noise</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "noise(float, float, float, float)",
      returns: "float",
      description: <>Executes <code>noise</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
