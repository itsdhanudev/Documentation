import ApiDoc from '@site/src/components/ApiDoc';

# AOnClickListener

Complete API reference for `AOnClickListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnClickListener
```

<ApiDoc
  methods={[
    {
      signature: "onClick(View)",
      returns: "void",
      description: <>Performs the onClick operation using the provided parameters (v).</>
    },
    {
      signature: "onClickEvent(View)",
      returns: "void",
      description: <>Executes <code>onClickEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
