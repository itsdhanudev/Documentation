import ApiDoc from '@site/src/components/ApiDoc';

# AOnCreateContextMenuListener

Complete API reference for `AOnCreateContextMenuListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnCreateContextMenuListener
```

<ApiDoc
  methods={[
    {
      signature: "onCreateContextMenu(ContextMenu, View, ContextMenu.ContextMenuInfo)",
      returns: "void",
      description: <>Performs the onCreateContextMenu operation using the provided parameters (menu, v, menuInfo).</>
    },
    {
      signature: "onCreateContextMenuEvent(ContextMenu, View, ContextMenu.ContextMenuInfo)",
      returns: "void",
      description: <>Executes <code>onCreateContextMenuEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
