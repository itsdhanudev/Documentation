import ApiDoc from '@site/src/components/ApiDoc';

# AOnDragListener

Complete API reference for `AOnDragListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnDragListener
```

<ApiDoc
  methods={[
    {
      signature: "onDrag(View, DragEvent)",
      returns: "boolean",
      description: <>Performs the onDrag operation using the provided parameters (v, event).</>
    },
    {
      signature: "onDragEvent(View, DragEvent)",
      returns: "boolean",
      description: <>Executes <code>onDragEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
