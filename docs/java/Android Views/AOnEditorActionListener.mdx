import ApiDoc from '@site/src/components/ApiDoc';

# AOnEditorActionListener

Complete API reference for `AOnEditorActionListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnEditorActionListener
```

<ApiDoc
  methods={[
    {
      signature: "onEditorAction(TextView, int, KeyEvent)",
      returns: "boolean",
      description: <>Performs the onEditorAction operation using the provided parameters (v, actionId, event).</>
    },
    {
      signature: "onEditorActionEvent(TextView, int, KeyEvent)",
      returns: "boolean",
      description: <>Executes <code>onEditorActionEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
