import ApiDoc from '@site/src/components/ApiDoc';

# AOnFocusChangeListener

Complete API reference for `AOnFocusChangeListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnFocusChangeListener
```

<ApiDoc
  methods={[
    {
      signature: "onFocusChange(View, boolean)",
      returns: "void",
      description: <>Performs the onFocusChange operation using the provided parameters (v, hasFocus).</>
    },
    {
      signature: "onFocusChangeEvent(View, boolean)",
      returns: "void",
      description: <>Executes <code>onFocusChangeEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
