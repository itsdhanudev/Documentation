import ApiDoc from '@site/src/components/ApiDoc';

# AOnGenericMotionListener

Complete API reference for `AOnGenericMotionListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnGenericMotionListener
```

<ApiDoc
  methods={[
    {
      signature: "onGenericMotion(View, MotionEvent)",
      returns: "boolean",
      description: <>Performs the onGenericMotion operation using the provided parameters (v, event).</>
    },
    {
      signature: "onGenericMotionEvent(View, MotionEvent)",
      returns: "boolean",
      description: <>Executes <code>onGenericMotionEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
