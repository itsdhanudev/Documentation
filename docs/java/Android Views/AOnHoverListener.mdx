import ApiDoc from '@site/src/components/ApiDoc';

# AOnHoverListener

Complete API reference for `AOnHoverListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnHoverListener
```

<ApiDoc
  methods={[
    {
      signature: "onHover(View, MotionEvent)",
      returns: "boolean",
      description: <>Performs the onHover operation using the provided parameters (v, event).</>
    },
    {
      signature: "onHoverEvent(View, MotionEvent)",
      returns: "boolean",
      description: <>Executes <code>onHoverEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
