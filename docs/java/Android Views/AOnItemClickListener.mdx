import ApiDoc from '@site/src/components/ApiDoc';

# AOnItemClickListener

Complete API reference for `AOnItemClickListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnItemClickListener
```

<ApiDoc
  methods={[
    {
      signature: "onItemClick(AdapterView&lt;?&gt;, View, int, long)",
      returns: "void",
      description: <>Performs the onItemClick operation using the provided parameters (parent, view, position, id).</>
    },
    {
      signature: "onItemClickEvent(AdapterView&lt;?&gt;, View, int, long)",
      returns: "void",
      description: <>Executes <code>onItemClickEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
