import ApiDoc from '@site/src/components/ApiDoc';

# AOnItemLongClickListener

Complete API reference for `AOnItemLongClickListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnItemLongClickListener
```

<ApiDoc
  methods={[
    {
      signature: "onItemLongClick(AdapterView&lt;?&gt;, View, int, long)",
      returns: "boolean",
      description: <>Performs the onItemLongClick operation using the provided parameters (parent, view, position, id).</>
    },
    {
      signature: "onItemLongClickEvent(AdapterView&lt;?&gt;, View, int, long)",
      returns: "boolean",
      description: <>Executes <code>onItemLongClickEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
