import ApiDoc from '@site/src/components/ApiDoc';

# AOnItemSelectedListener

Complete API reference for `AOnItemSelectedListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnItemSelectedListener
```

<ApiDoc
  methods={[
    {
      signature: "onItemSelected(AdapterView&lt;?&gt;, View, int, long)",
      returns: "void",
      description: <>Performs the onItemSelected operation using the provided parameters (parent, view, position, id).</>
    },
    {
      signature: "onNothingSelected(AdapterView&lt;?&gt;)",
      returns: "void",
      description: <>Performs the onNothingSelected operation using the provided parameters (parent).</>
    },
    {
      signature: "onItemSelectedEvent(AdapterView&lt;?&gt;, View, int, long)",
      returns: "boolean",
      description: <>Executes <code>onItemSelectedEvent</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onNothingSelectedEvent(AdapterView&lt;?&gt;)",
      returns: "boolean",
      description: <>Executes <code>onNothingSelectedEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
