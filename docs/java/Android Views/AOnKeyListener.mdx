import ApiDoc from '@site/src/components/ApiDoc';

# AOnKeyListener

Complete API reference for `AOnKeyListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnKeyListener
```

<ApiDoc
  methods={[
    {
      signature: "onKey(View, int, KeyEvent)",
      returns: "boolean",
      description: <>Performs the onKey operation using the provided parameters (v, keyCode, event).</>
    },
    {
      signature: "onKeyEvent(View, int, KeyEvent)",
      returns: "boolean",
      description: <>Executes <code>onKeyEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
