import ApiDoc from '@site/src/components/ApiDoc';

# AOnLongClickListener

Complete API reference for `AOnLongClickListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnLongClickListener
```

<ApiDoc
  methods={[
    {
      signature: "onLongClick(View)",
      returns: "boolean",
      description: <>Performs the onLongClick operation using the provided parameters (v).</>
    },
    {
      signature: "onLongClickEvent(View)",
      returns: "boolean",
      description: <>Executes <code>onLongClickEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
