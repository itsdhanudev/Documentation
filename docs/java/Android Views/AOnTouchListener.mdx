import ApiDoc from '@site/src/components/ApiDoc';

# AOnTouchListener

Complete API reference for `AOnTouchListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public class AOnTouchListener
```

<ApiDoc
  methods={[
    {
      signature: "onTouch(View, MotionEvent)",
      returns: "boolean",
      description: <>Performs the onTouch operation using the provided parameters (v, event).</>
    },
    {
      signature: "onTouchEvent(View, MotionEvent)",
      returns: "boolean",
      description: <>Executes <code>onTouchEvent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
