import ApiDoc from '@site/src/components/ApiDoc';

# AViewInstance

Complete API reference for `AViewInstance`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Android Views

**Component:** no

---

```java
public interface AViewInstance
```

<ApiDoc
  methods={[
    {
      signature: "getViewID()",
      returns: "String",
      description: <>Executes <code>getViewID</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "findByID(String)",
      returns: "<T extends View> T",
      description: <>Executes <code>findByID</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
