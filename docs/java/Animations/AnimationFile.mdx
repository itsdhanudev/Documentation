# AnimationFile

Complete API reference for `AnimationFile`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Animations

**Component:** no

---

```java
public class AnimationFile
```

## Constructors (2)

| Signature | Description |
| --- | --- |
| AnimationFile([String]) | Executes `AnimationFile` and returns after the underlying engine call finishes. |
| AnimationFile() | Executes `AnimationFile` and returns after the underlying engine call finishes. |

## Methods (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `toString()` | `String` | Returns the name of the class. |

## Static Methods (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `genericInterface()` | `GenericClassInspectorEntry` | Executes `genericInterface` and returns after the underlying engine call finishes. |
| `inspectorController()` | `ClassInspectorEntry` | Executes `inspectorController` and returns after the underlying engine call finishes. |
