import ApiDoc from '@site/src/components/ApiDoc';

# AnimationFile

Complete API reference for `AnimationFile`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Animations

**Component:** no

---

```java
public class AnimationFile
```

<ApiDoc
  constructors={[
    {
      signature: "AnimationFile(String)",
      description: <>Executes <code>AnimationFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "AnimationFile()",
      description: <>Executes <code>AnimationFile</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  methods={[
    {
      signature: "toString()",
      returns: "String",
      description: <>Returns the name of the class.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executes <code>genericInterface</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
