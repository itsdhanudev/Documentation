import ApiDoc from '@site/src/components/ApiDoc';

# AnimationTransition

Complete API reference for `AnimationTransition`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Animations

**Component:** no

---

```java
public class AnimationTransition
```

<ApiDoc
  constructors={[
    {
      signature: "AnimationTransition(AnimationTransition)",
      description: <>Executes <code>AnimationTransition</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "AnimationTransition",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "isFinished()",
      returns: "boolean",
      description: <>Returns whether the AnimationTransition has finished.</>
    }
  ]}
/>
