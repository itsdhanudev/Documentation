import ApiDoc from '@site/src/components/ApiDoc';

# AsyncRunnable

Complete API reference for `AsyncRunnable`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Async

**Component:** no

---

```java
public interface AsyncRunnable
```

<ApiDoc
  methods={[
    {
      signature: "onBackground(Object)",
      returns: "Object",
      description: <>Executes <code>onBackground</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onEngine(Object)",
      returns: "void",
      description: <>Executes <code>onEngine</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
