import ApiDoc from '@site/src/components/ApiDoc';

# AtomicBoolean

Complete API reference for `AtomicBoolean`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Atomic

**Component:** no

---

```java
public class AtomicBoolean
```

<ApiDoc
  constructors={[
    {
      signature: "AtomicBoolean(AtomicBoolean)",
      description: <>Executes <code>AtomicBoolean</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "AtomicBoolean()",
      description: <>Executes <code>AtomicBoolean</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "AtomicBoolean(boolean)",
      description: <>Creates a new instance of AtomicBoolean.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "AtomicBoolean",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "get()",
      returns: "boolean",
      description: <>Returns the current value.</>
    },
    {
      signature: "set(boolean)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "lazySet(boolean)",
      returns: "void",
      description: <>Performs the lazySet operation using the provided parameters (value).</>
    },
    {
      signature: "getAndSet(boolean)",
      returns: "boolean",
      description: <>Returns the current and set.</>
    },
    {
      signature: "compareAndSet(boolean, boolean)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "weakCompareAndSet(boolean, boolean)",
      returns: "boolean",
      description: <>Performs the weakCompareAndSet operation using the provided parameters (expect, update).</>
    }
  ]}
/>
