# Brush

Complete API reference for `Brush`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Brush

**Component:** no

---

```java
public interface Brush
```

## Methods (2)

| Signature                      | Returns | Description |
| -------------------------------- | --- | --- |
| `@MethodArgs([\{"x"], ["y"\}])` | `void` | Performs the @MethodArgs operation. |
| `getIntensity(float, float)` | `float` | Executes `getIntensity` and returns after the underlying engine call finishes. |
