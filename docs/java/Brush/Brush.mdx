import ApiDoc from '@site/src/components/ApiDoc';

# Brush

Complete API reference for `Brush`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Brush

**Component:** no

---

```java
public interface Brush
```

<ApiDoc
  methods={[
    {
      signature: "getIntensity(float, float)",
      returns: "float",
      description: <>Executes <code>getIntensity</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
