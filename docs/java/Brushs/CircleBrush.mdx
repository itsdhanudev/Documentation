import ApiDoc from '@site/src/components/ApiDoc';

# CircleBrush

Complete API reference for `CircleBrush`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Brushs

**Component:** no

---

```java
public class CircleBrush
```

<ApiDoc
  methods={[
    {
      signature: "getIntensity(float, float)",
      returns: "float",
      description: <>Returns the current intensity.</>
    }
  ]}
/>
