import ApiDoc from '@site/src/components/ApiDoc';

# NativeByteBuffer

Complete API reference for `NativeByteBuffer`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Buffers

**Component:** no

---

```java
public class NativeByteBuffer
```

<ApiDoc
  constructors={[
    {
      signature: "NativeByteBuffer(NativeByteBuffer)",
      description: <>Executes <code>NativeByteBuffer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NativeByteBuffer(Engine)",
      description: <>Executes <code>NativeByteBuffer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NativeByteBuffer(int)",
      description: <>Creates a new instance of NativeByteBuffer.</>
    }
  ]}
  attributes={[
    {
      name: "buffer",
      type: "NativeByteBuffer",
      description: <>Represents the <code>buffer</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "get(int)",
      returns: "byte",
      description: <>Returns the current value.</>
    },
    {
      signature: "set(int, byte)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "put(byte)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "capacity()",
      returns: "int",
      description: <>Executes <code>capacity</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getPosition()",
      returns: "int",
      description: <>Returns the current position.</>
    },
    {
      signature: "Position(int)",
      returns: "void",
      description: <>Performs the Position operation using the provided parameters (idx).</>
    },
    {
      signature: "setPosition(int)",
      returns: "void",
      description: <>Sets or updates the position.</>
    }
  ]}
/>
