import ApiDoc from '@site/src/components/ApiDoc';

# NativeCharBuffer

Complete API reference for `NativeCharBuffer`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Buffers

**Component:** no

---

```java
public class NativeCharBuffer
```

<ApiDoc
  constructors={[
    {
      signature: "NativeCharBuffer(NativeCharBuffer)",
      description: <>Executes <code>NativeCharBuffer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NativeCharBuffer(Engine)",
      description: <>Executes <code>NativeCharBuffer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NativeCharBuffer(int)",
      description: <>Creates a new instance of NativeCharBuffer.</>
    }
  ]}
  attributes={[
    {
      name: "buffer",
      type: "NativeCharBuffer",
      description: <>Represents the <code>buffer</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "get(int)",
      returns: "char",
      description: <>Returns the current value.</>
    },
    {
      signature: "set(int, char)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "put(char)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "capacity()",
      returns: "int",
      description: <>Executes <code>capacity</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getPosition()",
      returns: "int",
      description: <>Returns the current position.</>
    },
    {
      signature: "Position(int)",
      returns: "void",
      description: <>Performs the Position operation using the provided parameters (idx).</>
    },
    {
      signature: "setPosition(int)",
      returns: "void",
      description: <>Sets or updates the position.</>
    }
  ]}
/>
