import ApiDoc from '@site/src/components/ApiDoc';

# NativeIntBuffer

Complete API reference for `NativeIntBuffer`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Buffers

**Component:** no

---

```java
public class NativeIntBuffer
```

<ApiDoc
  constructors={[
    {
      signature: "NativeIntBuffer(NativeIntBuffer)",
      description: <>Executes <code>NativeIntBuffer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NativeIntBuffer(Engine)",
      description: <>Executes <code>NativeIntBuffer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NativeIntBuffer(int)",
      description: <>Creates a new instance of NativeIntBuffer.</>
    }
  ]}
  attributes={[
    {
      name: "buffer",
      type: "NativeIntBuffer",
      description: <>Represents the <code>buffer</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "get(int)",
      returns: "int",
      description: <>Returns the current value.</>
    },
    {
      signature: "set(int, int)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "put(int)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "capacity()",
      returns: "int",
      description: <>Executes <code>capacity</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getPosition()",
      returns: "int",
      description: <>Returns the current position.</>
    },
    {
      signature: "Position(int)",
      returns: "void",
      description: <>Performs the Position operation using the provided parameters (idx).</>
    },
    {
      signature: "setPosition(int)",
      returns: "void",
      description: <>Sets or updates the position.</>
    }
  ]}
/>
