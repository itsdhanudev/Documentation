import ApiDoc from '@site/src/components/ApiDoc';

# NativeShortBuffer

Complete API reference for `NativeShortBuffer`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Buffers

**Component:** no

---

```java
public class NativeShortBuffer
```

<ApiDoc
  constructors={[
    {
      signature: "NativeShortBuffer(NativeShortBuffer)",
      description: <>Executes <code>NativeShortBuffer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NativeShortBuffer(Engine)",
      description: <>Executes <code>NativeShortBuffer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NativeShortBuffer(int)",
      description: <>Creates a new instance of NativeShortBuffer.</>
    }
  ]}
  attributes={[
    {
      name: "buffer",
      type: "NativeShortBuffer",
      description: <>Represents the <code>buffer</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "vboEnabled",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isVboEnabled</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "get(int)",
      returns: "short",
      description: <>Returns the current value.</>
    },
    {
      signature: "set(int, short)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "put(short)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "capacity()",
      returns: "int",
      description: <>Executes <code>capacity</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getPosition()",
      returns: "int",
      description: <>Returns the current position.</>
    },
    {
      signature: "position(int)",
      returns: "int",
      description: <>Performs the position operation using the provided parameters (idx).</>
    },
    {
      signature: "setPosition(int)",
      returns: "void",
      description: <>Sets or updates the position.</>
    }
  ]}
/>
