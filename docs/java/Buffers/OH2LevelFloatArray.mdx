import ApiDoc from '@site/src/components/ApiDoc';

# OH2LevelFloatArray

Complete API reference for `OH2LevelFloatArray`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Buffers

**Component:** no

---

```java
public class OH2LevelFloatArray extends OHFloatDataBase
```

<ApiDoc
  constructors={[
    {
      signature: "OH2LevelFloatArray(Engine)",
      description: <>Creates a new instance of OH2LevelFloatArray.</>
    },
    {
      signature: "OH2LevelFloatArray(int, int)",
      description: <>Creates a new instance of OH2LevelFloatArray.</>
    },
    {
      signature: "OH2LevelFloatArray(int, int, boolean)",
      description: <>Creates a new instance of OH2LevelFloatArray.</>
    }
  ]}
  methods={[
    {
      signature: "get(int, int)",
      returns: "float",
      description: <>Returns the current value.</>
    },
    {
      signature: "set(int, int, float)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    }
  ]}
/>
