import ApiDoc from '@site/src/components/ApiDoc';

# OH3LevelFloatArray

Complete API reference for `OH3LevelFloatArray`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Buffers

**Component:** no

---

```java
public class OH3LevelFloatArray extends OHFloatDataBase
```

<ApiDoc
  constructors={[
    {
      signature: "OH3LevelFloatArray(OH3LevelFloatArray)",
      description: <>Executes <code>OH3LevelFloatArray</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "OH3LevelFloatArray(Engine)",
      description: <>Executes <code>OH3LevelFloatArray</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "OH3LevelFloatArray(int, int, int)",
      description: <>Creates a new instance of OH3LevelFloatArray.</>
    },
    {
      signature: "OH3LevelFloatArray(int, int, int, boolean)",
      description: <>Creates a new instance of OH3LevelFloatArray.</>
    }
  ]}
  attributes={[
    {
      name: "buffer",
      type: "OH3LevelFloatArray",
      description: <>Represents the <code>buffer</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "get(int, int, int)",
      returns: "float",
      description: <>Returns the current value.</>
    },
    {
      signature: "set(int, int, int, float)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "getSizeX()",
      returns: "int",
      description: <>Returns the current size x.</>
    },
    {
      signature: "getSizeY()",
      returns: "int",
      description: <>Returns the current size y.</>
    },
    {
      signature: "getSizeZ()",
      returns: "int",
      description: <>Returns the current size z.</>
    }
  ]}
/>
