import ApiDoc from '@site/src/components/ApiDoc';

# EnemyAITypeA

Complete API reference for `EnemyAITypeA`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** AI

**Component:** yes

---

```java
public class EnemyAITypeA extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "EnemyAITypeA(EnemyAITypeA)",
      description: <>Executes <code>EnemyAITypeA</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "EnemyAITypeA(Engine)",
      description: <>Executes <code>EnemyAITypeA</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "EnemyAITypeA()",
      description: <>Creates a new instance of EnemyAITypeA.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "EnemyAITypeA",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "attackDelay",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAttackDelay</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "fieldOfView",
      type: "TagEnemyFieldOfView",
      access: "get",
      description: <>Virtual attribute exposed by <code>getFieldOfView</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "points",
      type: "SpatialObject",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPoints</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "rayDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRayDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "rayOffset",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRayOffset</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "runSpeed",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRunSpeed</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "startAttackDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getStartAttackDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "state",
      type: "State",
      access: "get",
      description: <>Virtual attribute exposed by <code>getState</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "stickBottom",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setStickBottom</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "stickBottomOffset",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getStickBottomOffset</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "swapTargetDelay",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSwapTargetDelay</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "swapTargetDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSwapTargetDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "walkSpeed",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getWalkSpeed</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isStickBottom()",
      returns: "boolean",
      description: <>Returns whether stick bottom is true.</>
    }
  ]}
/>
