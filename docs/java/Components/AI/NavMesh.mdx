import ApiDoc from '@site/src/components/ApiDoc';

# NavMesh

Complete API reference for `NavMesh`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** AI

**Component:** yes

---

```java
public class NavMesh extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "NavMesh(BakedNavMesh)",
      description: <>Executes <code>NavMesh</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NavMesh(Engine)",
      description: <>Executes <code>NavMesh</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NavMesh()",
      description: <>Creates a new instance of NavMesh.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "BakedNavMesh",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "agentHeight",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAgentHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "agentMaxClimb",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAgentMaxClimb</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "agentMaxSlope",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAgentMaxSlope</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "agentWidth",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAgentWidth</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "cellHeight",
      type: "float",
      access: "get",
      description: <>Virtual attribute exposed by <code>getCellHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "cellSize",
      type: "float",
      access: "get",
      description: <>Virtual attribute exposed by <code>getCellSize</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "detailSampleDist",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getDetailSampleDist</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "detailSampleMaxError",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getDetailSampleMaxError</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "edgeMaxError",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getEdgeMaxError</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "edgeMaxLen",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getEdgeMaxLen</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxPathFails",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxPathFails</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxTiles",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxTiles</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "regionMergeArea",
      type: "float",
      access: "get",
      description: <>Virtual attribute exposed by <code>getRegionMergeArea</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "regionMergeSize",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRegionMergeSize</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "regionMinArea",
      type: "float",
      access: "get",
      description: <>Virtual attribute exposed by <code>getRegionMinArea</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "regionMinSize",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRegionMinSize</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "tileSize",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTileSize</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "generateBake()",
      returns: "void",
      description: <>Executes <code>generateBake</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
