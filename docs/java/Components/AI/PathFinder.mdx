import ApiDoc from '@site/src/components/ApiDoc';

# PathFinder

Complete API reference for `PathFinder`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** AI

**Component:** yes

---

```java
public class PathFinder extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "PathFinder(PathFinder)",
      description: <>Executes <code>PathFinder</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "PathFinder()",
      description: <>Executes <code>PathFinder</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "PathFinder",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "searchDelay",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSearchDelay</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "target",
      type: "SpatialObject",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTarget</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getTargetDistance()",
      returns: "float",
      description: <>Returns the current target distance.</>
    },
    {
      signature: "getTargetDistanceInPath()",
      returns: "float",
      description: <>Returns the current target distance in path.</>
    },
    {
      signature: "hasPath()",
      returns: "boolean",
      description: <>Returns whether path is true.</>
    },
    {
      signature: "hasTarget()",
      returns: "boolean",
      description: <>Returns whether target is true.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
