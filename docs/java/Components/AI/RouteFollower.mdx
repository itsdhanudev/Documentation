import ApiDoc from '@site/src/components/ApiDoc';

# RouteFollower

Complete API reference for `RouteFollower`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** AI

**Component:** yes

---

```java
public class RouteFollower extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "RouteFollower(RouteFollower)",
      description: <>Executes <code>RouteFollower</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "RouteFollower()",
      description: <>Executes <code>RouteFollower</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "RouteFollower",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "findNearestPoint",
      type: "Vector3",
      access: "get",
      description: <>Virtual attribute exposed by <code>getFindNearestPoint</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "findNearestPointIndex",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getFindNearestPointIndex</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "route",
      type: "Route",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRoute</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "findNearestPoint()",
      returns: "Vector3",
      description: <>Executes <code>findNearestPoint</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getNearestPoint()",
      returns: "Vector3",
      description: <>Returns the current nearest point.</>
    },
    {
      signature: "findNearestPointIndex()",
      returns: "int",
      description: <>Executes <code>findNearestPointIndex</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getNearestPointIndex()",
      returns: "int",
      description: <>Returns the current nearest point index.</>
    },
    {
      signature: "hasPath()",
      returns: "boolean",
      description: <>Returns whether path is true.</>
    },
    {
      signature: "isPathFinished()",
      returns: "boolean",
      description: <>Returns whether path finished is true.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
