import ApiDoc from '@site/src/components/ApiDoc';

# AnimationPlayer

Complete API reference for `AnimationPlayer`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Animations

**Component:** yes

---

```java
public class AnimationPlayer extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "AnimationPlayer(AnimationPlayer)",
      description: <>Executes <code>AnimationPlayer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "AnimationPlayer()",
      description: <>Creates a new instance of AnimationPlayer.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "AnimationPlayer",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "getAnimation(String)",
      returns: "Animation",
      description: <>Returns the animation with the specified name.</>
    },
    {
      signature: "getAnimation(int)",
      returns: "Animation",
      description: <>Returns the animation at the specified index.</>
    },
    {
      signature: "setAnimation(int, Animation)",
      returns: "void",
      description: <>Sets the animation at the specified index to the provided animation.</>
    },
    {
      signature: "getAnimationList()",
      returns: "List&lt;Animation&gt;",
      description: <>Returns a list of all animations in the AnimationPlayer.</>
    },
    {
      signature: "addAnimation(Animation)",
      returns: "void",
      description: <>Adds the specified Animation to the AnimationPlayer.</>
    },
    {
      signature: "addTransition(Animation, float)",
      returns: "AnimationTransition",
      description: <>Adds an animation transition between the current animation and the specified one (to) with a delay (delay).</>
    },
    {
      signature: "addTransition(Animation, float, Curve)",
      returns: "AnimationTransition",
      description: <>Adds an animation transition between the current animation and the specified one (to) with a delay (delay) and an interpolation curve (curve).</>
    },
    {
      signature: "addTransition(Animation, Animation, float)",
      returns: "AnimationTransition",
      description: <>Adds an animation transition between two animations (from and to) with a specified delay (delay).</>
    },
    {
      signature: "addTransition(Animation, Animation, float, Curve)",
      returns: "AnimationTransition",
      description: <>Adds an animation transition between two animations (from and to) with a specified delay (delay) and an interpolation curve (curve).</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
