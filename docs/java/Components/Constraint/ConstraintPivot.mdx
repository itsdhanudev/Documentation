import ApiDoc from '@site/src/components/ApiDoc';

# ConstraintPivot

Complete API reference for `ConstraintPivot`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Constraint

**Component:** yes

---

```java
public class ConstraintPivot extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ConstraintPivot(ConstraintPivot)",
      description: <>Executes <code>ConstraintPivot</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ConstraintPivot(Engine)",
      description: <>Executes <code>ConstraintPivot</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ConstraintPivot()",
      description: <>Creates a new instance of ConstraintPivot.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ConstraintPivot",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
/>
