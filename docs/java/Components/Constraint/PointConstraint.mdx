import ApiDoc from '@site/src/components/ApiDoc';

# PointConstraint

Complete API reference for `PointConstraint`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Constraint

**Component:** yes

---

```java
public class PointConstraint extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "PointConstraint(PointConstraint)",
      description: <>Executes <code>PointConstraint</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "PointConstraint()",
      description: <>Executes <code>PointConstraint</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "PointConstraint",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "pivot",
      type: "SpatialObject",
      access: "get",
      description: <>Virtual attribute exposed by <code>getPivot</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "target",
      type: "SpatialObject",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTarget</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
