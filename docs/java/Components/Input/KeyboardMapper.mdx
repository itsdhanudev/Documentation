import ApiDoc from '@site/src/components/ApiDoc';

# KeyboardMapper

Complete API reference for `KeyboardMapper`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Input

**Component:** yes

---

```java
public class KeyboardMapper extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "KeyboardMapper(KeyboardMapper)",
      description: <>Executes <code>KeyboardMapper</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "KeyboardMapper(Engine)",
      description: <>Executes <code>KeyboardMapper</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "KeyboardMapper()",
      description: <>Creates a new instance of KeyboardMapper.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "KeyboardMapper",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "keyboardKey",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getKeyboardKey</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "outputKey",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOutputKey</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
