import ApiDoc from '@site/src/components/ApiDoc';

# SpotLight

Complete API reference for `SpotLight`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Light

**Component:** yes

---

```java
public class SpotLight extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SpotLight(SpotLight)",
      description: <>Executes <code>SpotLight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SpotLight(Engine)",
      description: <>Executes <code>SpotLight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SpotLight()",
      description: <>Executes <code>SpotLight</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SpotLight",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "allowShadow",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isAllowShadow</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "falloffRadius",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getFalloffRadius</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "innerConeAngle",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getInnerConeAngle</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lumens",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLumens</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "outerConeAngle",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOuterConeAngle</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "shadowBias",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getShadowBias</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "shadowFarDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getShadowFarDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "shadowNormalBias",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getShadowNormalBias</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "shadowResolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getShadowResolutionID</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
