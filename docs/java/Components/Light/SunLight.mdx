import ApiDoc from '@site/src/components/ApiDoc';

# SunLight

Complete API reference for `SunLight`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Light

**Component:** yes

---

```java
public class SunLight extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SunLight(SunLight)",
      description: <>Executes <code>SunLight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SunLight(Engine)",
      description: <>Executes <code>SunLight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SunLight()",
      description: <>Executes <code>SunLight</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SunLight",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "allowShadow",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isAllowShadow</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "cascadeFarHint",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCascadeFarHint</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "cascadeNearHint",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCascadeNearHint</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLux</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "shadowBias",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getShadowBias</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "shadowCascades",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getShadowCascades</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "shadowFarDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getShadowFarDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "shadowNormalBias",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getShadowNormalBias</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "shadowResolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getShadowResolutionID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "stableCascades",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isStableCascades</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
