import ApiDoc from '@site/src/components/ApiDoc';

# ParticleEmitter

Complete API reference for `ParticleEmitter`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Particles

**Component:** yes

---

```java
public class ParticleEmitter extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ParticleEmitter(ParticleEmitter)",
      description: <>Executes <code>ParticleEmitter</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ParticleEmitter(Engine)",
      description: <>Executes <code>ParticleEmitter</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ParticleEmitter()",
      description: <>Creates a new instance of ParticleEmitter.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ParticleEmitter",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "allowEmission",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isAllowEmission</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "alphaBased",
      type: "AlphaBased",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAlphaBased</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "alphaOverLifeTimeCurve",
      type: "Curve",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAlphaOverLifeTimeCurve</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "colorBased",
      type: "ColorBased",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColorBased</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "colorMode",
      type: "ColorMode",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColorMode</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "coneShapeOptions",
      type: "ParticleConeShapeOptions",
      access: "get",
      description: <>Virtual attribute exposed by <code>getConeShapeOptions</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "emissionShape",
      type: "EmissionShape",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getEmissionShape</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "emissionsPerSecond",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getEmissionsPerSecond</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "gravityMultiplier",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGravityMultiplier</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "layer",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLayer</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxLifeSeconds",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxLifeSeconds</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "sizeBased",
      type: "SizeBased",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSizeBased</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "sizeOverLifeTimeCurve",
      type: "Curve",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSizeOverLifeTimeCurve</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "sphereShapeOptions",
      type: "ParticleSphereShapeOptions",
      access: "get",
      description: <>Virtual attribute exposed by <code>getSphereShapeOptions</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "startSize",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getStartSize</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "startSpeed",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getStartSpeed</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "textureInstance",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTextureInstance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "transparencyBased",
      type: "TransparencyBased",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTransparencyBased</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "emit(Vector3, Vector3)",
      returns: "Particle",
      description: <>Performs the emit operation using the provided parameters (position, movement).</>
    }
  ]}
/>
