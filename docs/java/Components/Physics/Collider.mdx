import ApiDoc from '@site/src/components/ApiDoc';

# Collider

Complete API reference for `Collider`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Physics

**Component:** yes

---

```java
public class Collider extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Collider(Collider)",
      description: <>Executes <code>Collider</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Collider()",
      description: <>Creates a new instance of Collider.</>
    },
    {
      signature: "Collider(Shape)",
      description: <>Executes <code>Collider</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "Collider",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "convexVertex",
      type: "Vertex",
      access: "set",
      description: <>Sets the convex vertex of the collider.</>
    },
    {
      name: "vertex",
      type: "Vertex",
      access: "get/set",
      description: <>Returns the collider vertex.</>
    },
    {
      name: "vertexFile",
      type: "VertexFile",
      access: "set",
      description: <>Sets the vertex file for the collider.</>
    }
  ]}
  methods={[
    {
      signature: "getShape()",
      returns: "Shape",
      description: <>Returns the shape type of the collider.</>
    },
    {
      signature: "setShape(Shape)",
      returns: "void",
      description: <>Sets the shape type of the collider.</>
    },
    {
      signature: "colliderWithName(String)",
      returns: "boolean",
      description: <>Checks if this object is colliding with an object with the specified name.</>
    },
    {
      signature: "isColliding()",
      returns: "boolean",
      description: <>Checks if this object is colliding with anything.</>
    },
    {
      signature: "getCollisionList()",
      returns: "List&lt;Collision&gt;",
      description: <>Returns a list of all current collisions.</>
    },
    {
      signature: "getCollisionsCount()",
      returns: "int",
      description: <>Returns the number of current collisions.</>
    },
    {
      signature: "getCollisionAt(int)",
      returns: "Collision",
      description: <>Returns the Collision at the specified index.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
