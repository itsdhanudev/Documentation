import ApiDoc from '@site/src/components/ApiDoc';

# PhysicsComponent

Complete API reference for `PhysicsComponent`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Physics

**Component:** yes

---

```java
public class PhysicsComponent extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "PhysicsComponent(Component)",
      description: <>Executes <code>PhysicsComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "velocity",
      type: "Vector3",
      access: "get",
      description: <>Returns the current velocity of the object.</>
    }
  ]}
  methods={[
    {
      signature: "colliderWithName(String)",
      returns: "boolean",
      description: <>Checks if this object is colliding with an object with the specified name.</>
    },
    {
      signature: "isColliding()",
      returns: "boolean",
      description: <>Checks if this object is colliding with anything.</>
    },
    {
      signature: "getCollisionList()",
      returns: "List&lt;Collision&gt;",
      description: <>Returns a list of all current collisions.</>
    },
    {
      signature: "getCollisionsCount()",
      returns: "int",
      description: <>Returns the number of current collisions.</>
    },
    {
      signature: "getCollisionAt(int)",
      returns: "Collision",
      description: <>Returns the Collision at the specified index.</>
    }
  ]}
/>
