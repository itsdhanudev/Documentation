import ApiDoc from '@site/src/components/ApiDoc';

# VehicleWheel

Complete API reference for `VehicleWheel`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Physics

**Component:** yes

---

```java
public class VehicleWheel extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VehicleWheel(VehicleWheel)",
      description: <>Executes <code>VehicleWheel</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VehicleWheel()",
      description: <>Executes <code>VehicleWheel</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "VehicleWheel",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "blockRotation",
      type: "float",
      access: "set",
      description: <>Virtual attribute exposed by <code>setBlockRotation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "frictionSlip",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getFrictionSlip</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "modelPosition",
      type: "Vector3",
      access: "get",
      description: <>Virtual attribute exposed by <code>getModelPosition</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "modelRotation",
      type: "Quaternion",
      access: "get",
      description: <>Virtual attribute exposed by <code>getModelRotation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "posPhysicsFunctionEnabled",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPosPhysicsFunctionEnabled</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "radius",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRadius</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "rotation",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRotation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "skidding",
      type: "float",
      access: "get",
      description: <>Virtual attribute exposed by <code>getSkidding</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "suspensionRestLength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSuspensionRestLength</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isGrounded()",
      returns: "boolean",
      description: <>Returns whether grounded is true.</>
    },
    {
      signature: "incrementRotation(float)",
      returns: "void",
      description: <>Performs the incrementRotation operation using the provided parameters (r).</>
    },
    {
      signature: "setTorque(float)",
      returns: "void",
      description: <>Sets or updates the torque.</>
    },
    {
      signature: "setBrake(float)",
      returns: "void",
      description: <>Sets or updates the brake.</>
    },
    {
      signature: "setSteer(float)",
      returns: "void",
      description: <>Sets or updates the steer.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
