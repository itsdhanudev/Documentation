import ApiDoc from '@site/src/components/ApiDoc';

# AmbientLight

Complete API reference for `AmbientLight`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class AmbientLight extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "AmbientLight(AmbientLight)",
      description: <>Executes <code>AmbientLight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "AmbientLight(Engine)",
      description: <>Executes <code>AmbientLight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "AmbientLight()",
      description: <>Executes <code>AmbientLight</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "AmbientLight",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "indirectLight",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getIndirectLight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLux</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxResolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxResolutionID</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
