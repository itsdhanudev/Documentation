import ApiDoc from '@site/src/components/ApiDoc';

# AtmosphericScattering

Complete API reference for `AtmosphericScattering`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class AtmosphericScattering extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "AtmosphericScattering(AtmosphericScattering)",
      description: <>Executes <code>AtmosphericScattering</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "AtmosphericScattering(Engine)",
      description: <>Executes <code>AtmosphericScattering</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "AtmosphericScattering()",
      description: <>Executes <code>AtmosphericScattering</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "AtmosphericScattering",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "ambientLight",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setAmbientLight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ambientLightResolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAmbientLightResolutionID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "atmosphereDensity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAtmosphereDensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "atmosphereDensityFalloff",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAtmosphereDensityFalloff</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "atmosphereExtent",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAtmosphereExtent</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "cirrusClouds",
      type: "CloudType",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCirrusClouds</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "cumulusClouds",
      type: "CloudType",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCumulusClouds</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "enableMoonball",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setEnableMoonball</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "enableSunball",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setEnableSunball</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "groundColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGroundColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLux</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxResolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxResolutionID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "rayLeigh",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRayLeigh</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "skyboxSimulationSpeed",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSkyboxSimulationSpeed</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "wetness",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getWetness</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isAmbientLight()",
      returns: "boolean",
      description: <>Executes <code>isAmbientLight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "isEnableMoonball()",
      returns: "boolean",
      description: <>Executes <code>isEnableMoonball</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "isEnableSunball()",
      returns: "boolean",
      description: <>Executes <code>isEnableSunball</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
