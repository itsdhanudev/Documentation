import ApiDoc from '@site/src/components/ApiDoc';

# Bloom

Complete API reference for `Bloom`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class Bloom extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Bloom(Bloom)",
      description: <>Executes <code>Bloom</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Bloom(Engine)",
      description: <>Executes <code>Bloom</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Bloom()",
      description: <>Executes <code>Bloom</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Bloom",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "chromaticAberration",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getChromaticAberration</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ghostCount",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGhostCount</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ghostSpacing",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGhostSpacing</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ghostThreshold",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGhostThreshold</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "haloRadius",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHaloRadius</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "haloThickness",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHaloThickness</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "haloThreshold",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHaloThreshold</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "highlight",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHighlight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lensFlare",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setLensFlare</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "levels",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLevels</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "quality",
      type: "QualityLevel",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQuality</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "resolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getResolutionID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "starburst",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setStarburst</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "strength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getStrength</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "threshold",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setThreshold</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isLensFlare()",
      returns: "boolean",
      description: <>Executes <code>isLensFlare</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "isStarburst()",
      returns: "boolean",
      description: <>Executes <code>isStarburst</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "isThreshold()",
      returns: "boolean",
      description: <>Executes <code>isThreshold</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
