import ApiDoc from '@site/src/components/ApiDoc';

# ContactShadows

Complete API reference for `ContactShadows`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class ContactShadows extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ContactShadows(ContactShadows)",
      description: <>Executes <code>ContactShadows</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ContactShadows(Engine)",
      description: <>Executes <code>ContactShadows</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ContactShadows()",
      description: <>Executes <code>ContactShadows</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ContactShadows",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "ssctContactDistanceMax",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSsctContactDistanceMax</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ssctDepthBias",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSsctDepthBias</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ssctDepthSlopeBias",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSsctDepthSlopeBias</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ssctIntensity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSsctIntensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ssctLightCone",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSsctLightCone</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ssctRayCount",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSsctRayCount</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ssctSampleCount",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSsctSampleCount</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ssctShadowDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSsctShadowDistance</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
