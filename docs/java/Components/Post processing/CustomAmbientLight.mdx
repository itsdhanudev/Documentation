import ApiDoc from '@site/src/components/ApiDoc';

# CustomAmbientLight

Complete API reference for `CustomAmbientLight`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class CustomAmbientLight extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "CustomAmbientLight(CustomAmbientLight)",
      description: <>Executes <code>CustomAmbientLight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "CustomAmbientLight(Engine)",
      description: <>Executes <code>CustomAmbientLight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "CustomAmbientLight()",
      description: <>Executes <code>CustomAmbientLight</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "CustomAmbientLight",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "ambientLight",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isAmbientLight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ambientLightResolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAmbientLightResolutionID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLux</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
