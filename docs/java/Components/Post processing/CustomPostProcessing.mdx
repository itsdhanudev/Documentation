import ApiDoc from '@site/src/components/ApiDoc';

# CustomPostProcessing

Complete API reference for `CustomPostProcessing`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class CustomPostProcessing extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "CustomPostProcessing(CustomPostProcessing)",
      description: <>Executes <code>CustomPostProcessing</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "CustomPostProcessing(Engine)",
      description: <>Executes <code>CustomPostProcessing</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "CustomPostProcessing()",
      description: <>Executes <code>CustomPostProcessing</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "CustomPostProcessing",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "param",
      type: "String",
      access: "set",
      description: <>Virtual attribute exposed by <code>setParam</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "shaderFile",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getShaderFile</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "time",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTime</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
