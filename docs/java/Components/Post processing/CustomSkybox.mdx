import ApiDoc from '@site/src/components/ApiDoc';

# CustomSkybox

Complete API reference for `CustomSkybox`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class CustomSkybox extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "CustomSkybox(CustomSkybox)",
      description: <>Executes <code>CustomSkybox</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "CustomSkybox(Engine)",
      description: <>Executes <code>CustomSkybox</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "CustomSkybox()",
      description: <>Executes <code>CustomSkybox</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "CustomSkybox",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "ambientLight",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isAmbientLight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ambientLightResolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAmbientLightResolutionID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLux</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxResolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxResolutionID</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
