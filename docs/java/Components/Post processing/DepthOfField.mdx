import ApiDoc from '@site/src/components/ApiDoc';

# DepthOfField

Complete API reference for `DepthOfField`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class DepthOfField extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "DepthOfField(DepthOfField)",
      description: <>Executes <code>DepthOfField</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "DepthOfField(Engine)",
      description: <>Executes <code>DepthOfField</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "DepthOfField()",
      description: <>Executes <code>DepthOfField</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "DepthOfField",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "autoFocus",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isAutoFocus</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "focusDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getFocusDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxApertureDiameterCm",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxApertureDiameterCm</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scale",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScale</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
