import ApiDoc from '@site/src/components/ApiDoc';

# FilmGrainScratch

Complete API reference for `FilmGrainScratch`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class FilmGrainScratch extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "FilmGrainScratch(FilmGrainScratch)",
      description: <>Executes <code>FilmGrainScratch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "FilmGrainScratch(Engine)",
      description: <>Executes <code>FilmGrainScratch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "FilmGrainScratch()",
      description: <>Executes <code>FilmGrainScratch</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "FilmGrainScratch",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "flickerStrength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getFlickerStrength</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "grainSize",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGrainSize</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "grainStrength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGrainStrength</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getIntensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scratchDensity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScratchDensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scratchStrength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScratchStrength</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scratchWidth",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScratchWidth</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "time",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTime</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
