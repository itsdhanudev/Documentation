import ApiDoc from '@site/src/components/ApiDoc';

# Fog

Complete API reference for `Fog`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class Fog extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Fog(Fog)",
      description: <>Executes <code>Fog</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Fog(Engine)",
      description: <>Executes <code>Fog</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Fog()",
      description: <>Executes <code>Fog</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Fog",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "cutOffDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCutOffDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "density",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getDensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "distance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "fogColorFromIbl",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setFogColorFromIbl</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "height",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "heightFalloff",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHeightFalloff</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "inScatteringSize",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getInScatteringSize</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "inScatteringStart",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getInScatteringStart</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isFogColorFromIbl()",
      returns: "boolean",
      description: <>Executes <code>isFogColorFromIbl</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
