import ApiDoc from '@site/src/components/ApiDoc';

# GradientSkybox

Complete API reference for `GradientSkybox`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class GradientSkybox extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "GradientSkybox(GradientSkybox)",
      description: <>Executes <code>GradientSkybox</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "GradientSkybox(Engine)",
      description: <>Executes <code>GradientSkybox</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "GradientSkybox()",
      description: <>Executes <code>GradientSkybox</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "GradientSkybox",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "ambientLight",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isAmbientLight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ambientLightResolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAmbientLightResolutionID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "colorGradient",
      type: "ColorGradient",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColorGradient</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLux</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxResolutionID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxResolutionID</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
