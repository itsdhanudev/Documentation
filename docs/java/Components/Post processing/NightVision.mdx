import ApiDoc from '@site/src/components/ApiDoc';

# NightVision

Complete API reference for `NightVision`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class NightVision extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "NightVision(NightVision)",
      description: <>Executes <code>NightVision</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NightVision(Engine)",
      description: <>Executes <code>NightVision</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "NightVision()",
      description: <>Executes <code>NightVision</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "NightVision",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "brightness",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBrightness</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "contrast",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getContrast</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getIntensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "noiseStrength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getNoiseStrength</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scanlineCount",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScanlineCount</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scanlineStrength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScanlineStrength</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "time",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTime</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "tintColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTintColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "vignetteStrength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getVignetteStrength</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
