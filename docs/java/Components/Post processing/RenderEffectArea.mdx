# RenderEffectArea

Complete API reference for `RenderEffectArea`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class RenderEffectArea extends Component
```

## Constructors (3)

| Signature | Description |
| --- | --- |
| RenderEffectArea([RenderEffectArea]) | Executes `RenderEffectArea` and returns after the underlying engine call finishes. |
| RenderEffectArea([Engine]) | Executes `RenderEffectArea` and returns after the underlying engine call finishes. |
| RenderEffectArea() | Executes `RenderEffectArea` and returns after the underlying engine call finishes. |

## Attributes (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [RenderEffectArea] | Represents the `instance` value stored on this instance. Read or assign to update the property. |
