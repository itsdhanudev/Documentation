import ApiDoc from '@site/src/components/ApiDoc';

# RenderEffectArea

Complete API reference for `RenderEffectArea`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class RenderEffectArea extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "RenderEffectArea(RenderEffectArea)",
      description: <>Executes <code>RenderEffectArea</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "RenderEffectArea(Engine)",
      description: <>Executes <code>RenderEffectArea</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "RenderEffectArea()",
      description: <>Executes <code>RenderEffectArea</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "RenderEffectArea",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
/>
