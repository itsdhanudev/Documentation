import ApiDoc from '@site/src/components/ApiDoc';

# SSAO

Complete API reference for `SSAO`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class SSAO extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SSAO(SSAO)",
      description: <>Executes <code>SSAO</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SSAO(Engine)",
      description: <>Executes <code>SSAO</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SSAO()",
      description: <>Executes <code>SSAO</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SSAO",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "bias",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBias</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "bilateralThreshold",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBilateralThreshold</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getIntensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lowPassFilter",
      type: "QualityLevel",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLowPassFilter</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "minHorizonAngleRad",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMinHorizonAngleRad</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "power",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPower</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "quality",
      type: "QualityLevel",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQuality</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "radius",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRadius</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "upsampling",
      type: "QualityLevel",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getUpsampling</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
