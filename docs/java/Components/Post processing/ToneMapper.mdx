import ApiDoc from '@site/src/components/ApiDoc';

# ToneMapper

Complete API reference for `ToneMapper`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class ToneMapper extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ToneMapper(ToneMapper)",
      description: <>Executes <code>ToneMapper</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ToneMapper(Engine)",
      description: <>Executes <code>ToneMapper</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ToneMapper()",
      description: <>Executes <code>ToneMapper</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ToneMapper",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "contrast",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getContrast</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "offsetB",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOffsetB</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "offsetG",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOffsetG</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "offsetR",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOffsetR</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "powerB",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPowerB</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "powerG",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPowerG</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "powerR",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPowerR</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "saturation",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSaturation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "slopeB",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSlopeB</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "slopeG",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSlopeG</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "slopeR",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSlopeR</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "temperature",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTemperature</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "tint",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTint</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "tonnemapType",
      type: "Type",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTonnemapType</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "vibrance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getVibrance</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
