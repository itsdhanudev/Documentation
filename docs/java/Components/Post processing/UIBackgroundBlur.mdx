import ApiDoc from '@site/src/components/ApiDoc';

# UIBackgroundBlur

Complete API reference for `UIBackgroundBlur`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class UIBackgroundBlur extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIBackgroundBlur(UIBackgroundBlur)",
      description: <>Executes <code>UIBackgroundBlur</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIBackgroundBlur(Engine)",
      description: <>Executes <code>UIBackgroundBlur</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIBackgroundBlur()",
      description: <>Executes <code>UIBackgroundBlur</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIBackgroundBlur",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "blurRadius",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBlurRadius</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "quality",
      type: "Quality",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQuality</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
