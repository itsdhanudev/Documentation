import ApiDoc from '@site/src/components/ApiDoc';

# UIFadeOut

Complete API reference for `UIFadeOut`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class UIFadeOut extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIFadeOut(UIFadeOut)",
      description: <>Executes <code>UIFadeOut</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIFadeOut(Engine)",
      description: <>Executes <code>UIFadeOut</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIFadeOut()",
      description: <>Executes <code>UIFadeOut</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIFadeOut",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "opacity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOpacity</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
