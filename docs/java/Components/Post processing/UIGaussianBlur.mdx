import ApiDoc from '@site/src/components/ApiDoc';

# UIGaussianBlur

Complete API reference for `UIGaussianBlur`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class UIGaussianBlur extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIGaussianBlur(UIGaussianBlur)",
      description: <>Executes <code>UIGaussianBlur</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIGaussianBlur(Engine)",
      description: <>Executes <code>UIGaussianBlur</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIGaussianBlur()",
      description: <>Executes <code>UIGaussianBlur</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIGaussianBlur",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "blurRadius",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBlurRadius</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "quality",
      type: "Quality",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQuality</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
