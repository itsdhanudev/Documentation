import ApiDoc from '@site/src/components/ApiDoc';

# Vignette

Complete API reference for `Vignette`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Post processing

**Component:** yes

---

```java
public class Vignette extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Vignette(Vignette)",
      description: <>Executes <code>Vignette</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Vignette(Engine)",
      description: <>Executes <code>Vignette</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Vignette()",
      description: <>Executes <code>Vignette</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Vignette",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "colorINT",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColorINT</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "feather",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getFeather</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "midPoint",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMidPoint</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "roundness",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRoundness</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
