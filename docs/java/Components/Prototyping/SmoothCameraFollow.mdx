import ApiDoc from '@site/src/components/ApiDoc';

# SmoothCameraFollow

Complete API reference for `SmoothCameraFollow`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Prototyping

**Component:** yes

---

```java
public class SmoothCameraFollow extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SmoothCameraFollow(SmoothCameraFollow)",
      description: <>Executes <code>SmoothCameraFollow</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SmoothCameraFollow(Engine)",
      description: <>Executes <code>SmoothCameraFollow</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SmoothCameraFollow()",
      description: <>Creates a new instance of SmoothCameraFollow.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SmoothCameraFollow",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "distance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "height",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "heightDamping",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHeightDamping</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxHeightDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxHeightDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "rotationDamping",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRotationDamping</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "target",
      type: "SpatialObject",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTarget</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
