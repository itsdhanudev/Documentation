import ApiDoc from '@site/src/components/ApiDoc';

# StandUp

Complete API reference for `StandUp`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Prototyping

**Component:** yes

---

```java
public class StandUp extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "StandUp(StandUp)",
      description: <>Executes <code>StandUp</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "StandUp(Engine)",
      description: <>Executes <code>StandUp</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "StandUp()",
      description: <>Creates a new instance of StandUp.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "StandUp",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "rotationY",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRotationY</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "lookTo(Vector3)",
      returns: "void",
      description: <>Performs the lookTo operation using the provided parameters (value).</>
    },
    {
      signature: "rotate(float)",
      returns: "void",
      description: <>Transforms or moves the object accordingly.</>
    },
    {
      signature: "rotateInSeconds(float)",
      returns: "void",
      description: <>Transforms or moves in seconds accordingly.</>
    }
  ]}
/>
