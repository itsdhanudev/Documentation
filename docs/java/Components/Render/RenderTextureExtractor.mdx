# RenderTextureExtractor

Complete API reference for `RenderTextureExtractor`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Render

**Component:** yes

---

```java
public class RenderTextureExtractor extends Component
```

## Constructors (3)

| Signature | Description |
| --- | --- |
| RenderTextureExtractor([RenderTextureExtractor]) | Executes `RenderTextureExtractor` and returns after the underlying engine call finishes. |
| RenderTextureExtractor([Engine]) | Executes `RenderTextureExtractor` and returns after the underlying engine call finishes. |
| RenderTextureExtractor() | Executes `RenderTextureExtractor` and returns after the underlying engine call finishes. |

## Attributes (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [RenderTextureExtractor] | Represents the `instance` value stored on this instance. Read or assign to update the property. |
