import ApiDoc from '@site/src/components/ApiDoc';

# RenderTextureExtractor

Complete API reference for `RenderTextureExtractor`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Render

**Component:** yes

---

```java
public class RenderTextureExtractor extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "RenderTextureExtractor(RenderTextureExtractor)",
      description: <>Executes <code>RenderTextureExtractor</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "RenderTextureExtractor(Engine)",
      description: <>Executes <code>RenderTextureExtractor</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "RenderTextureExtractor()",
      description: <>Executes <code>RenderTextureExtractor</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "RenderTextureExtractor",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
/>
