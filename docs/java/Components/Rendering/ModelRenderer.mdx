import ApiDoc from '@site/src/components/ApiDoc';

# ModelRenderer

Complete API reference for `ModelRenderer`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Rendering

**Component:** yes

---

```java
public class ModelRenderer extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ModelRenderer(ModelRenderer)",
      description: <>Executes <code>ModelRenderer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ModelRenderer()",
      description: <>Executes <code>ModelRenderer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ModelRenderer(Vertex)",
      description: <>Creates a new instance of ModelRenderer.</>
    }
  ]}
  attributes={[
    {
      name: "CUBE",
      type: "int",
      description: <>Represents the <code>CUBE</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "SPHERE",
      type: "int",
      description: <>Represents the <code>SPHERE</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "CONE",
      type: "int",
      description: <>Represents the <code>CONE</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "CYLINDER",
      type: "int",
      description: <>Represents the <code>CYLINDER</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "CIRCLE",
      type: "int",
      description: <>Represents the <code>CIRCLE</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "TORUS",
      type: "int",
      description: <>Represents the <code>TORUS</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "SQUARE",
      type: "int",
      description: <>Represents the <code>SQUARE</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "SQUARE90",
      type: "int",
      description: <>Represents the <code>SQUARE90</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "component",
      type: "ModelRenderer",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "castShadowEnabled",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isCastShadowEnabled</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "enableOutline",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isEnableOutline</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "material",
      type: "Material",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaterial</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "outlineColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOutlineColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "outlineScale",
      type: "float",
      access: "set",
      description: <>Virtual attribute exposed by <code>setOutlineScale</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "renderMatrix",
      type: "float[]",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRenderMatrix</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "vertex",
      type: "Vertex",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getVertex</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "setModelFile(VertexFile)",
      returns: "void",
      description: <>Sets or updates the model file.</>
    },
    {
      signature: "setMaterialFile(MaterialFile)",
      returns: "void",
      description: <>Sets or updates the material file.</>
    },
    {
      signature: "attachPrimitive(int)",
      returns: "void",
      description: <>Performs the attachPrimitive operation using the provided parameters (primitive).</>
    },
    {
      signature: "getOutlineScale()",
      returns: "float",
      description: <>Returns the current outline scale.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
