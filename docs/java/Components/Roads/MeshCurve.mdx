import ApiDoc from '@site/src/components/ApiDoc';

# MeshCurve

Complete API reference for `MeshCurve`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Roads

**Component:** yes

---

```java
public class MeshCurve extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "MeshCurve(MeshCurve)",
      description: <>Executes <code>MeshCurve</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "MeshCurve(Engine)",
      description: <>Executes <code>MeshCurve</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "MeshCurve()",
      description: <>Creates a new instance of MeshCurve.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "MeshCurve",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "spacing",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSpacing</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "startOffset",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getStartOffset</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "curveQuality",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCurveQuality</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "offset",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOffset</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scale",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScale</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "forwardAxis",
      type: "ForwardAxis",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getForwardAxis</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "upMode",
      type: "UpMode",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getUpMode</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "stickToTerrain",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isStickToTerrain</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isStickToTerrain()",
      returns: "boolean",
      description: <>Returns whether stick to terrain is enabled.</>
    }
  ]}
/>
