import ApiDoc from '@site/src/components/ApiDoc';

# Road

Complete API reference for `Road`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Roads

**Component:** yes

---

```java
public class Road extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Road(Road)",
      description: <>Executes <code>Road</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Road(Engine)",
      description: <>Executes <code>Road</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Road()",
      description: <>Creates a new instance of Road.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Road",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "generateCollision",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setGenerateCollision</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "quality",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQuality</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "roadBorder",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRoadBorder</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "roadBorderUV",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRoadBorderUV</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "roadBorderVerticalOffset",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRoadBorderVerticalOffset</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "terrainBorder",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTerrainBorder</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "textureMode",
      type: "TextureMode",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTextureMode</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "textureWidth",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTextureWidth</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "verticalOffset",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getVerticalOffset</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "width",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getWidth</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isGenerateCollision()",
      returns: "boolean",
      description: <>Returns whether generate collision is true.</>
    }
  ]}
/>
