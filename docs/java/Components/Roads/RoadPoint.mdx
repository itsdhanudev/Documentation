import ApiDoc from '@site/src/components/ApiDoc';

# RoadPoint

Complete API reference for `RoadPoint`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Roads

**Component:** yes

---

```java
public class RoadPoint extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "RoadPoint(RoadPoint)",
      description: <>Executes <code>RoadPoint</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "RoadPoint(Engine)",
      description: <>Executes <code>RoadPoint</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "RoadPoint()",
      description: <>Creates a new instance of RoadPoint.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "RoadPoint",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
/>
