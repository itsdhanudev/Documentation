import ApiDoc from '@site/src/components/ApiDoc';

# SkinJoint

Complete API reference for `SkinJoint`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Skin

**Component:** yes

---

```java
public class SkinJoint extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SkinJoint(SkinJoint)",
      description: <>Executes <code>SkinJoint</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SkinJoint(Engine)",
      description: <>Executes <code>SkinJoint</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SkinJoint()",
      description: <>Creates a new instance of SkinJoint.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SkinJoint",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "index",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getIndex</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
