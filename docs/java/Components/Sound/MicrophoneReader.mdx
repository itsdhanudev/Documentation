import ApiDoc from '@site/src/components/ApiDoc';

# MicrophoneReader

Complete API reference for `MicrophoneReader`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Sound

**Component:** yes

---

```java
public class MicrophoneReader extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "MicrophoneReader(MicrophoneReader)",
      description: <>Executes <code>MicrophoneReader</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "MicrophoneReader(Engine)",
      description: <>Executes <code>MicrophoneReader</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "MicrophoneReader()",
      description: <>Creates a new instance of MicrophoneReader.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "MicrophoneReader",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "enableRecord",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isEnableRecord</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "frameLength",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getFrameLength</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "framePCM",
      type: "float[]",
      access: "get",
      description: <>Virtual attribute exposed by <code>getFramePCM</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "sampleRate",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getSampleRate</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
