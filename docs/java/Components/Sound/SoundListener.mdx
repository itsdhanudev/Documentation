import ApiDoc from '@site/src/components/ApiDoc';

# SoundListener

Complete API reference for `SoundListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Sound

**Component:** yes

---

```java
public class SoundListener extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SoundListener(SoundListener)",
      description: <>Executes <code>SoundListener</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SoundListener()",
      description: <>Executes <code>SoundListener</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "SoundListener",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
