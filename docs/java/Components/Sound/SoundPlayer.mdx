import ApiDoc from '@site/src/components/ApiDoc';

# SoundPlayer

Complete API reference for `SoundPlayer`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Sound

**Component:** yes

---

```java
public class SoundPlayer extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SoundPlayer(SoundPlayer)",
      description: <>Executes <code>SoundPlayer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SoundPlayer()",
      description: <>Executes <code>SoundPlayer</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SoundPlayer",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "diameter",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getDiameter</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "loadMode",
      type: "LoadMode",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLoadMode</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "loop",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isLoop</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "mode",
      type: "Mode",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMode</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "paused",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isPaused</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "pitch",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPitch</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "playing",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isPlaying</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "speed",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSpeed</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "stopped",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isStopped</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "volume",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getVolume</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "setSoundFile(SoundFile)",
      returns: "void",
      description: <>Sets or updates the sound file.</>
    },
    {
      signature: "setSoundFile(File)",
      returns: "void",
      description: <>Sets or updates the sound file.</>
    },
    {
      signature: "play()",
      returns: "void",
      description: <>Executes <code>play</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "stop()",
      returns: "void",
      description: <>Executes <code>stop</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "pause()",
      returns: "void",
      description: <>Executes <code>pause</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "unPause()",
      returns: "void",
      description: <>Executes <code>unPause</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "seekTo(int)",
      returns: "void",
      description: <>Executes <code>seekTo</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
