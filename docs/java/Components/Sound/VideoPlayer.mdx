import ApiDoc from '@site/src/components/ApiDoc';

# VideoPlayer

Complete API reference for `VideoPlayer`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Sound

**Component:** yes

---

```java
public class VideoPlayer extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VideoPlayer(VideoPlayer)",
      description: <>Executes <code>VideoPlayer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VideoPlayer()",
      description: <>Executes <code>VideoPlayer</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "VideoPlayer",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "loop",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isLoop</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "paused",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isPaused</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "playing",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isPlaying</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "stopped",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isStopped</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "setVideoFile(VideoFile)",
      returns: "void",
      description: <>Sets or updates the video file.</>
    },
    {
      signature: "setVideoFile(File)",
      returns: "void",
      description: <>Sets or updates the video file.</>
    },
    {
      signature: "setOutputVideoFile(OutputVideoFile)",
      returns: "void",
      description: <>Sets or updates the output video file.</>
    },
    {
      signature: "setOutputVideoFile(File)",
      returns: "void",
      description: <>Sets or updates the output video file.</>
    },
    {
      signature: "play()",
      returns: "void",
      description: <>Executes <code>play</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "stop()",
      returns: "void",
      description: <>Executes <code>stop</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "pause()",
      returns: "void",
      description: <>Executes <code>pause</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "unPause()",
      returns: "void",
      description: <>Executes <code>unPause</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "seekTo(int)",
      returns: "void",
      description: <>Performs the seekTo operation using the provided parameters (miliSeconds).</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
