import ApiDoc from '@site/src/components/ApiDoc';

# Billboard

Complete API reference for `Billboard`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class Billboard extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Billboard(Billboard)",
      description: <>Executes <code>Billboard</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Billboard(Engine)",
      description: <>Executes <code>Billboard</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Billboard()",
      description: <>Executes <code>Billboard</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Billboard",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "zAlign",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>iszAlign</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
