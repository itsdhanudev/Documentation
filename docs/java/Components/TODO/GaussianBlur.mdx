import ApiDoc from '@site/src/components/ApiDoc';

# GaussianBlur

Complete API reference for `GaussianBlur`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class GaussianBlur extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "GaussianBlur(GaussianBlur)",
      description: <>Executes <code>GaussianBlur</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "GaussianBlur(Engine)",
      description: <>Executes <code>GaussianBlur</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "GaussianBlur()",
      description: <>Executes <code>GaussianBlur</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "GaussianBlur",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "blurRadius",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBlurRadius</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getIntensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "quality",
      type: "Quality",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQuality</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
