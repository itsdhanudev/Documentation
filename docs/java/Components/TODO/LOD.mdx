# LOD

Complete API reference for `LOD`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class LOD extends Component
```

## Constructors (3)

| Signature | Description |
| --- | --- |
| LOD([LOD]) | Executes `LOD` and returns after the underlying engine call finishes. |
| LOD([Engine]) | Executes `LOD` and returns after the underlying engine call finishes. |
| LOD() | Executes `LOD` and returns after the underlying engine call finishes. |

## Attributes (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [LOD] | Represents the `instance` value stored on this instance. Read or assign to update the property. |

## Virtual Attributes (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `maxVisibleDistance` | [float] | `get/set` | Virtual attribute exposed by `getMaxVisibleDistance`. Access it like a field to read or write the underlying value. |
