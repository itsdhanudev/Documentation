import ApiDoc from '@site/src/components/ApiDoc';

# LOD

Complete API reference for `LOD`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class LOD extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "LOD(LOD)",
      description: <>Executes <code>LOD</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "LOD(Engine)",
      description: <>Executes <code>LOD</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "LOD()",
      description: <>Executes <code>LOD</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "LOD",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "maxVisibleDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxVisibleDistance</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
