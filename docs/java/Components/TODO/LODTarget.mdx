# LODTarget

Complete API reference for `LODTarget`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class LODTarget extends Component
```

## Constructors (3)

| Signature | Description |
| --- | --- |
| LODTarget([LODTarget]) | Executes `LODTarget` and returns after the underlying engine call finishes. |
| LODTarget([Engine]) | Executes `LODTarget` and returns after the underlying engine call finishes. |
| LODTarget() | Executes `LODTarget` and returns after the underlying engine call finishes. |

## Attributes (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [LODTarget] | Represents the `instance` value stored on this instance. Read or assign to update the property. |
