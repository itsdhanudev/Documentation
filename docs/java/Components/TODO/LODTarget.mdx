import ApiDoc from '@site/src/components/ApiDoc';

# LODTarget

Complete API reference for `LODTarget`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class LODTarget extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "LODTarget(LODTarget)",
      description: <>Executes <code>LODTarget</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "LODTarget(Engine)",
      description: <>Executes <code>LODTarget</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "LODTarget()",
      description: <>Executes <code>LODTarget</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "LODTarget",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
/>
