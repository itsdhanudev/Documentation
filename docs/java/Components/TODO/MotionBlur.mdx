# MotionBlur

Complete API reference for `MotionBlur`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class MotionBlur extends Component
```

## Constructors (3)

| Signature | Description |
| --- | --- |
| MotionBlur([MotionBlur]) | Executes `MotionBlur` and returns after the underlying engine call finishes. |
| MotionBlur([Engine]) | Executes `MotionBlur` and returns after the underlying engine call finishes. |
| MotionBlur() | Executes `MotionBlur` and returns after the underlying engine call finishes. |

## Attributes (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [MotionBlur] | Represents the `instance` value stored on this instance. Read or assign to update the property. |

## Virtual Attributes (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `alpha` | [float] | `get/set` | Virtual attribute exposed by `getAlpha`. Access it like a field to read or write the underlying value. |
| `reset` | [float] | `get/set` | Virtual attribute exposed by `getReset`. Access it like a field to read or write the underlying value. |
