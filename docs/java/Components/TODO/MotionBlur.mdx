import ApiDoc from '@site/src/components/ApiDoc';

# MotionBlur

Complete API reference for `MotionBlur`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class MotionBlur extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "MotionBlur(MotionBlur)",
      description: <>Executes <code>MotionBlur</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "MotionBlur(Engine)",
      description: <>Executes <code>MotionBlur</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "MotionBlur()",
      description: <>Executes <code>MotionBlur</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "MotionBlur",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "alpha",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAlpha</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "reset",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getReset</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
