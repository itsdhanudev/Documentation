import ApiDoc from '@site/src/components/ApiDoc';

# Outline

Complete API reference for `Outline`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class Outline extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Outline(Outline)",
      description: <>Executes <code>Outline</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Outline(Engine)",
      description: <>Executes <code>Outline</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Outline()",
      description: <>Executes <code>Outline</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Outline",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "outlineColor",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOutlineColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "thickness",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getThickness</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "threshold",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getThreshold</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
