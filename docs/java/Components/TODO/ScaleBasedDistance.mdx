import ApiDoc from '@site/src/components/ApiDoc';

# ScaleBasedDistance

Complete API reference for `ScaleBasedDistance`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class ScaleBasedDistance extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ScaleBasedDistance(ScaleBasedDistance)",
      description: <>Executes <code>ScaleBasedDistance</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ScaleBasedDistance(Engine)",
      description: <>Executes <code>ScaleBasedDistance</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ScaleBasedDistance()",
      description: <>Executes <code>ScaleBasedDistance</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ScaleBasedDistance",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "scale",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScale</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
