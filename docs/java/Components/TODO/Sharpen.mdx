import ApiDoc from '@site/src/components/ApiDoc';

# Sharpen

Complete API reference for `Sharpen`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class Sharpen extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Sharpen(Sharpen)",
      description: <>Executes <code>Sharpen</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Sharpen(Engine)",
      description: <>Executes <code>Sharpen</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Sharpen()",
      description: <>Executes <code>Sharpen</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Sharpen",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "edgeThreshold",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getEdgeThreshold</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getIntensity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "sharpenStrength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSharpenStrength</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
