# UIBarHandler

Complete API reference for `UIBarHandler`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class UIBarHandler extends Component
```

## Constructors (3)

| Signature | Description |
| --- | --- |
| UIBarHandler([UIBarHandler]) | Executes `UIBarHandler` and returns after the underlying engine call finishes. |
| UIBarHandler([Engine]) | Executes `UIBarHandler` and returns after the underlying engine call finishes. |
| UIBarHandler() | Executes `UIBarHandler` and returns after the underlying engine call finishes. |

## Attributes (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIBarHandler] | Represents the `instance` value stored on this instance. Read or assign to update the property. |

## Virtual Attributes (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `align` | [Align] | `get/set` | Virtual attribute exposed by `getAlign`. Access it like a field to read or write the underlying value. |
