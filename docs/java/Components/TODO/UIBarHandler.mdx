import ApiDoc from '@site/src/components/ApiDoc';

# UIBarHandler

Complete API reference for `UIBarHandler`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** TODO

**Component:** yes

---

```java
public class UIBarHandler extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIBarHandler(UIBarHandler)",
      description: <>Executes <code>UIBarHandler</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIBarHandler(Engine)",
      description: <>Executes <code>UIBarHandler</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIBarHandler()",
      description: <>Executes <code>UIBarHandler</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIBarHandler",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "align",
      type: "Align",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAlign</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
