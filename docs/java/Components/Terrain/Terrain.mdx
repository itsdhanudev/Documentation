import ApiDoc from '@site/src/components/ApiDoc';

# Terrain

Complete API reference for `Terrain`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Terrain

**Component:** yes

---

```java
public class Terrain extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Terrain()",
      description: <>Executes <code>Terrain</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "Terrain",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "maxHeight",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "resolution",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getResolution</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "width",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getWidth</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isInsideTerrain(float, float)",
      returns: "boolean",
      description: <>Executes <code>isInsideTerrain</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "isInsideTerrain(Vector3)",
      returns: "boolean",
      description: <>Executes <code>isInsideTerrain</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "nearestVerticeAt(Vector3)",
      returns: "Point2",
      description: <>Executes <code>nearestVerticeAt</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "nearestLayerVerticeAt(Vector3)",
      returns: "Point2",
      description: <>Executes <code>nearestLayerVerticeAt</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getWorldY(float, float)",
      returns: "float",
      description: <>Executes <code>getWorldY</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getWorldY(Vector3)",
      returns: "float",
      description: <>Executes <code>getWorldY</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getHeight(Point2)",
      returns: "float",
      description: <>Executes <code>getHeight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getHeight(int, int)",
      returns: "float",
      description: <>Executes <code>getHeight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setHeight(Point2, float)",
      returns: "void",
      description: <>Executes <code>setHeight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setHeight(int, int, float)",
      returns: "void",
      description: <>Executes <code>setHeight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getLayerIntensity(int, int, int)",
      returns: "float",
      description: <>Executes <code>getLayerIntensity</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setLayerIntensity(int, int, int, float)",
      returns: "void",
      description: <>Executes <code>setLayerIntensity</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getLayersCount()",
      returns: "int",
      description: <>Executes <code>getLayersCount</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getLayerAt(int)",
      returns: "Layer",
      description: <>Executes <code>getLayerAt</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "addLayer(Layer)",
      returns: "void",
      description: <>Executes <code>addLayer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "traceRay(Vector3, Vector3)",
      returns: "LaserHit",
      description: <>Executes <code>traceRay</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "traceRay(Vector3, Vector3, float)",
      returns: "LaserHit",
      description: <>Executes <code>traceRay</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "applyRaiseLower(Vector2, float, float, Brush)",
      returns: "void",
      description: <>Executes <code>applyRaiseLower</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "applyPaint(Vector2, float, float, float, int, Brush)",
      returns: "void",
      description: <>Executes <code>applyPaint</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
