import ApiDoc from '@site/src/components/ApiDoc';

# UI3DCameraAimInputSystem

Complete API reference for `UI3DCameraAimInputSystem`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UI3DCameraAimInputSystem extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UI3DCameraAimInputSystem(UI3DCameraAimInputSystem)",
      description: <>Executes <code>UI3DCameraAimInputSystem</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UI3DCameraAimInputSystem(Engine)",
      description: <>Executes <code>UI3DCameraAimInputSystem</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UI3DCameraAimInputSystem()",
      description: <>Executes <code>UI3DCameraAimInputSystem</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UI3DCameraAimInputSystem",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "performClick()",
      returns: "void",
      description: <>Executes <code>performClick</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
