import ApiDoc from '@site/src/components/ApiDoc';

# UI3DInputSystem

Complete API reference for `UI3DInputSystem`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UI3DInputSystem extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UI3DInputSystem(UI3DInputSystem)",
      description: <>Executes <code>UI3DInputSystem</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UI3DInputSystem(Engine)",
      description: <>Executes <code>UI3DInputSystem</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UI3DInputSystem()",
      description: <>Executes <code>UI3DInputSystem</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UI3DInputSystem",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "maxHitDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxHitDistance</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
