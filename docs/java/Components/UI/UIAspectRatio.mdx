import ApiDoc from '@site/src/components/ApiDoc';

# UIAspectRatio

Complete API reference for `UIAspectRatio`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIAspectRatio extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIAspectRatio(UIAspectRatio)",
      description: <>Executes <code>UIAspectRatio</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIAspectRatio(Engine)",
      description: <>Executes <code>UIAspectRatio</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIAspectRatio()",
      description: <>Executes <code>UIAspectRatio</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIAspectRatio",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "mode",
      type: "Mode",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMode</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "proportion",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getProportion</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
