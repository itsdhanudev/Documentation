import ApiDoc from '@site/src/components/ApiDoc';

# UIAxisEventListener

Complete API reference for `UIAxisEventListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIAxisEventListener extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIAxisEventListener(UIAxisEventListener)",
      description: <>Executes <code>UIAxisEventListener</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIAxisEventListener(Engine)",
      description: <>Executes <code>UIAxisEventListener</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIAxisEventListener()",
      description: <>Creates a new instance of UIAxisEventListener.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIAxisEventListener",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "axisName",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAxisName</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "value",
      type: "Vector2",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getValue</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "setValue(float, float)",
      returns: "void",
      description: <>Sets or updates the value.</>
    }
  ]}
/>
