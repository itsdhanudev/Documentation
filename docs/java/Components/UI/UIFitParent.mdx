import ApiDoc from '@site/src/components/ApiDoc';

# UIFitParent

Complete API reference for `UIFitParent`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIFitParent extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIFitParent(UIFitParent)",
      description: <>Executes <code>UIFitParent</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIFitParent(Engine)",
      description: <>Executes <code>UIFitParent</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIFitParent()",
      description: <>Creates a new instance of UIFitParent.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIFitParent",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "fitHeight",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setFitHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "fitWidth",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setFitWidth</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isFitHeight()",
      returns: "boolean",
      description: <>Returns whether fit height is true.</>
    },
    {
      signature: "isFitWidth()",
      returns: "boolean",
      description: <>Returns whether fit width is true.</>
    }
  ]}
/>
