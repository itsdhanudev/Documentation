import ApiDoc from '@site/src/components/ApiDoc';

# UIFlexLayout

Complete API reference for `UIFlexLayout`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIFlexLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIFlexLayout(UIFlexLayout)",
      description: <>Executes <code>UIFlexLayout</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIFlexLayout(Engine)",
      description: <>Executes <code>UIFlexLayout</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIFlexLayout()",
      description: <>Executes <code>UIFlexLayout</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIFlexLayout",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "spacing",
      type: "int",
      access: "set",
      description: <>Virtual attribute exposed by <code>setSpacing</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getSpacing()",
      returns: "int",
      description: <>Executes <code>getSpacing</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
