import ApiDoc from '@site/src/components/ApiDoc';

# UIGridLayout

Complete API reference for `UIGridLayout`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIGridLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIGridLayout(UIGridLayout)",
      description: <>Executes <code>UIGridLayout</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIGridLayout(Engine)",
      description: <>Executes <code>UIGridLayout</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIGridLayout()",
      description: <>Executes <code>UIGridLayout</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIGridLayout",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "autoColumns",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setAutoColumns</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "columns",
      type: "int",
      access: "set",
      description: <>Virtual attribute exposed by <code>setColumns</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "spacing",
      type: "int",
      access: "set",
      description: <>Virtual attribute exposed by <code>setSpacing</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getSpacing()",
      returns: "int",
      description: <>Executes <code>getSpacing</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getColumns()",
      returns: "int",
      description: <>Executes <code>getColumns</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "isAutoColumns()",
      returns: "boolean",
      description: <>Executes <code>isAutoColumns</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
