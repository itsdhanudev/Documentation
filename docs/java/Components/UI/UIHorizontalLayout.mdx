import ApiDoc from '@site/src/components/ApiDoc';

# UIHorizontalLayout

Complete API reference for `UIHorizontalLayout`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIHorizontalLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIHorizontalLayout(UIHorizontalLayout)",
      description: <>Executes <code>UIHorizontalLayout</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIHorizontalLayout(Engine)",
      description: <>Executes <code>UIHorizontalLayout</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIHorizontalLayout()",
      description: <>Creates a new instance of UIHorizontalLayout.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIHorizontalLayout",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "spacing",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSpacing</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
