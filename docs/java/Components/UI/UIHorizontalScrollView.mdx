import ApiDoc from '@site/src/components/ApiDoc';

# UIHorizontalScrollView

Complete API reference for `UIHorizontalScrollView`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIHorizontalScrollView extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIHorizontalScrollView(UIHorizontalScrollView)",
      description: <>Executes <code>UIHorizontalScrollView</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIHorizontalScrollView(Engine)",
      description: <>Executes <code>UIHorizontalScrollView</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIHorizontalScrollView()",
      description: <>Creates a new instance of UIHorizontalScrollView.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIHorizontalScrollView",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "scroll",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScroll</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
