import ApiDoc from '@site/src/components/ApiDoc';

# UIImage

Complete API reference for `UIImage`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIImage extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIImage(UIImage)",
      description: <>Executes <code>UIImage</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIImage(Engine)",
      description: <>Executes <code>UIImage</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIImage()",
      description: <>Creates a new instance of UIImage.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIImage",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "border",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getBorder</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "texture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTexture</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
