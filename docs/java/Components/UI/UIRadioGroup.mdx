import ApiDoc from '@site/src/components/ApiDoc';

# UIRadioGroup

Complete API reference for `UIRadioGroup`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIRadioGroup extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIRadioGroup(UIRadioGroup)",
      description: <>Executes <code>UIRadioGroup</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIRadioGroup(Engine)",
      description: <>Executes <code>UIRadioGroup</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIRadioGroup()",
      description: <>Creates a new instance of UIRadioGroup.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIRadioGroup",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
/>
