import ApiDoc from '@site/src/components/ApiDoc';

# UIRectangularMask

Complete API reference for `UIRectangularMask`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIRectangularMask extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIRectangularMask(UIRectangularMask)",
      description: <>Executes <code>UIRectangularMask</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIRectangularMask(Engine)",
      description: <>Executes <code>UIRectangularMask</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIRectangularMask()",
      description: <>Creates a new instance of UIRectangularMask.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIRectangularMask",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
/>
