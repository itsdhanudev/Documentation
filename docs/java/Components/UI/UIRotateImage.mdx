import ApiDoc from '@site/src/components/ApiDoc';

# UIRotateImage

Complete API reference for `UIRotateImage`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIRotateImage extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIRotateImage(UIRotateImage)",
      description: <>Executes <code>UIRotateImage</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIRotateImage(Engine)",
      description: <>Executes <code>UIRotateImage</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIRotateImage()",
      description: <>Creates a new instance of UIRotateImage.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIRotateImage",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "angle",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getAngle</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "texture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTexture</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
