import ApiDoc from '@site/src/components/ApiDoc';

# UISlideArea

Complete API reference for `UISlideArea`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UISlideArea extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UISlideArea(UISlideArea)",
      description: <>Executes <code>UISlideArea</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UISlideArea(Engine)",
      description: <>Executes <code>UISlideArea</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UISlideArea()",
      description: <>Creates a new instance of UISlideArea.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UISlideArea",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "getValue()",
      returns: "Vector2",
      description: <>Returns the current value.</>
    }
  ]}
/>
