# UISlideArea

Complete API reference for `UISlideArea`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UISlideArea extends Component
```

## Constructors (3)

| Signature | Description |
| --- | --- |
| UISlideArea([UISlideArea]) | Executes `UISlideArea` and returns after the underlying engine call finishes. |
| UISlideArea([Engine]) | Executes `UISlideArea` and returns after the underlying engine call finishes. |
| UISlideArea() | Creates a new instance of UISlideArea. |

## Attributes (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UISlideArea] | Represents the `instance` value stored on this instance. Read or assign to update the property. |

## Methods (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `getValue()` | `Vector2` | Returns the current value. |
