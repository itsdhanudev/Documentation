import ApiDoc from '@site/src/components/ApiDoc';

# UITouchTrigger

Complete API reference for `UITouchTrigger`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UITouchTrigger extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UITouchTrigger(UITouchTrigger)",
      description: <>Executes <code>UITouchTrigger</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UITouchTrigger(Engine)",
      description: <>Executes <code>UITouchTrigger</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UITouchTrigger()",
      description: <>Creates a new instance of UITouchTrigger.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UITouchTrigger",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "selectedTouch",
      type: "Touch",
      access: "get",
      description: <>Virtual attribute exposed by <code>getSelectedTouch</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isDown()",
      returns: "boolean",
      description: <>Returns whether down is true.</>
    },
    {
      signature: "isPressed()",
      returns: "boolean",
      description: <>Returns whether pressed is true.</>
    },
    {
      signature: "isUp()",
      returns: "boolean",
      description: <>Returns whether up is true.</>
    }
  ]}
/>
