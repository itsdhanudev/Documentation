import ApiDoc from '@site/src/components/ApiDoc';

# UIVerticalLayout

Complete API reference for `UIVerticalLayout`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIVerticalLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIVerticalLayout(UIVerticalLayout)",
      description: <>Executes <code>UIVerticalLayout</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIVerticalLayout(Engine)",
      description: <>Executes <code>UIVerticalLayout</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIVerticalLayout()",
      description: <>Creates a new instance of UIVerticalLayout.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIVerticalLayout",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "spacing",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSpacing</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
