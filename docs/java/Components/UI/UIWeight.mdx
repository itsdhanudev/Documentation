import ApiDoc from '@site/src/components/ApiDoc';

# UIWeight

Complete API reference for `UIWeight`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** UI

**Component:** yes

---

```java
public class UIWeight extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIWeight(UIWeight)",
      description: <>Executes <code>UIWeight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIWeight(Engine)",
      description: <>Executes <code>UIWeight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "UIWeight()",
      description: <>Executes <code>UIWeight</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIWeight",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "weight",
      type: "float",
      access: "set",
      description: <>Virtual attribute exposed by <code>setWeight</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getWeight()",
      returns: "float",
      description: <>Executes <code>getWeight</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
