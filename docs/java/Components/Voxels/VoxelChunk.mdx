import ApiDoc from '@site/src/components/ApiDoc';

# VoxelChunk

Complete API reference for `VoxelChunk`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Voxels

**Component:** yes

---

```java
public class VoxelChunk extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelChunk(VoxelChunk)",
      description: <>Executes <code>VoxelChunk</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelChunk()",
      description: <>Executes <code>VoxelChunk</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "VoxelChunk",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "blocksMatrix",
      type: "OH3LevelIntArray",
      access: "set",
      description: <>Virtual attribute exposed by <code>setBlocksMatrix</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "blocksMatrixCritical",
      type: "OH3LevelIntArray",
      access: "get",
      description: <>Virtual attribute exposed by <code>getBlocksMatrixCritical</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "height",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "offset",
      type: "Vector2",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOffset</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "usePositionAsCoordinatesOffset",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isUsePositionAsCoordinatesOffset</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "width",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getWidth</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "apply()",
      returns: "void",
      description: <>Executes <code>apply</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "invalidateBlocks()",
      returns: "void",
      description: <>Executes <code>invalidateBlocks</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getMaxVerticesCount()",
      returns: "int",
      description: <>Returns the current max vertices count.</>
    },
    {
      signature: "getBlock(int, int, int)",
      returns: "int",
      description: <>Returns the current block.</>
    },
    {
      signature: "getBlockLocal(int, int, int)",
      returns: "int",
      description: <>Returns the current block local.</>
    },
    {
      signature: "setBlock(int, int, int, int)",
      returns: "void",
      description: <>Sets or updates the block.</>
    },
    {
      signature: "setLocalBlock(int, int, int, int)",
      returns: "void",
      description: <>Sets or updates the local block.</>
    },
    {
      signature: "containsBlock(int, int, int)",
      returns: "boolean",
      description: <>Performs the containsBlock operation using the provided parameters (x, y, z).</>
    },
    {
      signature: "containsLocalBlock(int, int, int)",
      returns: "boolean",
      description: <>Performs the containsLocalBlock operation using the provided parameters (x, y, z).</>
    },
    {
      signature: "isReady()",
      returns: "boolean",
      description: <>Returns whether ready is true.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
