import ApiDoc from '@site/src/components/ApiDoc';

# VoxelChunkSpawner

Complete API reference for `VoxelChunkSpawner`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Voxels

**Component:** yes

---

```java
public class VoxelChunkSpawner extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelChunkSpawner(VoxelChunkSpawner)",
      description: <>Executes <code>VoxelChunkSpawner</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelChunkSpawner(Engine)",
      description: <>Executes <code>VoxelChunkSpawner</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelChunkSpawner()",
      description: <>Creates a new instance of VoxelChunkSpawner.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "VoxelChunkSpawner",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "camera",
      type: "SpatialObject",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCamera</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "chunkBlueprint",
      type: "VoxelChunk",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getChunkBlueprint</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "onlyVisibleChunks",
      type: "boolean",
      access: "set",
      description: <>Virtual attribute exposed by <code>setOnlyVisibleChunks</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "renderChunks",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRenderChunks</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "updateDelay",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getUpdateDelay</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "visibleDot",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getVisibleDot</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "isOnlyVisibleChunks()",
      returns: "boolean",
      description: <>Returns whether only visible chunks is true.</>
    }
  ]}
/>
