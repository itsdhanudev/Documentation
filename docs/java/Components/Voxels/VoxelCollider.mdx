import ApiDoc from '@site/src/components/ApiDoc';

# VoxelCollider

Complete API reference for `VoxelCollider`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Voxels

**Component:** yes

---

```java
public class VoxelCollider extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelCollider(VoxelCollider)",
      description: <>Executes <code>VoxelCollider</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelCollider(Engine)",
      description: <>Executes <code>VoxelCollider</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelCollider()",
      description: <>Creates a new instance of VoxelCollider.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "VoxelCollider",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "horizontalSize",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHorizontalSize</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "positionOffset",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPositionOffset</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "verticalSize",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getVerticalSize</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
