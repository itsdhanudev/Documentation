import ApiDoc from '@site/src/components/ApiDoc';

# VoxelGenerator

Complete API reference for `VoxelGenerator`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Voxels

**Component:** yes

---

```java
public class VoxelGenerator extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelGenerator(VoxelGenerator)",
      description: <>Executes <code>VoxelGenerator</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelGenerator()",
      description: <>Executes <code>VoxelGenerator</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "VoxelGenerator",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "setListener(Component)",
      returns: "void",
      description: <>Sets or updates the listener.</>
    },
    {
      signature: "removeListener()",
      returns: "void",
      description: <>Removes listener from the collection.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
