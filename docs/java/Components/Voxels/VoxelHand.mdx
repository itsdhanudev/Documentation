import ApiDoc from '@site/src/components/ApiDoc';

# VoxelHand

Complete API reference for `VoxelHand`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Voxels

**Component:** yes

---

```java
public class VoxelHand extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelHand(VoxelHand)",
      description: <>Executes <code>VoxelHand</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelHand(Engine)",
      description: <>Executes <code>VoxelHand</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelHand()",
      description: <>Creates a new instance of VoxelHand.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "VoxelHand",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "blockBroken",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isBlockBroken</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "blockPlaced",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isBlockPlaced</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "crossHairColor",
      type: "Color",
      access: "get",
      description: <>Virtual attribute exposed by <code>getCrossHairColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "crossHairSize",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCrossHairSize</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "crossHairTexture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getCrossHairTexture</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "distance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lastHitBlockID",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getLastHitBlockID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "listener",
      type: "Listener",
      access: "set",
      description: <>Virtual attribute exposed by <code>setListener</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "selectedBlockID",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSelectedBlockID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "showCrosshair",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isShowCrosshair</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "requestPut()",
      returns: "void",
      description: <>Executes <code>requestPut</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "requestBreak()",
      returns: "void",
      description: <>Executes <code>requestBreak</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executes <code>componentClassMatch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executes <code>getEngineComponent</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
