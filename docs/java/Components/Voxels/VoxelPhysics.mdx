import ApiDoc from '@site/src/components/ApiDoc';

# VoxelPhysics

Complete API reference for `VoxelPhysics`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Voxels

**Component:** yes

---

```java
public class VoxelPhysics extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelPhysics(VoxelPhysics)",
      description: <>Executes <code>VoxelPhysics</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelPhysics(Engine)",
      description: <>Executes <code>VoxelPhysics</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelPhysics()",
      description: <>Creates a new instance of VoxelPhysics.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "VoxelPhysics",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "climbSpeed",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getClimbSpeed</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "gravity",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGravity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxClimbHeight",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxClimbHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxGravitySpeed",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxGravitySpeed</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "velocity",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getVelocity</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
