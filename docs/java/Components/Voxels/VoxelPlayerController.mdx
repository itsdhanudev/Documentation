import ApiDoc from '@site/src/components/ApiDoc';

# VoxelPlayerController

Complete API reference for `VoxelPlayerController`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Voxels

**Component:** yes

---

```java
public class VoxelPlayerController extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelPlayerController(VoxelPlayerController)",
      description: <>Executes <code>VoxelPlayerController</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelPlayerController(Engine)",
      description: <>Executes <code>VoxelPlayerController</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VoxelPlayerController()",
      description: <>Creates a new instance of VoxelPlayerController.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "VoxelPlayerController",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "horizontalSlideName",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHorizontalSlideName</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "horizontalSlideSens",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getHorizontalSlideSens</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "joystickName",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getJoystickName</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "runSpeed",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRunSpeed</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "walkSpeed",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getWalkSpeed</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
