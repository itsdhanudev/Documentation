import ApiDoc from '@site/src/components/ApiDoc';

# Gizmo

Complete API reference for `Gizmo`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Debug

**Component:** no

---

```java
public class Gizmo
```

<ApiDoc
  staticMethods={[
    {
      signature: "drawPoint(Vector3, float)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawPoint(Vector3, float, Color)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawSphere(Vector3, float)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawSphere(Vector3, float, Color)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawSphere(Vector3, Vector3, Color)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawQuad(Vector3, float, Texture)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawQuad(Vector3, Vector3, Texture)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawQuad(Vector3, Vector3, Texture, Color)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawTransparentQuad(Vector3, Vector3, Texture)",
      returns: "void",
      description: <>Executes <code>drawTransparentQuad</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "drawTransparentQuad(Vector3, Vector3, Texture, boolean)",
      returns: "void",
      description: <>Executes <code>drawTransparentQuad</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "drawTransparentQuad(Vector3, Vector3, Texture, Color)",
      returns: "void",
      description: <>Executes <code>drawTransparentQuad</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "drawTransparentQuad(Vector3, Vector3, Texture, Color, boolean)",
      returns: "void",
      description: <>Executes <code>drawTransparentQuad</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "drawCube(Vector3, float)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawCube(Vector3, float, Color)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawCube(Vector3, Vector3)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawCube(Vector3, Vector3, Color)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawCube(Vector3, Vector3, Color, Texture)",
      returns: "void",
      description: <>Executes <code>drawCube</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "drawCube(Vector3, float, Texture)",
      returns: "void",
      description: <>Executes <code>drawCube</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "drawTransparentCube(Vector3, Vector3, Color)",
      returns: "void",
      description: <>Executes <code>drawTransparentCube</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "drawVertex(Vertex, Vector3)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawVertex(Vertex, Vector3, Color)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawVertex(Vertex, Vector3, Quaternion)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawVertex(Vertex, Vector3, Vector3)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawVertex(Vertex, Vector3, Quaternion, Vector3)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawVertex(Vertex, Vector3, Quaternion, Vector3, Color)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawWireframeVertex(Vertex, Vector3, Quaternion, Vector3)",
      returns: "void",
      description: <>Executes <code>drawWireframeVertex</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "drawWireframeVertex(Vertex, Vector3, Quaternion, Vector3, Color)",
      returns: "void",
      description: <>Executes <code>drawWireframeVertex</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "drawWireframeVertex(Vertex, Vector3, Quaternion, Vector3, Color, Texture)",
      returns: "void",
      description: <>Executes <code>drawWireframeVertex</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "drawVertex(GizmoElement)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawGizmo(GizmoElement)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawElement(GizmoElement)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "draw(GizmoElement)",
      returns: "void",
      description: <>Renders the object to the current context.</>
    },
    {
      signature: "drawEngine(GizmoElement)",
      returns: "void",
      description: <>Executes <code>drawEngine</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
