import ApiDoc from '@site/src/components/ApiDoc';

# GizmoMatrix

Complete API reference for `GizmoMatrix`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Debug

**Component:** no

---

```java
public class GizmoMatrix extends GizmoElement
```

<ApiDoc
  constructors={[
    {
      signature: "GizmoMatrix()",
      description: <>Creates a new instance of GizmoMatrix.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "matrix",
      type: "float[]",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMatrix</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
