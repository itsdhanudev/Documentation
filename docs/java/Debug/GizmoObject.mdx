import ApiDoc from '@site/src/components/ApiDoc';

# GizmoObject

Complete API reference for `GizmoObject`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Debug

**Component:** no

---

```java
public class GizmoObject extends GizmoTransform
```

<ApiDoc
  constructors={[
    {
      signature: "GizmoObject()",
      description: <>Creates a new instance of GizmoObject.</>
    },
    {
      signature: "GizmoObject(Vertex)",
      description: <>Creates a new instance of GizmoObject.</>
    },
    {
      signature: "GizmoObject(Vertex, Vector3)",
      description: <>Creates a new instance of GizmoObject.</>
    },
    {
      signature: "GizmoObject(Vertex, Vector3, Quaternion)",
      description: <>Creates a new instance of GizmoObject.</>
    },
    {
      signature: "GizmoObject(Vertex, Vector3, Quaternion, Vector3)",
      description: <>Creates a new instance of GizmoObject.</>
    },
    {
      signature: "GizmoObject(Vertex, Vector3, Quaternion, Vector3, boolean)",
      description: <>Creates a new instance of GizmoObject.</>
    },
    {
      signature: "GizmoObject(Vertex, Vector3, Quaternion, Vector3, boolean, Color, Texture)",
      description: <>Creates a new instance of GizmoObject.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "dualFaceRender",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isDualFaceRender</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "enableTransparency",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isEnableTransparency</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "renderMode",
      type: "RenderMode",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRenderMode</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scaleBasedCamera",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isScaleBasedCamera</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "texture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTexture</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "vertex",
      type: "Vertex",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getVertex</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "wireFrame",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isWireFrame</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "wireFrameWidth",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getWireFrameWidth</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "randomizeColor()",
      returns: "void",
      description: <>Executes <code>randomizeColor</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
