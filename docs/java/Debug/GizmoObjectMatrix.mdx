import ApiDoc from '@site/src/components/ApiDoc';

# GizmoObjectMatrix

Complete API reference for `GizmoObjectMatrix`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Debug

**Component:** no

---

```java
public class GizmoObjectMatrix extends GizmoMatrix
```

<ApiDoc
  constructors={[
    {
      signature: "GizmoObjectMatrix()",
      description: <>Creates a new instance of GizmoObjectMatrix.</>
    },
    {
      signature: "GizmoObjectMatrix(Vertex)",
      description: <>Creates a new instance of GizmoObjectMatrix.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "dualFaceRender",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isDualFaceRender</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "outlineScale",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getOutlineScale</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "renderMode",
      type: "RenderMode",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRenderMode</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "texture",
      type: "Texture",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTexture</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "vertex",
      type: "Vertex",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getVertex</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "wireFrame",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isWireFrame</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "wireFrameWidth",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getWireFrameWidth</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getUserPointer()",
      returns: "Object",
      description: <>Returns the current user pointer.</>
    },
    {
      signature: "setUserPointer(Object)",
      returns: "void",
      description: <>Sets or updates the user pointer.</>
    }
  ]}
/>
