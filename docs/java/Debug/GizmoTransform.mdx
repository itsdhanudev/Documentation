import ApiDoc from '@site/src/components/ApiDoc';

# GizmoTransform

Complete API reference for `GizmoTransform`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Debug

**Component:** no

---

```java
public class GizmoTransform extends GizmoElement
```

<ApiDoc
  constructors={[
    {
      signature: "GizmoTransform()",
      description: <>Creates a new instance of GizmoTransform.</>
    },
    {
      signature: "GizmoTransform(Vector3)",
      description: <>Creates a new instance of GizmoTransform.</>
    },
    {
      signature: "GizmoTransform(Vector3, Quaternion)",
      description: <>Creates a new instance of GizmoTransform.</>
    },
    {
      signature: "GizmoTransform(Vector3, Quaternion, Vector3)",
      description: <>Creates a new instance of GizmoTransform.</>
    }
  ]}
  attributes={[
    {
      name: "rotMatrix",
      type: "float[]",
      description: <>Represents the <code>rotMatrix</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "matrix",
      type: "float[]",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMatrix</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "position",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPosition</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "rotation",
      type: "Quaternion",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRotation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scale",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScale</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "setPosition(float, float, float)",
      returns: "void",
      description: <>Sets or updates the position.</>
    },
    {
      signature: "setPosition(float)",
      returns: "void",
      description: <>Sets or updates the position.</>
    },
    {
      signature: "setRotation(float, float, float)",
      returns: "void",
      description: <>Sets or updates the rotation.</>
    },
    {
      signature: "setRotation(float, float, float, float)",
      returns: "void",
      description: <>Sets or updates the rotation.</>
    },
    {
      signature: "setScale(float, float, float)",
      returns: "void",
      description: <>Sets or updates the scale.</>
    },
    {
      signature: "setScale(float)",
      returns: "void",
      description: <>Sets or updates the scale.</>
    }
  ]}
/>
