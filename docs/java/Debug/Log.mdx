import ApiDoc from '@site/src/components/ApiDoc';

# Log

Complete API reference for `Log`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Debug

**Component:** no

---

```java
public class Log
```

<ApiDoc
  constructors={[
    {
      signature: "Log(ILog)",
      description: <>Executes <code>Log</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Log()",
      description: <>Executes <code>Log</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Log(String)",
      description: <>Creates a new instance of Log.</>
    },
    {
      signature: "Log(String, String)",
      description: <>Creates a new instance of Log.</>
    },
    {
      signature: "Log(String, String, String)",
      description: <>Creates a new instance of Log.</>
    },
    {
      signature: "Log(String, String, String, Color)",
      description: <>Creates a new instance of Log.</>
    }
  ]}
  attributes={[
    {
      name: "log",
      type: "ILog",
      description: <>Represents the <code>log</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getColor</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "link",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getLink</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "message",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMessage</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "quantity",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQuantity</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "tag",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTag</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "tittle",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTittle</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
