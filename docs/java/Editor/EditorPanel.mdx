import ApiDoc from '@site/src/components/ApiDoc';

# EditorPanel

Complete API reference for `EditorPanel`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Editor

**Component:** no

---

```java
public class EditorPanel
```

<ApiDoc
  constructors={[
    {
      signature: "EditorPanel(EditorPanel)",
      description: <>Executes <code>EditorPanel</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "EditorPanel(Engine)",
      description: <>Executes <code>EditorPanel</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "EditorPanel()",
      description: <>Executes <code>EditorPanel</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "panel",
      type: "EditorPanel",
      description: <>Represents the <code>panel</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "contentHeightPixels",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getContentHeightPixels</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "contentWidthPixels",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getContentWidthPixels</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "contentXPixels",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getContentXPixels</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "contentYPixels",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getContentYPixels</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "floating",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isFloating</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "requestCloseConfirmation",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isRequestCloseConfirmation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "tittle",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTittle</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "totalHeightPixels",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getTotalHeightPixels</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "totalWidthPixels",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getTotalWidthPixels</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "totalXPixels",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getTotalXPixels</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "totalYPixels",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getTotalYPixels</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "visible",
      type: "boolean",
      access: "get",
      description: <>Virtual attribute exposed by <code>isVisible</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "setPanel(EditorPanel)",
      returns: "void",
      description: <>Executes <code>setPanel</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "isTouchInside(Touch)",
      returns: "boolean",
      description: <>Returns whether touch inside is true.</>
    },
    {
      signature: "determineTouch(GUIUtils.TouchFilter)",
      returns: "Touch",
      description: <>Performs the determineTouch operation using the provided parameters (touchFilter).</>
    },
    {
      signature: "determineTouch(GUIUtils.TouchFilter, int)",
      returns: "Touch",
      description: <>Performs the determineTouch operation using the provided parameters (touchFilter, offset).</>
    },
    {
      signature: "determineTouchPosition(Touch)",
      returns: "Vector2",
      description: <>Performs the determineTouchPosition operation using the provided parameters (touch).</>
    },
    {
      signature: "determineTouchPosition(Touch, Vector2)",
      returns: "Vector2",
      description: <>Performs the determineTouchPosition operation using the provided parameters (touch, out).</>
    },
    {
      signature: "requestDetach()",
      returns: "void",
      description: <>Executes <code>requestDetach</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "blink()",
      returns: "void",
      description: <>Executes <code>blink</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "isAllowCloseX()",
      returns: "boolean",
      description: <>Returns whether allow close x is true.</>
    },
    {
      signature: "setAllowCloseX(boolean)",
      returns: "void",
      description: <>Sets or updates the allow close x.</>
    },
    {
      signature: "copy()",
      returns: "EditorPanel",
      description: <>Returns a copy of this EditorPanel instance.</>
    }
  ]}
/>
