import ApiDoc from '@site/src/components/ApiDoc';

# ViewPanel

Complete API reference for `ViewPanel`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Editor

**Component:** no

---

```java
public class ViewPanel extends EditorPanel
```

<ApiDoc
  constructors={[
    {
      signature: "ViewPanel(JavaViewPanel)",
      description: <>Executes <code>ViewPanel</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ViewPanel()",
      description: <>Executes <code>ViewPanel</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  methods={[
    {
      signature: "updateUI()",
      returns: "void",
      description: <>Sets or updates the ui.</>
    },
    {
      signature: "updateUIVisible()",
      returns: "void",
      description: <>Sets or updates the uivisible.</>
    },
    {
      signature: "minimizedUpdate()",
      returns: "void",
      description: <>Executes <code>minimizedUpdate</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onBindView()",
      returns: "void",
      description: <>Executes <code>onBindView</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onUnbindView()",
      returns: "void",
      description: <>Executes <code>onUnbindView</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onAttach()",
      returns: "View",
      description: <>Executes <code>onAttach</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onDetach()",
      returns: "void",
      description: <>Executes <code>onDetach</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  staticMethods={[
    {
      signature: "findViewByID(View, String)",
      returns: "<T extends View> T",
      description: <>Performs the findViewByID operation using the provided parameters (view, id).</>
    },
    {
      signature: "toView(Object)",
      returns: "View",
      description: <>Performs the toView operation using the provided parameters (o).</>
    },
    {
      signature: "getIdOf(View)",
      returns: "String",
      description: <>Returns the current id of.</>
    }
  ]}
/>
