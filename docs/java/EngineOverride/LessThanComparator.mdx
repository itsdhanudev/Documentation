# LessThanComparator

Complete API reference for `LessThanComparator`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** EngineOverride

**Component:** no

---

```java
public interface LessThanComparator
```

## Methods (20)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"otherObject"}])` | `void` | Checks if the current object is less than the given object. |
| `lessThan(Object)` | `boolean` | Executes `lessThan` and returns after the underlying engine call finishes. |
| `@MethodArgs([{"value"}])` | `void` | Checks if the current object is less than the given float value. |
| `lessThan(float)` | `boolean` | Executes `lessThan` and returns after the underlying engine call finishes. |
| `@MethodArgs([{"value"}])` | `void` | Checks if the current object is less than the given integer value. |
| `lessThan(int)` | `boolean` | Executes `lessThan` and returns after the underlying engine call finishes. |
| `@MethodArgs([{"value"}])` | `void` | Checks if the current object is less than the given long value. |
| `lessThan(long)` | `boolean` | Executes `lessThan` and returns after the underlying engine call finishes. |
| `@MethodArgs([{"value"}])` | `void` | Checks if the current object is less than the given double value. |
| `lessThan(double)` | `boolean` | Executes `lessThan` and returns after the underlying engine call finishes. |
| `@MethodArgs([{"otherObject"}])` | `void` | Checks if the current object is less than or equal to the given object. |
| `lessOrEqualsThan(Object)` | `boolean` | Executes `lessOrEqualsThan` and returns after the underlying engine call finishes. |
| `@MethodArgs([{"value"}])` | `void` | Checks if the current object is less than or equal to the given float value. |
| `lessOrEqualsThan(float)` | `boolean` | Executes `lessOrEqualsThan` and returns after the underlying engine call finishes. |
| `@MethodArgs([{"value"}])` | `void` | Checks if the current object is less than or equal to the given integer value. |
| `lessOrEqualsThan(int)` | `boolean` | Executes `lessOrEqualsThan` and returns after the underlying engine call finishes. |
| `@MethodArgs([{"value"}])` | `void` | Checks if the current object is less than or equal to the given long value. |
| `lessOrEqualsThan(long)` | `boolean` | Executes `lessOrEqualsThan` and returns after the underlying engine call finishes. |
| `@MethodArgs([{"value"}])` | `void` | Checks if the current object is less than or equal to the given double value. |
| `lessOrEqualsThan(double)` | `boolean` | Executes `lessOrEqualsThan` and returns after the underlying engine call finishes. |
