import ApiDoc from '@site/src/components/ApiDoc';

# File

Complete API reference for `File`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Files

**Component:** no

---

```java
public class File extends File
```

<ApiDoc
  constructors={[
    {
      signature: "File(String, boolean)",
      description: <>Creates a new instance of File.</>
    },
    {
      signature: "File(File, boolean)",
      description: <>Executes <code>File</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "File(File, String, boolean)",
      description: <>Executes <code>File</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "File(String)",
      description: <>Creates a new instance of File.</>
    },
    {
      signature: "File(File)",
      description: <>Executes <code>File</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "File(String, String)",
      description: <>Creates a new instance of File.</>
    },
    {
      signature: "File(File, String)",
      description: <>Executes <code>File</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "File(URI)",
      description: <>Creates a new instance of File.</>
    }
  ]}
  methods={[
    {
      signature: "getName()",
      returns: "String",
      description: <>Returns the current name.</>
    },
    {
      signature: "getParent()",
      returns: "String",
      description: <>Returns the current parent.</>
    },
    {
      signature: "getParentFile()",
      returns: "File",
      description: <>Returns the current parent file.</>
    },
    {
      signature: "getPath()",
      returns: "String",
      description: <>Returns the current path.</>
    },
    {
      signature: "isAbsolute()",
      returns: "boolean",
      description: <>Returns whether absolute is true.</>
    },
    {
      signature: "getAbsolutePath()",
      returns: "String",
      description: <>Returns the current absolute path.</>
    },
    {
      signature: "getAbsoluteFile()",
      returns: "File",
      description: <>Returns the current absolute file.</>
    },
    {
      signature: "getCanonicalPath()",
      returns: "String",
      description: <>Executes <code>getCanonicalPath</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getCanonicalFile()",
      returns: "File",
      description: <>Executes <code>getCanonicalFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "toURL()",
      returns: "URL",
      description: <>Executes <code>toURL</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "toURI()",
      returns: "URI",
      description: <>Executes <code>toURI</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "canRead()",
      returns: "boolean",
      description: <>Returns whether read is true.</>
    },
    {
      signature: "canWrite()",
      returns: "boolean",
      description: <>Returns whether write is true.</>
    },
    {
      signature: "exists()",
      returns: "boolean",
      description: <>Executes <code>exists</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "isDirectory()",
      returns: "boolean",
      description: <>Returns whether directory is true.</>
    },
    {
      signature: "isFile()",
      returns: "boolean",
      description: <>Returns whether file is true.</>
    },
    {
      signature: "isHidden()",
      returns: "boolean",
      description: <>Returns whether hidden is true.</>
    },
    {
      signature: "lastModified()",
      returns: "long",
      description: <>Executes <code>lastModified</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "length()",
      returns: "long",
      description: <>Executes <code>length</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "createNewFile()",
      returns: "boolean",
      description: <>Executes <code>createNewFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "delete()",
      returns: "boolean",
      description: <>Removes an item from the collection.</>
    },
    {
      signature: "deleteOnExit()",
      returns: "void",
      description: <>Removes on exit from the collection.</>
    },
    {
      signature: "list()",
      returns: "String[]",
      description: <>Executes <code>list</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "list(FilenameFilter)",
      returns: "String[]",
      description: <>Performs the list operation using the provided parameters (filter).</>
    },
    {
      signature: "listFiles()",
      returns: "File[]",
      description: <>Executes <code>listFiles</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "listFiles(FilenameFilter)",
      returns: "File[]",
      description: <>Performs the listFiles operation using the provided parameters (filter).</>
    },
    {
      signature: "listFiles(FileFilter)",
      returns: "File[]",
      description: <>Performs the listFiles operation using the provided parameters (filter).</>
    },
    {
      signature: "mkdir()",
      returns: "boolean",
      description: <>Executes <code>mkdir</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "mkdirs()",
      returns: "boolean",
      description: <>Executes <code>mkdirs</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "renameTo(File)",
      returns: "boolean",
      description: <>Performs the renameTo operation using the provided parameters (dest).</>
    },
    {
      signature: "setLastModified(long)",
      returns: "boolean",
      description: <>Sets or updates the last modified.</>
    },
    {
      signature: "setReadOnly()",
      returns: "boolean",
      description: <>Sets or updates the read only.</>
    },
    {
      signature: "setWritable(boolean, boolean)",
      returns: "boolean",
      description: <>Sets or updates the writable.</>
    },
    {
      signature: "setWritable(boolean)",
      returns: "boolean",
      description: <>Sets or updates the writable.</>
    },
    {
      signature: "setReadable(boolean, boolean)",
      returns: "boolean",
      description: <>Sets or updates the readable.</>
    },
    {
      signature: "setReadable(boolean)",
      returns: "boolean",
      description: <>Sets or updates the readable.</>
    },
    {
      signature: "setExecutable(boolean, boolean)",
      returns: "boolean",
      description: <>Sets or updates the executable.</>
    },
    {
      signature: "setExecutable(boolean)",
      returns: "boolean",
      description: <>Sets or updates the executable.</>
    },
    {
      signature: "canExecute()",
      returns: "boolean",
      description: <>Returns whether execute is true.</>
    },
    {
      signature: "getTotalSpace()",
      returns: "long",
      description: <>Returns the current total space.</>
    },
    {
      signature: "getFreeSpace()",
      returns: "long",
      description: <>Returns the current free space.</>
    },
    {
      signature: "getUsableSpace()",
      returns: "long",
      description: <>Returns the current usable space.</>
    },
    {
      signature: "compareTo(File)",
      returns: "int",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "equals(Object)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "hashCode()",
      returns: "int",
      description: <>Returns whether code is true.</>
    },
    {
      signature: "toPath()",
      returns: "Path",
      description: <>Executes <code>toPath</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "isAccessible()",
      returns: "boolean",
      description: <>Returns whether accessible is true.</>
    },
    {
      signature: "isAccessible(String)",
      returns: "boolean",
      description: <>Returns whether accessible is true.</>
    }
  ]}
  staticMethods={[
    {
      signature: "createTempFile(String, String, File)",
      returns: "File",
      description: <>Executes <code>createTempFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "createTempFile(String, String)",
      returns: "File",
      description: <>Executes <code>createTempFile</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
