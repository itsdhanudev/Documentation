import ApiDoc from '@site/src/components/ApiDoc';

# FileLoader

Complete API reference for `FileLoader`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Files

**Component:** no

---

```java
public class FileLoader
```

<ApiDoc
  staticMethods={[
    {
      signature: "exportTextToFile(String, String)",
      returns: "void",
      description: <>Executes <code>exportTextToFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "exportTextToFile(String, File)",
      returns: "void",
      description: <>Executes <code>exportTextToFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "loadTextFromFile(InputStream)",
      returns: "String",
      description: <>Executes <code>loadTextFromFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "loadTextFromFile(File)",
      returns: "String",
      description: <>Executes <code>loadTextFromFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "loadTextFromFile(ProjectFile)",
      returns: "String",
      description: <>Loads text from file into the object.</>
    }
  ]}
/>
