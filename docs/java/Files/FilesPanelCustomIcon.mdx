import ApiDoc from '@site/src/components/ApiDoc';

# FilesPanelCustomIcon

Complete API reference for `FilesPanelCustomIcon`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Files

**Component:** no

---

```java
public class FilesPanelCustomIcon
```

<ApiDoc
  constructors={[
    {
      signature: "FilesPanelCustomIcon()",
      description: <>Creates a new instance of FilesPanelCustomIcon.</>
    }
  ]}
  methods={[
    {
      signature: "getIconForFile(File)",
      returns: "File",
      description: <>Returns the current icon for file.</>
    },
    {
      signature: "applyIcon(File, ImageView)",
      returns: "boolean",
      description: <>Performs the applyIcon operation using the provided parameters (file, imageView).</>
    },
    {
      signature: "supportFile(File)",
      returns: "boolean",
      description: <>Performs the supportFile operation using the provided parameters (file).</>
    }
  ]}
/>
