import ApiDoc from '@site/src/components/ApiDoc';

# ObjectFile

Complete API reference for `ObjectFile`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Files

**Component:** no

---

```java
public class ObjectFile
```

<ApiDoc
  constructors={[
    {
      signature: "ObjectFile(String)",
      description: <>Executes <code>ObjectFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ObjectFile()",
      description: <>Creates a new instance of ObjectFile.</>
    }
  ]}
  methods={[
    {
      signature: "instantiate()",
      returns: "GameObject",
      description: <>Executes <code>instantiate</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setFilePath(String)",
      returns: "void",
      description: <>Executes <code>setFilePath</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "toString()",
      returns: "String",
      description: <>Returns the string representation.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executes <code>genericInterface</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
