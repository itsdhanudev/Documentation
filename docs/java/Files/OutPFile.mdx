import ApiDoc from '@site/src/components/ApiDoc';

# OutPFile

Complete API reference for `OutPFile`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Files

**Component:** no

---

```java
public class OutPFile
```

<ApiDoc
  constructors={[
    {
      signature: "OutPFile()",
      description: <>Creates a new instance of OutPFile.</>
    },
    {
      signature: "OutPFile(String)",
      description: <>Creates a new instance of OutPFile.</>
    }
  ]}
  methods={[
    {
      signature: "getFilePath()",
      returns: "String",
      description: <>Returns the current file path.</>
    },
    {
      signature: "setFilePath(String)",
      returns: "void",
      description: <>Sets or updates the file path.</>
    },
    {
      signature: "getAbsolutePath()",
      returns: "String",
      description: <>Returns the current absolute path.</>
    },
    {
      signature: "toString()",
      returns: "String",
      description: <>Returns the string representation.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executes <code>genericInterface</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
