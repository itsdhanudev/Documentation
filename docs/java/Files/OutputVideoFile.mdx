import ApiDoc from '@site/src/components/ApiDoc';

# OutputVideoFile

Complete API reference for `OutputVideoFile`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Files

**Component:** no

---

```java
public class OutputVideoFile
```

<ApiDoc
  constructors={[
    {
      signature: "OutputVideoFile(String)",
      description: <>Executes <code>OutputVideoFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "OutputVideoFile()",
      description: <>Executes <code>OutputVideoFile</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  methods={[
    {
      signature: "toString()",
      returns: "String",
      description: <>Returns the string representation.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executes <code>genericInterface</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
