import ApiDoc from '@site/src/components/ApiDoc';

# ProjectFile

Complete API reference for `ProjectFile`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Files

**Component:** no

---

```java
public class ProjectFile
```

<ApiDoc
  constructors={[
    {
      signature: "ProjectFile(String, String)",
      description: <>Executes <code>ProjectFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ProjectFile(String)",
      description: <>Creates a new instance of ProjectFile.</>
    }
  ]}
  methods={[
    {
      signature: "getFormat()",
      returns: "String",
      description: <>Returns the current format.</>
    },
    {
      signature: "getInputStream()",
      returns: "InputStream",
      description: <>Returns the current input stream.</>
    },
    {
      signature: "loadText()",
      returns: "String",
      description: <>Loads text into the object.</>
    },
    {
      signature: "toString()",
      returns: "String",
      description: <>Returns the string representation.</>
    },
    {
      signature: "copyTo(File)",
      returns: "boolean",
      description: <>Returns a copy of this ProjectFile instance.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executes <code>genericInterface</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
