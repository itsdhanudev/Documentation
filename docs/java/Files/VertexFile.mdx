import ApiDoc from '@site/src/components/ApiDoc';

# VertexFile

Complete API reference for `VertexFile`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Files

**Component:** no

---

```java
public class VertexFile
```

<ApiDoc
  constructors={[
    {
      signature: "VertexFile(String)",
      description: <>Executes <code>VertexFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "VertexFile()",
      description: <>Executes <code>VertexFile</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  methods={[
    {
      signature: "toString()",
      returns: "String",
      description: <>Returns the string representation.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executes <code>genericInterface</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
