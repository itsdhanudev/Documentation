import ApiDoc from '@site/src/components/ApiDoc';

# Font

Complete API reference for `Font`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Fonts

**Component:** no

---

```java
public class Font
```

<ApiDoc
  constructors={[
    {
      signature: "Font(Font)",
      description: <>Executes <code>Font</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Font(Engine)",
      description: <>Executes <code>Font</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "font",
      type: "Font",
      description: <>Represents the <code>font</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "getResolution()",
      returns: "int",
      description: <>Returns the resolution of the font.</>
    }
  ]}
/>
