import ApiDoc from '@site/src/components/ApiDoc';

# FontFile

Complete API reference for `FontFile`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Fonts

**Component:** no

---

```java
public class FontFile
```

<ApiDoc
  constructors={[
    {
      signature: "FontFile(String)",
      description: <>Executes <code>FontFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "FontFile()",
      description: <>Executes <code>FontFile</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  methods={[
    {
      signature: "toString()",
      returns: "String",
      description: <>Returns the class name.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executes <code>genericInterface</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
