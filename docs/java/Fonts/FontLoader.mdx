import ApiDoc from '@site/src/components/ApiDoc';

# FontLoader

Complete API reference for `FontLoader`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Fonts

**Component:** no

---

```java
public class FontLoader
```

<ApiDoc
  constructors={[
    {
      signature: "FontLoader(Font)",
      description: <>Executes <code>FontLoader</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "FontLoader(Engine)",
      description: <>Executes <code>FontLoader</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "font",
      type: "Font",
      description: <>Represents the <code>font</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  staticMethods={[
    {
      signature: "load(int)",
      returns: "Font",
      description: <>Loads data into the object.</>
    },
    {
      signature: "load(FontFile, int)",
      returns: "Font",
      description: <>Loads data into the object.</>
    },
    {
      signature: "load(File, int)",
      returns: "Font",
      description: <>Loads data into the object.</>
    }
  ]}
/>
