# FixedFrameListener

Complete API reference for `FixedFrameListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Framerate

**Component:** no

---

```java
public interface FixedFrameListener
```

## Methods (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"deltaTime"}])` | `void` | Performs the @MethodArgs operation. |
| `repeat(float)` | `void` | Executes `repeat` and returns after the underlying engine call finishes. |
