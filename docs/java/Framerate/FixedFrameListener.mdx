import ApiDoc from '@site/src/components/ApiDoc';

# FixedFrameListener

Complete API reference for `FixedFrameListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Framerate

**Component:** no

---

```java
public interface FixedFrameListener
```

<ApiDoc
  methods={[
    {
      signature: "repeat(float)",
      returns: "void",
      description: <>Executes <code>repeat</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
