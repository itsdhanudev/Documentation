import ApiDoc from '@site/src/components/ApiDoc';

# FixedFraming

Complete API reference for `FixedFraming`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Framerate

**Component:** no

---

```java
public class FixedFraming
```

<ApiDoc
  constructors={[
    {
      signature: "FixedFraming(int, FixedFrameListener)",
      description: <>Executes <code>FixedFraming</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "fixedFrames",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getFixedFrames</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "engineRepeat()",
      returns: "void",
      description: <>Executes <code>engineRepeat</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setAllowTimeScale(boolean)",
      returns: "void",
      description: <>Sets or updates the allow time scale.</>
    },
    {
      signature: "setDesyncLowFrameRate(boolean)",
      returns: "void",
      description: <>Sets or updates the desync low frame rate.</>
    },
    {
      signature: "setDesyncHighFrameRate(boolean)",
      returns: "void",
      description: <>Sets or updates the desync high frame rate.</>
    },
    {
      signature: "setFixedFrameListener(FixedFrameListener)",
      returns: "void",
      description: <>Sets or updates the fixed frame listener.</>
    }
  ]}
/>
