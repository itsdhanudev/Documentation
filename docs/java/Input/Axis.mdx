import ApiDoc from '@site/src/components/ApiDoc';

# Axis

Complete API reference for `Axis`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Input

**Component:** no

---

```java
public class Axis
```

<ApiDoc
  constructors={[
    {
      signature: "Axis(Axis)",
      description: <>Executes <code>Axis</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Axis(String)",
      description: <>Executes <code>Axis</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "axis",
      type: "Axis",
      description: <>Represents the <code>axis</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "name",
      type: "String",
      access: "get",
      description: <>Returns the name of the Axis.</>
    },
    {
      name: "value",
      type: "Vector2",
      access: "get/set",
      description: <>Returns the value of the Axis.</>
    }
  ]}
/>
