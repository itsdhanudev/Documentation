import ApiDoc from '@site/src/components/ApiDoc';

# InputDialogListener

Complete API reference for `InputDialogListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Input

**Component:** no

---

```java
public interface InputDialogListener
```

<ApiDoc
  methods={[
    {
      signature: "onFinish(String)",
      returns: "void",
      description: <>Executes <code>onFinish</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onCancel()",
      returns: "void",
      description: <>Executes <code>onCancel</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
