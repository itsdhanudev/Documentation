import ApiDoc from '@site/src/components/ApiDoc';

# Key

Complete API reference for `Key`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Input

**Component:** no

---

```java
public class Key
```

<ApiDoc
  constructors={[
    {
      signature: "Key(Key)",
      description: <>Executes <code>Key</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Key(String)",
      description: <>Executes <code>Key</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "key",
      type: "Key",
      description: <>Represents the <code>key</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "down",
      type: "boolean",
      access: "get",
      description: <>Checks whether the Key was just pressed. This returns true only on the first frame it was pressed.</>
    },
    {
      name: "name",
      type: "String",
      access: "get",
      description: <>Returns the name of the Key.</>
    },
    {
      name: "pressed",
      type: "boolean",
      access: "get",
      description: <>Checks whether the Key is currently being held down. This returns true on every frame while it remains pressed.</>
    },
    {
      name: "up",
      type: "boolean",
      access: "get",
      description: <>Checks whether the Key was just released. This returns true only on the first frame after it was released.</>
    }
  ]}
  methods={[
    {
      signature: "requestPress(Object)",
      returns: "void",
      description: <>Registers that the key is being held down by the specified reference.</>
    },
    {
      signature: "releasePress(Object)",
      returns: "void",
      description: <>Unregisters the key press held by the specified reference.</>
    }
  ]}
/>
