import ApiDoc from '@site/src/components/ApiDoc';

# PopupDialogListener

Complete API reference for `PopupDialogListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Input

**Component:** no

---

```java
public interface PopupDialogListener
```

<ApiDoc
  methods={[
    {
      signature: "onClicked()",
      returns: "void",
      description: <>Executes <code>onClicked</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
