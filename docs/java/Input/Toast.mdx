import ApiDoc from '@site/src/components/ApiDoc';

# Toast

Complete API reference for `Toast`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Input

**Component:** no

---

```java
public class Toast
```

<ApiDoc
  attributes={[
    {
      name: "LENGTH_LONG",
      type: "int",
      description: <>Represents the <code>LENGTH_LONG</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "LENGTH_SHORT",
      type: "int",
      description: <>Represents the <code>LENGTH_SHORT</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  staticMethods={[
    {
      signature: "showText(String, int)",
      returns: "void",
      description: <>Shows a toast message on the screen with the given text and duration (use Toast.LENGTH_SHORT or Toast.LENGTH_LONG for length).</>
    }
  ]}
/>
