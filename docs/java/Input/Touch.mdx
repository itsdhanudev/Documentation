import ApiDoc from '@site/src/components/ApiDoc';

# Touch

Complete API reference for `Touch`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Input

**Component:** no

---

```java
public class Touch
```

<ApiDoc
  constructors={[
    {
      signature: "Touch(Touch)",
      description: <>Executes <code>Touch</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Touch()",
      description: <>Executes <code>Touch</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "touch",
      type: "Touch",
      description: <>Represents the <code>touch</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "down",
      type: "boolean",
      access: "get/set",
      description: <>Checks whether the touch was just pressed. This returns true only on the first frame it was pressed.</>
    },
    {
      name: "fromMouse",
      type: "boolean",
      access: "get",
      description: <>Returns whether this touch came from a mouse click.</>
    },
    {
      name: "isFromMouse",
      type: "boolean",
      access: "set",
      description: <>Sets whether this touch came from a mouse click.</>
    },
    {
      name: "longDown",
      type: "boolean",
      access: "get/set",
      description: <>Checks whether the touch was just long-pressed. This returns true only on the first frame it was long-pressed.</>
    },
    {
      name: "longPressed",
      type: "boolean",
      access: "get/set",
      description: <>Checks whether the touch is being long-pressed. This returns true on every frame while it remains pressed for a long duration.</>
    },
    {
      name: "position",
      type: "Vector2",
      access: "get/set",
      description: <>Returns the position of the touch.</>
    },
    {
      name: "pressed",
      type: "boolean",
      access: "get/set",
      description: <>Checks whether the touch is currently being held down. This returns true on every frame while it remains pressed.</>
    },
    {
      name: "slide",
      type: "Vector2",
      access: "get/set",
      description: <>Returns the slide vector of the touch.</>
    },
    {
      name: "slided",
      type: "boolean",
      access: "get/set",
      description: <>Checks if the touch is slided.</>
    },
    {
      name: "up",
      type: "boolean",
      access: "get/set",
      description: <>Checks whether the touch was just released. This returns true only on the first frame after it was released.</>
    }
  ]}
  methods={[
    {
      signature: "isPerformClick()",
      returns: "boolean",
      description: <>Executes <code>isPerformClick</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setPerformClick()",
      returns: "void",
      description: <>Executes <code>setPerformClick</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
