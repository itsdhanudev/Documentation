import ApiDoc from '@site/src/components/ApiDoc';

# IntSlider

Complete API reference for `IntSlider`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Inspector

**Component:** no

---

```java
public class IntSlider
```

<ApiDoc
  constructors={[
    {
      signature: "IntSlider(int, int, int)",
      description: <>Creates a new instance of IntSlider.</>
    }
  ]}
  attributes={[
    {
      name: "value",
      type: "int",
      description: <>Represents the <code>value</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "min",
      type: "int",
      description: <>Represents the <code>min</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "max",
      type: "int",
      description: <>Represents the <code>max</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "max",
      type: "int",
      access: "get/set",
      description: <>Returns the maximum allowed value of the slider.</>
    },
    {
      name: "min",
      type: "int",
      access: "get/set",
      description: <>Returns the minimum allowed value of the slider.</>
    },
    {
      name: "value",
      type: "int",
      access: "get/set",
      description: <>Returns the current value of the slider.</>
    }
  ]}
  methods={[
    {
      signature: "toString()",
      returns: "String",
      description: <>Returns a string representation of the IntSlider.</>
    },
    {
      signature: "serialize()",
      returns: "String",
      description: <>Serializes the IntSlider into a string in the format '[value;min;max;step]'.</>
    }
  ]}
  staticMethods={[
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "deserialize(String)",
      returns: "IntSlider",
      description: <>Deserializes a string to create a IntSlider instance.</>
    }
  ]}
/>
