import ApiDoc from '@site/src/components/ApiDoc';

# PropertiesButtonListener

Complete API reference for `PropertiesButtonListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Inspector

**Component:** no

---

```java
public interface PropertiesButtonListener
```

<ApiDoc
  methods={[
    {
      signature: "onClicked()",
      returns: "void",
      description: <>Executes <code>onClicked</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
