import ApiDoc from '@site/src/components/ApiDoc';

# JClass

Complete API reference for `JClass`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Java

**Component:** no

---

```java
public class JClass
```

<ApiDoc
  constructors={[
    {
      signature: "JClass(Class, String, boolean)",
      description: <>Creates a new instance of JClass.</>
    }
  ]}
  attributes={[
    {
      name: "name",
      type: "String",
      description: <>Represents the <code>name</code> value stored on this instance. Read or assign to update the property.</>
    },
    {
      name: "isEngineNative",
      type: "boolean",
      description: <>Represents the <code>isEngineNative</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "getClassAddress()",
      returns: "Class",
      description: <>Returns the current class address.</>
    },
    {
      signature: "getName()",
      returns: "String",
      description: <>Returns the current name.</>
    },
    {
      signature: "isEngineNative()",
      returns: "boolean",
      description: <>Returns whether engine native is true.</>
    }
  ]}
/>
