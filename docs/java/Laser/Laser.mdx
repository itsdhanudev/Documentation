import ApiDoc from '@site/src/components/ApiDoc';

# Laser

Complete API reference for `Laser`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Laser

**Component:** no

---

```java
public class Laser
```

<ApiDoc
  constructors={[
    {
      signature: "Laser(Raycast)",
      description: <>Executes <code>Laser</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Laser()",
      description: <>Creates a new instance of Laser.</>
    },
    {
      signature: "Laser(Color)",
      description: <>Creates a new instance of Laser.</>
    }
  ]}
  attributes={[
    {
      name: "raycast",
      type: "Raycast",
      description: <>Represents the <code>raycast</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "calculateFaceUV",
      type: "boolean",
      access: "get/set",
      description: <>Returns whether the laser should try to calculate the UV of the face at the hit point.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Returns the color shown in the editor when the Laser hits something.</>
    },
    {
      name: "determineHitCollider",
      type: "boolean",
      access: "get/set",
      description: <>Returns whether the laser should try to find out which child collider was hit.</>
    },
    {
      name: "noHitColor",
      type: "Color",
      access: "get/set",
      description: <>Returns the color shown in the editor when the Laser hits nothing.</>
    },
    {
      name: "showGizmo",
      type: "boolean",
      access: "get/set",
      description: <>Returns whether the gizmo (laser visualization) is shown in the editor.</>
    }
  ]}
  methods={[
    {
      signature: "trace(JAVARuntime.Ray)",
      returns: "LaserHit",
      description: <>Traces a laser using the specified Ray.</>
    },
    {
      signature: "trace(Vector3, Vector3)",
      returns: "LaserHit",
      description: <>Traces a laser from a specified position and direction.</>
    },
    {
      signature: "trace(Vector3, Vector3, float)",
      returns: "LaserHit",
      description: <>Traces a laser from a specified position and direction, with a defined maximum distance.</>
    },
    {
      signature: "trace(Vector3, Vector3, float, PhysicsLayer)",
      returns: "LaserHit",
      description: <>Traces a laser from a specified position, direction, and distance with the indicated PhysicsLayer.</>
    },
    {
      signature: "trace(Vector3, Vector3, PhysicsLayer)",
      returns: "LaserHit",
      description: <>Traces a laser from a specified position and direction, in the indicated PhysicsLayer.</>
    },
    {
      signature: "traceAll(JAVARuntime.Ray)",
      returns: "List&lt;LaserHit&gt;",
      description: <>Traces a laser using the specified Ray and return all collisions.</>
    },
    {
      signature: "traceAll(Vector3, Vector3)",
      returns: "List&lt;LaserHit&gt;",
      description: <>Traces a laser from a specified position and direction and return all collisions.</>
    },
    {
      signature: "traceAll(Vector3, Vector3, float)",
      returns: "List&lt;LaserHit&gt;",
      description: <>Traces a laser from a specified position and direction, with a defined maximum distance and return all collisions.</>
    },
    {
      signature: "traceAll(Vector3, Vector3, float, PhysicsLayer)",
      returns: "List&lt;LaserHit&gt;",
      description: <>Traces a laser from a specified position, direction, and distance with the indicated PhysicsLayer and return all collisions.</>
    },
    {
      signature: "traceAll(Vector3, Vector3, PhysicsLayer)",
      returns: "List&lt;LaserHit&gt;",
      description: <>Traces a laser from a specified position and direction, in the indicated PhysicsLayer and return all collisions.</>
    },
    {
      signature: "traceSphere(JAVARuntime.Ray, float)",
      returns: "LaserHit",
      description: <>Traces a sphere sweep test laser using the specified Ray.</>
    },
    {
      signature: "traceSphere(Vector3, Vector3, float)",
      returns: "LaserHit",
      description: <>Traces a sphere sweep test laser from a specified position and direction.</>
    },
    {
      signature: "traceSphere(Vector3, Vector3, float, float)",
      returns: "LaserHit",
      description: <>Traces a sphere sweep test laser from a specified position and direction, with a defined maximum distance.</>
    },
    {
      signature: "traceSphere(Vector3, Vector3, float, PhysicsLayer, float)",
      returns: "LaserHit",
      description: <>Traces a sphere sweep test laser from a specified position, direction, and distance with the indicated PhysicsLayer.</>
    },
    {
      signature: "traceSphere(Vector3, Vector3, PhysicsLayer, float)",
      returns: "LaserHit",
      description: <>Traces a sphere sweep test laser from a specified position and direction, in the indicated PhysicsLayer.</>
    },
    {
      signature: "traceSphereAll(JAVARuntime.Ray, float)",
      returns: "List&lt;LaserHit&gt;",
      description: <>Traces a sphere sweep test laser using the specified Ray and return all collisions.</>
    },
    {
      signature: "traceSphereAll(Vector3, Vector3, float)",
      returns: "List&lt;LaserHit&gt;",
      description: <>Traces a sphere sweep test laser from a specified position and direction and return all collisions.</>
    },
    {
      signature: "traceSphereAll(Vector3, Vector3, float, float)",
      returns: "List&lt;LaserHit&gt;",
      description: <>Traces a sphere sweep test laser from a specified position and direction, with a defined maximum distance and return all collisions.</>
    },
    {
      signature: "traceSphereAll(Vector3, Vector3, float, PhysicsLayer, float)",
      returns: "List&lt;LaserHit&gt;",
      description: <>Traces a sphere sweep test laser from a specified position, direction, and distance with the indicated PhysicsLayer and return all collisions.</>
    },
    {
      signature: "traceSphereAll(Vector3, Vector3, PhysicsLayer, float)",
      returns: "List&lt;LaserHit&gt;",
      description: <>Traces a sphere sweep test laser from a specified position and direction, in the indicated PhysicsLayer and return all collisions.</>
    }
  ]}
/>
