import ApiDoc from '@site/src/components/ApiDoc';

# LaserHit

Complete API reference for `LaserHit`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Laser

**Component:** no

---

```java
public class LaserHit
```

<ApiDoc
  constructors={[
    {
      signature: "LaserHit(LaserHit)",
      description: <>Executes <code>LaserHit</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "LaserHit()",
      description: <>Executes <code>LaserHit</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "laserHit",
      type: "LaserHit",
      description: <>Represents the <code>laserHit</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "collider",
      type: "Collider",
      access: "get/set",
      description: <>Returns the Collider the Laser collided with.</>
    },
    {
      name: "colliderObject",
      type: "SpatialObject",
      access: "get/set",
      description: <>Returns the object the Laser collided with.</>
    },
    {
      name: "distance",
      type: "float",
      access: "get/set",
      description: <>Returns the distance between the Laser origin and the point it collided.</>
    },
    {
      name: "faceNormal",
      type: "Vector3",
      access: "get/set",
      description: <>Returns the normal vector of the face the Laser collided with.</>
    },
    {
      name: "normal",
      type: "Vector3",
      access: "get/set",
      description: <>Returns the normal vector of the Laser's direction.</>
    },
    {
      name: "object",
      type: "SpatialObject",
      access: "get/set",
      description: <>Returns the object hit by the Laser.</>
    },
    {
      name: "point",
      type: "Vector3",
      access: "get/set",
      description: <>Returns the point where the Laser hit.</>
    },
    {
      name: "uVCoord",
      type: "Vector2",
      access: "get/set",
      description: <>Returns the UV coordinates of the point the Laser collided with.</>
    }
  ]}
/>
