import ApiDoc from '@site/src/components/ApiDoc';

# RayDirection

Complete API reference for `RayDirection`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Laser

**Component:** no

---

```java
public class RayDirection
```

<ApiDoc
  constructors={[
    {
      signature: "RayDirection(RayDirection)",
      description: <>Executes <code>RayDirection</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "RayDirection()",
      description: <>Executes <code>RayDirection</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "RayDirection(Vector3, Vector3)",
      description: <>Creates a new instance of RayDirection.</>
    }
  ]}
  attributes={[
    {
      name: "rayDirection",
      type: "RayDirection",
      description: <>Represents the <code>rayDirection</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "getOrigin()",
      returns: "Vector3",
      description: <>Returns the origin position of this RayDirection.</>
    },
    {
      signature: "setOrigin(Vector3)",
      returns: "void",
      description: <>Sets the origin position of this RayDirection.</>
    },
    {
      signature: "getDirection()",
      returns: "Vector3",
      description: <>Returns the direction of this RayDirection.</>
    },
    {
      signature: "setDirection(Vector3)",
      returns: "void",
      description: <>Sets the direction of this RayDirection.</>
    }
  ]}
/>
