import ApiDoc from '@site/src/components/ApiDoc';

# ListStreamListener

Complete API reference for `ListStreamListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Lists

**Component:** no

---

```java
public interface ListStreamListener
```

<ApiDoc
  methods={[
    {
      signature: "execute(Object, int, Object)",
      returns: "void",
      description: <>Executes <code>execute</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onError(Exception, Error, int, Object)",
      returns: "void",
      description: <>Executes <code>onError</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onFinish(Object)",
      returns: "void",
      description: <>Executes <code>onFinish</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
