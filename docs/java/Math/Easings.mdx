import ApiDoc from '@site/src/components/ApiDoc';

# Easings

Complete API reference for `Easings`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Math

**Component:** no

---

```java
public class Easings
```

<ApiDoc
  staticMethods={[
    {
      signature: "easeInSine(float)",
      returns: "float",
      description: <>Performs the easeInSine operation using the provided parameters (x).</>
    },
    {
      signature: "easeOutSine(float)",
      returns: "float",
      description: <>Performs the easeOutSine operation using the provided parameters (x).</>
    },
    {
      signature: "easeInOutSine(float)",
      returns: "float",
      description: <>Performs the easeInOutSine operation using the provided parameters (x).</>
    },
    {
      signature: "easeInQuad(float)",
      returns: "float",
      description: <>Performs the easeInQuad operation using the provided parameters (x).</>
    },
    {
      signature: "easeOutQuad(float)",
      returns: "float",
      description: <>Performs the easeOutQuad operation using the provided parameters (x).</>
    },
    {
      signature: "easeInOutQuad(float)",
      returns: "float",
      description: <>Performs the easeInOutQuad operation using the provided parameters (x).</>
    },
    {
      signature: "easeInCubic(float)",
      returns: "float",
      description: <>Performs the easeInCubic operation using the provided parameters (x).</>
    },
    {
      signature: "easeOutCubic(float)",
      returns: "float",
      description: <>Performs the easeOutCubic operation using the provided parameters (x).</>
    },
    {
      signature: "easeInOutCubic(float)",
      returns: "float",
      description: <>Performs the easeInOutCubic operation using the provided parameters (x).</>
    },
    {
      signature: "easeInQuart(float)",
      returns: "float",
      description: <>Performs the easeInQuart operation using the provided parameters (x).</>
    },
    {
      signature: "easeOutQuart(float)",
      returns: "float",
      description: <>Performs the easeOutQuart operation using the provided parameters (x).</>
    },
    {
      signature: "easeInOutQuart(float)",
      returns: "float",
      description: <>Performs the easeInOutQuart operation using the provided parameters (x).</>
    },
    {
      signature: "easeInQuint(float)",
      returns: "float",
      description: <>Performs the easeInQuint operation using the provided parameters (x).</>
    },
    {
      signature: "easeOutQuint(float)",
      returns: "float",
      description: <>Performs the easeOutQuint operation using the provided parameters (x).</>
    },
    {
      signature: "easeInOutQuint(float)",
      returns: "float",
      description: <>Performs the easeInOutQuint operation using the provided parameters (x).</>
    },
    {
      signature: "easeInExpo(float)",
      returns: "float",
      description: <>Performs the easeInExpo operation using the provided parameters (x).</>
    },
    {
      signature: "easeOutExpo(float)",
      returns: "float",
      description: <>Performs the easeOutExpo operation using the provided parameters (x).</>
    },
    {
      signature: "easeInOutExpo(float)",
      returns: "float",
      description: <>Performs the easeInOutExpo operation using the provided parameters (x).</>
    },
    {
      signature: "easeInCirc(float)",
      returns: "float",
      description: <>Performs the easeInCirc operation using the provided parameters (x).</>
    },
    {
      signature: "easeOutCirc(float)",
      returns: "float",
      description: <>Performs the easeOutCirc operation using the provided parameters (x).</>
    },
    {
      signature: "easeInOutCirc(float)",
      returns: "float",
      description: <>Performs the easeInOutCirc operation using the provided parameters (x).</>
    },
    {
      signature: "easeInBack(float)",
      returns: "float",
      description: <>Performs the easeInBack operation using the provided parameters (x).</>
    },
    {
      signature: "easeOutBack(float)",
      returns: "float",
      description: <>Performs the easeOutBack operation using the provided parameters (x).</>
    },
    {
      signature: "easeInOutBack(float)",
      returns: "float",
      description: <>Performs the easeInOutBack operation using the provided parameters (x).</>
    },
    {
      signature: "easeInElastic(float)",
      returns: "float",
      description: <>Performs the easeInElastic operation using the provided parameters (x).</>
    },
    {
      signature: "easeOutElastic(float)",
      returns: "float",
      description: <>Performs the easeOutElastic operation using the provided parameters (x).</>
    },
    {
      signature: "easeInOutElastic(float)",
      returns: "float",
      description: <>Performs the easeInOutElastic operation using the provided parameters (x).</>
    },
    {
      signature: "easeInBounce(float)",
      returns: "float",
      description: <>Performs the easeInBounce operation using the provided parameters (x).</>
    },
    {
      signature: "easeOutBounce(float)",
      returns: "float",
      description: <>Performs the easeOutBounce operation using the provided parameters (x).</>
    },
    {
      signature: "easeInOutBounce(float)",
      returns: "float",
      description: <>Performs the easeInOutBounce operation using the provided parameters (x).</>
    }
  ]}
/>
