import ApiDoc from '@site/src/components/ApiDoc';

# Random

Complete API reference for `Random`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Math

**Component:** no

---

```java
public class Random
```

<ApiDoc
  staticMethods={[
    {
      signature: "range(int, int)",
      returns: "int",
      description: <>Returns a random integer value between min and max.</>
    },
    {
      signature: "range(float, float)",
      returns: "float",
      description: <>Returns a random float value between min and max.</>
    }
  ]}
/>
