import ApiDoc from '@site/src/components/ApiDoc';

# SpatialObject

Complete API reference for `SpatialObject`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Object

**Component:** no

---

```java
public class SpatialObject
```

<ApiDoc
  constructors={[
    {
      signature: "SpatialObject(GameObject)",
      description: <>Executes <code>SpatialObject</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SpatialObject(Engine)",
      description: <>Executes <code>SpatialObject</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SpatialObject()",
      description: <>Executes <code>SpatialObject</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "SpatialObject(SpatialObject)",
      description: <>Creates a new instance of SpatialObject.</>
    },
    {
      signature: "SpatialObject(String)",
      description: <>Creates a new instance of SpatialObject.</>
    },
    {
      signature: "SpatialObject(String, SpatialObject)",
      description: <>Creates a new instance of SpatialObject.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "WeakReference&lt;GameObject&gt;",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "allowAnimations",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isAllowAnimations</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "childCount",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getChildCount</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "childList",
      type: "List&lt;SpatialObject&gt;",
      access: "get",
      description: <>Virtual attribute exposed by <code>getChildList</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "componentsList",
      type: "List&lt;Component&gt;",
      access: "get",
      description: <>Virtual attribute exposed by <code>getComponentsList</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "dontDestroyOnLoad",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isDontDestroyOnLoad</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "enabled",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isEnabled</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "gUID",
      type: "GUID",
      access: "get",
      description: <>Virtual attribute exposed by <code>getGUID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "globalMatrix",
      type: "float[]",
      access: "get",
      description: <>Virtual attribute exposed by <code>getGlobalMatrix</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "globalPosition",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGlobalPosition</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "globalRotation",
      type: "Quaternion",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGlobalRotation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "globalScale",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getGlobalScale</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "localMatrix",
      type: "float[]",
      access: "get",
      description: <>Virtual attribute exposed by <code>getLocalMatrix</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "mainParent",
      type: "SpatialObject",
      access: "get",
      description: <>Virtual attribute exposed by <code>getMainParent</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "metaID",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMetaID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "name",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getName</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "parent",
      type: "SpatialObject",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getParent</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "position",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getPosition</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "rotation",
      type: "Quaternion",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRotation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scale",
      type: "Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScale</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "static",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isStatic</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "tag",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTag</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "transform",
      type: "Transform",
      access: "get",
      description: <>Virtual attribute exposed by <code>getTransform</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "setNameOH(JAVARuntime.OHString)",
      returns: "void",
      description: <>Sets or updates the name oh.</>
    },
    {
      signature: "compareName(String)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "duplicate()",
      returns: "SpatialObject",
      description: <>Executes <code>duplicate</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "destroy()",
      returns: "void",
      description: <>Executes <code>destroy</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEvent(String)",
      returns: "void",
      description: <>Executes <code>sendEvent</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEvent(String, int)",
      returns: "void",
      description: <>Executes <code>sendEvent</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEvent(String, float)",
      returns: "void",
      description: <>Executes <code>sendEvent</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEvent(String, String)",
      returns: "void",
      description: <>Executes <code>sendEvent</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEvent(String, boolean)",
      returns: "void",
      description: <>Executes <code>sendEvent</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEvent(String, Object...)",
      returns: "void",
      description: <>Executes <code>sendEvent</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEventToHierarchy(String)",
      returns: "void",
      description: <>Executes <code>sendEventToHierarchy</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEventToHierarchy(String, int)",
      returns: "void",
      description: <>Executes <code>sendEventToHierarchy</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEventToHierarchy(String, float)",
      returns: "void",
      description: <>Executes <code>sendEventToHierarchy</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEventToHierarchy(String, String)",
      returns: "void",
      description: <>Executes <code>sendEventToHierarchy</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEventToHierarchy(String, boolean)",
      returns: "void",
      description: <>Executes <code>sendEventToHierarchy</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendEventToHierarchy(String, Object...)",
      returns: "void",
      description: <>Executes <code>sendEventToHierarchy</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "callFunction(String)",
      returns: "void",
      description: <>Performs the callFunction operation using the provided parameters (name).</>
    },
    {
      signature: "callFunction(String, int)",
      returns: "void",
      description: <>Performs the callFunction operation using the provided parameters (name, args).</>
    },
    {
      signature: "callFunction(String, float)",
      returns: "void",
      description: <>Performs the callFunction operation using the provided parameters (name, args).</>
    },
    {
      signature: "callFunction(String, String)",
      returns: "void",
      description: <>Performs the callFunction operation using the provided parameters (name, args).</>
    },
    {
      signature: "callFunction(String, boolean)",
      returns: "void",
      description: <>Performs the callFunction operation using the provided parameters (name, args).</>
    },
    {
      signature: "callFunction(String, Object...)",
      returns: "void",
      description: <>Performs the callFunction operation using the provided parameters (name, args).</>
    },
    {
      signature: "instantiate(ObjectFile)",
      returns: "SpatialObject",
      description: <>Performs the instantiate operation using the provided parameters (pFile).</>
    },
    {
      signature: "instantiate(ObjectFile, Vector3)",
      returns: "SpatialObject",
      description: <>Performs the instantiate operation using the provided parameters (pFile, position).</>
    },
    {
      signature: "instantiate(ObjectFile, Vector3, Quaternion)",
      returns: "SpatialObject",
      description: <>Performs the instantiate operation using the provided parameters (pFile, position, rotation).</>
    },
    {
      signature: "instantiate(ObjectFile, Vector3, Quaternion, Vector3)",
      returns: "SpatialObject",
      description: <>Performs the instantiate operation using the provided parameters (pFile, position, rotation, scale).</>
    },
    {
      signature: "instantiateHasChild(ObjectFile)",
      returns: "SpatialObject",
      description: <>Performs the instantiateHasChild operation using the provided parameters (pFile).</>
    },
    {
      signature: "instantiateHasChild(ObjectFile, SpatialObject)",
      returns: "SpatialObject",
      description: <>Performs the instantiateHasChild operation using the provided parameters (pFile, parent).</>
    },
    {
      signature: "instantiateAsChild(ObjectFile)",
      returns: "SpatialObject",
      description: <>Performs the instantiateAsChild operation using the provided parameters (pFile).</>
    },
    {
      signature: "instantiateAsChild(ObjectFile, SpatialObject)",
      returns: "SpatialObject",
      description: <>Performs the instantiateAsChild operation using the provided parameters (pFile, parent).</>
    },
    {
      signature: "findComponent(String)",
      returns: "<T extends Component> T",
      description: <>Performs the findComponent operation using the provided parameters (tittle).</>
    },
    {
      signature: "findComponents(String)",
      returns: "List&lt;Component&gt;",
      description: <>Performs the findComponents operation using the provided parameters (tittle).</>
    },
    {
      signature: "findComponent(Class)",
      returns: "<T extends Component> T",
      description: <>Performs the findComponent operation using the provided parameters (classReference).</>
    },
    {
      signature: "findComponents(Class)",
      returns: "List&lt;Component&gt;",
      description: <>Performs the findComponents operation using the provided parameters (classReference).</>
    },
    {
      signature: "findComponentInChildren(String)",
      returns: "<T extends Component> T",
      description: <>Performs the findComponentInChildren operation using the provided parameters (tittle).</>
    },
    {
      signature: "findComponentInChildren(Class)",
      returns: "<T extends Component> T",
      description: <>Performs the findComponentInChildren operation using the provided parameters (classReference).</>
    },
    {
      signature: "findComponentInParent(String)",
      returns: "<T extends Component> T",
      description: <>Executes <code>findComponentInParent</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "findComponentInParent(Class)",
      returns: "<T extends Component> T",
      description: <>Executes <code>findComponentInParent</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "removeComponent(Component)",
      returns: "void",
      description: <>Removes component from the collection.</>
    },
    {
      signature: "addComponent(Component)",
      returns: "void",
      description: <>Adds component to the collection.</>
    },
    {
      signature: "addComponent(Component, Class)",
      returns: "void",
      description: <>Adds component to the collection.</>
    },
    {
      signature: "addComponent(Component)",
      returns: "void",
      description: <>Adds component to the collection.</>
    },
    {
      signature: "removeParent()",
      returns: "void",
      description: <>Removes parent from the collection.</>
    },
    {
      signature: "findChildObject(String)",
      returns: "SpatialObject",
      description: <>Performs the findChildObject operation using the provided parameters (name).</>
    },
    {
      signature: "findChildObject(JAVARuntime.OHString)",
      returns: "SpatialObject",
      description: <>Performs the findChildObject operation using the provided parameters (name).</>
    },
    {
      signature: "getChildAt(int)",
      returns: "SpatialObject",
      description: <>Returns the current child at.</>
    },
    {
      signature: "getChildIndex(SpatialObject)",
      returns: "int",
      description: <>Returns the current child index.</>
    },
    {
      signature: "swapChildrenPosition(SpatialObject, SpatialObject)",
      returns: "void",
      description: <>Performs the swapChildrenPosition operation using the provided parameters (a, b).</>
    },
    {
      signature: "componentCount()",
      returns: "int",
      description: <>Executes <code>componentCount</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getComponentAt(int)",
      returns: "Component",
      description: <>Returns the current component at.</>
    },
    {
      signature: "getComponentsAt(int)",
      returns: "Component",
      description: <>Returns the current components at.</>
    },
    {
      signature: "componentAt(int)",
      returns: "Component",
      description: <>Performs the componentAt operation using the provided parameters (idx).</>
    },
    {
      signature: "distance(SpatialObject)",
      returns: "float",
      description: <>Performs the distance operation using the provided parameters (other).</>
    },
    {
      signature: "distance(Transform)",
      returns: "float",
      description: <>Performs the distance operation using the provided parameters (other).</>
    },
    {
      signature: "distance(Vector3)",
      returns: "float",
      description: <>Performs the distance operation using the provided parameters (other).</>
    },
    {
      signature: "sqrtDistance(SpatialObject)",
      returns: "float",
      description: <>Performs the sqrtDistance operation using the provided parameters (other).</>
    },
    {
      signature: "sqrtDistance(Transform)",
      returns: "float",
      description: <>Performs the sqrtDistance operation using the provided parameters (other).</>
    },
    {
      signature: "sqrtDistance(Vector3)",
      returns: "float",
      description: <>Performs the sqrtDistance operation using the provided parameters (other).</>
    },
    {
      signature: "setPosition(float, float, float)",
      returns: "void",
      description: <>Sets or updates the position.</>
    },
    {
      signature: "setPositionX(float)",
      returns: "void",
      description: <>Sets or updates the position x.</>
    },
    {
      signature: "setPositionY(float)",
      returns: "void",
      description: <>Sets or updates the position y.</>
    },
    {
      signature: "setPositionZ(float)",
      returns: "void",
      description: <>Sets or updates the position z.</>
    },
    {
      signature: "setRotation(float, float, float)",
      returns: "void",
      description: <>Sets or updates the rotation.</>
    },
    {
      signature: "setRotation(float, float, float, float)",
      returns: "void",
      description: <>Sets or updates the rotation.</>
    },
    {
      signature: "setScale(float, float, float)",
      returns: "void",
      description: <>Sets or updates the scale.</>
    },
    {
      signature: "setScale(float)",
      returns: "void",
      description: <>Sets or updates the scale.</>
    },
    {
      signature: "getGlobalPosition(Vector3)",
      returns: "Vector3",
      description: <>Returns the current global position.</>
    },
    {
      signature: "getGlobalRotation(Quaternion)",
      returns: "Quaternion",
      description: <>Returns the current global rotation.</>
    },
    {
      signature: "getGlobalScale(Vector3)",
      returns: "Vector3",
      description: <>Returns the current global scale.</>
    },
    {
      signature: "translate(float, float, float)",
      returns: "void",
      description: <>Transforms or moves the object accordingly.</>
    },
    {
      signature: "move(float, float, float)",
      returns: "void",
      description: <>Transforms or moves the object accordingly.</>
    },
    {
      signature: "rotate(float, float, float)",
      returns: "void",
      description: <>Transforms or moves the object accordingly.</>
    },
    {
      signature: "translateInSeconds(float, float, float)",
      returns: "void",
      description: <>Transforms or moves in seconds accordingly.</>
    },
    {
      signature: "moveInSeconds(float, float, float)",
      returns: "void",
      description: <>Transforms or moves in seconds accordingly.</>
    },
    {
      signature: "rotateInSeconds(float, float, float)",
      returns: "void",
      description: <>Transforms or moves in seconds accordingly.</>
    },
    {
      signature: "translateInSeconds(int, int, int)",
      returns: "void",
      description: <>Executes <code>translateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "translateInSeconds(int, int, float)",
      returns: "void",
      description: <>Executes <code>translateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "translateInSeconds(int, float, int)",
      returns: "void",
      description: <>Executes <code>translateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "translateInSeconds(int, float, float)",
      returns: "void",
      description: <>Executes <code>translateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "translateInSeconds(float, int, int)",
      returns: "void",
      description: <>Executes <code>translateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "translateInSeconds(float, int, float)",
      returns: "void",
      description: <>Executes <code>translateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "translateInSeconds(float, float, int)",
      returns: "void",
      description: <>Executes <code>translateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "moveInSeconds(int, int, int)",
      returns: "void",
      description: <>Executes <code>moveInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "moveInSeconds(int, int, float)",
      returns: "void",
      description: <>Executes <code>moveInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "moveInSeconds(int, float, int)",
      returns: "void",
      description: <>Executes <code>moveInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "moveInSeconds(int, float, float)",
      returns: "void",
      description: <>Executes <code>moveInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "moveInSeconds(float, int, int)",
      returns: "void",
      description: <>Executes <code>moveInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "moveInSeconds(float, int, float)",
      returns: "void",
      description: <>Executes <code>moveInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "moveInSeconds(float, float, int)",
      returns: "void",
      description: <>Executes <code>moveInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "rotateInSeconds(int, int, int)",
      returns: "void",
      description: <>Executes <code>rotateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "rotateInSeconds(int, int, float)",
      returns: "void",
      description: <>Executes <code>rotateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "rotateInSeconds(int, float, int)",
      returns: "void",
      description: <>Executes <code>rotateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "rotateInSeconds(int, float, float)",
      returns: "void",
      description: <>Executes <code>rotateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "rotateInSeconds(float, int, int)",
      returns: "void",
      description: <>Executes <code>rotateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "rotateInSeconds(float, int, float)",
      returns: "void",
      description: <>Executes <code>rotateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "rotateInSeconds(float, float, int)",
      returns: "void",
      description: <>Executes <code>rotateInSeconds</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "lookTo(SpatialObject)",
      returns: "void",
      description: <>Executes <code>lookTo</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "lookTo(Vector3)",
      returns: "void",
      description: <>Performs the lookTo operation using the provided parameters (position).</>
    },
    {
      signature: "lookTo(float, float, float)",
      returns: "void",
      description: <>Performs the lookTo operation using the provided parameters (x, y, z).</>
    },
    {
      signature: "lookTo(SpatialObject, boolean, boolean)",
      returns: "void",
      description: <>Executes <code>lookTo</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "lookTo(Vector3, boolean, boolean)",
      returns: "void",
      description: <>Executes <code>lookTo</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "lookTo(float, float, float, boolean, boolean)",
      returns: "void",
      description: <>Executes <code>lookTo</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "lookToIgnoreY(SpatialObject)",
      returns: "void",
      description: <>Performs the lookToIgnoreY operation using the provided parameters (object).</>
    },
    {
      signature: "lookToIgnoreY(Vector3)",
      returns: "void",
      description: <>Performs the lookToIgnoreY operation using the provided parameters (position).</>
    },
    {
      signature: "teleportTo(SpatialObject)",
      returns: "void",
      description: <>Performs the teleportTo operation using the provided parameters (object).</>
    },
    {
      signature: "teleportTo(Vector3)",
      returns: "void",
      description: <>Performs the teleportTo operation using the provided parameters (position).</>
    },
    {
      signature: "smoothLookTo(SpatialObject, float)",
      returns: "void",
      description: <>Performs the smoothLookTo operation using the provided parameters (other, lerpSpeed).</>
    },
    {
      signature: "smoothLookTo(Vector3, float)",
      returns: "void",
      description: <>Performs the smoothLookTo operation using the provided parameters (other, lerpSpeed).</>
    },
    {
      signature: "lerpLookTo(SpatialObject, float)",
      returns: "void",
      description: <>Performs the lerpLookTo operation using the provided parameters (other, lerpSpeed).</>
    },
    {
      signature: "lerpLookTo(Vector3, float)",
      returns: "void",
      description: <>Performs the lerpLookTo operation using the provided parameters (other, lerpSpeed).</>
    },
    {
      signature: "lerpLookToIgnoreY(SpatialObject, float)",
      returns: "void",
      description: <>Performs the lerpLookToIgnoreY operation using the provided parameters (other, lerpSpeed).</>
    },
    {
      signature: "lerpLookToIgnoreY(Vector3, float)",
      returns: "void",
      description: <>Performs the lerpLookToIgnoreY operation using the provided parameters (other, lerpSpeed).</>
    },
    {
      signature: "transformPoint(Vector3)",
      returns: "Vector3",
      description: <>Performs the transformPoint operation using the provided parameters (vector3).</>
    },
    {
      signature: "transformPoint(Vector3, Vector3)",
      returns: "void",
      description: <>Performs the transformPoint operation using the provided parameters (vector3, out).</>
    },
    {
      signature: "inverseTransformPoint(Vector3)",
      returns: "Vector3",
      description: <>Performs the inverseTransformPoint operation using the provided parameters (vector3).</>
    },
    {
      signature: "inverseTransformPoint(Vector3, Vector3)",
      returns: "void",
      description: <>Performs the inverseTransformPoint operation using the provided parameters (vector3, out).</>
    },
    {
      signature: "transformDirection(Vector3)",
      returns: "Vector3",
      description: <>Performs the transformDirection operation using the provided parameters (vector3).</>
    },
    {
      signature: "transformDirection(Vector3, Vector3)",
      returns: "void",
      description: <>Performs the transformDirection operation using the provided parameters (vector3, out).</>
    },
    {
      signature: "inverseTransformDirection(Vector3)",
      returns: "Vector3",
      description: <>Performs the inverseTransformDirection operation using the provided parameters (vector3).</>
    },
    {
      signature: "inverseTransformDirection(Vector3, Vector3)",
      returns: "void",
      description: <>Performs the inverseTransformDirection operation using the provided parameters (vector3, out).</>
    },
    {
      signature: "forward()",
      returns: "Vector3",
      description: <>Executes <code>forward</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "back()",
      returns: "Vector3",
      description: <>Executes <code>back</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "right()",
      returns: "Vector3",
      description: <>Executes <code>right</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "left()",
      returns: "Vector3",
      description: <>Executes <code>left</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "up()",
      returns: "Vector3",
      description: <>Executes <code>up</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "down()",
      returns: "Vector3",
      description: <>Executes <code>down</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "forward(Vector3)",
      returns: "Vector3",
      description: <>Performs the forward operation using the provided parameters (out).</>
    },
    {
      signature: "back(Vector3)",
      returns: "Vector3",
      description: <>Performs the back operation using the provided parameters (out).</>
    },
    {
      signature: "right(Vector3)",
      returns: "Vector3",
      description: <>Performs the right operation using the provided parameters (out).</>
    },
    {
      signature: "left(Vector3)",
      returns: "Vector3",
      description: <>Performs the left operation using the provided parameters (out).</>
    },
    {
      signature: "up(Vector3)",
      returns: "Vector3",
      description: <>Performs the up operation using the provided parameters (out).</>
    },
    {
      signature: "down(Vector3)",
      returns: "Vector3",
      description: <>Performs the down operation using the provided parameters (out).</>
    },
    {
      signature: "getState()",
      returns: "int",
      description: <>Returns the current state.</>
    },
    {
      signature: "setState(int)",
      returns: "void",
      description: <>Sets or updates the state.</>
    },
    {
      signature: "getGlobalMatrix(float[])",
      returns: "float[]",
      description: <>Returns the current global matrix.</>
    },
    {
      signature: "getLocalMatrix(float[])",
      returns: "float[]",
      description: <>Returns the current local matrix.</>
    },
    {
      signature: "mulGlobalVector3(Vector3)",
      returns: "Vector3",
      description: <>Performs the mulGlobalVector3 operation using the provided parameters (vector3).</>
    },
    {
      signature: "mulLocalVector3(Vector3)",
      returns: "Vector3",
      description: <>Performs the mulLocalVector3 operation using the provided parameters (vector3).</>
    },
    {
      signature: "recalculateMatrices()",
      returns: "void",
      description: <>Executes <code>recalculateMatrices</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "toJson()",
      returns: "String",
      description: <>Executes <code>toJson</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "exists()",
      returns: "boolean",
      description: <>Executes <code>exists</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "compareTag(String)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "validate()",
      returns: "boolean",
      description: <>Executes <code>validate</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  staticMethods={[
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "loadFile(ObjectFile)",
      returns: "SpatialObject",
      description: <>Loads file into the object.</>
    }
  ]}
/>
