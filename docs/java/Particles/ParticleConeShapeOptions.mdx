import ApiDoc from '@site/src/components/ApiDoc';

# ParticleConeShapeOptions

Complete API reference for `ParticleConeShapeOptions`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Particles

**Component:** no

---

```java
public class ParticleConeShapeOptions
```

<ApiDoc
  constructors={[
    {
      signature: "ParticleConeShapeOptions(ParticleConeShapeOptions)",
      description: <>Executes <code>ParticleConeShapeOptions</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ParticleConeShapeOptions(Engine)",
      description: <>Executes <code>ParticleConeShapeOptions</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ParticleConeShapeOptions()",
      description: <>Creates a new instance of ParticleConeShapeOptions.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ParticleConeShapeOptions",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "maxAngle",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxAngle</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "minAngle",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMinAngle</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "radius",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRadius</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
