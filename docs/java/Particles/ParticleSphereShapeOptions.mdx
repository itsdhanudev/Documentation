import ApiDoc from '@site/src/components/ApiDoc';

# ParticleSphereShapeOptions

Complete API reference for `ParticleSphereShapeOptions`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Particles

**Component:** no

---

```java
public class ParticleSphereShapeOptions
```

<ApiDoc
  constructors={[
    {
      signature: "ParticleSphereShapeOptions(ParticleSphereShapeOptions)",
      description: <>Executes <code>ParticleSphereShapeOptions</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ParticleSphereShapeOptions(Engine)",
      description: <>Executes <code>ParticleSphereShapeOptions</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ParticleSphereShapeOptions()",
      description: <>Creates a new instance of ParticleSphereShapeOptions.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ParticleSphereShapeOptions",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "radius",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRadius</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "randomizeRotation",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRandomizeRotation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "spherizeRotation",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSpherizeRotation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "volume",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getVolume</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
