# AreaTrigger

Complete API reference for `AreaTrigger`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Physics

**Component:** no

---

```java
public class AreaTrigger extends PhysicsComponent
```

## Constructors (2)

| Signature | Description |
| --- | --- |
| AreaTrigger([AreaTrigger]) | Executes `AreaTrigger` and returns after the underlying engine call finishes. |
| AreaTrigger() | Executes `AreaTrigger` and returns after the underlying engine call finishes. |

## Attributes (1)

| Name | Type | Description |
| --- | --- | --- |
| `component` | [AreaTrigger] | Represents the `component` value stored on this instance. Read or assign to update the property. |
