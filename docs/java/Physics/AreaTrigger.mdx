import ApiDoc from '@site/src/components/ApiDoc';

# AreaTrigger

Complete API reference for `AreaTrigger`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Physics

**Component:** no

---

```java
public class AreaTrigger extends PhysicsComponent
```

<ApiDoc
  constructors={[
    {
      signature: "AreaTrigger(AreaTrigger)",
      description: <>Executes <code>AreaTrigger</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "AreaTrigger()",
      description: <>Executes <code>AreaTrigger</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "AreaTrigger",
      description: <>Represents the <code>component</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
/>
