import ApiDoc from '@site/src/components/ApiDoc';

# Collision

Complete API reference for `Collision`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Physics

**Component:** no

---

```java
public class Collision
```

<ApiDoc
  constructors={[
    {
      signature: "Collision(Collision)",
      description: <>Executes <code>Collision</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Collision()",
      description: <>Executes <code>Collision</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Collision",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "collider",
      type: "Collider",
      access: "get/set",
      description: <>Returns the collider associated with this collision.</>
    },
    {
      name: "colliderObject",
      type: "SpatialObject",
      access: "get/set",
      description: <>Returns the object associated with the Collider in this collision.</>
    },
    {
      name: "contactCount",
      type: "int",
      access: "get",
      description: <>Returns the number of contact points in the collision.</>
    },
    {
      name: "contactPoint",
      type: "Vector3",
      access: "get/set",
      description: <>Returns the contact position of the first contact point (deprecated, get a Contact using contactAt() or getContactList() and call getContactPoint() on it).</>
    },
    {
      name: "distance",
      type: "float",
      access: "get/set",
      description: <>Returns the penetration distance at the first contact point (deprecated, get a Contact using contactAt() or getContactList() and call getDistance() on it).</>
    },
    {
      name: "impulse",
      type: "float",
      access: "get/set",
      description: <>Returns the impulse magnitude at the first contact point (deprecated, get a Contact using contactAt() or getContactList() and call getImpulse() on it).</>
    },
    {
      name: "normal",
      type: "Vector3",
      access: "get/set",
      description: <>Returns the normal vector of the first contact point (deprecated, get a Contact using contactAt() or getContactList() and call getNormal() on it).</>
    },
    {
      name: "object",
      type: "SpatialObject",
      access: "get/set",
      description: <>Returns the other object involved in the collision.</>
    },
    {
      name: "physicsComponent",
      type: "PhysicsComponent",
      access: "get/set",
      description: <>Returns the physics of the other object involved in the collision.</>
    }
  ]}
  methods={[
    {
      signature: "contactCount()",
      returns: "int",
      description: <>Returns the number of contact points in the collision.</>
    },
    {
      signature: "contactAt(int)",
      returns: "Contact",
      description: <>Returns the contact at the specified index.</>
    },
    {
      signature: "getContactList()",
      returns: "List&lt;Contact&gt;",
      description: <>Returns the list of contact points in the collision.</>
    }
  ]}
/>
