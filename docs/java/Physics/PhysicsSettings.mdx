import ApiDoc from '@site/src/components/ApiDoc';

# PhysicsSettings

Complete API reference for `PhysicsSettings`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Physics

**Component:** no

---

```java
public class PhysicsSettings
```

<ApiDoc
  constructors={[
    {
      signature: "PhysicsSettings(PhysicsSettings)",
      description: <>Executes <code>PhysicsSettings</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "PhysicsSettings()",
      description: <>Executes <code>PhysicsSettings</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "physicsSettings",
      type: "PhysicsSettings",
      description: <>Represents the <code>physicsSettings</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "gravity",
      type: "Vector3",
      access: "get/set",
      description: <>Returns the gravity of the world.</>
    }
  ]}
  methods={[
    {
      signature: "setGravity(float, float, float)",
      returns: "void",
      description: <>Sets the gravity of the world with the specified x, y, and z values.</>
    }
  ]}
/>
