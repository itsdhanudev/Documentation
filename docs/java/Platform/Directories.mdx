# Directories

Complete API reference for `Directories`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Platform

**Component:** no

---

```java
public class Directories
```

## Static Methods (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `internal()` | `String` | Executes `internal` and returns after the underlying engine call finishes. |
| `getProjectFolder()` | `String` | Returns the current project folder. |
