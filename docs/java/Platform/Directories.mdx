import ApiDoc from '@site/src/components/ApiDoc';

# Directories

Complete API reference for `Directories`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Platform

**Component:** no

---

```java
public class Directories
```

<ApiDoc
  staticMethods={[
    {
      signature: "internal()",
      returns: "String",
      description: <>Executes <code>internal</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getProjectFolder()",
      returns: "String",
      description: <>Returns the current project folder.</>
    }
  ]}
/>
