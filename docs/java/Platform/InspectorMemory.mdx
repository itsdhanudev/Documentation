import ApiDoc from '@site/src/components/ApiDoc';

# InspectorMemory

Complete API reference for `InspectorMemory`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Platform

**Component:** no

---

```java
public class InspectorMemory
```

<ApiDoc
  methods={[
    {
      signature: "HashMap&lt;&gt;()",
      returns: "Map&lt;String, String&gt; dataBase = new",
      description: <>Performs the HashMap&lt;&gt; operation.</>
    },
    {
      signature: "store(String, String)",
      returns: "void",
      description: <>Executes <code>store</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "load(String)",
      returns: "String",
      description: <>Executes <code>load</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "clear()",
      returns: "void",
      description: <>Executes <code>clear</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
