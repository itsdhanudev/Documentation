import ApiDoc from '@site/src/components/ApiDoc';

# TagEnemyFieldOfView

Complete API reference for `TagEnemyFieldOfView`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Prototyping

**Component:** no

---

```java
public class TagEnemyFieldOfView
```

<ApiDoc
  constructors={[
    {
      signature: "TagEnemyFieldOfView(TagEnemyFieldOfView)",
      description: <>Executes <code>TagEnemyFieldOfView</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "TagEnemyFieldOfView(Engine)",
      description: <>Executes <code>TagEnemyFieldOfView</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "TagEnemyFieldOfView()",
      description: <>Creates a new instance of TagEnemyFieldOfView.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "TagEnemyFieldOfView",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "active",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isActive</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "fov",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getFov</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMaxDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "minDistance",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMinDistance</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "targetTag",
      type: "String",
      access: "get",
      description: <>Virtual attribute exposed by <code>getTargetTag</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "targetsTag",
      type: "String",
      access: "set",
      description: <>Virtual attribute exposed by <code>setTargetsTag</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
