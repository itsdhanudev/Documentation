import ApiDoc from '@site/src/components/ApiDoc';

# Json

Complete API reference for `Json`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Serializer

**Component:** no

---

```java
public class Json
```

<ApiDoc
  constructors={[
    {
      signature: "Json()",
      description: <>Creates a new instance of Json.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "ignorePrivateFields",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isIgnorePrivateFields</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ignoreProtectedFields",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isIgnoreProtectedFields</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "ignorePublicFields",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isIgnorePublicFields</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "lenient",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isLenient</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "prettyPrinting",
      type: "boolean",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>isPrettyPrinting</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "toJsonString(Object)",
      returns: "String",
      description: <>Returns the string representation.</>
    },
    {
      signature: "fromJsonString(String, Class)",
      returns: "Object",
      description: <>Performs the fromJsonString operation using the provided parameters (json, classType).</>
    }
  ]}
  staticMethods={[
    {
      signature: "toJson(Object)",
      returns: "String",
      description: <>Performs the toJson operation using the provided parameters (object).</>
    },
    {
      signature: "toJson(Object, boolean)",
      returns: "String",
      description: <>Performs the toJson operation using the provided parameters (object, prettyPrinting).</>
    },
    {
      signature: "toJson(Object, boolean, boolean)",
      returns: "String",
      description: <>Performs the toJson operation using the provided parameters (object, prettyPrinting, lenient).</>
    },
    {
      signature: "toJson(Object, boolean, boolean, boolean)",
      returns: "String",
      description: <>Performs the toJson operation using the provided parameters (object, prettyPrinting, lenient, ignorePrivateFields).</>
    },
    {
      signature: "toJson(Object, boolean, boolean, boolean, boolean)",
      returns: "String",
      description: <>Performs the toJson operation using the provided parameters (object, prettyPrinting, lenient, ignorePrivateFields, ignoreProtectedFields).</>
    },
    {
      signature: "toJson(Object, boolean, boolean, boolean, boolean, boolean)",
      returns: "String",
      description: <>Performs the toJson operation using the provided parameters (object, prettyPrinting, lenient, ignorePrivateFields, ignoreProtectedFields, ignorePublicFields).</>
    },
    {
      signature: "fromJson(String, Class)",
      returns: "Object",
      description: <>Performs the fromJson operation using the provided parameters (json, classType).</>
    },
    {
      signature: "fromJson(String, Class, boolean)",
      returns: "Object",
      description: <>Performs the fromJson operation using the provided parameters (json, classType, lenient).</>
    }
  ]}
/>
