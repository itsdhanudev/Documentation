import ApiDoc from '@site/src/components/ApiDoc';

# CompileErrorListener

Complete API reference for `CompileErrorListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Shaders

**Component:** no

---

```java
public interface CompileErrorListener
```

<ApiDoc
  methods={[
    {
      signature: "onError(String)",
      returns: "void",
      description: <>Executes <code>onError</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
