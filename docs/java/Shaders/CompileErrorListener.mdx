# CompileErrorListener

Complete API reference for `CompileErrorListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Shaders

**Component:** no

---

```java
public interface CompileErrorListener
```

## Methods (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"message"}])` | `void` | Performs the @MethodArgs operation. |
| `onError(String)` | `void` | Executes `onError` and returns after the underlying engine call finishes. |
