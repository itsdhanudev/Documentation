import ApiDoc from '@site/src/components/ApiDoc';

# FrameBuffer

Complete API reference for `FrameBuffer`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Shaders

**Component:** no

---

```java
public class FrameBuffer
```

<ApiDoc
  constructors={[
    {
      signature: "FrameBuffer(FrameBuffer)",
      description: <>Executes <code>FrameBuffer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "FrameBuffer(int, int)",
      description: <>Executes <code>FrameBuffer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "FrameBuffer(int, int, int)",
      description: <>Creates a new instance of FrameBuffer.</>
    },
    {
      signature: "FrameBuffer(int, int, int, int)",
      description: <>Creates a new instance of FrameBuffer.</>
    },
    {
      signature: "FrameBuffer(int, int, int, int, Texture)",
      description: <>Creates a new instance of FrameBuffer.</>
    }
  ]}
  attributes={[
    {
      name: "frameBuffer",
      type: "FrameBuffer",
      description: <>Represents the <code>frameBuffer</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "colorTexture",
      type: "Texture",
      access: "get",
      description: <>Virtual attribute exposed by <code>getColorTexture</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "colorTexturesCount",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getColorTexturesCount</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "depthTexture",
      type: "Texture",
      access: "get",
      description: <>Virtual attribute exposed by <code>getDepthTexture</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "height",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxHeight",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getMaxHeight</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "maxWidth",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getMaxWidth</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "renderPercentage",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRenderPercentage</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "width",
      type: "int",
      access: "get",
      description: <>Virtual attribute exposed by <code>getWidth</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "resize(int, int)",
      returns: "void",
      description: <>Performs the resize operation using the provided parameters (width, height).</>
    },
    {
      signature: "resize(int, int, int)",
      returns: "void",
      description: <>Performs the resize operation using the provided parameters (width, height, renderPercentage).</>
    },
    {
      signature: "getColorTexture(int)",
      returns: "Texture",
      description: <>Returns the current color texture.</>
    },
    {
      signature: "getColorTextureArray()",
      returns: "Texture[]",
      description: <>Returns the current color texture array.</>
    },
    {
      signature: "extractGPUImageToBtm(ExtractCallback)",
      returns: "void",
      description: <>Executes <code>extractGPUImageToBtm</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "saveGPUImageToFile(File)",
      returns: "void",
      description: <>Executes <code>saveGPUImageToFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "saveGPUImageToFile(File, Runnable)",
      returns: "void",
      description: <>Executes <code>saveGPUImageToFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "destroy()",
      returns: "void",
      description: <>Executes <code>destroy</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "validate()",
      returns: "boolean",
      description: <>Executes <code>validate</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
