import ApiDoc from '@site/src/components/ApiDoc';

# AudioEffect

Complete API reference for `AudioEffect`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** SoundEffects

**Component:** no

---

```java
public class AudioEffect
```

<ApiDoc
  constructors={[
    {
      signature: "AudioEffect(AudioEffect)",
      description: <>Executes <code>AudioEffect</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "AudioEffect(String)",
      description: <>Executes <code>AudioEffect</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "AudioEffect",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "tittle",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTittle</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
