import ApiDoc from '@site/src/components/ApiDoc';

# BassBoostEffect

Complete API reference for `BassBoostEffect`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** SoundEffects

**Component:** no

---

```java
public class BassBoostEffect extends AudioEffect
```

<ApiDoc
  constructors={[
    {
      signature: "BassBoostEffect(BassBoost)",
      description: <>Executes <code>BassBoostEffect</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "BassBoostEffect()",
      description: <>Executes <code>BassBoostEffect</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "effect",
      type: "BassBoost",
      description: <>Represents the <code>effect</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "strength",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getStrength</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
