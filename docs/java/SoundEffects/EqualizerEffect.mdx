import ApiDoc from '@site/src/components/ApiDoc';

# EqualizerEffect

Complete API reference for `EqualizerEffect`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** SoundEffects

**Component:** no

---

```java
public class EqualizerEffect extends AudioEffect
```

<ApiDoc
  constructors={[
    {
      signature: "EqualizerEffect(Equalizer)",
      description: <>Executes <code>EqualizerEffect</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "EqualizerEffect()",
      description: <>Executes <code>EqualizerEffect</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "effect",
      type: "Equalizer",
      description: <>Represents the <code>effect</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "getBandGain(int)",
      returns: "float",
      description: <>Returns the current band gain.</>
    },
    {
      signature: "setBandGain(int, float)",
      returns: "void",
      description: <>Sets or updates the band gain.</>
    }
  ]}
/>
