import ApiDoc from '@site/src/components/ApiDoc';

# ClassCategory

Complete API reference for `ClassCategory`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public interface ClassCategory
```

<ApiDoc
  methods={[
    {
      signature: "cat()",
      returns: "String[]",
      description: <>Executes <code>cat</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
