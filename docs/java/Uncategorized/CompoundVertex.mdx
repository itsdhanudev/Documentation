import ApiDoc from '@site/src/components/ApiDoc';

# CompoundVertex

Complete API reference for `CompoundVertex`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class CompoundVertex
```

<ApiDoc
  constructors={[
    {
      signature: "CompoundVertex(CompoundVertex)",
      description: <>Executes <code>CompoundVertex</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "CompoundVertex()",
      description: <>Executes <code>CompoundVertex</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "CompoundVertex",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "addVertex(Vertex, Transform)",
      returns: "Part",
      description: <>Adds vertex to the collection.</>
    },
    {
      signature: "refreshMatrix(Part)",
      returns: "void",
      description: <>Performs the refreshMatrix operation using the provided parameters (part).</>
    },
    {
      signature: "remove(Part)",
      returns: "void",
      description: <>Removes an item from the collection.</>
    },
    {
      signature: "removeAll(List&lt;Part&gt;)",
      returns: "void",
      description: <>Removes all from the collection.</>
    },
    {
      signature: "getBake()",
      returns: "Vertex",
      description: <>Returns the current bake.</>
    }
  ]}
/>
