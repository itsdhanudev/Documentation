import ApiDoc from '@site/src/components/ApiDoc';

# ConnectionListener

Complete API reference for `ConnectionListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public interface ConnectionListener
```

<ApiDoc
  methods={[
    {
      signature: "onConnected()",
      returns: "void",
      description: <>Executes <code>onConnected</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onDisconnected()",
      returns: "void",
      description: <>Executes <code>onDisconnected</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onError(String, int)",
      returns: "void",
      description: <>Executes <code>onError</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
