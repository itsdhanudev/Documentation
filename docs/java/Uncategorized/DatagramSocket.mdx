import ApiDoc from '@site/src/components/ApiDoc';

# DatagramSocket

Complete API reference for `DatagramSocket`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class DatagramSocket extends DatagramSocket
```

<ApiDoc
  constructors={[
    {
      signature: "DatagramSocket()",
      description: <>Executes <code>DatagramSocket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "DatagramSocket(DatagramSocketImpl)",
      description: <>Creates a new instance of DatagramSocket.</>
    },
    {
      signature: "DatagramSocket(SocketAddress)",
      description: <>Executes <code>DatagramSocket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "DatagramSocket(int)",
      description: <>Executes <code>DatagramSocket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "DatagramSocket(int, InetAddress)",
      description: <>Executes <code>DatagramSocket</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  methods={[
    {
      signature: "bind(SocketAddress)",
      returns: "void",
      description: <>Executes <code>bind</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "connect(InetAddress, int)",
      returns: "void",
      description: <>Performs the connect operation using the provided parameters (address, port).</>
    },
    {
      signature: "connect(SocketAddress)",
      returns: "void",
      description: <>Executes <code>connect</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
