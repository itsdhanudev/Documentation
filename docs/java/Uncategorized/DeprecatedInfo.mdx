import ApiDoc from '@site/src/components/ApiDoc';

# DeprecatedInfo

Complete API reference for `DeprecatedInfo`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public interface DeprecatedInfo
```

<ApiDoc
  methods={[
    {
      signature: "info()",
      returns: "String[]",
      description: <>Executes <code>info</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
