import ApiDoc from '@site/src/components/ApiDoc';

# ExecutionAllow

Complete API reference for `ExecutionAllow`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class ExecutionAllow
```

<ApiDoc
  constructors={[
    {
      signature: "ExecutionAllow(boolean, boolean)",
      description: <>Creates a new instance of ExecutionAllow.</>
    }
  ]}
  methods={[
    {
      signature: "onGameStopped()",
      returns: "boolean",
      description: <>Executes <code>onGameStopped</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setOnGameStopped(boolean)",
      returns: "void",
      description: <>Sets or updates the on game stopped.</>
    },
    {
      signature: "onGameRunning()",
      returns: "boolean",
      description: <>Executes <code>onGameRunning</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setOnGameRunning(boolean)",
      returns: "void",
      description: <>Sets or updates the on game running.</>
    }
  ]}
/>
