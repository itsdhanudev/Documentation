# FilePicker

Complete API reference for `FilePicker`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class FilePicker
```

## Static Methods (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `open(Listener)` | `void` | Performs the open operation using the provided parameters (listener). |
| `importFile(DFile, File)` | `boolean` | Performs the importFile operation using the provided parameters (documentFile, destination). |
