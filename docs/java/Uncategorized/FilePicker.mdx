import ApiDoc from '@site/src/components/ApiDoc';

# FilePicker

Complete API reference for `FilePicker`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class FilePicker
```

<ApiDoc
  staticMethods={[
    {
      signature: "open(Listener)",
      returns: "void",
      description: <>Performs the open operation using the provided parameters (listener).</>
    },
    {
      signature: "importFile(DFile, File)",
      returns: "boolean",
      description: <>Performs the importFile operation using the provided parameters (documentFile, destination).</>
    }
  ]}
/>
