import ApiDoc from '@site/src/components/ApiDoc';

# GUID

Complete API reference for `GUID`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class GUID
```

<ApiDoc
  constructors={[
    {
      signature: "GUID(GUID)",
      description: <>Executes <code>GUID</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "GUID()",
      description: <>Executes <code>GUID</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "guid",
      type: "GUID",
      description: <>Represents the <code>guid</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "duplicableGUID",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getDuplicableGUID</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "metaID",
      type: "String",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getMetaID</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "getUniqueGUID()",
      returns: "String",
      description: <>Returns the current unique guid.</>
    }
  ]}
/>
