import ApiDoc from '@site/src/components/ApiDoc';

# JRDoc_EN

Complete API reference for `JRDoc_EN`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public interface JRDoc_EN
```

<ApiDoc
  methods={[
    {
      signature: "value()",
      returns: "String",
      description: <>Executes <code>value</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
