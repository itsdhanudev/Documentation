import ApiDoc from '@site/src/components/ApiDoc';

# JavaPool

Complete API reference for `JavaPool`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class JavaPool
```

<ApiDoc
  staticMethods={[
    {
      signature: "acquire(Class)",
      returns: "T",
      description: <>Executes <code>acquire</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "release(Object)",
      returns: "void",
      description: <>Executes <code>release</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "acquireFloatArray(int)",
      returns: "float[]",
      description: <>Executes <code>acquireFloatArray</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "acquireFloatArray(int, boolean)",
      returns: "float[]",
      description: <>Executes <code>acquireFloatArray</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "releaseFloatArray(float[])",
      returns: "void",
      description: <>Executes <code>releaseFloatArray</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
