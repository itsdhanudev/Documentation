import ApiDoc from '@site/src/components/ApiDoc';

# JsonSerializer

Complete API reference for `JsonSerializer`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public interface JsonSerializer
```

<ApiDoc
  methods={[
    {
      signature: "serializeToString()",
      returns: "String",
      description: <>Executes <code>serializeToString</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "deserializeLocal(String)",
      returns: "void",
      description: <>Executes <code>deserializeLocal</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
