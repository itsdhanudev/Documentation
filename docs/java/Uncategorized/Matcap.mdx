import ApiDoc from '@site/src/components/ApiDoc';

# Matcap

Complete API reference for `Matcap`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class Matcap
```

<ApiDoc
  constructors={[
    {
      signature: "Matcap(Engine)",
      description: <>Creates a new instance of Matcap.</>
    },
    {
      signature: "Matcap(String)",
      description: <>Creates a new instance of Matcap.</>
    },
    {
      signature: "Matcap()",
      description: <>Creates a new instance of Matcap.</>
    }
  ]}
  methods={[
    {
      signature: "getFile()",
      returns: "String",
      description: <>Returns the current file.</>
    },
    {
      signature: "getTextureFile()",
      returns: "String",
      description: <>Returns the current texture file.</>
    },
    {
      signature: "setTextureFile(String)",
      returns: "void",
      description: <>Sets or updates the texture file.</>
    },
    {
      signature: "toJson()",
      returns: "String",
      description: <>Executes <code>toJson</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getTexture()",
      returns: "Texture",
      description: <>Returns the current texture.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executes <code>genericInterface</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "fromJson(String)",
      returns: "Matcap",
      description: <>Performs the fromJson operation using the provided parameters (json).</>
    }
  ]}
/>
