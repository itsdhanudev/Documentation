import ApiDoc from '@site/src/components/ApiDoc';

# Order

Complete API reference for `Order`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public interface Order
```

<ApiDoc
  methods={[
    {
      signature: "idx()",
      returns: "int[]",
      description: <>Executes <code>idx</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
