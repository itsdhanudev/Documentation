import ApiDoc from '@site/src/components/ApiDoc';

# PanelInflateMenu

Complete API reference for `PanelInflateMenu`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public interface PanelInflateMenu
```

<ApiDoc
  methods={[
    {
      signature: "value()",
      returns: "String",
      description: <>Executes <code>value</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
