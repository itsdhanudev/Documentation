import ApiDoc from '@site/src/components/ApiDoc';

# PhysicsLayer

Complete API reference for `PhysicsLayer`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class PhysicsLayer
```

<ApiDoc
  constructors={[
    {
      signature: "PhysicsLayer(Layer)",
      description: <>Executes <code>PhysicsLayer</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "PhysicsLayer(String)",
      description: <>Executes <code>PhysicsLayer</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "layer",
      type: "Layer",
      description: <>Represents the <code>layer</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "addIgnoreLayer(PhysicsLayer)",
      returns: "void",
      description: <>Adds the specified PhysicsLayer to the ignore list of this layer.</>
    },
    {
      signature: "removeIgnoreLayer(PhysicsLayer)",
      returns: "void",
      description: <>Removes the specified PhysicsLayer from the ignore list of this layer.</>
    },
    {
      signature: "getIgnoreAt(int)",
      returns: "PhysicsLayer",
      description: <>Returns the PhysicsLayer at the specified index in the ignore list.</>
    },
    {
      signature: "ignoreCount()",
      returns: "int",
      description: <>Returns the number of PhysicsLayers in the ignore list.</>
    }
  ]}
/>
