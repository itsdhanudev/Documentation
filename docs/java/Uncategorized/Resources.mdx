import ApiDoc from '@site/src/components/ApiDoc';

# Resources

Complete API reference for `Resources`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class Resources
```

<ApiDoc
  staticMethods={[
    {
      signature: "getFile(String)",
      returns: "File",
      description: <>Returns the current file.</>
    },
    {
      signature: "isFolder(String)",
      returns: "boolean",
      description: <>Returns whether folder is true.</>
    }
  ]}
/>
