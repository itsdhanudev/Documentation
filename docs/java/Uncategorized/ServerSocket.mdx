import ApiDoc from '@site/src/components/ApiDoc';

# ServerSocket

Complete API reference for `ServerSocket`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class ServerSocket extends ServerSocket
```

<ApiDoc
  constructors={[
    {
      signature: "ServerSocket()",
      description: <>Executes <code>ServerSocket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ServerSocket(int)",
      description: <>Executes <code>ServerSocket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ServerSocket(int, int)",
      description: <>Executes <code>ServerSocket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "ServerSocket(int, int, InetAddress)",
      description: <>Executes <code>ServerSocket</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  methods={[
    {
      signature: "bind(SocketAddress)",
      returns: "void",
      description: <>Executes <code>bind</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "bind(SocketAddress, int)",
      returns: "void",
      description: <>Executes <code>bind</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "accept()",
      returns: "Socket",
      description: <>Executes <code>accept</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
