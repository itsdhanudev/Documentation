import ApiDoc from '@site/src/components/ApiDoc';

# Socket

Complete API reference for `Socket`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class Socket extends Socket
```

<ApiDoc
  constructors={[
    {
      signature: "Socket()",
      description: <>Creates a new instance of Socket.</>
    },
    {
      signature: "Socket(Socket)",
      description: <>Executes <code>Socket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Socket(Proxy)",
      description: <>Creates a new instance of Socket.</>
    },
    {
      signature: "Socket(SocketImpl)",
      description: <>Executes <code>Socket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Socket(String, int)",
      description: <>Executes <code>Socket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Socket(InetAddress, int)",
      description: <>Executes <code>Socket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Socket(String, int, InetAddress, int)",
      description: <>Executes <code>Socket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Socket(InetAddress, int, InetAddress, int)",
      description: <>Executes <code>Socket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Socket(String, int, boolean)",
      description: <>Executes <code>Socket</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Socket(InetAddress, int, boolean)",
      description: <>Executes <code>Socket</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  methods={[
    {
      signature: "connect(SocketAddress)",
      returns: "void",
      description: <>Executes <code>connect</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "connect(SocketAddress, int)",
      returns: "void",
      description: <>Executes <code>connect</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "bind(SocketAddress)",
      returns: "void",
      description: <>Executes <code>bind</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getInetAddress()",
      returns: "InetAddress",
      description: <>Returns the current inet address.</>
    },
    {
      signature: "getLocalAddress()",
      returns: "InetAddress",
      description: <>Returns the current local address.</>
    },
    {
      signature: "getPort()",
      returns: "int",
      description: <>Returns the current port.</>
    },
    {
      signature: "getLocalPort()",
      returns: "int",
      description: <>Returns the current local port.</>
    },
    {
      signature: "getRemoteSocketAddress()",
      returns: "SocketAddress",
      description: <>Returns the current remote socket address.</>
    },
    {
      signature: "getLocalSocketAddress()",
      returns: "SocketAddress",
      description: <>Returns the current local socket address.</>
    },
    {
      signature: "getChannel()",
      returns: "SocketChannel",
      description: <>Returns the current channel.</>
    },
    {
      signature: "getInputStream()",
      returns: "InputStream",
      description: <>Executes <code>getInputStream</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getOutputStream()",
      returns: "OutputStream",
      description: <>Executes <code>getOutputStream</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setTcpNoDelay(boolean)",
      returns: "void",
      description: <>Executes <code>setTcpNoDelay</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getTcpNoDelay()",
      returns: "boolean",
      description: <>Executes <code>getTcpNoDelay</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setSoLinger(boolean, int)",
      returns: "void",
      description: <>Executes <code>setSoLinger</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getSoLinger()",
      returns: "int",
      description: <>Executes <code>getSoLinger</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sendUrgentData(int)",
      returns: "void",
      description: <>Executes <code>sendUrgentData</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setOOBInline(boolean)",
      returns: "void",
      description: <>Executes <code>setOOBInline</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getOOBInline()",
      returns: "boolean",
      description: <>Executes <code>getOOBInline</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setSoTimeout(int)",
      returns: "void",
      description: <>Executes <code>setSoTimeout</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getSoTimeout()",
      returns: "int",
      description: <>Executes <code>getSoTimeout</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setSendBufferSize(int)",
      returns: "void",
      description: <>Executes <code>setSendBufferSize</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getSendBufferSize()",
      returns: "int",
      description: <>Executes <code>getSendBufferSize</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setReceiveBufferSize(int)",
      returns: "void",
      description: <>Executes <code>setReceiveBufferSize</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getReceiveBufferSize()",
      returns: "int",
      description: <>Executes <code>getReceiveBufferSize</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setKeepAlive(boolean)",
      returns: "void",
      description: <>Executes <code>setKeepAlive</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getKeepAlive()",
      returns: "boolean",
      description: <>Executes <code>getKeepAlive</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setTrafficClass(int)",
      returns: "void",
      description: <>Executes <code>setTrafficClass</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getTrafficClass()",
      returns: "int",
      description: <>Executes <code>getTrafficClass</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "setReuseAddress(boolean)",
      returns: "void",
      description: <>Executes <code>setReuseAddress</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getReuseAddress()",
      returns: "boolean",
      description: <>Executes <code>getReuseAddress</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "close()",
      returns: "void",
      description: <>Executes <code>close</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "shutdownInput()",
      returns: "void",
      description: <>Executes <code>shutdownInput</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "shutdownOutput()",
      returns: "void",
      description: <>Executes <code>shutdownOutput</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "toString()",
      returns: "String",
      description: <>Returns the string representation.</>
    },
    {
      signature: "isConnected()",
      returns: "boolean",
      description: <>Returns whether connected is true.</>
    },
    {
      signature: "isBound()",
      returns: "boolean",
      description: <>Returns whether bound is true.</>
    },
    {
      signature: "isClosed()",
      returns: "boolean",
      description: <>Returns whether closed is true.</>
    },
    {
      signature: "isInputShutdown()",
      returns: "boolean",
      description: <>Returns whether input shutdown is true.</>
    },
    {
      signature: "isOutputShutdown()",
      returns: "boolean",
      description: <>Returns whether output shutdown is true.</>
    },
    {
      signature: "setPerformancePreferences(int, int, int)",
      returns: "void",
      description: <>Sets or updates the performance preferences.</>
    },
    {
      signature: "hashCode()",
      returns: "int",
      description: <>Returns whether code is true.</>
    },
    {
      signature: "equals(Object)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    }
  ]}
/>
