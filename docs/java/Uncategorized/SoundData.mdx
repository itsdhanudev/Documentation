import ApiDoc from '@site/src/components/ApiDoc';

# SoundData

Complete API reference for `SoundData`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class SoundData
```

<ApiDoc
  constructors={[
    {
      signature: "SoundData()",
      description: <>Creates a new instance of SoundData.</>
    },
    {
      signature: "SoundData(NativeFloatBuffer, int)",
      description: <>Creates a new instance of SoundData.</>
    },
    {
      signature: "SoundData(float[], int)",
      description: <>Creates a new instance of SoundData.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "dataBuffer",
      type: "NativeFloatBuffer",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getDataBuffer</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "sampleRate",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getSampleRate</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
/>
