import ApiDoc from '@site/src/components/ApiDoc';

# SoundDecoder

Complete API reference for `SoundDecoder`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class SoundDecoder
```

<ApiDoc
  staticMethods={[
    {
      signature: "decodeFile(File)",
      returns: "SoundData",
      description: <>Executes <code>decodeFile</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "streamDecodeFile(File)",
      returns: "SoundEmitter",
      description: <>Performs the streamDecodeFile operation using the provided parameters (soundFile).</>
    }
  ]}
/>
