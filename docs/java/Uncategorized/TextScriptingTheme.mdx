# TextScriptingTheme

Complete API reference for `TextScriptingTheme`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public class TextScriptingTheme
```

## Methods (29)

| Signature | Returns | Description |
| --- | --- | --- |
| `Color(["#ABB7C5"])` | [Color] [textColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#1f1f1f"])` | [Color] [backgroundColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#2e2e2e"])` | [Color] [gutterColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#555555"])` | [Color] [gutterDividerColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#A4A3A3"])` | [Color] [gutterCurrentLineNumberColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#616366"])` | [Color] [gutterTextColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#3A3A3A"])` | [Color] [selectedLineColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#28427F"])` | [Color] [selectionColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#987DAC"])` | [Color] [suggestionQueryColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#33654B"])` | [Color] [findResultBackgroundColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#33654B"])` | [Color] [delimiterBackgroundColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#6897BB"])` | [Color] [numberColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#E8E2B7"])` | [Color] [operatorColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#EC7600"])` | [Color] [keywordColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#EC7600"])` | [Color] [typeColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#EC7600"])` | [Color] [langConstColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#C9C54E"])` | [Color] [preprocessorColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#9378A7"])` | [Color] [variableColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#FEC76C"])` | [Color] [methodColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#6E875A"])` | [Color] [stringColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#66747B"])` | [Color] [commentColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#E2C077"])` | [Color] [tagColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#E2C077"])` | [Color] [tagNameColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#BABABA"])` | [Color] [attrNameColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#ABC16D"])` | [Color] [attrValueColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#6897BB"])` | [Color] [entityRefColor] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#ff0000"])` | [Color] [gutterLineError] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `Color(["#f1c40f"])` | [Color] [gutterLineAlert] = [new] | Executes `Color` and returns after the underlying engine call finishes. |
| `convertInternal()` | `ColorScheme` | Executes `convertInternal` and returns after the underlying engine call finishes. |
