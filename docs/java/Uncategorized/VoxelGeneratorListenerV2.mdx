import ApiDoc from '@site/src/components/ApiDoc';

# VoxelGeneratorListenerV2

Complete API reference for `VoxelGeneratorListenerV2`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public interface VoxelGeneratorListenerV2
```

<ApiDoc
  methods={[
    {
      signature: "getGroundHeight(int, int)",
      returns: "int",
      description: <>Executes <code>getGroundHeight</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getBlockType(int, int, int, int)",
      returns: "int",
      description: <>Executes <code>getBlockType</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "storeChunk(OH3LevelIntArray, int, int)",
      returns: "void",
      description: <>Executes <code>storeChunk</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "loadChunk(int, int)",
      returns: "OH3LevelIntArray",
      description: <>Executes <code>loadChunk</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onChunkCreated(SpatialObject, VoxelChunk)",
      returns: "void",
      description: <>Executes <code>onChunkCreated</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onChunkBuilt(SpatialObject, VoxelChunk)",
      returns: "void",
      description: <>Executes <code>onChunkBuilt</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "drawFace(int, int, int, Point3)",
      returns: "boolean",
      description: <>Executes <code>drawFace</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getTextureIndex(int, int, int)",
      returns: "int",
      description: <>Executes <code>getTextureIndex</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "onFaceBuilt(SpatialObject, VoxelChunk, int, int, int, Point3, int, int, int, int)",
      returns: "void",
      description: <>Executes <code>onFaceBuilt</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
