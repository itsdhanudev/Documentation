import ApiDoc from '@site/src/components/ApiDoc';

# VoxelVertexGeneratorListener

Complete API reference for `VoxelVertexGeneratorListener`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Uncategorized

**Component:** no

---

```java
public interface VoxelVertexGeneratorListener
```

<ApiDoc
  methods={[
    {
      signature: "drawFace(int, int, int)",
      returns: "boolean",
      description: <>Executes <code>drawFace</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
