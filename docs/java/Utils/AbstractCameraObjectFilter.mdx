import ApiDoc from '@site/src/components/ApiDoc';

# AbstractCameraObjectFilter

Complete API reference for `AbstractCameraObjectFilter`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Utils

**Component:** no

---

```java
public class AbstractCameraObjectFilter
```

<ApiDoc
  methods={[
    {
      signature: "filterShadow()",
      returns: "boolean",
      description: <>Filters which Shadow will be rendered. Return true to render and false to ignore.</>
    },
    {
      signature: "filterRenderer(ModelRenderer)",
      returns: "boolean",
      description: <>Filters which ModelRenderer will be rendered. Return true to render and false to ignore.</>
    },
    {
      signature: "filterRenderer(SkinnedModelRenderer)",
      returns: "boolean",
      description: <>Filters which SkinnedModelRenderer will be rendered. Return true to render and false to ignore.</>
    },
    {
      signature: "filterMaterialBake(Material, Vertex)",
      returns: "boolean",
      description: <>Filters which Bake will be rendered. Return true to render and false to ignore.</>
    },
    {
      signature: "filter3DText(Component)",
      returns: "boolean",
      description: <>Filters which 3DText will be rendered. Return true to render and false to ignore.</>
    },
    {
      signature: "renderFog()",
      returns: "boolean",
      description: <>Defines whether the Fog will render. Return true to render and false to ignore.</>
    },
    {
      signature: "renderParticles()",
      returns: "boolean",
      description: <>Defines whether Particles will render. Return true to render and false to ignore.</>
    },
    {
      signature: "filterParticle(ParticleEmitter)",
      returns: "boolean",
      description: <>Filters which ParticleEmitter will be rendered. Return true to render and false to ignore.</>
    },
    {
      signature: "overrideAmbientLight()",
      returns: "Color",
      description: <>Overrides the Ambient Light Color. Return the Color that will override.</>
    },
    {
      signature: "overrideIndirectLight()",
      returns: "Color",
      description: <>Overrides the Indirect Light Color. Return the Color that will override.</>
    }
  ]}
/>
