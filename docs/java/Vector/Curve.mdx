import ApiDoc from '@site/src/components/ApiDoc';

# Curve

Complete API reference for `Curve`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Vector

**Component:** no

---

```java
public class Curve
```

<ApiDoc
  constructors={[
    {
      signature: "Curve(Curve)",
      description: <>Executes <code>Curve</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Curve()",
      description: <>Executes <code>Curve</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Curve",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  methods={[
    {
      signature: "addPoint(float, float)",
      returns: "void",
      description: <>Adds point to the collection.</>
    },
    {
      signature: "setPoint(int, float, float)",
      returns: "void",
      description: <>Sets or updates the point.</>
    },
    {
      signature: "pointAt(int)",
      returns: "Vector2",
      description: <>Performs the pointAt operation using the provided parameters (idx).</>
    },
    {
      signature: "pointAt(int, Vector2)",
      returns: "Vector2",
      description: <>Performs the pointAt operation using the provided parameters (idx, out).</>
    },
    {
      signature: "clear()",
      returns: "void",
      description: <>Clears or resets the object's state.</>
    },
    {
      signature: "pointsCount()",
      returns: "int",
      description: <>Executes <code>pointsCount</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "evaluate(float)",
      returns: "float",
      description: <>Performs the evaluate operation using the provided parameters (x).</>
    },
    {
      signature: "apply()",
      returns: "void",
      description: <>Executes <code>apply</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "getWrapMode()",
      returns: "WrapMode",
      description: <>Returns the current wrap mode.</>
    },
    {
      signature: "setWrapMode(WrapMode)",
      returns: "void",
      description: <>Sets or updates the wrap mode.</>
    },
    {
      signature: "toJson()",
      returns: "String",
      description: <>Executes <code>toJson</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "fromJson(String)",
      returns: "Curve",
      description: <>Performs the fromJson operation using the provided parameters (json).</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executes <code>genericInterface</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
