import ApiDoc from '@site/src/components/ApiDoc';

# Matrix4

Complete API reference for `Matrix4`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Vector

**Component:** no

---

```java
public class Matrix4
```

<ApiDoc
  constructors={[
    {
      signature: "Matrix4(Matrix4)",
      description: <>Executes <code>Matrix4</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Matrix4()",
      description: <>Executes <code>Matrix4</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Matrix4(float[])",
      description: <>Creates a new instance of Matrix4.</>
    },
    {
      signature: "Matrix4(float, float, float, float, float, float, float, float, float, float, float, float, float, float, float, float)",
      description: <>Executes <code>Matrix4</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "matrix4",
      type: "Matrix4",
      description: <>Represents the <code>matrix4</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "rotation",
      type: "JAVARuntime.Quaternion",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getRotation</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "scale",
      type: "JAVARuntime.Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getScale</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "translation",
      type: "JAVARuntime.Vector3",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getTranslation</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "mul(Matrix4)",
      returns: "Matrix4",
      description: <>Performs the mul operation using the provided parameters (value).</>
    },
    {
      signature: "mul(Matrix4, Matrix4)",
      returns: "void",
      description: <>Performs the mul operation using the provided parameters (value, out).</>
    },
    {
      signature: "inverse()",
      returns: "Matrix4",
      description: <>Executes <code>inverse</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "inverse(Matrix4)",
      returns: "void",
      description: <>Performs the inverse operation using the provided parameters (out).</>
    },
    {
      signature: "mul(JAVARuntime.Vector3)",
      returns: "JAVARuntime.Vector3",
      description: <>Performs the mul operation using the provided parameters (vector3).</>
    },
    {
      signature: "mul(JAVARuntime.Vector3, JAVARuntime.Vector3)",
      returns: "JAVARuntime.Vector3",
      description: <>Performs the mul operation using the provided parameters (vector3, out).</>
    },
    {
      signature: "mul(float, float, float)",
      returns: "JAVARuntime.Vector3",
      description: <>Performs the mul operation using the provided parameters (x, y, z).</>
    },
    {
      signature: "mul(float, float, float, JAVARuntime.Vector3)",
      returns: "JAVARuntime.Vector3",
      description: <>Performs the mul operation using the provided parameters (x, y, z, out).</>
    },
    {
      signature: "mulNormal(JAVARuntime.Vector3)",
      returns: "JAVARuntime.Vector3",
      description: <>Performs the mulNormal operation using the provided parameters (vector3).</>
    },
    {
      signature: "mulNormal(JAVARuntime.Vector3, JAVARuntime.Vector3)",
      returns: "JAVARuntime.Vector3",
      description: <>Performs the mulNormal operation using the provided parameters (vector3, out).</>
    },
    {
      signature: "mulAcross(JAVARuntime.Vector3)",
      returns: "JAVARuntime.Vector3",
      description: <>Performs the mulAcross operation using the provided parameters (vector3).</>
    },
    {
      signature: "mulAcross(JAVARuntime.Vector3, JAVARuntime.Vector3)",
      returns: "JAVARuntime.Vector3",
      description: <>Performs the mulAcross operation using the provided parameters (vector3, out).</>
    },
    {
      signature: "getTranslation(JAVARuntime.Vector3)",
      returns: "JAVARuntime.Vector3",
      description: <>Returns the current translation.</>
    },
    {
      signature: "setTranslation(float, float, float)",
      returns: "void",
      description: <>Sets or updates the translation.</>
    },
    {
      signature: "getScale(JAVARuntime.Vector3)",
      returns: "JAVARuntime.Vector3",
      description: <>Returns the current scale.</>
    },
    {
      signature: "setScale(float, float, float)",
      returns: "void",
      description: <>Sets or updates the scale.</>
    },
    {
      signature: "getRotation(Quaternion)",
      returns: "JAVARuntime.Quaternion",
      description: <>Returns the current rotation.</>
    },
    {
      signature: "setRotation(float, float, float, float)",
      returns: "void",
      description: <>Sets or updates the rotation.</>
    },
    {
      signature: "set(float[])",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "setTransform(JAVARuntime.Vector3, Quaternion, JAVARuntime.Vector3)",
      returns: "void",
      description: <>Sets or updates the transform.</>
    },
    {
      signature: "toTranslationVector()",
      returns: "JAVARuntime.Vector3",
      description: <>Executes <code>toTranslationVector</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "toTranslationVector(JAVARuntime.Vector3)",
      returns: "JAVARuntime.Vector3",
      description: <>Performs the toTranslationVector operation using the provided parameters (out).</>
    },
    {
      signature: "toRotationQuat()",
      returns: "JAVARuntime.Quaternion",
      description: <>Executes <code>toRotationQuat</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "toRotationQuat(Quaternion)",
      returns: "JAVARuntime.Quaternion",
      description: <>Performs the toRotationQuat operation using the provided parameters (out).</>
    },
    {
      signature: "toScaleVector()",
      returns: "JAVARuntime.Vector3",
      description: <>Executes <code>toScaleVector</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "toScaleVector(JAVARuntime.Vector3)",
      returns: "JAVARuntime.Vector3",
      description: <>Performs the toScaleVector operation using the provided parameters (out).</>
    },
    {
      signature: "fillFloatArray(float[])",
      returns: "void",
      description: <>Performs the fillFloatArray operation using the provided parameters (floatArray).</>
    },
    {
      signature: "fillFloatArray(float[], boolean)",
      returns: "void",
      description: <>Performs the fillFloatArray operation using the provided parameters (floatArray, columnMajor).</>
    }
  ]}
/>
