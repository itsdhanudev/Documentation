import ApiDoc from '@site/src/components/ApiDoc';

# Point2

Complete API reference for `Point2`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Vector

**Component:** no

---

```java
public class Point2
```

<ApiDoc
  constructors={[
    {
      signature: "Point2(Vector2)",
      description: <>Executes <code>Point2</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Point2()",
      description: <>Executes <code>Point2</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Point2(int, int)",
      description: <>Creates a new instance of Point2.</>
    }
  ]}
  attributes={[
    {
      name: "vector2",
      type: "Vector2",
      description: <>Represents the <code>vector2</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "x",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getX</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "y",
      type: "int",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getY</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "mul(Point2)",
      returns: "Point2",
      description: <>Performs the mul operation using the provided parameters (value).</>
    },
    {
      signature: "mul(int, int)",
      returns: "Point2",
      description: <>Performs the mul operation using the provided parameters (x, y).</>
    },
    {
      signature: "mul(int)",
      returns: "Point2",
      description: <>Performs the mul operation using the provided parameters (a).</>
    },
    {
      signature: "div(Point2)",
      returns: "Point2",
      description: <>Performs the div operation using the provided parameters (value).</>
    },
    {
      signature: "div(int, int)",
      returns: "Point2",
      description: <>Performs the div operation using the provided parameters (x, y).</>
    },
    {
      signature: "div(int)",
      returns: "Point2",
      description: <>Performs the div operation using the provided parameters (a).</>
    },
    {
      signature: "sum(Point2)",
      returns: "Point2",
      description: <>Performs the sum operation using the provided parameters (value).</>
    },
    {
      signature: "sum(int, int)",
      returns: "Point2",
      description: <>Performs the sum operation using the provided parameters (x, y).</>
    },
    {
      signature: "sum(int)",
      returns: "Point2",
      description: <>Performs the sum operation using the provided parameters (a).</>
    },
    {
      signature: "sub(Point2)",
      returns: "Point2",
      description: <>Performs the sub operation using the provided parameters (value).</>
    },
    {
      signature: "sub(int, int)",
      returns: "Point2",
      description: <>Performs the sub operation using the provided parameters (x, y).</>
    },
    {
      signature: "sub(int)",
      returns: "Point2",
      description: <>Performs the sub operation using the provided parameters (a).</>
    },
    {
      signature: "equals(Point2)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "equals(int, int)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "equals(int)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "set(Point2)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "set(Vector2)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "set(int, int)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "set(int)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "lerp(Point2, int)",
      returns: "void",
      description: <>Performs the lerp operation using the provided parameters (vector3, speed).</>
    },
    {
      signature: "lerp(int, int, int)",
      returns: "void",
      description: <>Performs the lerp operation using the provided parameters (x, y, speed).</>
    },
    {
      signature: "lerp(int, int)",
      returns: "void",
      description: <>Performs the lerp operation using the provided parameters (a, speed).</>
    },
    {
      signature: "lerpInSeconds(Point2, int)",
      returns: "void",
      description: <>Performs the lerpInSeconds operation using the provided parameters (vector3, speed).</>
    },
    {
      signature: "lerpInSeconds(int, int, int)",
      returns: "void",
      description: <>Performs the lerpInSeconds operation using the provided parameters (x, y, speed).</>
    },
    {
      signature: "lerpInSeconds(int, int)",
      returns: "void",
      description: <>Performs the lerpInSeconds operation using the provided parameters (a, speed).</>
    },
    {
      signature: "blend(Point2, int)",
      returns: "void",
      description: <>Performs the blend operation using the provided parameters (vector3, blend).</>
    },
    {
      signature: "blend(int, int, int)",
      returns: "void",
      description: <>Performs the blend operation using the provided parameters (x, y, blend).</>
    },
    {
      signature: "blend(int, int)",
      returns: "void",
      description: <>Performs the blend operation using the provided parameters (a, blend).</>
    },
    {
      signature: "normalize()",
      returns: "Point2",
      description: <>Executes <code>normalize</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "normalizeLocal()",
      returns: "void",
      description: <>Executes <code>normalizeLocal</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "length()",
      returns: "float",
      description: <>Executes <code>length</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "sqrLength()",
      returns: "float",
      description: <>Executes <code>sqrLength</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "distance(Point2)",
      returns: "float",
      description: <>Performs the distance operation using the provided parameters (vector3).</>
    },
    {
      signature: "sqrDistance(Point2)",
      returns: "float",
      description: <>Performs the sqrDistance operation using the provided parameters (vector3).</>
    },
    {
      signature: "dot(Point2)",
      returns: "float",
      description: <>Performs the dot operation using the provided parameters (vector3).</>
    },
    {
      signature: "dot(int, int)",
      returns: "float",
      description: <>Performs the dot operation using the provided parameters (x, y).</>
    },
    {
      signature: "dot(int)",
      returns: "float",
      description: <>Performs the dot operation using the provided parameters (a).</>
    },
    {
      signature: "cross(Point2)",
      returns: "Vector3",
      description: <>Performs the cross operation using the provided parameters (value).</>
    },
    {
      signature: "cross(int, int)",
      returns: "Vector3",
      description: <>Performs the cross operation using the provided parameters (x, y).</>
    },
    {
      signature: "copy()",
      returns: "Point2",
      description: <>Returns a copy of this Point2 instance.</>
    },
    {
      signature: "toString()",
      returns: "String",
      description: <>Returns the string representation.</>
    }
  ]}
  staticMethods={[
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executes <code>inspectorController</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "zero()",
      returns: "Point2",
      description: <>Executes <code>zero</code> and returns after the underlying engine call finishes.</>
    }
  ]}
/>
