import ApiDoc from '@site/src/components/ApiDoc';

# Quaternion

Complete API reference for `Quaternion`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** Vector

**Component:** no

---

```java
public class Quaternion
```

<ApiDoc
  constructors={[
    {
      signature: "Quaternion(Quaternion)",
      description: <>Executes <code>Quaternion</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Quaternion()",
      description: <>Executes <code>Quaternion</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "Quaternion(float, float, float, float)",
      description: <>Creates a new instance of Quaternion.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Quaternion",
      description: <>Represents the <code>instance</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "qw",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQW</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "qx",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQX</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "qy",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQY</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "qz",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getQZ</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "x",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getX</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "y",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getY</code>. Access it like a field to read or write the underlying value.</>
    },
    {
      name: "z",
      type: "float",
      access: "get/set",
      description: <>Virtual attribute exposed by <code>getZ</code>. Access it like a field to read or write the underlying value.</>
    }
  ]}
  methods={[
    {
      signature: "increment(float, float, float)",
      returns: "void",
      description: <>Performs the increment operation using the provided parameters (eulerX, eulerY, eulerZ).</>
    },
    {
      signature: "mul(Quaternion)",
      returns: "Quaternion",
      description: <>Performs the mul operation using the provided parameters (value).</>
    },
    {
      signature: "mul(float, float, float, float)",
      returns: "Quaternion",
      description: <>Performs the mul operation using the provided parameters (w, x, y, z).</>
    },
    {
      signature: "mulLocal(Quaternion)",
      returns: "void",
      description: <>Performs the mulLocal operation using the provided parameters (value).</>
    },
    {
      signature: "mulLocal(float, float, float, float)",
      returns: "void",
      description: <>Performs the mulLocal operation using the provided parameters (w, x, y, z).</>
    },
    {
      signature: "multiply(Quaternion)",
      returns: "Quaternion",
      description: <>Performs the multiply operation using the provided parameters (value).</>
    },
    {
      signature: "multiply(float, float, float, float)",
      returns: "Quaternion",
      description: <>Performs the multiply operation using the provided parameters (w, x, y, z).</>
    },
    {
      signature: "sum(Quaternion)",
      returns: "Quaternion",
      description: <>Performs the sum operation using the provided parameters (value).</>
    },
    {
      signature: "sum(float, float, float, float)",
      returns: "Quaternion",
      description: <>Performs the sum operation using the provided parameters (w, x, y, z).</>
    },
    {
      signature: "sumLocal(Quaternion)",
      returns: "void",
      description: <>Performs the sumLocal operation using the provided parameters (value).</>
    },
    {
      signature: "sumLocal(float, float, float, float)",
      returns: "void",
      description: <>Performs the sumLocal operation using the provided parameters (w, x, y, z).</>
    },
    {
      signature: "add(Quaternion)",
      returns: "Quaternion",
      description: <>Adds an item to the collection.</>
    },
    {
      signature: "add(float, float, float, float)",
      returns: "Quaternion",
      description: <>Adds an item to the collection.</>
    },
    {
      signature: "normalize()",
      returns: "Quaternion",
      description: <>Executes <code>normalize</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "normalizeLocal()",
      returns: "void",
      description: <>Executes <code>normalizeLocal</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "equals(Quaternion)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "equals(float, float, float, float)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "equals(float)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "blend(Quaternion, float)",
      returns: "Quaternion",
      description: <>Performs the blend operation using the provided parameters (quaternion, blend).</>
    },
    {
      signature: "blendLocal(Quaternion, float)",
      returns: "void",
      description: <>Executes <code>blendLocal</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "selfLookTo(Vector3)",
      returns: "void",
      description: <>Performs the selfLookTo operation using the provided parameters (direction).</>
    },
    {
      signature: "selfLookTo(Vector3, Vector3)",
      returns: "void",
      description: <>Performs the selfLookTo operation using the provided parameters (from, to).</>
    },
    {
      signature: "localLookTo(Vector3)",
      returns: "void",
      description: <>Performs the localLookTo operation using the provided parameters (direction).</>
    },
    {
      signature: "localLookTo(Vector3, Vector3)",
      returns: "void",
      description: <>Performs the localLookTo operation using the provided parameters (from, to).</>
    },
    {
      signature: "lookTo(Vector3)",
      returns: "void",
      description: <>Performs the lookTo operation using the provided parameters (direction).</>
    },
    {
      signature: "lookTo(Vector3, Vector3)",
      returns: "void",
      description: <>Performs the lookTo operation using the provided parameters (from, to).</>
    },
    {
      signature: "lookTo(Vector3, Vector3, Vector3)",
      returns: "void",
      description: <>Performs the lookTo operation using the provided parameters (from, to, up).</>
    },
    {
      signature: "lookToLocal(Vector3)",
      returns: "void",
      description: <>Executes <code>lookToLocal</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "lookToLocal(Vector3, Vector3)",
      returns: "void",
      description: <>Executes <code>lookToLocal</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "lookToLocal(Vector3, boolean, boolean)",
      returns: "void",
      description: <>Executes <code>lookToLocal</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "lookToLocal(Vector3, Vector3, boolean, boolean)",
      returns: "void",
      description: <>Executes <code>lookToLocal</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "slerp(Quaternion, float)",
      returns: "Quaternion",
      description: <>Performs the slerp operation using the provided parameters (b, t).</>
    },
    {
      signature: "slerpLocal(Quaternion, float)",
      returns: "void",
      description: <>Performs the slerpLocal operation using the provided parameters (b, t).</>
    },
    {
      signature: "slerpLocal(Quaternion, Quaternion, float)",
      returns: "void",
      description: <>Performs the slerpLocal operation using the provided parameters (a, b, t).</>
    },
    {
      signature: "set(Quaternion)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "set(float, float, float, float)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "set(float)",
      returns: "void",
      description: <>Sets or updates the the value.</>
    },
    {
      signature: "setIdentity()",
      returns: "void",
      description: <>Sets or updates the identity.</>
    },
    {
      signature: "setFromEuler(float, float, float)",
      returns: "void",
      description: <>Sets or updates the from euler.</>
    },
    {
      signature: "setFromEuler(Vector3)",
      returns: "void",
      description: <>Sets or updates the from euler.</>
    },
    {
      signature: "toEuler()",
      returns: "Vector3",
      description: <>Executes <code>toEuler</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "copy()",
      returns: "Quaternion",
      description: <>Returns a copy of this Quaternion instance.</>
    },
    {
      signature: "rotateVector(Vector3)",
      returns: "Vector3",
      description: <>Transforms or moves vector accordingly.</>
    },
    {
      signature: "rotateVector(Vector3, Vector3)",
      returns: "Vector3",
      description: <>Transforms or moves vector accordingly.</>
    },
    {
      signature: "equals(Object)",
      returns: "boolean",
      description: <>Compares this instance with another for equality/order.</>
    },
    {
      signature: "pointerEquals(Object)",
      returns: "boolean",
      description: <>Performs the pointerEquals operation using the provided parameters (otherObject).</>
    },
    {
      signature: "notEquals(Object)",
      returns: "boolean",
      description: <>Performs the notEquals operation using the provided parameters (otherObject).</>
    },
    {
      signature: "mul(Object)",
      returns: "T",
      description: <>Performs the mul operation using the provided parameters (otherObject).</>
    },
    {
      signature: "mulEqual(Object)",
      returns: "void",
      description: <>Performs the mulEqual operation using the provided parameters (otherObject).</>
    },
    {
      signature: "serializeToString()",
      returns: "String",
      description: <>Executes <code>serializeToString</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "deserializeLocal(String)",
      returns: "void",
      description: <>Performs the deserializeLocal operation using the provided parameters (str).</>
    }
  ]}
  staticMethods={[
    {
      signature: "fromEuler(Vector3)",
      returns: "Quaternion",
      description: <>Performs the fromEuler operation using the provided parameters (value).</>
    },
    {
      signature: "fromEuler(float, float, float)",
      returns: "Quaternion",
      description: <>Performs the fromEuler operation using the provided parameters (x, y, z).</>
    },
    {
      signature: "createFromEuler(Vector3)",
      returns: "Quaternion",
      description: <>Creates a new from euler.</>
    },
    {
      signature: "createFromEuler(float, float, float)",
      returns: "Quaternion",
      description: <>Creates a new from euler.</>
    },
    {
      signature: "zero()",
      returns: "Quaternion",
      description: <>Executes <code>zero</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "slerp(Quaternion, Quaternion, float)",
      returns: "Quaternion",
      description: <>Performs the slerp operation using the provided parameters (a, b, t).</>
    },
    {
      signature: "lookAt(Vector3, Vector3)",
      returns: "Quaternion",
      description: <>Performs the lookAt operation using the provided parameters (from, to).</>
    },
    {
      signature: "lookAt(Vector3, Vector3, Vector3)",
      returns: "Quaternion",
      description: <>Performs the lookAt operation using the provided parameters (from, to, up).</>
    },
    {
      signature: "angleAxis(float, Vector3)",
      returns: "Quaternion",
      description: <>Performs the angleAxis operation using the provided parameters (angle, value).</>
    },
    {
      signature: "angleAxis(float, float, float, float)",
      returns: "Quaternion",
      description: <>Performs the angleAxis operation using the provided parameters (angle, x, y, z).</>
    },
    {
      signature: "lookRotation(Vector3)",
      returns: "Quaternion",
      description: <>Performs the lookRotation operation using the provided parameters (forward).</>
    },
    {
      signature: "lookRotation(Vector3, Vector3)",
      returns: "Quaternion",
      description: <>Performs the lookRotation operation using the provided parameters (forward, upwards).</>
    },
    {
      signature: "isNull(Object)",
      returns: "boolean",
      description: <>Returns whether null is true.</>
    }
  ]}
/>
