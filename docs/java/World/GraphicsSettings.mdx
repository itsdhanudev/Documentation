# GraphicsSettings

Complete API reference for `GraphicsSettings`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** World

**Component:** no

---

```java
public class GraphicsSettings
```

## Constructors (2)

| Signature | Description |
| --- | --- |
| GraphicsSettings([GraphicsSettings]) | Executes `GraphicsSettings` and returns after the underlying engine call finishes. |
| GraphicsSettings() | Executes `GraphicsSettings` and returns after the underlying engine call finishes. |

## Attributes (1)

| Name | Type | Description |
| --- | --- | --- |
| `graphicsSettings` | [GraphicsSettings] | Represents the `graphicsSettings` value stored on this instance. Read or assign to update the property. |
