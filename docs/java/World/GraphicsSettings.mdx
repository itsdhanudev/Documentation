import ApiDoc from '@site/src/components/ApiDoc';

# GraphicsSettings

Complete API reference for `GraphicsSettings`, including constructors, attributes, virtual attributes, and methods.

**Package:** `JAVARuntime`

**Category:** World

**Component:** no

---

```java
public class GraphicsSettings
```

<ApiDoc
  constructors={[
    {
      signature: "GraphicsSettings(GraphicsSettings)",
      description: <>Executes <code>GraphicsSettings</code> and returns after the underlying engine call finishes.</>
    },
    {
      signature: "GraphicsSettings()",
      description: <>Executes <code>GraphicsSettings</code> and returns after the underlying engine call finishes.</>
    }
  ]}
  attributes={[
    {
      name: "graphicsSettings",
      type: "GraphicsSettings",
      description: <>Represents the <code>graphicsSettings</code> value stored on this instance. Read or assign to update the property.</>
    }
  ]}
/>
