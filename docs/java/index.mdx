# Java API

All runtime classes live inside `JAVARuntime` and are organized here by category.

- If a class extends `Component`, it appears under `Java API/Components`, with the category shown as `"<Category>"`.
- All other classes appear under `Java API`, with the category shown as `"<Category>"`.
- Classes without a category annotation are grouped under `Uncategorized`.
