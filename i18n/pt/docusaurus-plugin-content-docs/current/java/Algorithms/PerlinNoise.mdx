import ApiDoc from '@site/src/components/ApiDoc';

# PerlinNoise

Referência completa da API de `PerlinNoise`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Algorithms

**Componente:** não

---

```java
public class PerlinNoise
```

<ApiDoc
  constructors={[
    {
      signature: "PerlinNoise()",
      description: <>Executa <code>PerlinNoise</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "PerlinNoise(float)",
      description: <>Executa <code>PerlinNoise</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "scale",
      type: "float",
      description: <>Representa o valor de <code>scale</code> armazenado nesta instância. Leia ou atribua para atualizar
        a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "scale",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScale</code>. Acesse como um campo para ler ou escrever o
        valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "noise(double, double)",
      returns: "double",
      description: <>Executa <code>noise</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "noise(double, double, double)",
      returns: "double",
      description: <>Executa <code>noise</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "noise(double, double, double, double)",
      returns: "double",
      description: <>Executa <code>noise</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "noise(int, int)",
      returns: "float",
      description: <>Executa <code>noise</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "noise(int, int, int)",
      returns: "float",
      description: <>Executa <code>noise</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "noise(int, int, int, int)",
      returns: "float",
      description: <>Executa <code>noise</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "noise(float, float)",
      returns: "float",
      description: <>Executa <code>noise</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "noise(float, float, float)",
      returns: "float",
      description: <>Executa <code>noise</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "noise(float, float, float, float)",
      returns: "float",
      description: <>Executa <code>noise</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
