import ApiDoc from '@site/src/components/ApiDoc';

# AImageButton

Referência completa da API de `AImageButton`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AImageButton extends ImageButton
```

<ApiDoc
  constructors={[
    {
      signature: "AImageButton()",
      description: <>Cria uma nova instância de AImageButton.</>
    }
  ]}
  methods={[
    {
      signature: "setOnLongClickListener(OnLongClickListener)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>on long click listener``.</>
    },
    {
      signature: "setOnClickListener(OnClickListener)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>on click listener``.</>
    },
    {
      signature: "setOnTouchListener(OnTouchListener)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>on touch listener``.</>
    },
    {
      signature: "setOnHoverListener(OnHoverListener)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>on hover listener``.</>
    },
    {
      signature: "setOnDragListener(OnDragListener)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>on drag listener``.</>
    },
    {
      signature: "setOnKeyListener(OnKeyListener)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>on key listener``.</>
    },
    {
      signature: "setOnScrollChangeListener(OnScrollChangeListener)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>on scroll change listener``.</>
    },
    {
      signature: "setOnFocusChangeListener(OnFocusChangeListener)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>on focus change listener``.</>
    },
    {
      signature: "setOnCreateContextMenuListener(OnCreateContextMenuListener)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>on create context menu listener``.</>
    }
  ]}
/>
