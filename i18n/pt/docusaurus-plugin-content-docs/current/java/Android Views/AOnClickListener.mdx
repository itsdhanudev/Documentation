# AOnClickListener

Referência completa da API de `AOnClickListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnClickListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `onClick(View)` | `void` | Executa a operação onClick usando os parâmetros informados (v). |
| `onClickEvent(View)` | `void` | Executa `onClickEvent` e retorna quando a chamada do motor termina. |
