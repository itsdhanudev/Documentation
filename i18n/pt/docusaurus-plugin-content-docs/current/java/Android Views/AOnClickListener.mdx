import ApiDoc from '@site/src/components/ApiDoc';

# AOnClickListener

Referência completa da API de `AOnClickListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnClickListener
```

<ApiDoc
  methods={[
    {
      signature: "onClick(View)",
      returns: "void",
      description: <>Executa a operação onClick usando os parâmetros informados (v).</>
    },
    {
      signature: "onClickEvent(View)",
      returns: "void",
      description: <>Executa <code>onClickEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
