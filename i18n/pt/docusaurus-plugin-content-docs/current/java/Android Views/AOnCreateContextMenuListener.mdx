# AOnCreateContextMenuListener

Referência completa da API de `AOnCreateContextMenuListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnCreateContextMenuListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `onCreateContextMenu(ContextMenu, View, ContextMenu.ContextMenuInfo)` | `void` | Executa a operação onCreateContextMenu usando os parâmetros informados (menu, v, menuInfo). |
| `onCreateContextMenuEvent(ContextMenu, View, ContextMenu.ContextMenuInfo)` | `void` | Executa `onCreateContextMenuEvent` e retorna quando a chamada do motor termina. |
