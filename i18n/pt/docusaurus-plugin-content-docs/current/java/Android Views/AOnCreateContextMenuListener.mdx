import ApiDoc from '@site/src/components/ApiDoc';

# AOnCreateContextMenuListener

Referência completa da API de `AOnCreateContextMenuListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnCreateContextMenuListener
```

<ApiDoc
  methods={[
    {
      signature: "onCreateContextMenu(ContextMenu, View, ContextMenu.ContextMenuInfo)",
      returns: "void",
      description: <>Executa a operação onCreateContextMenu usando os parâmetros informados (menu, v, menuInfo).</>
    },
    {
      signature: "onCreateContextMenuEvent(ContextMenu, View, ContextMenu.ContextMenuInfo)",
      returns: "void",
      description: <>Executa <code>onCreateContextMenuEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
