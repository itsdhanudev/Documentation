# AOnDragListener

Referência completa da API de `AOnDragListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnDragListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `onDrag(View, DragEvent)` | `boolean` | Executa a operação onDrag usando os parâmetros informados (v, event). |
| `onDragEvent(View, DragEvent)` | `boolean` | Executa `onDragEvent` e retorna quando a chamada do motor termina. |
