import ApiDoc from '@site/src/components/ApiDoc';

# AOnDragListener

Referência completa da API de `AOnDragListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnDragListener
```

<ApiDoc
  methods={[
    {
      signature: "onDrag(View, DragEvent)",
      returns: "boolean",
      description: <>Executa a operação onDrag usando os parâmetros informados (v, event).</>
    },
    {
      signature: "onDragEvent(View, DragEvent)",
      returns: "boolean",
      description: <>Executa <code>onDragEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
