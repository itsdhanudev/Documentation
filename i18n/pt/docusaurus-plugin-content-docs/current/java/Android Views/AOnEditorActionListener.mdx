# AOnEditorActionListener

Referência completa da API de `AOnEditorActionListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnEditorActionListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `onEditorAction(TextView, int, KeyEvent)` | `boolean` | Executa a operação onEditorAction usando os parâmetros informados (v, actionId, event). |
| `onEditorActionEvent(TextView, int, KeyEvent)` | `boolean` | Executa `onEditorActionEvent` e retorna quando a chamada do motor termina. |
