import ApiDoc from '@site/src/components/ApiDoc';

# AOnEditorActionListener

Referência completa da API de `AOnEditorActionListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnEditorActionListener
```

<ApiDoc
  methods={[
    {
      signature: "onEditorAction(TextView, int, KeyEvent)",
      returns: "boolean",
      description: <>Executa a operação onEditorAction usando os parâmetros informados (v, actionId, event).</>
    },
    {
      signature: "onEditorActionEvent(TextView, int, KeyEvent)",
      returns: "boolean",
      description: <>Executa <code>onEditorActionEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
