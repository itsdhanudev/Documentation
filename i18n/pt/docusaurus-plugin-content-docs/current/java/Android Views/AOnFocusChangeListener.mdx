# AOnFocusChangeListener

Referência completa da API de `AOnFocusChangeListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnFocusChangeListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `onFocusChange(View, boolean)` | `void` | Executa a operação onFocusChange usando os parâmetros informados (v, hasFocus). |
| `onFocusChangeEvent(View, boolean)` | `void` | Executa `onFocusChangeEvent` e retorna quando a chamada do motor termina. |
