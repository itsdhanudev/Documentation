import ApiDoc from '@site/src/components/ApiDoc';

# AOnFocusChangeListener

Referência completa da API de `AOnFocusChangeListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnFocusChangeListener
```

<ApiDoc
  methods={[
    {
      signature: "onFocusChange(View, boolean)",
      returns: "void",
      description: <>Executa a operação onFocusChange usando os parâmetros informados (v, hasFocus).</>
    },
    {
      signature: "onFocusChangeEvent(View, boolean)",
      returns: "void",
      description: <>Executa <code>onFocusChangeEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
