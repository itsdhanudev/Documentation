import ApiDoc from '@site/src/components/ApiDoc';

# AOnGenericMotionListener

Referência completa da API de `AOnGenericMotionListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnGenericMotionListener
```

<ApiDoc
  methods={[
    {
      signature: "onGenericMotion(View, MotionEvent)",
      returns: "boolean",
      description: <>Executa a operação onGenericMotion usando os parâmetros informados (v, event).</>
    },
    {
      signature: "onGenericMotionEvent(View, MotionEvent)",
      returns: "boolean",
      description: <>Executa <code>onGenericMotionEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
