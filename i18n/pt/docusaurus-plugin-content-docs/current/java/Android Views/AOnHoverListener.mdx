# AOnHoverListener

Referência completa da API de `AOnHoverListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnHoverListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `onHover(View, MotionEvent)` | `boolean` | Executa a operação onHover usando os parâmetros informados (v, event). |
| `onHoverEvent(View, MotionEvent)` | `boolean` | Executa `onHoverEvent` e retorna quando a chamada do motor termina. |
