import ApiDoc from '@site/src/components/ApiDoc';

# AOnHoverListener

Referência completa da API de `AOnHoverListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnHoverListener
```

<ApiDoc
  methods={[
    {
      signature: "onHover(View, MotionEvent)",
      returns: "boolean",
      description: <>Executa a operação onHover usando os parâmetros informados (v, event).</>
    },
    {
      signature: "onHoverEvent(View, MotionEvent)",
      returns: "boolean",
      description: <>Executa <code>onHoverEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
