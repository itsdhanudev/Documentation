import ApiDoc from '@site/src/components/ApiDoc';

# AOnItemClickListener

Referência completa da API de `AOnItemClickListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnItemClickListener
```

<ApiDoc
  methods={[
    {
      signature: "onItemClick(AdapterView&lt;?&gt;, View, int, long)",
      returns: "void",
      description: <>Executa a operação onItemClick usando os parâmetros informados (parent, view, position, id).</>
    },
    {
      signature: "onItemClickEvent(AdapterView&lt;?&gt;, View, int, long)",
      returns: "void",
      description: <>Executa <code>onItemClickEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
