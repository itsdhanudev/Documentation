import ApiDoc from '@site/src/components/ApiDoc';

# AOnItemLongClickListener

Referência completa da API de `AOnItemLongClickListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnItemLongClickListener
```

<ApiDoc
  methods={[
    {
      signature: "onItemLongClick(AdapterView&lt;?&gt;, View, int, long)",
      returns: "boolean",
      description: <>Executa a operação onItemLongClick usando os parâmetros informados (parent, view, position, id).</>
    },
    {
      signature: "onItemLongClickEvent(AdapterView&lt;?&gt;, View, int, long)",
      returns: "boolean",
      description: <>Executa <code>onItemLongClickEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
