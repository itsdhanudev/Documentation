# AOnItemLongClickListener

Referência completa da API de `AOnItemLongClickListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnItemLongClickListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `onItemLongClick([AdapterView&lt;?&gt;], View, int, long)` | `boolean` | Executa a operação onItemLongClick usando os parâmetros informados (parent, view, position, id). |
| `onItemLongClickEvent([AdapterView&lt;?&gt;], View, int, long)` | `boolean` | Executa `onItemLongClickEvent` e retorna quando a chamada do motor termina. |
