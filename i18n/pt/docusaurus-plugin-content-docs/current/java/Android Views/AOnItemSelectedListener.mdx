# AOnItemSelectedListener

Referência completa da API de `AOnItemSelectedListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnItemSelectedListener
```

## Métodos (4)

| Signature | Returns | Description |
| --- | --- | --- |
| `onItemSelected([AdapterView&lt;?&gt;], View, int, long)` | `void` | Executa a operação onItemSelected usando os parâmetros informados (parent, view, position, id). |
| `onNothingSelected([AdapterView&lt;?&gt;])` | `void` | Executa a operação onNothingSelected usando os parâmetros informados (parent). |
| `onItemSelectedEvent([AdapterView&lt;?&gt;], View, int, long)` | `boolean` | Executa `onItemSelectedEvent` e retorna quando a chamada do motor termina. |
| `onNothingSelectedEvent([AdapterView&lt;?&gt;])` | `boolean` | Executa `onNothingSelectedEvent` e retorna quando a chamada do motor termina. |
