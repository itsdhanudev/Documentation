import ApiDoc from '@site/src/components/ApiDoc';

# AOnItemSelectedListener

Referência completa da API de `AOnItemSelectedListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnItemSelectedListener
```

<ApiDoc
  methods={[
    {
      signature: "onItemSelected(AdapterView&lt;?&gt;, View, int, long)",
      returns: "void",
      description: <>Executa a operação onItemSelected usando os parâmetros informados (parent, view, position, id).</>
    },
    {
      signature: "onNothingSelected(AdapterView&lt;?&gt;)",
      returns: "void",
      description: <>Executa a operação onNothingSelected usando os parâmetros informados (parent).</>
    },
    {
      signature: "onItemSelectedEvent(AdapterView&lt;?&gt;, View, int, long)",
      returns: "boolean",
      description: <>Executa <code>onItemSelectedEvent</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "onNothingSelectedEvent(AdapterView&lt;?&gt;)",
      returns: "boolean",
      description: <>Executa <code>onNothingSelectedEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
