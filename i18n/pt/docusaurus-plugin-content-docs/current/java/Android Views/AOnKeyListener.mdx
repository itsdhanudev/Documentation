# AOnKeyListener

Referência completa da API de `AOnKeyListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnKeyListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `onKey(View, int, KeyEvent)` | `boolean` | Executa a operação onKey usando os parâmetros informados (v, keyCode, event). |
| `onKeyEvent(View, int, KeyEvent)` | `boolean` | Executa `onKeyEvent` e retorna quando a chamada do motor termina. |
