import ApiDoc from '@site/src/components/ApiDoc';

# AOnKeyListener

Referência completa da API de `AOnKeyListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnKeyListener
```

<ApiDoc
  methods={[
    {
      signature: "onKey(View, int, KeyEvent)",
      returns: "boolean",
      description: <>Executa a operação onKey usando os parâmetros informados (v, keyCode, event).</>
    },
    {
      signature: "onKeyEvent(View, int, KeyEvent)",
      returns: "boolean",
      description: <>Executa <code>onKeyEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
