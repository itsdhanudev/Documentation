import ApiDoc from '@site/src/components/ApiDoc';

# AOnLongClickListener

Referência completa da API de `AOnLongClickListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnLongClickListener
```

<ApiDoc
  methods={[
    {
      signature: "onLongClick(View)",
      returns: "boolean",
      description: <>Executa a operação onLongClick usando os parâmetros informados (v).</>
    },
    {
      signature: "onLongClickEvent(View)",
      returns: "boolean",
      description: <>Executa <code>onLongClickEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
