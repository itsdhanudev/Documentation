import ApiDoc from '@site/src/components/ApiDoc';

# AOnScrollChangeListener

Referência completa da API de `AOnScrollChangeListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnScrollChangeListener
```

<ApiDoc
  methods={[
    {
      signature: "onScrollChange(View, int, int, int, int)",
      returns: "void",
      description: <>Executa a operação onScrollChange usando os parâmetros informados (v, scrollX, scrollY, oldScrollX, oldScrollY).</>
    },
    {
      signature: "onScrollChangeEvent(View, int, int, int, int)",
      returns: "void",
      description: <>Executa <code>onScrollChangeEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
