# AOnScrollChangeListener

Referência completa da API de `AOnScrollChangeListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnScrollChangeListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `onScrollChange(View, int, int, int, int)` | `void` | Executa a operação onScrollChange usando os parâmetros informados (v, scrollX, scrollY, oldScrollX, oldScrollY). |
| `onScrollChangeEvent(View, int, int, int, int)` | `void` | Executa `onScrollChangeEvent` e retorna quando a chamada do motor termina. |
