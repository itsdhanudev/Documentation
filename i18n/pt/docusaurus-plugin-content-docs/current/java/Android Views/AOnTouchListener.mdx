import ApiDoc from '@site/src/components/ApiDoc';

# AOnTouchListener

Referência completa da API de `AOnTouchListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnTouchListener
```

<ApiDoc
  methods={[
    {
      signature: "onTouch(View, MotionEvent)",
      returns: "boolean",
      description: <>Executa a operação onTouch usando os parâmetros informados (v, event).</>
    },
    {
      signature: "onTouchEvent(View, MotionEvent)",
      returns: "boolean",
      description: <>Executa <code>onTouchEvent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
