# AOnTouchListener

Referência completa da API de `AOnTouchListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public class AOnTouchListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `onTouch(View, MotionEvent)` | `boolean` | Executa a operação onTouch usando os parâmetros informados (v, event). |
| `onTouchEvent(View, MotionEvent)` | `boolean` | Executa `onTouchEvent` e retorna quando a chamada do motor termina. |
