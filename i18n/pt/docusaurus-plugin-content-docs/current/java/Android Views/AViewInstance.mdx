import ApiDoc from '@site/src/components/ApiDoc';

# AViewInstance

Referência completa da API de `AViewInstance`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public interface AViewInstance
```

<ApiDoc
  methods={[
    {
      signature: "getViewID()",
      returns: "String",
      description: <>Executa <code>getViewID</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "findByID(String)",
      returns: "<T extends View> T",
      description: <>Executa <code>findByID</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
