# AViewInstance

Referência completa da API de `AViewInstance`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Android Views

**Componente:** não

---

```java
public interface AViewInstance
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `getViewID()` | `String` | Executa `getViewID` e retorna quando a chamada do motor termina. |
| `findByID(String)` | `<T extends View> T` | Executa `findByID` e retorna quando a chamada do motor termina. |
