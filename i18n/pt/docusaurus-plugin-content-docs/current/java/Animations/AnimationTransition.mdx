import ApiDoc from '@site/src/components/ApiDoc';

# AnimationTransition

Referência completa da API de `AnimationTransition`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Animations

**Componente:** não

---

```java
public class AnimationTransition
```

<ApiDoc
  constructors={[
    {
      signature: "AnimationTransition(AnimationTransition)",
      description: <>Executa <code>AnimationTransition</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "AnimationTransition",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "isFinished()",
      returns: "boolean",
      description: <>Retorna se a transição de animação terminou.</>
    }
  ]}
/>
