import ApiDoc from '@site/src/components/ApiDoc';

# AsyncTask

Referência completa da API de `AsyncTask`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Async

**Componente:** não

---

```java
public class AsyncTask
```

<ApiDoc
  constructors={[
    {
      signature: "AsyncTask(AsyncRunnable)",
      description: <>Cria uma nova instância de AsyncTask.</>
    },
    {
      signature: "AsyncTask(Object, AsyncRunnable)",
      description: <>Cria uma nova instância de AsyncTask.</>
    }
  ]}
/>
