# AtomicBoolean

Referência completa da API de `AtomicBoolean`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Atomic

**Componente:** não

---

```java
public class AtomicBoolean
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| AtomicBoolean([AtomicBoolean]) | Executa `AtomicBoolean` e retorna quando a chamada do motor termina. |
| AtomicBoolean() | Executa `AtomicBoolean` e retorna quando a chamada do motor termina. |
| AtomicBoolean([boolean]) | Cria uma nova instância de AtomicBoolean. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [AtomicBoolean] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (6)

| Signature | Returns | Description |
| --- | --- | --- |
| `get()` | `boolean` | Retorna a propriedade `propriedade `valor value atual``. |
| `set(boolean)` | `void` | Define ou atualiza the value. |
| `lazySet(boolean)` | `void` | Executa a operação lazySet usando os parâmetros informados (value). |
| `getAndSet(boolean)` | `boolean` | Retorna a propriedade `propriedade `and set atual``. |
| `compareAndSet(boolean, boolean)` | `boolean` | Compara esta instância com outra para igualdade/ordem. |
| `weakCompareAndSet(boolean, boolean)` | `boolean` | Executa a operação weakCompareAndSet usando os parâmetros informados (expect, update). |
