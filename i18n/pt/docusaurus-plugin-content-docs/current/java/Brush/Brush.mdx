# Brush

Referência completa da API de `Brush`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Brush

**Componente:** não

---

```java
public interface Brush
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([\{"x"], ["y"\}])` | `void` | Executa a operação @MethodArgs. |
| `getIntensity(float, float)` | `float` | Executa `getIntensity` e retorna quando a chamada do motor termina. |
