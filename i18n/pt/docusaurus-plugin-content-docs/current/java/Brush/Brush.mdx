import ApiDoc from '@site/src/components/ApiDoc';

# Brush

Referência completa da API de `Brush`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Brush

**Componente:** não

---

```java
public interface Brush
```

<ApiDoc
  methods={[
    {
      signature: "getIntensity(float, float)",
      returns: "float",
      description: <>Executa <code>getIntensity</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
