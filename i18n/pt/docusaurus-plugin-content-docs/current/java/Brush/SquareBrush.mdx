import ApiDoc from '@site/src/components/ApiDoc';

# SquareBrush

Referência completa da API de `SquareBrush`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Brush

**Componente:** não

---

```java
public class SquareBrush
```

<ApiDoc
  methods={[
    {
      signature: "getIntensity(float, float)",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>intensity atual``.</>
    }
  ]}
/>
