import ApiDoc from '@site/src/components/ApiDoc';

# CircleBrush

Referência completa da API de `CircleBrush`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Brushs

**Componente:** não

---

```java
public class CircleBrush
```

<ApiDoc
  methods={[
    {
      signature: "getIntensity(float, float)",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>intensity atual``.</>
    }
  ]}
/>
