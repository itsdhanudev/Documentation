# BufferUtils

Referência completa da API de `BufferUtils`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class BufferUtils
```

## Métodos Estáticos (12)

| Signature | Returns | Description |
| --- | --- | --- |
| `createByteBuffer(int)` | `ByteBuffer` | Cria um novo byte buffer. |
| `createByteBuffer(byte[])` | `ByteBuffer` | Cria um novo byte buffer. |
| `createFloatBuffer(int)` | `FloatBuffer` | Cria um novo float buffer. |
| `createFloatBuffer(float[])` | `FloatBuffer` | Cria um novo float buffer. |
| `createIntBuffer(int)` | `IntBuffer` | Cria um novo int buffer. |
| `createIntBuffer(int[])` | `IntBuffer` | Cria um novo int buffer. |
| `createVector3Buffer(int)` | `Vector3Buffer` | Cria um novo vector3 buffer. |
| `createVector3Buffer([Vector3[]])` | `Vector3Buffer` | Cria um novo vector3 buffer. |
| `createVector2Buffer(int)` | `Vector2Buffer` | Cria um novo vector2 buffer. |
| `createVector2Buffer([Vector2[]])` | `Vector2Buffer` | Cria um novo vector2 buffer. |
| `createPoint3Buffer(int)` | `Point3Buffer` | Cria um novo point3 buffer. |
| `createPoint3Buffer([Point3[]])` | `Point3Buffer` | Cria um novo point3 buffer. |
