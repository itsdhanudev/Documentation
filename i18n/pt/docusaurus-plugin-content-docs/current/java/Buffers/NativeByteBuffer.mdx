# NativeByteBuffer

Referência completa da API de `NativeByteBuffer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class NativeByteBuffer
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| NativeByteBuffer([NativeByteBuffer]) | Executa `NativeByteBuffer` e retorna quando a chamada do motor termina. |
| NativeByteBuffer([Engine]) | Executa `NativeByteBuffer` e retorna quando a chamada do motor termina. |
| NativeByteBuffer([int]) | Cria uma nova instância de NativeByteBuffer. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `buffer` | [NativeByteBuffer] | Representa o valor de `buffer` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (7)

| Signature | Returns | Description |
| --- | --- | --- |
| `get(int)` | `byte` | Retorna a propriedade `propriedade `valor value atual``. |
| `set(int, byte)` | `void` | Define ou atualiza the value. |
| `put(byte)` | `void` | Define ou atualiza the value. |
| `capacity()` | `int` | Executa a operação capacity. |
| `getPosition()` | `int` | Retorna a propriedade `propriedade `position atual``. |
| `Position(int)` | `void` | Executa a operação Position usando os parâmetros informados (idx). |
| `setPosition(int)` | `void` | Define ou atualiza a propriedade `propriedade `position``. |
