import ApiDoc from '@site/src/components/ApiDoc';

# NativeByteBuffer

Referência completa da API de `NativeByteBuffer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class NativeByteBuffer
```

<ApiDoc
  constructors={[
    {
      signature: "NativeByteBuffer(NativeByteBuffer)",
      description: <>Executa <code>NativeByteBuffer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NativeByteBuffer(Engine)",
      description: <>Executa <code>NativeByteBuffer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NativeByteBuffer(int)",
      description: <>Cria uma nova instância de NativeByteBuffer.</>
    }
  ]}
  attributes={[
    {
      name: "buffer",
      type: "NativeByteBuffer",
      description: <>Representa o valor de <code>buffer</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "get(int)",
      returns: "byte",
      description: <>Retorna a propriedade <code>propriedade </code>valor value atual``.</>
    },
    {
      signature: "set(int, byte)",
      returns: "void",
      description: <>Define ou atualiza the value.</>
    },
    {
      signature: "put(byte)",
      returns: "void",
      description: <>Define ou atualiza the value.</>
    },
    {
      signature: "capacity()",
      returns: "int",
      description: <>Executa a operação capacity.</>
    },
    {
      signature: "getPosition()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>position atual``.</>
    },
    {
      signature: "Position(int)",
      returns: "void",
      description: <>Executa a operação Position usando os parâmetros informados (idx).</>
    },
    {
      signature: "setPosition(int)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>position``.</>
    }
  ]}
/>
