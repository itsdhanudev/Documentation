import ApiDoc from '@site/src/components/ApiDoc';

# NativeCharBuffer

Referência completa da API de `NativeCharBuffer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class NativeCharBuffer
```

<ApiDoc
  constructors={[
    {
      signature: "NativeCharBuffer(NativeCharBuffer)",
      description: <>Executa <code>NativeCharBuffer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NativeCharBuffer(Engine)",
      description: <>Executa <code>NativeCharBuffer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NativeCharBuffer(int)",
      description: <>Cria uma nova instância de NativeCharBuffer.</>
    }
  ]}
  attributes={[
    {
      name: "buffer",
      type: "NativeCharBuffer",
      description: <>Representa o valor de <code>buffer</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "get(int)",
      returns: "char",
      description: <>Retorna a propriedade <code>propriedade </code>valor value atual``.</>
    },
    {
      signature: "set(int, char)",
      returns: "void",
      description: <>Define ou atualiza the value.</>
    },
    {
      signature: "put(char)",
      returns: "void",
      description: <>Define ou atualiza the value.</>
    },
    {
      signature: "capacity()",
      returns: "int",
      description: <>Executa a operação capacity.</>
    },
    {
      signature: "getPosition()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>position atual``.</>
    },
    {
      signature: "Position(int)",
      returns: "void",
      description: <>Executa a operação Position usando os parâmetros informados (idx).</>
    },
    {
      signature: "setPosition(int)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>position``.</>
    }
  ]}
/>
