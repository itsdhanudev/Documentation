# NativeCharBuffer

Referência completa da API de `NativeCharBuffer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class NativeCharBuffer
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| NativeCharBuffer([NativeCharBuffer]) | Executa `NativeCharBuffer` e retorna quando a chamada do motor termina. |
| NativeCharBuffer([Engine]) | Executa `NativeCharBuffer` e retorna quando a chamada do motor termina. |
| NativeCharBuffer([int]) | Cria uma nova instância de NativeCharBuffer. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `buffer` | [NativeCharBuffer] | Representa o valor de `buffer` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (7)

| Signature | Returns | Description |
| --- | --- | --- |
| `get(int)` | `char` | Retorna a propriedade `propriedade `valor value atual``. |
| `set(int, char)` | `void` | Define ou atualiza the value. |
| `put(char)` | `void` | Define ou atualiza the value. |
| `capacity()` | `int` | Executa a operação capacity. |
| `getPosition()` | `int` | Retorna a propriedade `propriedade `position atual``. |
| `Position(int)` | `void` | Executa a operação Position usando os parâmetros informados (idx). |
| `setPosition(int)` | `void` | Define ou atualiza a propriedade `propriedade `position``. |
