# NativeFloatBuffer

Referência completa da API de `NativeFloatBuffer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class NativeFloatBuffer
```

## Construtores (4)

| Signature | Description |
| --- | --- |
| NativeFloatBuffer([NativeFloatBuffer]) | Executa `NativeFloatBuffer` e retorna quando a chamada do motor termina. |
| NativeFloatBuffer([Engine]) | Executa `NativeFloatBuffer` e retorna quando a chamada do motor termina. |
| NativeFloatBuffer([int]) | Cria uma nova instância de NativeFloatBuffer. |
| NativeFloatBuffer([int], [Precision]) | Cria uma nova instância de NativeFloatBuffer. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `buffer` | [NativeFloatBuffer] | Representa o valor de `buffer` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (19)

| Signature | Returns | Description |
| --- | --- | --- |
| `get(int)` | `float` | Retorna a propriedade `propriedade `valor value atual``. |
| `get(float[])` | `void` | Retorna a propriedade `propriedade `valor value atual``. |
| `get(float[], int)` | `void` | Retorna a propriedade `propriedade `valor value atual``. |
| `get(int, float[])` | `void` | Retorna a propriedade `propriedade `valor value atual``. |
| `get(int, float[], int, int)` | `void` | Retorna a propriedade `propriedade `valor value atual``. |
| `set(int, float)` | `void` | Define ou atualiza the value. |
| `put(float)` | `void` | Define ou atualiza the value. |
| `put(float[])` | `void` | Define ou atualiza the value. |
| `put(short[])` | `void` | Define ou atualiza the value. |
| `put(NativeFloatBuffer)` | `void` | Executa `put` e retorna quando a chamada do motor termina. |
| `capacity()` | `int` | Executa a operação capacity. |
| `getPosition()` | `int` | Retorna a propriedade `propriedade `position atual``. |
| `position(int)` | `int` | Executa a operação position usando os parâmetros informados (idx). |
| `setPosition(int)` | `void` | Define ou atualiza a propriedade `propriedade `position``. |
| `set(float[])` | `void` | Define ou atualiza the value. |
| `set(float[], int)` | `void` | Define ou atualiza the value. |
| `set(int, float[])` | `void` | Define ou atualiza the value. |
| `set(int, float[], int, int)` | `void` | Define ou atualiza the value. |
| `getPrecision()` | `Precision` | Retorna a propriedade `propriedade `precision atual``. |
