import ApiDoc from '@site/src/components/ApiDoc';

# NativeIntBuffer

Referência completa da API de `NativeIntBuffer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class NativeIntBuffer
```

<ApiDoc
  constructors={[
    {
      signature: "NativeIntBuffer(NativeIntBuffer)",
      description: <>Executa <code>NativeIntBuffer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NativeIntBuffer(Engine)",
      description: <>Executa <code>NativeIntBuffer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NativeIntBuffer(int)",
      description: <>Cria uma nova instância de NativeIntBuffer.</>
    }
  ]}
  attributes={[
    {
      name: "buffer",
      type: "NativeIntBuffer",
      description: <>Representa o valor de <code>buffer</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "get(int)",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>valor value atual``.</>
    },
    {
      signature: "set(int, int)",
      returns: "void",
      description: <>Define ou atualiza the value.</>
    },
    {
      signature: "put(int)",
      returns: "void",
      description: <>Define ou atualiza the value.</>
    },
    {
      signature: "capacity()",
      returns: "int",
      description: <>Executa a operação capacity.</>
    },
    {
      signature: "getPosition()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>position atual``.</>
    },
    {
      signature: "Position(int)",
      returns: "void",
      description: <>Executa a operação Position usando os parâmetros informados (idx).</>
    },
    {
      signature: "setPosition(int)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>position``.</>
    }
  ]}
/>
