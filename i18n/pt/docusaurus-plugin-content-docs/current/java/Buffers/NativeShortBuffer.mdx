import ApiDoc from '@site/src/components/ApiDoc';

# NativeShortBuffer

Referência completa da API de `NativeShortBuffer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class NativeShortBuffer
```

<ApiDoc
  constructors={[
    {
      signature: "NativeShortBuffer(NativeShortBuffer)",
      description: <>Executa <code>NativeShortBuffer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NativeShortBuffer(Engine)",
      description: <>Executa <code>NativeShortBuffer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NativeShortBuffer(int)",
      description: <>Cria uma nova instância de NativeShortBuffer.</>
    }
  ]}
  attributes={[
    {
      name: "buffer",
      type: "NativeShortBuffer",
      description: <>Representa o valor de <code>buffer</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "vboEnabled",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isVboEnabled</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "get(int)",
      returns: "short",
      description: <>Retorna a propriedade <code>propriedade </code>valor value atual``.</>
    },
    {
      signature: "set(int, short)",
      returns: "void",
      description: <>Define ou atualiza the value.</>
    },
    {
      signature: "put(short)",
      returns: "void",
      description: <>Define ou atualiza the value.</>
    },
    {
      signature: "capacity()",
      returns: "int",
      description: <>Executa a operação capacity.</>
    },
    {
      signature: "getPosition()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>position atual``.</>
    },
    {
      signature: "position(int)",
      returns: "int",
      description: <>Executa a operação position usando os parâmetros informados (idx).</>
    },
    {
      signature: "setPosition(int)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>position``.</>
    }
  ]}
/>
