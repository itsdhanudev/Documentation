# OH2LevelFloatArray

Referência completa da API de `OH2LevelFloatArray`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class OH2LevelFloatArray extends OHFloatDataBase
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| OH2LevelFloatArray([Engine]) | Cria uma nova instância de OH2LevelFloatArray. |
| OH2LevelFloatArray([int], [int]) | Cria uma nova instância de OH2LevelFloatArray. |
| OH2LevelFloatArray([int], [int], [boolean]) | Cria uma nova instância de OH2LevelFloatArray. |

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `get(int, int)` | `float` | Retorna a propriedade `propriedade `valor value atual``. |
| `set(int, int, float)` | `void` | Define ou atualiza the value. |
