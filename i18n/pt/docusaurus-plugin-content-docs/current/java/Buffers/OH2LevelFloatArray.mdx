import ApiDoc from '@site/src/components/ApiDoc';

# OH2LevelFloatArray

Referência completa da API de `OH2LevelFloatArray`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class OH2LevelFloatArray extends OHFloatDataBase
```

<ApiDoc
  constructors={[
    {
      signature: "OH2LevelFloatArray(Engine)",
      description: <>Cria uma nova instância de OH2LevelFloatArray.</>
    },
    {
      signature: "OH2LevelFloatArray(int, int)",
      description: <>Cria uma nova instância de OH2LevelFloatArray.</>
    },
    {
      signature: "OH2LevelFloatArray(int, int, boolean)",
      description: <>Cria uma nova instância de OH2LevelFloatArray.</>
    }
  ]}
  methods={[
    {
      signature: "get(int, int)",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>valor value atual``.</>
    },
    {
      signature: "set(int, int, float)",
      returns: "void",
      description: <>Define ou atualiza the value.</>
    }
  ]}
/>
