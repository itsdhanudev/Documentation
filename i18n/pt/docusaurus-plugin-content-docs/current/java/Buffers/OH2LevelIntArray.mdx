# OH2LevelIntArray

Referência completa da API de `OH2LevelIntArray`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class OH2LevelIntArray extends OHIntDataBase
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| OH2LevelIntArray([Engine]) | Cria uma nova instância de OH2LevelIntArray. |
| OH2LevelIntArray([int], [int]) | Cria uma nova instância de OH2LevelIntArray. |
| OH2LevelIntArray([int], [int], [boolean]) | Cria uma nova instância de OH2LevelIntArray. |

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `get(int, int)` | `int` | Retorna a propriedade `propriedade `valor value atual``. |
| `set(int, int, int)` | `void` | Define ou atualiza the value. |
