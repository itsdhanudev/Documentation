# OH3LevelFloatArray

Referência completa da API de `OH3LevelFloatArray`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class OH3LevelFloatArray extends OHFloatDataBase
```

## Construtores (4)

| Signature | Description |
| --- | --- |
| OH3LevelFloatArray([OH3LevelFloatArray]) | Executa `OH3LevelFloatArray` e retorna quando a chamada do motor termina. |
| OH3LevelFloatArray([Engine]) | Executa `OH3LevelFloatArray` e retorna quando a chamada do motor termina. |
| OH3LevelFloatArray([int], [int], [int]) | Cria uma nova instância de OH3LevelFloatArray. |
| OH3LevelFloatArray([int], [int], [int], [boolean]) | Cria uma nova instância de OH3LevelFloatArray. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `buffer` | [OH3LevelFloatArray] | Representa o valor de `buffer` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (5)

| Signature | Returns | Description |
| --- | --- | --- |
| `get(int, int, int)` | `float` | Retorna a propriedade `propriedade `valor value atual``. |
| `set(int, int, int, float)` | `void` | Define ou atualiza the value. |
| `getSizeX()` | `int` | Retorna a propriedade `propriedade `size x atual``. |
| `getSizeY()` | `int` | Retorna a propriedade `propriedade `size y atual``. |
| `getSizeZ()` | `int` | Retorna a propriedade `propriedade `size z atual``. |
