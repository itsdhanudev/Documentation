import ApiDoc from '@site/src/components/ApiDoc';

# OH3LevelFloatArray

Referência completa da API de `OH3LevelFloatArray`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class OH3LevelFloatArray extends OHFloatDataBase
```

<ApiDoc
  constructors={[
    {
      signature: "OH3LevelFloatArray(OH3LevelFloatArray)",
      description: <>Executa <code>OH3LevelFloatArray</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "OH3LevelFloatArray(Engine)",
      description: <>Executa <code>OH3LevelFloatArray</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "OH3LevelFloatArray(int, int, int)",
      description: <>Cria uma nova instância de OH3LevelFloatArray.</>
    },
    {
      signature: "OH3LevelFloatArray(int, int, int, boolean)",
      description: <>Cria uma nova instância de OH3LevelFloatArray.</>
    }
  ]}
  attributes={[
    {
      name: "buffer",
      type: "OH3LevelFloatArray",
      description: <>Representa o valor de <code>buffer</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "get(int, int, int)",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>valor value atual``.</>
    },
    {
      signature: "set(int, int, int, float)",
      returns: "void",
      description: <>Define ou atualiza the value.</>
    },
    {
      signature: "getSizeX()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>size x atual``.</>
    },
    {
      signature: "getSizeY()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>size y atual``.</>
    },
    {
      signature: "getSizeZ()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>size z atual``.</>
    }
  ]}
/>
