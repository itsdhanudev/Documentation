# OH3LevelIntArray

Referência completa da API de `OH3LevelIntArray`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class OH3LevelIntArray extends OHIntDataBase
```

## Construtores (4)

| Signature | Description |
| --- | --- |
| OH3LevelIntArray([OH3LevelIntArray]) | Executa `OH3LevelIntArray` e retorna quando a chamada do motor termina. |
| OH3LevelIntArray([Engine]) | Executa `OH3LevelIntArray` e retorna quando a chamada do motor termina. |
| OH3LevelIntArray([int], [int], [int]) | Cria uma nova instância de OH3LevelIntArray. |
| OH3LevelIntArray([int], [int], [int], [boolean]) | Cria uma nova instância de OH3LevelIntArray. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `buffer` | [OH3LevelIntArray] | Representa o valor de `buffer` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (5)

| Signature | Returns | Description |
| --- | --- | --- |
| `get(int, int, int)` | `int` | Retorna a propriedade `propriedade `valor value atual``. |
| `set(int, int, int, int)` | `void` | Define ou atualiza the value. |
| `getSizeX()` | `int` | Retorna a propriedade `propriedade `size x atual``. |
| `getSizeY()` | `int` | Retorna a propriedade `propriedade `size y atual``. |
| `getSizeZ()` | `int` | Retorna a propriedade `propriedade `size z atual``. |
