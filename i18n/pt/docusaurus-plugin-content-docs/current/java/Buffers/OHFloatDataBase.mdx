# OHFloatDataBase

Referência completa da API de `OHFloatDataBase`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class OHFloatDataBase
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| OHFloatDataBase([OHFloatDataBase]) | Executa `OHFloatDataBase` e retorna quando a chamada do motor termina. |
| OHFloatDataBase([Engine]) | Executa `OHFloatDataBase` e retorna quando a chamada do motor termina. |
| OHFloatDataBase([int]) | Cria uma nova instância de OHFloatDataBase. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `buffer` | [OHFloatDataBase] | Representa o valor de `buffer` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (12)

| Signature | Returns | Description |
| --- | --- | --- |
| `incrementSize(int)` | `void` | Executa a operação incrementSize usando os parâmetros informados (size). |
| `decrementSize(int)` | `void` | Executa a operação decrementSize usando os parâmetros informados (size). |
| `resize(int)` | `void` | Executa a operação resize usando os parâmetros informados (newCapacity). |
| `resize(int, boolean)` | `void` | Executa a operação resize usando os parâmetros informados (newCapacity, copyData). |
| `position(int)` | `void` | Executa a operação position usando os parâmetros informados (position). |
| `put(float[])` | `void` | Define ou atualiza the value. |
| `put(float)` | `void` | Define ou atualiza the value. |
| `set(int, float)` | `void` | Define ou atualiza the value. |
| `get(int)` | `float` | Retorna a propriedade `propriedade `valor value atual``. |
| `getBytes()` | [float][] | Retorna a propriedade `propriedade `bytes atual``. |
| `size()` | `int` | Executa a operação size. |
| `capacity()` | `int` | Executa a operação capacity. |
