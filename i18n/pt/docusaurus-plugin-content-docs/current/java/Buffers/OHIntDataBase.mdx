# OHIntDataBase

Referência completa da API de `OHIntDataBase`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Buffers

**Componente:** não

---

```java
public class OHIntDataBase
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| OHIntDataBase([OHIntDataBase]) | Executa `OHIntDataBase` e retorna quando a chamada do motor termina. |
| OHIntDataBase([Engine]) | Executa `OHIntDataBase` e retorna quando a chamada do motor termina. |
| OHIntDataBase([int]) | Cria uma nova instância de OHIntDataBase. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `buffer` | [OHIntDataBase] | Representa o valor de `buffer` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (12)

| Signature | Returns | Description |
| --- | --- | --- |
| `incrementSize(int)` | `void` | Executa a operação incrementSize usando os parâmetros informados (size). |
| `decrementSize(int)` | `void` | Executa a operação decrementSize usando os parâmetros informados (size). |
| `resize(int)` | `void` | Executa a operação resize usando os parâmetros informados (newCapacity). |
| `resize(int, boolean)` | `void` | Executa a operação resize usando os parâmetros informados (newCapacity, copyData). |
| `position(int)` | `void` | Executa a operação position usando os parâmetros informados (position). |
| `put(int[])` | `void` | Define ou atualiza the value. |
| `put(int)` | `void` | Define ou atualiza the value. |
| `set(int, int)` | `void` | Define ou atualiza the value. |
| `get(int)` | `int` | Retorna a propriedade `propriedade `valor value atual``. |
| `getBytes()` | [int][] | Retorna a propriedade `propriedade `bytes atual``. |
| `size()` | `int` | Executa a operação size. |
| `capacity()` | `int` | Executa a operação capacity. |
