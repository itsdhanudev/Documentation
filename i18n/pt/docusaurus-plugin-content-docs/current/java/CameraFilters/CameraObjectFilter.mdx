# CameraObjectFilter

Referência completa da API de `CameraObjectFilter`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** CameraFilters

**Componente:** não

---

```java
public interface CameraObjectFilter
```

## Métodos (15)

| Signature | Returns | Description |
| --- | --- | --- |
| `filterShadow()` | `boolean` | Filtra qual Sombra será renderizada. Retorne verdadeiro para renderizar e falso para ignorar. |
| `@MethodArgs([\{"renderer"\}])` | `void` | Filtra qual ModelRenderer será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `filterRenderer(ModelRenderer)` | `boolean` | Executa `filterRenderer` e retorna quando a chamada do motor termina. |
| `@MethodArgs([\{"renderer"\}])` | `void` | Filtra qual SkinnedModelRenderer será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `filterRenderer(SkinnedModelRenderer)` | `boolean` | Executa `filterRenderer` e retorna quando a chamada do motor termina. |
| `@MethodArgs([\{"material"], ["bake"\}])` | `void` | Filtra qual Bake será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `filterMaterialBake(Material, Vertex)` | `boolean` | Executa `filterMaterialBake` e retorna quando a chamada do motor termina. |
| `@MethodArgs([\{"component"\}])` | `void` | Filtra qual Texto 3D será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `filter3DText(Component)` | `boolean` | Executa `filter3DText` e retorna quando a chamada do motor termina. |
| `renderFog()` | `boolean` | Define se o Fog será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `renderParticles()` | `boolean` | Define se as Partículas serão renderizadas. Retorne verdadeiro para renderizar e falso para ignorar. |
| `@MethodArgs([\{"particleEmitter"\}])` | `void` | Filtra qual Emissor de Partículas será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `filterParticle(ParticleEmitter)` | `boolean` | Executa `filterParticle` e retorna quando a chamada do motor termina. |
| `overrideAmbientLight()` | `Color` | Substitui a Cor da Luz Ambiente. Retorne a Cor que será substituída. |
| `overrideIndirectLight()` | `Color` | Substitui a Cor da Luz Indireta. Retorne a Cor que será substituída. |
