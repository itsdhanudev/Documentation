import ApiDoc from '@site/src/components/ApiDoc';

# EnemyAITypeA

Referência completa da API de `EnemyAITypeA`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** AI

**Componente:** sim

---

```java
public class EnemyAITypeA extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "EnemyAITypeA(EnemyAITypeA)",
      description: <>Executa <code>EnemyAITypeA</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "EnemyAITypeA(Engine)",
      description: <>Executa <code>EnemyAITypeA</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "EnemyAITypeA()",
      description: <>Cria uma nova instância de EnemyAITypeA.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "EnemyAITypeA",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "attackDelay",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAttackDelay</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "fieldOfView",
      type: "TagEnemyFieldOfView",
      access: "get",
      description: <>Atributo virtual exposto por <code>getFieldOfView</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "points",
      type: "SpatialObject",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPoints</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "rayDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRayDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "rayOffset",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRayOffset</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "runSpeed",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRunSpeed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "startAttackDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getStartAttackDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "state",
      type: "State",
      access: "get",
      description: <>Atributo virtual exposto por <code>getState</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "stickBottom",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setStickBottom</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "stickBottomOffset",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getStickBottomOffset</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "swapTargetDelay",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSwapTargetDelay</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "swapTargetDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSwapTargetDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "walkSpeed",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getWalkSpeed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isStickBottom()",
      returns: "boolean",
      description: <>Retorna se stick bottom é verdadeiro.</>
    }
  ]}
/>
