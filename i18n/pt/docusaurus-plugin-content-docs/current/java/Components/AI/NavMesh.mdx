import ApiDoc from '@site/src/components/ApiDoc';

# NavMesh

Referência completa da API de `NavMesh`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** AI

**Componente:** sim

---

```java
public class NavMesh extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "NavMesh(BakedNavMesh)",
      description: <>Executa <code>NavMesh</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NavMesh(Engine)",
      description: <>Executa <code>NavMesh</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NavMesh()",
      description: <>Cria uma nova instância de NavMesh.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "BakedNavMesh",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "agentHeight",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAgentHeight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "agentMaxClimb",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAgentMaxClimb</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "agentMaxSlope",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAgentMaxSlope</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "agentWidth",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAgentWidth</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "cellHeight",
      type: "float",
      access: "get",
      description: <>Atributo virtual exposto por <code>getCellHeight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "cellSize",
      type: "float",
      access: "get",
      description: <>Atributo virtual exposto por <code>getCellSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "detailSampleDist",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getDetailSampleDist</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "detailSampleMaxError",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getDetailSampleMaxError</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "edgeMaxError",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getEdgeMaxError</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "edgeMaxLen",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getEdgeMaxLen</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxPathFails",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxPathFails</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxTiles",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxTiles</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "regionMergeArea",
      type: "float",
      access: "get",
      description: <>Atributo virtual exposto por <code>getRegionMergeArea</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "regionMergeSize",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRegionMergeSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "regionMinArea",
      type: "float",
      access: "get",
      description: <>Atributo virtual exposto por <code>getRegionMinArea</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "regionMinSize",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRegionMinSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "tileSize",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTileSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "generateBake()",
      returns: "void",
      description: <>Executa a operação generateBake.</>
    }
  ]}
/>
