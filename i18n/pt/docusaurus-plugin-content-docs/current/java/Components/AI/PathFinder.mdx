import ApiDoc from '@site/src/components/ApiDoc';

# PathFinder

Referência completa da API de `PathFinder`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** AI

**Componente:** sim

---

```java
public class PathFinder extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "PathFinder(PathFinder)",
      description: <>Executa <code>PathFinder</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "PathFinder()",
      description: <>Executa <code>PathFinder</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "PathFinder",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "searchDelay",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSearchDelay</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "target",
      type: "SpatialObject",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTarget</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getTargetDistance()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>target distance atual``.</>
    },
    {
      signature: "getTargetDistanceInPath()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>target distance in path atual``.</>
    },
    {
      signature: "hasPath()",
      returns: "boolean",
      description: <>Retorna se path é verdadeiro.</>
    },
    {
      signature: "hasTarget()",
      returns: "boolean",
      description: <>Retorna se target é verdadeiro.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
