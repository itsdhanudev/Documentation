import ApiDoc from '@site/src/components/ApiDoc';

# RouteFollower

Referência completa da API de `RouteFollower`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** AI

**Componente:** sim

---

```java
public class RouteFollower extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "RouteFollower(RouteFollower)",
      description: <>Executa <code>RouteFollower</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "RouteFollower()",
      description: <>Executa <code>RouteFollower</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "RouteFollower",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "findNearestPoint",
      type: "Vector3",
      access: "get",
      description: <>Atributo virtual exposto por <code>getFindNearestPoint</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "findNearestPointIndex",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getFindNearestPointIndex</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "route",
      type: "Route",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRoute</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "findNearestPoint()",
      returns: "Vector3",
      description: <>Executa a operação findNearestPoint.</>
    },
    {
      signature: "getNearestPoint()",
      returns: "Vector3",
      description: <>Retorna a propriedade <code>propriedade </code>nearest point atual``.</>
    },
    {
      signature: "findNearestPointIndex()",
      returns: "int",
      description: <>Executa a operação findNearestPointIndex.</>
    },
    {
      signature: "getNearestPointIndex()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>nearest point index atual``.</>
    },
    {
      signature: "hasPath()",
      returns: "boolean",
      description: <>Retorna se path é verdadeiro.</>
    },
    {
      signature: "isPathFinished()",
      returns: "boolean",
      description: <>Retorna se path finished é verdadeiro.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
