# RouteFollower

Referência completa da API de `RouteFollower`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** AI

**Componente:** sim

---

```java
public class RouteFollower extends Component
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| RouteFollower([RouteFollower]) | Executa `RouteFollower` e retorna quando a chamada do motor termina. |
| RouteFollower() | Executa `RouteFollower` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `component` | [RouteFollower] | Representa o valor de `component` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (3)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `findNearestPoint` | [Vector3] | `get` | Atributo virtual exposto por `getFindNearestPoint`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `findNearestPointIndex` | [int] | `get` | Atributo virtual exposto por `getFindNearestPointIndex`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `route` | [Route] | `get/set` | Atributo virtual exposto por `getRoute`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (8)

| Signature | Returns | Description |
| --- | --- | --- |
| `findNearestPoint()` | `Vector3` | Executa a operação findNearestPoint. |
| `getNearestPoint()` | `Vector3` | Retorna a propriedade `propriedade `nearest point atual``. |
| `findNearestPointIndex()` | `int` | Executa a operação findNearestPointIndex. |
| `getNearestPointIndex()` | `int` | Retorna a propriedade `propriedade `nearest point index atual``. |
| `hasPath()` | `boolean` | Retorna se path é verdadeiro. |
| `isPathFinished()` | `boolean` | Retorna se path finished é verdadeiro. |
| `componentClassMatch(Component, Component)` | `boolean` | Executa `componentClassMatch` e retorna quando a chamada do motor termina. |
| `getEngineComponent()` | `Component` | Executa `getEngineComponent` e retorna quando a chamada do motor termina. |
