import ApiDoc from '@site/src/components/ApiDoc';

# AnimationPlayer

Referência completa da API de `AnimationPlayer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Animations

**Componente:** sim

---

```java
public class AnimationPlayer extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "AnimationPlayer(AnimationPlayer)",
      description: <>Executa <code>AnimationPlayer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "AnimationPlayer()",
      description: <>Cria uma nova instância de AnimationPlayer.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "AnimationPlayer",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "getAnimation(String)",
      returns: "Animation",
      description: <>Retorna a propriedade <code>animação com o nome especificado</code>.</>
    },
    {
      signature: "getAnimation(int)",
      returns: "Animation",
      description: <>Retorna a propriedade <code>animação do índice especificado</code>.</>
    },
    {
      signature: "setAnimation(int, Animation)",
      returns: "void",
      description: <>Define a propriedade <code>animação no índice especificado para a animação fornecida</code>.</>
    },
    {
      signature: "getAnimationList()",
      returns: "List&lt;Animation&gt;",
      description: <>Retorna uma lista com todas as animações do AnimationPlayer.</>
    },
    {
      signature: "addAnimation(Animation)",
      returns: "void",
      description: <>Adiciona a Animação especificada no AnimationPlayer.</>
    },
    {
      signature: "addTransition(Animation, float)",
      returns: "AnimationTransition",
      description: <>Adiciona uma transição de animação entre a animação atual e a especificada (to) com um atraso (delay).</>
    },
    {
      signature: "addTransition(Animation, float, Curve)",
      returns: "AnimationTransition",
      description: <>Adiciona uma transição de animação entre a animação atual e a especificada (to) com um atraso (delay) e uma curva de interpolação (curve).</>
    },
    {
      signature: "addTransition(Animation, Animation, float)",
      returns: "AnimationTransition",
      description: <>Adiciona uma transição de animação entre duas animações (from e to) com um atraso (delay) especificado.</>
    },
    {
      signature: "addTransition(Animation, Animation, float, Curve)",
      returns: "AnimationTransition",
      description: <>Adiciona uma transição de animação entre duas animações (from e to) com um atraso (delay) e uma curva de interpolação (curve) especificados.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
