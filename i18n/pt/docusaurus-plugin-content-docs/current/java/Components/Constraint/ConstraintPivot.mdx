import ApiDoc from '@site/src/components/ApiDoc';

# ConstraintPivot

Referência completa da API de `ConstraintPivot`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Constraint

**Componente:** sim

---

```java
public class ConstraintPivot extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ConstraintPivot(ConstraintPivot)",
      description: <>Executa <code>ConstraintPivot</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ConstraintPivot(Engine)",
      description: <>Executa <code>ConstraintPivot</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ConstraintPivot()",
      description: <>Cria uma nova instância de ConstraintPivot.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ConstraintPivot",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
/>
