# ConstraintPivot

Referência completa da API de `ConstraintPivot`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Constraint

**Componente:** sim

---

```java
public class ConstraintPivot extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| ConstraintPivot([ConstraintPivot]) | Executa `ConstraintPivot` e retorna quando a chamada do motor termina. |
| ConstraintPivot([Engine]) | Executa `ConstraintPivot` e retorna quando a chamada do motor termina. |
| ConstraintPivot() | Cria uma nova instância de ConstraintPivot. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [ConstraintPivot] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
