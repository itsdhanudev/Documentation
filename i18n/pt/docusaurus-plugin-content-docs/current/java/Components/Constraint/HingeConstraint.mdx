import ApiDoc from '@site/src/components/ApiDoc';

# HingeConstraint

Referência completa da API de `HingeConstraint`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Constraint

**Componente:** sim

---

```java
public class HingeConstraint extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "HingeConstraint(HingeConstraint)",
      description: <>Executa <code>HingeConstraint</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "HingeConstraint()",
      description: <>Executa <code>HingeConstraint</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "HingeConstraint",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "pivot",
      type: "SpatialObject",
      access: "get",
      description: <>Atributo virtual exposto por <code>getPivot</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "target",
      type: "SpatialObject",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTarget</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
