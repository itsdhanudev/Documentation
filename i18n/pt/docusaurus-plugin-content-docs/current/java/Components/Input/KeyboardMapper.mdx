# KeyboardMapper

Referência completa da API de `KeyboardMapper`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Input

**Componente:** sim

---

```java
public class KeyboardMapper extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| KeyboardMapper([KeyboardMapper]) | Executa `KeyboardMapper` e retorna quando a chamada do motor termina. |
| KeyboardMapper([Engine]) | Executa `KeyboardMapper` e retorna quando a chamada do motor termina. |
| KeyboardMapper() | Cria uma nova instância de KeyboardMapper. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [KeyboardMapper] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `keyboardKey` | [String] | `get/set` | Atributo virtual exposto por `getKeyboardKey`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `outputKey` | [String] | `get/set` | Atributo virtual exposto por `getOutputKey`. Acesse como um campo para ler ou escrever o valor subjacente. |
