# PointLight

Referência completa da API de `PointLight`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Light

**Componente:** sim

---

```java
public class PointLight extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| PointLight([PointLight]) | Executa `PointLight` e retorna quando a chamada do motor termina. |
| PointLight([Engine]) | Executa `PointLight` e retorna quando a chamada do motor termina. |
| PointLight() | Executa `PointLight` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [PointLight] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (8)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `allowShadow` | [boolean] | `get/set` | Atributo virtual exposto por `isAllowShadow`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `color` | [Color] | `get/set` | Atributo virtual exposto por `getColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `falloffRadius` | [float] | `get/set` | Atributo virtual exposto por `getFalloffRadius`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `lumens` | [float] | `get/set` | Atributo virtual exposto por `getLumens`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `shadowBias` | [float] | `get/set` | Atributo virtual exposto por `getShadowBias`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `shadowFarDistance` | [float] | `get/set` | Atributo virtual exposto por `getShadowFarDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `shadowNormalBias` | [float] | `get/set` | Atributo virtual exposto por `getShadowNormalBias`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `shadowResolutionID` | [int] | `get/set` | Atributo virtual exposto por `getShadowResolutionID`. Acesse como um campo para ler ou escrever o valor subjacente. |
