import ApiDoc from '@site/src/components/ApiDoc';

# PointLight

Referência completa da API de `PointLight`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Light

**Componente:** sim

---

```java
public class PointLight extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "PointLight(PointLight)",
      description: <>Executa <code>PointLight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "PointLight(Engine)",
      description: <>Executa <code>PointLight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "PointLight()",
      description: <>Executa <code>PointLight</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "PointLight",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "allowShadow",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isAllowShadow</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "falloffRadius",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFalloffRadius</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "lumens",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLumens</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "shadowBias",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getShadowBias</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "shadowFarDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getShadowFarDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "shadowNormalBias",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getShadowNormalBias</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "shadowResolutionID",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getShadowResolutionID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
