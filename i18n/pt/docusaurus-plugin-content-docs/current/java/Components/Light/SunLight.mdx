import ApiDoc from '@site/src/components/ApiDoc';

# SunLight

Referência completa da API de `SunLight`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Light

**Componente:** sim

---

```java
public class SunLight extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SunLight(SunLight)",
      description: <>Executa <code>SunLight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SunLight(Engine)",
      description: <>Executa <code>SunLight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SunLight()",
      description: <>Executa <code>SunLight</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SunLight",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "allowShadow",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isAllowShadow</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "cascadeFarHint",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCascadeFarHint</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "cascadeNearHint",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCascadeNearHint</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLux</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "shadowBias",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getShadowBias</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "shadowCascades",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getShadowCascades</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "shadowFarDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getShadowFarDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "shadowNormalBias",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getShadowNormalBias</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "shadowResolutionID",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getShadowResolutionID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "stableCascades",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isStableCascades</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
