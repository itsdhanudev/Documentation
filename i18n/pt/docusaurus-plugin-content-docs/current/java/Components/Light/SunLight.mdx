# SunLight

Referência completa da API de `SunLight`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Light

**Componente:** sim

---

```java
public class SunLight extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| SunLight([SunLight]) | Executa `SunLight` e retorna quando a chamada do motor termina. |
| SunLight([Engine]) | Executa `SunLight` e retorna quando a chamada do motor termina. |
| SunLight() | Executa `SunLight` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [SunLight] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (11)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `allowShadow` | [boolean] | `get/set` | Atributo virtual exposto por `isAllowShadow`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `cascadeFarHint` | [float] | `get/set` | Atributo virtual exposto por `getCascadeFarHint`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `cascadeNearHint` | [float] | `get/set` | Atributo virtual exposto por `getCascadeNearHint`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `color` | [Color] | `get/set` | Atributo virtual exposto por `getColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `lux` | [float] | `get/set` | Atributo virtual exposto por `getLux`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `shadowBias` | [float] | `get/set` | Atributo virtual exposto por `getShadowBias`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `shadowCascades` | [int] | `get/set` | Atributo virtual exposto por `getShadowCascades`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `shadowFarDistance` | [float] | `get/set` | Atributo virtual exposto por `getShadowFarDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `shadowNormalBias` | [float] | `get/set` | Atributo virtual exposto por `getShadowNormalBias`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `shadowResolutionID` | [int] | `get/set` | Atributo virtual exposto por `getShadowResolutionID`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `stableCascades` | [boolean] | `get/set` | Atributo virtual exposto por `isStableCascades`. Acesse como um campo para ler ou escrever o valor subjacente. |
