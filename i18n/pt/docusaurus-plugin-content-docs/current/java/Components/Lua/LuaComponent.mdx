# LuaComponent

Referência completa da API de `LuaComponent`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Lua

**Componente:** sim

---

```java
public class LuaComponent extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| LuaComponent([LuaComponent]) | Executa `LuaComponent` e retorna quando a chamada do motor termina. |
| LuaComponent([Engine]) | Executa `LuaComponent` e retorna quando a chamada do motor termina. |
| LuaComponent() | Cria uma nova instância de LuaComponent. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [LuaComponent] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (28)

| Signature | Returns | Description |
| --- | --- | --- |
| `get(String)` | `LuaValue` | Retorna a propriedade `propriedade `valor value atual``. |
| `set(String, LuaValue)` | `void` | Define ou atualiza the value. |
| `set(String, float)` | `void` | Define ou atualiza the value. |
| `set(String, double)` | `void` | Define ou atualiza the value. |
| `set(String, int)` | `void` | Define ou atualiza the value. |
| `set(String, long)` | `void` | Define ou atualiza the value. |
| `set(String, String)` | `void` | Define ou atualiza the value. |
| `set(String, boolean)` | `void` | Define ou atualiza the value. |
| `getFloat(String)` | `float` | Retorna a propriedade `propriedade `float atual``. |
| `getDouble(String)` | `double` | Retorna a propriedade `propriedade `double atual``. |
| `getInt(String)` | `float` | Retorna a propriedade `propriedade `int atual``. |
| `getLong(String)` | `long` | Retorna a propriedade `propriedade `long atual``. |
| `getString(String)` | `String` | Retorna a propriedade `propriedade `string atual``. |
| `getBoolean(String)` | `boolean` | Retorna a propriedade `propriedade `boolean atual``. |
| `getVar(String)` | `ValueObject` | Retorna a propriedade `propriedade `var atual``. |
| `setVar(String, ValueObject)` | `void` | Define ou atualiza a propriedade `propriedade `var``. |
| `setFloat(String, float)` | `void` | Define ou atualiza a propriedade `propriedade `float``. |
| `setDouble(String, double)` | `void` | Define ou atualiza a propriedade `propriedade `double``. |
| `setInt(String, int)` | `void` | Define ou atualiza a propriedade `propriedade `int``. |
| `setLong(String, long)` | `void` | Define ou atualiza a propriedade `propriedade `long``. |
| `setString(String, String)` | `void` | Define ou atualiza a propriedade `propriedade `string``. |
| `setBoolean(String, boolean)` | `void` | Define ou atualiza a propriedade `propriedade `boolean``. |
| `call(String)` | `void` | Executa a operação call usando os parâmetros informados (name). |
| `call(String, int)` | `void` | Executa a operação call usando os parâmetros informados (name, args). |
| `call(String, float)` | `void` | Executa a operação call usando os parâmetros informados (name, args). |
| `call(String, String)` | `void` | Executa a operação call usando os parâmetros informados (name, args). |
| `call(String, boolean)` | `void` | Executa a operação call usando os parâmetros informados (name, args). |
| `call(String, Object...)` | `void` | Executa a operação call usando os parâmetros informados (name, args). |
