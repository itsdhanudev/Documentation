# ObjectPool

Referência completa da API de `ObjectPool`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** ObjectPool

**Componente:** sim

---

```java
public class ObjectPool extends Component
```

## Construtores (1)

| Signature | Description |
| --- | --- |
| ObjectPool([ObjectPool]) | Executa `ObjectPool` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [ObjectPool] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (11)

| Signature | Returns | Description |
| --- | --- | --- |
| `add(Vector3)` | [ObjectPool].[Object] | Adiciona um HPO na posição especificada e retorna seu índice. |
| `objectCount()` | `int` | Retorna a propriedade `quantidade de objetos no HPOP atual`. |
| `getObject(int)` | [ObjectPool].[Object] | Executa `getObject` e retorna quando a chamada do motor termina. |
| `notifyObjectChanged(ObjectPool.Object)` | `void` | Atualiza o objeto. |
| `delete([List&lt;SearchResult&gt;])` | `void` | Exclui vários objects com base nos resultados de busca fornecidos. |
| `delete(ObjectPool.Object)` | `void` | Exclui o HPO no índice especificado. |
| `findInCircle(Vector3, float)` | [List]&[lt];[SearchResult]&[gt]; | Encontra e retorna todos os objects dentro de um círculo especificado pelo centro e raio. |
| `findInSphere(Vector3, float)` | [List]&[lt];[SearchResult]&[gt]; | Encontra e retorna todos os objects dentro de uma esfera especificada pelo centro e raio. |
| `findInSquare(Vector2, Vector2)` | [List]&[lt];[SearchResult]&[gt]; | Encontra e retorna todos os objects dentro de uma área retangular definida por dois cantos: min e max. |
| `componentClassMatch(Component, Component)` | `boolean` | Executa `componentClassMatch` e retorna quando a chamada do motor termina. |
| `getEngineComponent()` | `Component` | Executa `getEngineComponent` e retorna quando a chamada do motor termina. |
