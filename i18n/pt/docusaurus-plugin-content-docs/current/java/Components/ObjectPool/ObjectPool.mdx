import ApiDoc from '@site/src/components/ApiDoc';

# ObjectPool

Referência completa da API de `ObjectPool`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** ObjectPool

**Componente:** sim

---

```java
public class ObjectPool extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ObjectPool(ObjectPool)",
      description: <>Executa <code>ObjectPool</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ObjectPool",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "add(Vector3)",
      returns: "ObjectPool.Object",
      description: <>Adiciona um HPO na posição especificada e retorna seu índice.</>
    },
    {
      signature: "objectCount()",
      returns: "int",
      description: <>Retorna a propriedade <code>quantidade de objetos no HPOP atual</code>.</>
    },
    {
      signature: "getObject(int)",
      returns: "ObjectPool.Object",
      description: <>Executa <code>getObject</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "notifyObjectChanged(ObjectPool.Object)",
      returns: "void",
      description: <>Atualiza o objeto.</>
    },
    {
      signature: "delete(List&lt;SearchResult&gt;)",
      returns: "void",
      description: <>Exclui vários objects com base nos resultados de busca fornecidos.</>
    },
    {
      signature: "delete(ObjectPool.Object)",
      returns: "void",
      description: <>Exclui o HPO no índice especificado.</>
    },
    {
      signature: "findInCircle(Vector3, float)",
      returns: "List&lt;SearchResult&gt;",
      description: <>Encontra e retorna todos os objects dentro de um círculo especificado pelo centro e raio.</>
    },
    {
      signature: "findInSphere(Vector3, float)",
      returns: "List&lt;SearchResult&gt;",
      description: <>Encontra e retorna todos os objects dentro de uma esfera especificada pelo centro e raio.</>
    },
    {
      signature: "findInSquare(Vector2, Vector2)",
      returns: "List&lt;SearchResult&gt;",
      description: <>Encontra e retorna todos os objects dentro de uma área retangular definida por dois cantos: min e max.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
