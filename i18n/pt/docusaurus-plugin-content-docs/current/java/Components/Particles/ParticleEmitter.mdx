import ApiDoc from '@site/src/components/ApiDoc';

# ParticleEmitter

Referência completa da API de `ParticleEmitter`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Particles

**Componente:** sim

---

```java
public class ParticleEmitter extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ParticleEmitter(ParticleEmitter)",
      description: <>Executa <code>ParticleEmitter</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ParticleEmitter(Engine)",
      description: <>Executa <code>ParticleEmitter</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ParticleEmitter()",
      description: <>Cria uma nova instância de ParticleEmitter.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ParticleEmitter",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "allowEmission",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isAllowEmission</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "alphaBased",
      type: "AlphaBased",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAlphaBased</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "alphaOverLifeTimeCurve",
      type: "Curve",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAlphaOverLifeTimeCurve</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "colorBased",
      type: "ColorBased",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColorBased</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "colorMode",
      type: "ColorMode",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColorMode</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "coneShapeOptions",
      type: "ParticleConeShapeOptions",
      access: "get",
      description: <>Atributo virtual exposto por <code>getConeShapeOptions</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "emissionShape",
      type: "EmissionShape",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getEmissionShape</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "emissionsPerSecond",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getEmissionsPerSecond</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "gravityMultiplier",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getGravityMultiplier</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "layer",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLayer</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxLifeSeconds",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxLifeSeconds</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "sizeBased",
      type: "SizeBased",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSizeBased</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "sizeOverLifeTimeCurve",
      type: "Curve",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSizeOverLifeTimeCurve</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "sphereShapeOptions",
      type: "ParticleSphereShapeOptions",
      access: "get",
      description: <>Atributo virtual exposto por <code>getSphereShapeOptions</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "startSize",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getStartSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "startSpeed",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getStartSpeed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "textureInstance",
      type: "Texture",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTextureInstance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "transparencyBased",
      type: "TransparencyBased",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTransparencyBased</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "emit(Vector3, Vector3)",
      returns: "Particle",
      description: <>Executa a operação emit usando os parâmetros informados (position, movement).</>
    }
  ]}
/>
