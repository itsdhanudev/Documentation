# ParticleEmitter

Referência completa da API de `ParticleEmitter`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Particles

**Componente:** sim

---

```java
public class ParticleEmitter extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| ParticleEmitter([ParticleEmitter]) | Executa `ParticleEmitter` e retorna quando a chamada do motor termina. |
| ParticleEmitter([Engine]) | Executa `ParticleEmitter` e retorna quando a chamada do motor termina. |
| ParticleEmitter() | Cria uma nova instância de ParticleEmitter. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [ParticleEmitter] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (19)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `allowEmission` | [boolean] | `get/set` | Atributo virtual exposto por `isAllowEmission`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `alphaBased` | [AlphaBased] | `get/set` | Atributo virtual exposto por `getAlphaBased`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `alphaOverLifeTimeCurve` | [Curve] | `get/set` | Atributo virtual exposto por `getAlphaOverLifeTimeCurve`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `color` | [Color] | `get/set` | Atributo virtual exposto por `getColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `colorBased` | [ColorBased] | `get/set` | Atributo virtual exposto por `getColorBased`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `colorMode` | [ColorMode] | `get/set` | Atributo virtual exposto por `getColorMode`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `coneShapeOptions` | [ParticleConeShapeOptions] | `get` | Atributo virtual exposto por `getConeShapeOptions`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `emissionShape` | [EmissionShape] | `get/set` | Atributo virtual exposto por `getEmissionShape`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `emissionsPerSecond` | [float] | `get/set` | Atributo virtual exposto por `getEmissionsPerSecond`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `gravityMultiplier` | [float] | `get/set` | Atributo virtual exposto por `getGravityMultiplier`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `layer` | [int] | `get/set` | Atributo virtual exposto por `getLayer`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `maxLifeSeconds` | [float] | `get/set` | Atributo virtual exposto por `getMaxLifeSeconds`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `sizeBased` | [SizeBased] | `get/set` | Atributo virtual exposto por `getSizeBased`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `sizeOverLifeTimeCurve` | [Curve] | `get/set` | Atributo virtual exposto por `getSizeOverLifeTimeCurve`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `sphereShapeOptions` | [ParticleSphereShapeOptions] | `get` | Atributo virtual exposto por `getSphereShapeOptions`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `startSize` | [float] | `get/set` | Atributo virtual exposto por `getStartSize`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `startSpeed` | [float] | `get/set` | Atributo virtual exposto por `getStartSpeed`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `textureInstance` | [Texture] | `get/set` | Atributo virtual exposto por `getTextureInstance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `transparencyBased` | [TransparencyBased] | `get/set` | Atributo virtual exposto por `getTransparencyBased`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `emit(Vector3, Vector3)` | `Particle` | Executa a operação emit usando os parâmetros informados (position, movement). |
