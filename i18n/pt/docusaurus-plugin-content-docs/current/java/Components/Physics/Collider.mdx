import ApiDoc from '@site/src/components/ApiDoc';

# Collider

Referência completa da API de `Collider`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Physics

**Componente:** sim

---

```java
public class Collider extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Collider(Collider)",
      description: <>Executa <code>Collider</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Collider()",
      description: <>Cria uma nova instância de Collider.</>
    },
    {
      signature: "Collider(Shape)",
      description: <>Executa <code>Collider</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "Collider",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "convexVertex",
      type: "Vertex",
      access: "set",
      description: <>Define a propriedade <code>propriedade </code>vertex convexo do collider``.</>
    },
    {
      name: "vertex",
      type: "Vertex",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>vertex do collider``.</>
    },
    {
      name: "vertexFile",
      type: "VertexFile",
      access: "set",
      description: <>Define a propriedade <code>propriedade </code>arquivo do vertex para o collider``.</>
    }
  ]}
  methods={[
    {
      signature: "getShape()",
      returns: "Shape",
      description: <>Retorna a propriedade <code>propriedade </code>tipo de forma do collider``.</>
    },
    {
      signature: "setShape(Shape)",
      returns: "void",
      description: <>Define a propriedade <code>propriedade </code>tipo de forma do collider``.</>
    },
    {
      signature: "colliderWithName(String)",
      returns: "boolean",
      description: <>Verifica se este objeto está colidindo com um objeto com o nome especificado.</>
    },
    {
      signature: "isColliding()",
      returns: "boolean",
      description: <>Verifica se este objeto está colidindo com algo.</>
    },
    {
      signature: "getCollisionList()",
      returns: "List&lt;Collision&gt;",
      description: <>Retorna uma lista de todas as colisões atuais.</>
    },
    {
      signature: "getCollisionsCount()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>número de colisões atuais``.</>
    },
    {
      signature: "getCollisionAt(int)",
      returns: "Collision",
      description: <>Retorna a propriedade <code>colisão no índice especificado</code>.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
