import ApiDoc from '@site/src/components/ApiDoc';

# PhysicsComponent

Referência completa da API de `PhysicsComponent`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Physics

**Componente:** sim

---

```java
public class PhysicsComponent extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "PhysicsComponent(Component)",
      description: <>Executa <code>PhysicsComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "velocity",
      type: "Vector3",
      description: <><code>get</code></>
    }
  ]}
  virtualAttributes={[
    {
      name: "velocity",
      type: "Vector3",
      access: "get",
      description: <>Retorna a propriedade <code>velocidade atual do objeto</code>.</>
    }
  ]}
  methods={[
    {
      signature: "colliderWithName(String)",
      returns: "boolean",
      description: <>Verifica se este objeto está colidindo com um objeto com o nome especificado.</>
    },
    {
      signature: "isColliding()",
      returns: "boolean",
      description: <>Verifica se este objeto está colidindo com algo.</>
    },
    {
      signature: "getCollisionList()",
      returns: "List&lt;Collision&gt;",
      description: <>Retorna uma lista de todas as colisões atuais.</>
    },
    {
      signature: "getCollisionsCount()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>número de colisões atuais``.</>
    },
    {
      signature: "getCollisionAt(int)",
      returns: "Collision",
      description: <>Retorna a propriedade <code>colisão no índice especificado</code>.</>
    }
  ]}
/>
