import ApiDoc from '@site/src/components/ApiDoc';

# VehicleWheel

Referência completa da API de `VehicleWheel`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Physics

**Componente:** sim

---

```java
public class VehicleWheel extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VehicleWheel(VehicleWheel)",
      description: <>Executa <code>VehicleWheel</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VehicleWheel()",
      description: <>Executa <code>VehicleWheel</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "VehicleWheel",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "blockRotation",
      type: "float",
      access: "set",
      description: <>Atributo virtual exposto por <code>setBlockRotation</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "frictionSlip",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFrictionSlip</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "modelPosition",
      type: "Vector3",
      access: "get",
      description: <>Atributo virtual exposto por <code>getModelPosition</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "modelRotation",
      type: "Quaternion",
      access: "get",
      description: <>Atributo virtual exposto por <code>getModelRotation</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "posPhysicsFunctionEnabled",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPosPhysicsFunctionEnabled</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "radius",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRadius</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "rotation",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRotation</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "skidding",
      type: "float",
      access: "get",
      description: <>Atributo virtual exposto por <code>getSkidding</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "suspensionRestLength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSuspensionRestLength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isGrounded()",
      returns: "boolean",
      description: <>Retorna se grounded é verdadeiro.</>
    },
    {
      signature: "incrementRotation(float)",
      returns: "void",
      description: <>Executa a operação incrementRotation usando os parâmetros informados (r).</>
    },
    {
      signature: "setTorque(float)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>torque``.</>
    },
    {
      signature: "setBrake(float)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>brake``.</>
    },
    {
      signature: "setSteer(float)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>steer``.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
