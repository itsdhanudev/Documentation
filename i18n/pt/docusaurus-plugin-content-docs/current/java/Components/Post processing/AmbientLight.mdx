import ApiDoc from '@site/src/components/ApiDoc';

# AmbientLight

Referência completa da API de `AmbientLight`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class AmbientLight extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "AmbientLight(AmbientLight)",
      description: <>Executa <code>AmbientLight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "AmbientLight(Engine)",
      description: <>Executa <code>AmbientLight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "AmbientLight()",
      description: <>Executa <code>AmbientLight</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "AmbientLight",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "indirectLight",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getIndirectLight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLux</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxResolutionID",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxResolutionID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
