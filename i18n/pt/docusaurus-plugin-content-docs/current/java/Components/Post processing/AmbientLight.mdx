# AmbientLight

Referência completa da API de `AmbientLight`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class AmbientLight extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| AmbientLight([AmbientLight]) | Executa `AmbientLight` e retorna quando a chamada do motor termina. |
| AmbientLight([Engine]) | Executa `AmbientLight` e retorna quando a chamada do motor termina. |
| AmbientLight() | Executa `AmbientLight` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [AmbientLight] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (3)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `indirectLight` | [Color] | `get/set` | Atributo virtual exposto por `getIndirectLight`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `lux` | [float] | `get/set` | Atributo virtual exposto por `getLux`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `maxResolutionID` | [int] | `get/set` | Atributo virtual exposto por `getMaxResolutionID`. Acesse como um campo para ler ou escrever o valor subjacente. |
