import ApiDoc from '@site/src/components/ApiDoc';

# AtmosphericScattering

Referência completa da API de `AtmosphericScattering`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class AtmosphericScattering extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "AtmosphericScattering(AtmosphericScattering)",
      description: <>Executa <code>AtmosphericScattering</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "AtmosphericScattering(Engine)",
      description: <>Executa <code>AtmosphericScattering</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "AtmosphericScattering()",
      description: <>Executa <code>AtmosphericScattering</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "AtmosphericScattering",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "ambientLight",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setAmbientLight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ambientLightResolutionID",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAmbientLightResolutionID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "atmosphereDensity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAtmosphereDensity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "atmosphereDensityFalloff",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAtmosphereDensityFalloff</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "atmosphereExtent",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAtmosphereExtent</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "cirrusClouds",
      type: "CloudType",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCirrusClouds</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "cumulusClouds",
      type: "CloudType",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCumulusClouds</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "enableMoonball",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setEnableMoonball</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "enableSunball",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setEnableSunball</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "groundColor",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getGroundColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLux</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxResolutionID",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxResolutionID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "rayLeigh",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRayLeigh</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "skyboxSimulationSpeed",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSkyboxSimulationSpeed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "wetness",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getWetness</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isAmbientLight()",
      returns: "boolean",
      description: <>Executa <code>isAmbientLight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "isEnableMoonball()",
      returns: "boolean",
      description: <>Executa <code>isEnableMoonball</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "isEnableSunball()",
      returns: "boolean",
      description: <>Executa <code>isEnableSunball</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
