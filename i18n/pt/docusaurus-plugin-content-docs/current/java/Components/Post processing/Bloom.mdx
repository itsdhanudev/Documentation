# Bloom

Referência completa da API de `Bloom`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class Bloom extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| Bloom([Bloom]) | Executa `Bloom` e retorna quando a chamada do motor termina. |
| Bloom([Engine]) | Executa `Bloom` e retorna quando a chamada do motor termina. |
| Bloom() | Executa `Bloom` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [Bloom] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (15)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `chromaticAberration` | [float] | `get/set` | Atributo virtual exposto por `getChromaticAberration`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ghostCount` | [int] | `get/set` | Atributo virtual exposto por `getGhostCount`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ghostSpacing` | [float] | `get/set` | Atributo virtual exposto por `getGhostSpacing`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ghostThreshold` | [float] | `get/set` | Atributo virtual exposto por `getGhostThreshold`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `haloRadius` | [float] | `get/set` | Atributo virtual exposto por `getHaloRadius`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `haloThickness` | [float] | `get/set` | Atributo virtual exposto por `getHaloThickness`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `haloThreshold` | [float] | `get/set` | Atributo virtual exposto por `getHaloThreshold`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `highlight` | [float] | `get/set` | Atributo virtual exposto por `getHighlight`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `lensFlare` | [boolean] | `set` | Atributo virtual exposto por `setLensFlare`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `levels` | [int] | `get/set` | Atributo virtual exposto por `getLevels`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `quality` | [QualityLevel] | `get/set` | Atributo virtual exposto por `getQuality`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `resolutionID` | [int] | `get/set` | Atributo virtual exposto por `getResolutionID`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `starburst` | [boolean] | `set` | Atributo virtual exposto por `setStarburst`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `strength` | [float] | `get/set` | Atributo virtual exposto por `getStrength`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `threshold` | [boolean] | `set` | Atributo virtual exposto por `setThreshold`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `isLensFlare()` | `boolean` | Executa `isLensFlare` e retorna quando a chamada do motor termina. |
| `isStarburst()` | `boolean` | Executa `isStarburst` e retorna quando a chamada do motor termina. |
| `isThreshold()` | `boolean` | Executa `isThreshold` e retorna quando a chamada do motor termina. |
