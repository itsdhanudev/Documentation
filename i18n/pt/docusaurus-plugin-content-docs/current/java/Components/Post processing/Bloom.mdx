import ApiDoc from '@site/src/components/ApiDoc';

# Bloom

Referência completa da API de `Bloom`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class Bloom extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Bloom(Bloom)",
      description: <>Executa <code>Bloom</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Bloom(Engine)",
      description: <>Executa <code>Bloom</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Bloom()",
      description: <>Executa <code>Bloom</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Bloom",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "chromaticAberration",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getChromaticAberration</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ghostCount",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getGhostCount</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ghostSpacing",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getGhostSpacing</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ghostThreshold",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getGhostThreshold</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "haloRadius",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHaloRadius</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "haloThickness",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHaloThickness</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "haloThreshold",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHaloThreshold</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "highlight",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHighlight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "lensFlare",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setLensFlare</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "levels",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLevels</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "quality",
      type: "QualityLevel",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getQuality</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "resolutionID",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getResolutionID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "starburst",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setStarburst</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "strength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "threshold",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setThreshold</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isLensFlare()",
      returns: "boolean",
      description: <>Executa <code>isLensFlare</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "isStarburst()",
      returns: "boolean",
      description: <>Executa <code>isStarburst</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "isThreshold()",
      returns: "boolean",
      description: <>Executa <code>isThreshold</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
