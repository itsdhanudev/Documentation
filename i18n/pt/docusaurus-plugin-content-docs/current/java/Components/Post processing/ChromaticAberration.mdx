import ApiDoc from '@site/src/components/ApiDoc';

# ChromaticAberration

Referência completa da API de `ChromaticAberration`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class ChromaticAberration extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ChromaticAberration(ChromaticAberration)",
      description: <>Executa <code>ChromaticAberration</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ChromaticAberration(Engine)",
      description: <>Executa <code>ChromaticAberration</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ChromaticAberration()",
      description: <>Executa <code>ChromaticAberration</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ChromaticAberration",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "falloff",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFalloff</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "strength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
