# ContactShadows

Referência completa da API de `ContactShadows`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class ContactShadows extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| ContactShadows([ContactShadows]) | Executa `ContactShadows` e retorna quando a chamada do motor termina. |
| ContactShadows([Engine]) | Executa `ContactShadows` e retorna quando a chamada do motor termina. |
| ContactShadows() | Executa `ContactShadows` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [ContactShadows] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (8)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `ssctContactDistanceMax` | [float] | `get/set` | Atributo virtual exposto por `getSsctContactDistanceMax`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ssctDepthBias` | [float] | `get/set` | Atributo virtual exposto por `getSsctDepthBias`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ssctDepthSlopeBias` | [float] | `get/set` | Atributo virtual exposto por `getSsctDepthSlopeBias`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ssctIntensity` | [float] | `get/set` | Atributo virtual exposto por `getSsctIntensity`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ssctLightCone` | [float] | `get/set` | Atributo virtual exposto por `getSsctLightCone`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ssctRayCount` | [int] | `get/set` | Atributo virtual exposto por `getSsctRayCount`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ssctSampleCount` | [int] | `get/set` | Atributo virtual exposto por `getSsctSampleCount`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ssctShadowDistance` | [float] | `get/set` | Atributo virtual exposto por `getSsctShadowDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
