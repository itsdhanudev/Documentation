import ApiDoc from '@site/src/components/ApiDoc';

# ContactShadows

Referência completa da API de `ContactShadows`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class ContactShadows extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ContactShadows(ContactShadows)",
      description: <>Executa <code>ContactShadows</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ContactShadows(Engine)",
      description: <>Executa <code>ContactShadows</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ContactShadows()",
      description: <>Executa <code>ContactShadows</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ContactShadows",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "ssctContactDistanceMax",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSsctContactDistanceMax</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ssctDepthBias",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSsctDepthBias</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ssctDepthSlopeBias",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSsctDepthSlopeBias</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ssctIntensity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSsctIntensity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ssctLightCone",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSsctLightCone</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ssctRayCount",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSsctRayCount</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ssctSampleCount",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSsctSampleCount</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ssctShadowDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSsctShadowDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
