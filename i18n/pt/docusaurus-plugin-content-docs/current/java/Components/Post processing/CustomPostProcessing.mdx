# CustomPostProcessing

Referência completa da API de `CustomPostProcessing`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class CustomPostProcessing extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| CustomPostProcessing([CustomPostProcessing]) | Executa `CustomPostProcessing` e retorna quando a chamada do motor termina. |
| CustomPostProcessing([Engine]) | Executa `CustomPostProcessing` e retorna quando a chamada do motor termina. |
| CustomPostProcessing() | Executa `CustomPostProcessing` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [CustomPostProcessing] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (3)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `param` | [String] | `set` | Atributo virtual exposto por `setParam`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `shaderFile` | [String] | `get/set` | Atributo virtual exposto por `getShaderFile`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `time` | [float] | `get/set` | Atributo virtual exposto por `getTime`. Acesse como um campo para ler ou escrever o valor subjacente. |
