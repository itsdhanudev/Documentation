import ApiDoc from '@site/src/components/ApiDoc';

# DepthOfField

Referência completa da API de `DepthOfField`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class DepthOfField extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "DepthOfField(DepthOfField)",
      description: <>Executa <code>DepthOfField</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "DepthOfField(Engine)",
      description: <>Executa <code>DepthOfField</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "DepthOfField()",
      description: <>Executa <code>DepthOfField</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "DepthOfField",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "autoFocus",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isAutoFocus</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "focusDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFocusDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxApertureDiameterCm",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxApertureDiameterCm</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "scale",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScale</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
