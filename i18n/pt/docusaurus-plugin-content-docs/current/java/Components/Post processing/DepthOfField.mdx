# DepthOfField

Referência completa da API de `DepthOfField`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class DepthOfField extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| DepthOfField([DepthOfField]) | Executa `DepthOfField` e retorna quando a chamada do motor termina. |
| DepthOfField([Engine]) | Executa `DepthOfField` e retorna quando a chamada do motor termina. |
| DepthOfField() | Executa `DepthOfField` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [DepthOfField] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (4)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `autoFocus` | [boolean] | `get/set` | Atributo virtual exposto por `isAutoFocus`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `focusDistance` | [float] | `get/set` | Atributo virtual exposto por `getFocusDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `maxApertureDiameterCm` | [float] | `get/set` | Atributo virtual exposto por `getMaxApertureDiameterCm`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `scale` | [float] | `get/set` | Atributo virtual exposto por `getScale`. Acesse como um campo para ler ou escrever o valor subjacente. |
