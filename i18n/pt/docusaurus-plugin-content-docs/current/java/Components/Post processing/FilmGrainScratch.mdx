# FilmGrainScratch

Referência completa da API de `FilmGrainScratch`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class FilmGrainScratch extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| FilmGrainScratch([FilmGrainScratch]) | Executa `FilmGrainScratch` e retorna quando a chamada do motor termina. |
| FilmGrainScratch([Engine]) | Executa `FilmGrainScratch` e retorna quando a chamada do motor termina. |
| FilmGrainScratch() | Executa `FilmGrainScratch` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [FilmGrainScratch] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (8)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `flickerStrength` | [float] | `get/set` | Atributo virtual exposto por `getFlickerStrength`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `grainSize` | [float] | `get/set` | Atributo virtual exposto por `getGrainSize`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `grainStrength` | [float] | `get/set` | Atributo virtual exposto por `getGrainStrength`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `intensity` | [float] | `get/set` | Atributo virtual exposto por `getIntensity`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `scratchDensity` | [float] | `get/set` | Atributo virtual exposto por `getScratchDensity`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `scratchStrength` | [float] | `get/set` | Atributo virtual exposto por `getScratchStrength`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `scratchWidth` | [float] | `get/set` | Atributo virtual exposto por `getScratchWidth`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `time` | [float] | `get/set` | Atributo virtual exposto por `getTime`. Acesse como um campo para ler ou escrever o valor subjacente. |
