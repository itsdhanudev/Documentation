import ApiDoc from '@site/src/components/ApiDoc';

# FilmGrainScratch

Referência completa da API de `FilmGrainScratch`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class FilmGrainScratch extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "FilmGrainScratch(FilmGrainScratch)",
      description: <>Executa <code>FilmGrainScratch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "FilmGrainScratch(Engine)",
      description: <>Executa <code>FilmGrainScratch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "FilmGrainScratch()",
      description: <>Executa <code>FilmGrainScratch</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "FilmGrainScratch",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "flickerStrength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFlickerStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "grainSize",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getGrainSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "grainStrength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getGrainStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getIntensity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "scratchDensity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScratchDensity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "scratchStrength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScratchStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "scratchWidth",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScratchWidth</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "time",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTime</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
