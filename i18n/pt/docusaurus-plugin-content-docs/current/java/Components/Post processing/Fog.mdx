import ApiDoc from '@site/src/components/ApiDoc';

# Fog

Referência completa da API de `Fog`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class Fog extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Fog(Fog)",
      description: <>Executa <code>Fog</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Fog(Engine)",
      description: <>Executa <code>Fog</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Fog()",
      description: <>Executa <code>Fog</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Fog",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "cutOffDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCutOffDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "density",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getDensity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "distance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "fogColorFromIbl",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setFogColorFromIbl</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "height",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHeight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "heightFalloff",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHeightFalloff</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "inScatteringSize",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getInScatteringSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "inScatteringStart",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getInScatteringStart</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isFogColorFromIbl()",
      returns: "boolean",
      description: <>Executa <code>isFogColorFromIbl</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
