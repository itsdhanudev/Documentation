# Fog

Referência completa da API de `Fog`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class Fog extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| Fog([Fog]) | Executa `Fog` e retorna quando a chamada do motor termina. |
| Fog([Engine]) | Executa `Fog` e retorna quando a chamada do motor termina. |
| Fog() | Executa `Fog` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [Fog] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (9)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `color` | [Color] | `get/set` | Atributo virtual exposto por `getColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `cutOffDistance` | [float] | `get/set` | Atributo virtual exposto por `getCutOffDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `density` | [float] | `get/set` | Atributo virtual exposto por `getDensity`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `distance` | [float] | `get/set` | Atributo virtual exposto por `getDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `fogColorFromIbl` | [boolean] | `set` | Atributo virtual exposto por `setFogColorFromIbl`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `height` | [float] | `get/set` | Atributo virtual exposto por `getHeight`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `heightFalloff` | [float] | `get/set` | Atributo virtual exposto por `getHeightFalloff`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `inScatteringSize` | [float] | `get/set` | Atributo virtual exposto por `getInScatteringSize`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `inScatteringStart` | [float] | `get/set` | Atributo virtual exposto por `getInScatteringStart`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `isFogColorFromIbl()` | `boolean` | Executa `isFogColorFromIbl` e retorna quando a chamada do motor termina. |
