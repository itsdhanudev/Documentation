import ApiDoc from '@site/src/components/ApiDoc';

# GradientLight

Referência completa da API de `GradientLight`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class GradientLight extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "GradientLight(GradientLight)",
      description: <>Executa <code>GradientLight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "GradientLight(Engine)",
      description: <>Executa <code>GradientLight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "GradientLight()",
      description: <>Executa <code>GradientLight</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "GradientLight",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "ambientLight",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isAmbientLight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "ambientLightResolutionID",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAmbientLightResolutionID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "colorGradient",
      type: "ColorGradient",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColorGradient</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLux</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxResolutionID",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxResolutionID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
