import ApiDoc from '@site/src/components/ApiDoc';

# LensDistortion

Referência completa da API de `LensDistortion`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class LensDistortion extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "LensDistortion(LensDistortion)",
      description: <>Executa <code>LensDistortion</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "LensDistortion(Engine)",
      description: <>Executa <code>LensDistortion</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "LensDistortion()",
      description: <>Executa <code>LensDistortion</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "LensDistortion",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "border",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBorder</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "center",
      type: "Vector2",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCenter</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getIntensity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "mode",
      type: "Mode",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMode</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
