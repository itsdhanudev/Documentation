# LensDistortion

Referência completa da API de `LensDistortion`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class LensDistortion extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| LensDistortion([LensDistortion]) | Executa `LensDistortion` e retorna quando a chamada do motor termina. |
| LensDistortion([Engine]) | Executa `LensDistortion` e retorna quando a chamada do motor termina. |
| LensDistortion() | Executa `LensDistortion` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [LensDistortion] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (4)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `border` | [float] | `get/set` | Atributo virtual exposto por `getBorder`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `center` | [Vector2] | `get/set` | Atributo virtual exposto por `getCenter`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `intensity` | [float] | `get/set` | Atributo virtual exposto por `getIntensity`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `mode` | [Mode] | `get/set` | Atributo virtual exposto por `getMode`. Acesse como um campo para ler ou escrever o valor subjacente. |
