# NightVision

Referência completa da API de `NightVision`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class NightVision extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| NightVision([NightVision]) | Executa `NightVision` e retorna quando a chamada do motor termina. |
| NightVision([Engine]) | Executa `NightVision` e retorna quando a chamada do motor termina. |
| NightVision() | Executa `NightVision` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [NightVision] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (9)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `brightness` | [float] | `get/set` | Atributo virtual exposto por `getBrightness`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `contrast` | [float] | `get/set` | Atributo virtual exposto por `getContrast`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `intensity` | [float] | `get/set` | Atributo virtual exposto por `getIntensity`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `noiseStrength` | [float] | `get/set` | Atributo virtual exposto por `getNoiseStrength`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `scanlineCount` | [float] | `get/set` | Atributo virtual exposto por `getScanlineCount`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `scanlineStrength` | [float] | `get/set` | Atributo virtual exposto por `getScanlineStrength`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `time` | [float] | `get/set` | Atributo virtual exposto por `getTime`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `tintColor` | [Color] | `get/set` | Atributo virtual exposto por `getTintColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `vignetteStrength` | [float] | `get/set` | Atributo virtual exposto por `getVignetteStrength`. Acesse como um campo para ler ou escrever o valor subjacente. |
