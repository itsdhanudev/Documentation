import ApiDoc from '@site/src/components/ApiDoc';

# NightVision

Referência completa da API de `NightVision`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class NightVision extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "NightVision(NightVision)",
      description: <>Executa <code>NightVision</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NightVision(Engine)",
      description: <>Executa <code>NightVision</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "NightVision()",
      description: <>Executa <code>NightVision</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "NightVision",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "brightness",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBrightness</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "contrast",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getContrast</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getIntensity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "noiseStrength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getNoiseStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "scanlineCount",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScanlineCount</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "scanlineStrength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScanlineStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "time",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTime</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "tintColor",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTintColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "vignetteStrength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getVignetteStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
