# RenderEffectArea

Referência completa da API de `RenderEffectArea`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class RenderEffectArea extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| RenderEffectArea([RenderEffectArea]) | Executa `RenderEffectArea` e retorna quando a chamada do motor termina. |
| RenderEffectArea([Engine]) | Executa `RenderEffectArea` e retorna quando a chamada do motor termina. |
| RenderEffectArea() | Executa `RenderEffectArea` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [RenderEffectArea] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
