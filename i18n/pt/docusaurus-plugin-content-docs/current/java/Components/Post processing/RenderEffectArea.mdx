import ApiDoc from '@site/src/components/ApiDoc';

# RenderEffectArea

Referência completa da API de `RenderEffectArea`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class RenderEffectArea extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "RenderEffectArea(RenderEffectArea)",
      description: <>Executa <code>RenderEffectArea</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "RenderEffectArea(Engine)",
      description: <>Executa <code>RenderEffectArea</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "RenderEffectArea()",
      description: <>Executa <code>RenderEffectArea</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "RenderEffectArea",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
/>
