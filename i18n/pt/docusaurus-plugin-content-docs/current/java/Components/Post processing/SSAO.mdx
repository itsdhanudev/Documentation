import ApiDoc from '@site/src/components/ApiDoc';

# SSAO

Referência completa da API de `SSAO`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class SSAO extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SSAO(SSAO)",
      description: <>Executa <code>SSAO</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SSAO(Engine)",
      description: <>Executa <code>SSAO</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SSAO()",
      description: <>Executa <code>SSAO</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SSAO",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "bias",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBias</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "bilateralThreshold",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBilateralThreshold</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getIntensity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "lowPassFilter",
      type: "QualityLevel",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLowPassFilter</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "minHorizonAngleRad",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMinHorizonAngleRad</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "power",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPower</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "quality",
      type: "QualityLevel",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getQuality</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "radius",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRadius</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "upsampling",
      type: "QualityLevel",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getUpsampling</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
