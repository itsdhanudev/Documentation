import ApiDoc from '@site/src/components/ApiDoc';

# SSR

Referência completa da API de `SSR`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class SSR extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SSR(SSR)",
      description: <>Executa <code>SSR</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SSR(Engine)",
      description: <>Executa <code>SSR</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SSR()",
      description: <>Executa <code>SSR</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SSR",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "bias",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBias</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "stride",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getStride</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "thickness",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getThickness</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
