# SSR

Referência completa da API de `SSR`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class SSR extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| SSR([SSR]) | Executa `SSR` e retorna quando a chamada do motor termina. |
| SSR([Engine]) | Executa `SSR` e retorna quando a chamada do motor termina. |
| SSR() | Executa `SSR` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [SSR] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (4)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `bias` | [float] | `get/set` | Atributo virtual exposto por `getBias`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `maxDistance` | [float] | `get/set` | Atributo virtual exposto por `getMaxDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `stride` | [float] | `get/set` | Atributo virtual exposto por `getStride`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `thickness` | [float] | `get/set` | Atributo virtual exposto por `getThickness`. Acesse como um campo para ler ou escrever o valor subjacente. |
