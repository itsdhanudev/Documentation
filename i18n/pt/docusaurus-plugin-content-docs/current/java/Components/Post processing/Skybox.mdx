import ApiDoc from '@site/src/components/ApiDoc';

# Skybox

Referência completa da API de `Skybox`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class Skybox extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Skybox(Skybox)",
      description: <>Executa <code>Skybox</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Skybox(Engine)",
      description: <>Executa <code>Skybox</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Skybox()",
      description: <>Executa <code>Skybox</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Skybox",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "lux",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLux</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxResolutionID",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxResolutionID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "showSun",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setShowSun</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isShowSun()",
      returns: "boolean",
      description: <>Executa <code>isShowSun</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
