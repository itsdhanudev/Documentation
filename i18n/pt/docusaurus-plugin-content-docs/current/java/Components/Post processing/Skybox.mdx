# Skybox

Referência completa da API de `Skybox`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class Skybox extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| Skybox([Skybox]) | Executa `Skybox` e retorna quando a chamada do motor termina. |
| Skybox([Engine]) | Executa `Skybox` e retorna quando a chamada do motor termina. |
| Skybox() | Executa `Skybox` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [Skybox] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (4)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `color` | [Color] | `get/set` | Atributo virtual exposto por `getColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `lux` | [float] | `get/set` | Atributo virtual exposto por `getLux`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `maxResolutionID` | [int] | `get/set` | Atributo virtual exposto por `getMaxResolutionID`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `showSun` | [boolean] | `set` | Atributo virtual exposto por `setShowSun`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `isShowSun()` | `boolean` | Executa `isShowSun` e retorna quando a chamada do motor termina. |
