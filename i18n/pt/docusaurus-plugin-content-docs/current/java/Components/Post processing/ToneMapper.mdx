# ToneMapper

Referência completa da API de `ToneMapper`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class ToneMapper extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| ToneMapper([ToneMapper]) | Executa `ToneMapper` e retorna quando a chamada do motor termina. |
| ToneMapper([Engine]) | Executa `ToneMapper` e retorna quando a chamada do motor termina. |
| ToneMapper() | Executa `ToneMapper` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [ToneMapper] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (15)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `contrast` | [float] | `get/set` | Atributo virtual exposto por `getContrast`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `offsetB` | [float] | `get/set` | Atributo virtual exposto por `getOffsetB`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `offsetG` | [float] | `get/set` | Atributo virtual exposto por `getOffsetG`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `offsetR` | [float] | `get/set` | Atributo virtual exposto por `getOffsetR`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `powerB` | [float] | `get/set` | Atributo virtual exposto por `getPowerB`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `powerG` | [float] | `get/set` | Atributo virtual exposto por `getPowerG`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `powerR` | [float] | `get/set` | Atributo virtual exposto por `getPowerR`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `saturation` | [float] | `get/set` | Atributo virtual exposto por `getSaturation`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `slopeB` | [float] | `get/set` | Atributo virtual exposto por `getSlopeB`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `slopeG` | [float] | `get/set` | Atributo virtual exposto por `getSlopeG`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `slopeR` | [float] | `get/set` | Atributo virtual exposto por `getSlopeR`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `temperature` | [float] | `get/set` | Atributo virtual exposto por `getTemperature`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `tint` | [float] | `get/set` | Atributo virtual exposto por `getTint`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `tonnemapType` | [Type] | `get/set` | Atributo virtual exposto por `getTonnemapType`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `vibrance` | [float] | `get/set` | Atributo virtual exposto por `getVibrance`. Acesse como um campo para ler ou escrever o valor subjacente. |
