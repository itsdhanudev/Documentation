import ApiDoc from '@site/src/components/ApiDoc';

# ToneMapper

Referência completa da API de `ToneMapper`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class ToneMapper extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ToneMapper(ToneMapper)",
      description: <>Executa <code>ToneMapper</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ToneMapper(Engine)",
      description: <>Executa <code>ToneMapper</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ToneMapper()",
      description: <>Executa <code>ToneMapper</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ToneMapper",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "contrast",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getContrast</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "offsetB",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOffsetB</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "offsetG",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOffsetG</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "offsetR",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOffsetR</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "powerB",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPowerB</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "powerG",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPowerG</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "powerR",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPowerR</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "saturation",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSaturation</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "slopeB",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSlopeB</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "slopeG",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSlopeG</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "slopeR",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSlopeR</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "temperature",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTemperature</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "tint",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTint</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "tonnemapType",
      type: "Type",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTonnemapType</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "vibrance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getVibrance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
