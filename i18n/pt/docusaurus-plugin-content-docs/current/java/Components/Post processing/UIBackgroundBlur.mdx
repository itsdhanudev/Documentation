import ApiDoc from '@site/src/components/ApiDoc';

# UIBackgroundBlur

Referência completa da API de `UIBackgroundBlur`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIBackgroundBlur extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIBackgroundBlur(UIBackgroundBlur)",
      description: <>Executa <code>UIBackgroundBlur</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIBackgroundBlur(Engine)",
      description: <>Executa <code>UIBackgroundBlur</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIBackgroundBlur()",
      description: <>Executa <code>UIBackgroundBlur</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIBackgroundBlur",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "blurRadius",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBlurRadius</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "quality",
      type: "Quality",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getQuality</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
