# UIBackgroundBlur

Referência completa da API de `UIBackgroundBlur`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIBackgroundBlur extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIBackgroundBlur([UIBackgroundBlur]) | Executa `UIBackgroundBlur` e retorna quando a chamada do motor termina. |
| UIBackgroundBlur([Engine]) | Executa `UIBackgroundBlur` e retorna quando a chamada do motor termina. |
| UIBackgroundBlur() | Executa `UIBackgroundBlur` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIBackgroundBlur] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `blurRadius` | [float] | `get/set` | Atributo virtual exposto por `getBlurRadius`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `quality` | [Quality] | `get/set` | Atributo virtual exposto por `getQuality`. Acesse como um campo para ler ou escrever o valor subjacente. |
