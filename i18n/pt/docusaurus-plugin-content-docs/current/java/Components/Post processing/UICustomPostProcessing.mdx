# UICustomPostProcessing

Referência completa da API de `UICustomPostProcessing`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UICustomPostProcessing extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UICustomPostProcessing([UICustomPostProcessing]) | Executa `UICustomPostProcessing` e retorna quando a chamada do motor termina. |
| UICustomPostProcessing([Engine]) | Executa `UICustomPostProcessing` e retorna quando a chamada do motor termina. |
| UICustomPostProcessing() | Executa `UICustomPostProcessing` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UICustomPostProcessing] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (3)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `param` | [String] | `set` | Atributo virtual exposto por `setParam`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `shaderFile` | [String] | `get/set` | Atributo virtual exposto por `getShaderFile`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `time` | [float] | `get/set` | Atributo virtual exposto por `getTime`. Acesse como um campo para ler ou escrever o valor subjacente. |
