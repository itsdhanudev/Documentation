import ApiDoc from '@site/src/components/ApiDoc';

# UICustomPostProcessing

Referência completa da API de `UICustomPostProcessing`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UICustomPostProcessing extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UICustomPostProcessing(UICustomPostProcessing)",
      description: <>Executa <code>UICustomPostProcessing</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UICustomPostProcessing(Engine)",
      description: <>Executa <code>UICustomPostProcessing</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UICustomPostProcessing()",
      description: <>Executa <code>UICustomPostProcessing</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UICustomPostProcessing",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "param",
      type: "String",
      access: "set",
      description: <>Atributo virtual exposto por <code>setParam</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "shaderFile",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getShaderFile</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "time",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTime</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
