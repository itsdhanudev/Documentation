import ApiDoc from '@site/src/components/ApiDoc';

# UIFadeOut

Referência completa da API de `UIFadeOut`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIFadeOut extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIFadeOut(UIFadeOut)",
      description: <>Executa <code>UIFadeOut</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIFadeOut(Engine)",
      description: <>Executa <code>UIFadeOut</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIFadeOut()",
      description: <>Executa <code>UIFadeOut</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIFadeOut",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "opacity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOpacity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
