# UIGaussianBlur

Referência completa da API de `UIGaussianBlur`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIGaussianBlur extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIGaussianBlur([UIGaussianBlur]) | Executa `UIGaussianBlur` e retorna quando a chamada do motor termina. |
| UIGaussianBlur([Engine]) | Executa `UIGaussianBlur` e retorna quando a chamada do motor termina. |
| UIGaussianBlur() | Executa `UIGaussianBlur` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIGaussianBlur] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `blurRadius` | [float] | `get/set` | Atributo virtual exposto por `getBlurRadius`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `quality` | [Quality] | `get/set` | Atributo virtual exposto por `getQuality`. Acesse como um campo para ler ou escrever o valor subjacente. |
