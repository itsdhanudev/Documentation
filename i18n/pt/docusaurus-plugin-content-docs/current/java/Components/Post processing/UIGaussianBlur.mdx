import ApiDoc from '@site/src/components/ApiDoc';

# UIGaussianBlur

Referência completa da API de `UIGaussianBlur`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class UIGaussianBlur extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIGaussianBlur(UIGaussianBlur)",
      description: <>Executa <code>UIGaussianBlur</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIGaussianBlur(Engine)",
      description: <>Executa <code>UIGaussianBlur</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIGaussianBlur()",
      description: <>Executa <code>UIGaussianBlur</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIGaussianBlur",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "blurRadius",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBlurRadius</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "quality",
      type: "Quality",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getQuality</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
