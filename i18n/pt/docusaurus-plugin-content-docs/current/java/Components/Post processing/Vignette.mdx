# Vignette

Referência completa da API de `Vignette`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class Vignette extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| Vignette([Vignette]) | Executa `Vignette` e retorna quando a chamada do motor termina. |
| Vignette([Engine]) | Executa `Vignette` e retorna quando a chamada do motor termina. |
| Vignette() | Executa `Vignette` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [Vignette] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (4)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `colorINT` | [Color] | `get/set` | Atributo virtual exposto por `getColorINT`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `feather` | [float] | `get/set` | Atributo virtual exposto por `getFeather`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `midPoint` | [float] | `get/set` | Atributo virtual exposto por `getMidPoint`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `roundness` | [float] | `get/set` | Atributo virtual exposto por `getRoundness`. Acesse como um campo para ler ou escrever o valor subjacente. |
