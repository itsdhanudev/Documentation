import ApiDoc from '@site/src/components/ApiDoc';

# Vignette

Referência completa da API de `Vignette`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Post processing

**Componente:** sim

---

```java
public class Vignette extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Vignette(Vignette)",
      description: <>Executa <code>Vignette</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Vignette(Engine)",
      description: <>Executa <code>Vignette</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Vignette()",
      description: <>Executa <code>Vignette</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Vignette",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "colorINT",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColorINT</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "feather",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFeather</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "midPoint",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMidPoint</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "roundness",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRoundness</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
