import ApiDoc from '@site/src/components/ApiDoc';

# SlideRotate

Referência completa da API de `SlideRotate`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Prototyping

**Componente:** sim

---

```java
public class SlideRotate extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SlideRotate(SlideRotate)",
      description: <>Executa <code>SlideRotate</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SlideRotate(Engine)",
      description: <>Executa <code>SlideRotate</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SlideRotate()",
      description: <>Cria uma nova instância de SlideRotate.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SlideRotate",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "axisName",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAxisName</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "currentAngle",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCurrentAngle</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "direction",
      type: "Direction",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getDirection</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "limited",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setLimited</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxAngle",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxAngle</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "minAngle",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMinAngle</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "sensibility",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSensibility</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isLimited()",
      returns: "boolean",
      description: <>Retorna se limited é verdadeiro.</>
    }
  ]}
/>
