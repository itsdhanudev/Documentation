# SmoothCameraFollow

Referência completa da API de `SmoothCameraFollow`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Prototyping

**Componente:** sim

---

```java
public class SmoothCameraFollow extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| SmoothCameraFollow([SmoothCameraFollow]) | Executa `SmoothCameraFollow` e retorna quando a chamada do motor termina. |
| SmoothCameraFollow([Engine]) | Executa `SmoothCameraFollow` e retorna quando a chamada do motor termina. |
| SmoothCameraFollow() | Cria uma nova instância de SmoothCameraFollow. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [SmoothCameraFollow] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (6)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `distance` | [float] | `get/set` | Atributo virtual exposto por `getDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `height` | [float] | `get/set` | Atributo virtual exposto por `getHeight`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `heightDamping` | [float] | `get/set` | Atributo virtual exposto por `getHeightDamping`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `maxHeightDistance` | [float] | `get/set` | Atributo virtual exposto por `getMaxHeightDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `rotationDamping` | [float] | `get/set` | Atributo virtual exposto por `getRotationDamping`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `target` | [SpatialObject] | `get/set` | Atributo virtual exposto por `getTarget`. Acesse como um campo para ler ou escrever o valor subjacente. |
