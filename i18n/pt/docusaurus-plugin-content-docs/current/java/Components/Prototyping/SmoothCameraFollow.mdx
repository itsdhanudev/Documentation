import ApiDoc from '@site/src/components/ApiDoc';

# SmoothCameraFollow

Referência completa da API de `SmoothCameraFollow`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Prototyping

**Componente:** sim

---

```java
public class SmoothCameraFollow extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SmoothCameraFollow(SmoothCameraFollow)",
      description: <>Executa <code>SmoothCameraFollow</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SmoothCameraFollow(Engine)",
      description: <>Executa <code>SmoothCameraFollow</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SmoothCameraFollow()",
      description: <>Cria uma nova instância de SmoothCameraFollow.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SmoothCameraFollow",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "distance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "height",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHeight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "heightDamping",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHeightDamping</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxHeightDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxHeightDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "rotationDamping",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRotationDamping</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "target",
      type: "SpatialObject",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTarget</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
