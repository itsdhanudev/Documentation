import ApiDoc from '@site/src/components/ApiDoc';

# StandUp

Referência completa da API de `StandUp`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Prototyping

**Componente:** sim

---

```java
public class StandUp extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "StandUp(StandUp)",
      description: <>Executa <code>StandUp</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "StandUp(Engine)",
      description: <>Executa <code>StandUp</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "StandUp()",
      description: <>Cria uma nova instância de StandUp.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "StandUp",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "rotationY",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRotationY</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "lookTo(Vector3)",
      returns: "void",
      description: <>Executa a operação lookTo usando os parâmetros informados (value).</>
    },
    {
      signature: "rotate(float)",
      returns: "void",
      description: <>Transforma ou move the object de acordo.</>
    },
    {
      signature: "rotateInSeconds(float)",
      returns: "void",
      description: <>Transforma ou move in seconds de acordo.</>
    }
  ]}
/>
