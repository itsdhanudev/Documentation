# StandUp

Referência completa da API de `StandUp`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Prototyping

**Componente:** sim

---

```java
public class StandUp extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| StandUp([StandUp]) | Executa `StandUp` e retorna quando a chamada do motor termina. |
| StandUp([Engine]) | Executa `StandUp` e retorna quando a chamada do motor termina. |
| StandUp() | Cria uma nova instância de StandUp. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [StandUp] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `rotationY` | [float] | `get/set` | Atributo virtual exposto por `getRotationY`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `lookTo(Vector3)` | `void` | Executa a operação lookTo usando os parâmetros informados (value). |
| `rotate(float)` | `void` | Transforma ou move the object de acordo. |
| `rotateInSeconds(float)` | `void` | Transforma ou move in seconds de acordo. |
