import ApiDoc from '@site/src/components/ApiDoc';

# RenderTextureExtractor

Referência completa da API de `RenderTextureExtractor`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Render

**Componente:** sim

---

```java
public class RenderTextureExtractor extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "RenderTextureExtractor(RenderTextureExtractor)",
      description: <>Executa <code>RenderTextureExtractor</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "RenderTextureExtractor(Engine)",
      description: <>Executa <code>RenderTextureExtractor</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "RenderTextureExtractor()",
      description: <>Executa <code>RenderTextureExtractor</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "RenderTextureExtractor",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
/>
