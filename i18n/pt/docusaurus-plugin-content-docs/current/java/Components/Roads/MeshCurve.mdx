import ApiDoc from '@site/src/components/ApiDoc';

# MeshCurve

Referência completa da API de `MeshCurve`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Roads

**Componente:** sim

---

```java
public class MeshCurve extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "MeshCurve(MeshCurve)",
      description: <>Executa <code>MeshCurve</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "MeshCurve(Engine)",
      description: <>Executa <code>MeshCurve</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "MeshCurve()",
      description: <>Cria uma nova instância de MeshCurve.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "MeshCurve",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "spacing",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSpacing</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "startOffset",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getStartOffset</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "curveQuality",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCurveQuality</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "offset",
      type: "Vector3",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOffset</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "scale",
      type: "Vector3",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScale</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "forwardAxis",
      type: "ForwardAxis",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getForwardAxis</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "upMode",
      type: "UpMode",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getUpMode</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "stickToTerrain",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isStickToTerrain</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isStickToTerrain()",
      returns: "boolean",
      description: <>Retorna se stick to terrain está habilitado.</>
    }
  ]}
/>
