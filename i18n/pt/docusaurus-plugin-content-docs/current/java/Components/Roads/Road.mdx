import ApiDoc from '@site/src/components/ApiDoc';

# Road

Referência completa da API de `Road`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Roads

**Componente:** sim

---

```java
public class Road extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Road(Road)",
      description: <>Executa <code>Road</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Road(Engine)",
      description: <>Executa <code>Road</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Road()",
      description: <>Cria uma nova instância de Road.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Road",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "generateCollision",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setGenerateCollision</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "quality",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getQuality</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "roadBorder",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRoadBorder</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "roadBorderUV",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRoadBorderUV</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "roadBorderVerticalOffset",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRoadBorderVerticalOffset</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "terrainBorder",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTerrainBorder</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "textureMode",
      type: "TextureMode",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTextureMode</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "textureWidth",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTextureWidth</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "verticalOffset",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getVerticalOffset</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "width",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getWidth</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isGenerateCollision()",
      returns: "boolean",
      description: <>Retorna se generate collision é verdadeiro.</>
    }
  ]}
/>
