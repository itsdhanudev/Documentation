# Road

Referência completa da API de `Road`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Roads

**Componente:** sim

---

```java
public class Road extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| Road([Road]) | Executa `Road` e retorna quando a chamada do motor termina. |
| Road([Engine]) | Executa `Road` e retorna quando a chamada do motor termina. |
| Road() | Cria uma nova instância de Road. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [Road] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (10)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `generateCollision` | [boolean] | `set` | Atributo virtual exposto por `setGenerateCollision`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `quality` | [float] | `get/set` | Atributo virtual exposto por `getQuality`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `roadBorder` | [float] | `get/set` | Atributo virtual exposto por `getRoadBorder`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `roadBorderUV` | [float] | `get/set` | Atributo virtual exposto por `getRoadBorderUV`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `roadBorderVerticalOffset` | [float] | `get/set` | Atributo virtual exposto por `getRoadBorderVerticalOffset`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `terrainBorder` | [float] | `get/set` | Atributo virtual exposto por `getTerrainBorder`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `textureMode` | [TextureMode] | `get/set` | Atributo virtual exposto por `getTextureMode`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `textureWidth` | [float] | `get/set` | Atributo virtual exposto por `getTextureWidth`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `verticalOffset` | [float] | `get/set` | Atributo virtual exposto por `getVerticalOffset`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `width` | [float] | `get/set` | Atributo virtual exposto por `getWidth`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `isGenerateCollision()` | `boolean` | Retorna se generate collision é verdadeiro. |
