import ApiDoc from '@site/src/components/ApiDoc';

# RoadPoint

Referência completa da API de `RoadPoint`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Roads

**Componente:** sim

---

```java
public class RoadPoint extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "RoadPoint(RoadPoint)",
      description: <>Executa <code>RoadPoint</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "RoadPoint(Engine)",
      description: <>Executa <code>RoadPoint</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "RoadPoint()",
      description: <>Cria uma nova instância de RoadPoint.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "RoadPoint",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
/>
