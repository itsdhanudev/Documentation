# RoadPoint

Referência completa da API de `RoadPoint`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Roads

**Componente:** sim

---

```java
public class RoadPoint extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| RoadPoint([RoadPoint]) | Executa `RoadPoint` e retorna quando a chamada do motor termina. |
| RoadPoint([Engine]) | Executa `RoadPoint` e retorna quando a chamada do motor termina. |
| RoadPoint() | Cria uma nova instância de RoadPoint. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [RoadPoint] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
