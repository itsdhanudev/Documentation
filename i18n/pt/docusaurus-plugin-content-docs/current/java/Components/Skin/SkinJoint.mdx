# SkinJoint

Referência completa da API de `SkinJoint`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Skin

**Componente:** sim

---

```java
public class SkinJoint extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| SkinJoint([SkinJoint]) | Executa `SkinJoint` e retorna quando a chamada do motor termina. |
| SkinJoint([Engine]) | Executa `SkinJoint` e retorna quando a chamada do motor termina. |
| SkinJoint() | Cria uma nova instância de SkinJoint. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [SkinJoint] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `index` | [int] | `get` | Atributo virtual exposto por `getIndex`. Acesse como um campo para ler ou escrever o valor subjacente. |
