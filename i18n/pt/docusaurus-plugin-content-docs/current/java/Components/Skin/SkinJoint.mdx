import ApiDoc from '@site/src/components/ApiDoc';

# SkinJoint

Referência completa da API de `SkinJoint`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Skin

**Componente:** sim

---

```java
public class SkinJoint extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SkinJoint(SkinJoint)",
      description: <>Executa <code>SkinJoint</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SkinJoint(Engine)",
      description: <>Executa <code>SkinJoint</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SkinJoint()",
      description: <>Cria uma nova instância de SkinJoint.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SkinJoint",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "index",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getIndex</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
