import ApiDoc from '@site/src/components/ApiDoc';

# MicrophoneReader

Referência completa da API de `MicrophoneReader`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Sound

**Componente:** sim

---

```java
public class MicrophoneReader extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "MicrophoneReader(MicrophoneReader)",
      description: <>Executa <code>MicrophoneReader</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "MicrophoneReader(Engine)",
      description: <>Executa <code>MicrophoneReader</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "MicrophoneReader()",
      description: <>Cria uma nova instância de MicrophoneReader.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "MicrophoneReader",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "enableRecord",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isEnableRecord</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "frameLength",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getFrameLength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "framePCM",
      type: "float[]",
      access: "get",
      description: <>Atributo virtual exposto por <code>getFramePCM</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "sampleRate",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getSampleRate</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
