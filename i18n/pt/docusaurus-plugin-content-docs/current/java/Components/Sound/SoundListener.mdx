import ApiDoc from '@site/src/components/ApiDoc';

# SoundListener

Referência completa da API de `SoundListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Sound

**Componente:** sim

---

```java
public class SoundListener extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SoundListener(SoundListener)",
      description: <>Executa <code>SoundListener</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SoundListener()",
      description: <>Executa <code>SoundListener</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "SoundListener",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
