import ApiDoc from '@site/src/components/ApiDoc';

# SoundPlayer

Referência completa da API de `SoundPlayer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Sound

**Componente:** sim

---

```java
public class SoundPlayer extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "SoundPlayer(SoundPlayer)",
      description: <>Executa <code>SoundPlayer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SoundPlayer()",
      description: <>Executa <code>SoundPlayer</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "SoundPlayer",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "diameter",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getDiameter</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "loadMode",
      type: "LoadMode",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLoadMode</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "loop",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isLoop</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "mode",
      type: "Mode",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMode</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "paused",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isPaused</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "pitch",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPitch</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "playing",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isPlaying</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "speed",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSpeed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "stopped",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isStopped</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "volume",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getVolume</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "setSoundFile(SoundFile)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>sound file``.</>
    },
    {
      signature: "setSoundFile(File)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>sound file``.</>
    },
    {
      signature: "play()",
      returns: "void",
      description: <>Executa a operação play.</>
    },
    {
      signature: "stop()",
      returns: "void",
      description: <>Executa a operação stop.</>
    },
    {
      signature: "pause()",
      returns: "void",
      description: <>Executa a operação pause.</>
    },
    {
      signature: "unPause()",
      returns: "void",
      description: <>Executa a operação unPause.</>
    },
    {
      signature: "seekTo(int)",
      returns: "void",
      description: <>Executa <code>seekTo</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
