import ApiDoc from '@site/src/components/ApiDoc';

# VideoPlayer

Referência completa da API de `VideoPlayer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Sound

**Componente:** sim

---

```java
public class VideoPlayer extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VideoPlayer(VideoPlayer)",
      description: <>Executa <code>VideoPlayer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VideoPlayer()",
      description: <>Executa <code>VideoPlayer</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "VideoPlayer",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "loop",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isLoop</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "paused",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isPaused</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "playing",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isPlaying</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "stopped",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isStopped</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "setVideoFile(VideoFile)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>video file``.</>
    },
    {
      signature: "setVideoFile(File)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>video file``.</>
    },
    {
      signature: "setOutputVideoFile(OutputVideoFile)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>output video file``.</>
    },
    {
      signature: "setOutputVideoFile(File)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>output video file``.</>
    },
    {
      signature: "play()",
      returns: "void",
      description: <>Executa a operação play.</>
    },
    {
      signature: "stop()",
      returns: "void",
      description: <>Executa a operação stop.</>
    },
    {
      signature: "pause()",
      returns: "void",
      description: <>Executa a operação pause.</>
    },
    {
      signature: "unPause()",
      returns: "void",
      description: <>Executa a operação unPause.</>
    },
    {
      signature: "seekTo(int)",
      returns: "void",
      description: <>Executa a operação seekTo usando os parâmetros informados (miliSeconds).</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
