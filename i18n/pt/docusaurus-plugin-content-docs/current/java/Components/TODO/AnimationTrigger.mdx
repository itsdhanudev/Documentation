import ApiDoc from '@site/src/components/ApiDoc';

# AnimationTrigger

Referência completa da API de `AnimationTrigger`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class AnimationTrigger extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "AnimationTrigger(AnimationTrigger)",
      description: <>Executa <code>AnimationTrigger</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "AnimationTrigger(Engine)",
      description: <>Executa <code>AnimationTrigger</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "AnimationTrigger()",
      description: <>Executa <code>AnimationTrigger</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "AnimationTrigger",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "animName",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAnimName</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "animSpeed",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAnimSpeed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "animSpeedFrom",
      type: "AnimSpeedFrom",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAnimSpeedFrom</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "loopOnce",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isLoopOnce</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "onActiveEventName",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getOnActiveEventName</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "running",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isRunning</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "transitionDelay",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTransitionDelay</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "enterConditionCount()",
      returns: "int",
      description: <>Executa <code>enterConditionCount</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "enterConditionAt(int)",
      returns: "Condition",
      description: <>Executa <code>enterConditionAt</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "addEnterCondition(Condition)",
      returns: "void",
      description: <>Executa <code>addEnterCondition</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "removeEnterCondition(Condition)",
      returns: "void",
      description: <>Executa <code>removeEnterCondition</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "exitConditionCount()",
      returns: "int",
      description: <>Executa <code>exitConditionCount</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "exitConditionAt(int)",
      returns: "Condition",
      description: <>Executa <code>exitConditionAt</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "addExitCondition(Condition)",
      returns: "void",
      description: <>Executa <code>addExitCondition</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "removeExitCondition(Condition)",
      returns: "void",
      description: <>Executa <code>removeExitCondition</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
