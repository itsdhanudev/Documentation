import ApiDoc from '@site/src/components/ApiDoc';

# Billboard

Referência completa da API de `Billboard`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class Billboard extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Billboard(Billboard)",
      description: <>Executa <code>Billboard</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Billboard(Engine)",
      description: <>Executa <code>Billboard</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Billboard()",
      description: <>Executa <code>Billboard</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Billboard",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "zAlign",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>iszAlign</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
