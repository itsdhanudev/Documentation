# Billboard

Referência completa da API de `Billboard`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class Billboard extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| Billboard([Billboard]) | Executa `Billboard` e retorna quando a chamada do motor termina. |
| Billboard([Engine]) | Executa `Billboard` e retorna quando a chamada do motor termina. |
| Billboard() | Executa `Billboard` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [Billboard] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `zAlign` | [boolean] | `get/set` | Atributo virtual exposto por `iszAlign`. Acesse como um campo para ler ou escrever o valor subjacente. |
