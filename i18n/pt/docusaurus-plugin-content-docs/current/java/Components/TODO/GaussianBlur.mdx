import ApiDoc from '@site/src/components/ApiDoc';

# GaussianBlur

Referência completa da API de `GaussianBlur`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class GaussianBlur extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "GaussianBlur(GaussianBlur)",
      description: <>Executa <code>GaussianBlur</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "GaussianBlur(Engine)",
      description: <>Executa <code>GaussianBlur</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "GaussianBlur()",
      description: <>Executa <code>GaussianBlur</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "GaussianBlur",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "blurRadius",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getBlurRadius</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getIntensity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "quality",
      type: "Quality",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getQuality</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
