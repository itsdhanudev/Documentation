# GaussianBlur

Referência completa da API de `GaussianBlur`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class GaussianBlur extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| GaussianBlur([GaussianBlur]) | Executa `GaussianBlur` e retorna quando a chamada do motor termina. |
| GaussianBlur([Engine]) | Executa `GaussianBlur` e retorna quando a chamada do motor termina. |
| GaussianBlur() | Executa `GaussianBlur` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [GaussianBlur] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (3)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `blurRadius` | [float] | `get/set` | Atributo virtual exposto por `getBlurRadius`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `intensity` | [float] | `get/set` | Atributo virtual exposto por `getIntensity`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `quality` | [Quality] | `get/set` | Atributo virtual exposto por `getQuality`. Acesse como um campo para ler ou escrever o valor subjacente. |
