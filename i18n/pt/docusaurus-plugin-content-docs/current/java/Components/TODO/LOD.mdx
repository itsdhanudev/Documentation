import ApiDoc from '@site/src/components/ApiDoc';

# LOD

Referência completa da API de `LOD`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class LOD extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "LOD(LOD)",
      description: <>Executa <code>LOD</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "LOD(Engine)",
      description: <>Executa <code>LOD</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "LOD()",
      description: <>Executa <code>LOD</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "LOD",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "maxVisibleDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxVisibleDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
