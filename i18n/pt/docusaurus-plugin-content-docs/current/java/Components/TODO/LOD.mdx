# LOD

Referência completa da API de `LOD`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class LOD extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| LOD([LOD]) | Executa `LOD` e retorna quando a chamada do motor termina. |
| LOD([Engine]) | Executa `LOD` e retorna quando a chamada do motor termina. |
| LOD() | Executa `LOD` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [LOD] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `maxVisibleDistance` | [float] | `get/set` | Atributo virtual exposto por `getMaxVisibleDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
