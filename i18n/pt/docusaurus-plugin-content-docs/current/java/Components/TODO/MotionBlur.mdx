import ApiDoc from '@site/src/components/ApiDoc';

# MotionBlur

Referência completa da API de `MotionBlur`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class MotionBlur extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "MotionBlur(MotionBlur)",
      description: <>Executa <code>MotionBlur</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "MotionBlur(Engine)",
      description: <>Executa <code>MotionBlur</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "MotionBlur()",
      description: <>Executa <code>MotionBlur</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "MotionBlur",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "alpha",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAlpha</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "reset",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getReset</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
