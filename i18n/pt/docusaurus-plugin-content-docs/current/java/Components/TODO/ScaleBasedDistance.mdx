import ApiDoc from '@site/src/components/ApiDoc';

# ScaleBasedDistance

Referência completa da API de `ScaleBasedDistance`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class ScaleBasedDistance extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "ScaleBasedDistance(ScaleBasedDistance)",
      description: <>Executa <code>ScaleBasedDistance</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ScaleBasedDistance(Engine)",
      description: <>Executa <code>ScaleBasedDistance</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ScaleBasedDistance()",
      description: <>Executa <code>ScaleBasedDistance</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ScaleBasedDistance",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "scale",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScale</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
