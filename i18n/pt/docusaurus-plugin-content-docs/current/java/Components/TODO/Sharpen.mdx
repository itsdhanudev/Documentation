# Sharpen

Referência completa da API de `Sharpen`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class Sharpen extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| Sharpen([Sharpen]) | Executa `Sharpen` e retorna quando a chamada do motor termina. |
| Sharpen([Engine]) | Executa `Sharpen` e retorna quando a chamada do motor termina. |
| Sharpen() | Executa `Sharpen` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [Sharpen] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (3)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `edgeThreshold` | [float] | `get/set` | Atributo virtual exposto por `getEdgeThreshold`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `intensity` | [float] | `get/set` | Atributo virtual exposto por `getIntensity`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `sharpenStrength` | [float] | `get/set` | Atributo virtual exposto por `getSharpenStrength`. Acesse como um campo para ler ou escrever o valor subjacente. |
