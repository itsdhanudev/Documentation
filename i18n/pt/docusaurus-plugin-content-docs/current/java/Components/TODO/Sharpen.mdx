import ApiDoc from '@site/src/components/ApiDoc';

# Sharpen

Referência completa da API de `Sharpen`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class Sharpen extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Sharpen(Sharpen)",
      description: <>Executa <code>Sharpen</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Sharpen(Engine)",
      description: <>Executa <code>Sharpen</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Sharpen()",
      description: <>Executa <code>Sharpen</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Sharpen",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "edgeThreshold",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getEdgeThreshold</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "intensity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getIntensity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "sharpenStrength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSharpenStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
