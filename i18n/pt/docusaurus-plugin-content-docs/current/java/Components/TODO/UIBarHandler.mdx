import ApiDoc from '@site/src/components/ApiDoc';

# UIBarHandler

Referência completa da API de `UIBarHandler`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class UIBarHandler extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIBarHandler(UIBarHandler)",
      description: <>Executa <code>UIBarHandler</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIBarHandler(Engine)",
      description: <>Executa <code>UIBarHandler</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIBarHandler()",
      description: <>Executa <code>UIBarHandler</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIBarHandler",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "align",
      type: "Align",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAlign</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
