# UIBarHandler

Referência completa da API de `UIBarHandler`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** TODO

**Componente:** sim

---

```java
public class UIBarHandler extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIBarHandler([UIBarHandler]) | Executa `UIBarHandler` e retorna quando a chamada do motor termina. |
| UIBarHandler([Engine]) | Executa `UIBarHandler` e retorna quando a chamada do motor termina. |
| UIBarHandler() | Executa `UIBarHandler` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIBarHandler] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `align` | [Align] | `get/set` | Atributo virtual exposto por `getAlign`. Acesse como um campo para ler ou escrever o valor subjacente. |
