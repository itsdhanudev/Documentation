import ApiDoc from '@site/src/components/ApiDoc';

# Terrain

Referência completa da API de `Terrain`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Terrain

**Componente:** sim

---

```java
public class Terrain extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "Terrain()",
      description: <>Executa <code>Terrain</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "Terrain",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "maxHeight",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxHeight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "resolution",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getResolution</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "width",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getWidth</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isInsideTerrain(float, float)",
      returns: "boolean",
      description: <>Executa <code>isInsideTerrain</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "isInsideTerrain(Vector3)",
      returns: "boolean",
      description: <>Executa <code>isInsideTerrain</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "nearestVerticeAt(Vector3)",
      returns: "Point2",
      description: <>Executa <code>nearestVerticeAt</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "nearestLayerVerticeAt(Vector3)",
      returns: "Point2",
      description: <>Executa <code>nearestLayerVerticeAt</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getWorldY(float, float)",
      returns: "float",
      description: <>Executa <code>getWorldY</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getWorldY(Vector3)",
      returns: "float",
      description: <>Executa <code>getWorldY</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getHeight(Point2)",
      returns: "float",
      description: <>Executa <code>getHeight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getHeight(int, int)",
      returns: "float",
      description: <>Executa <code>getHeight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "setHeight(Point2, float)",
      returns: "void",
      description: <>Executa <code>setHeight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "setHeight(int, int, float)",
      returns: "void",
      description: <>Executa <code>setHeight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getLayerIntensity(int, int, int)",
      returns: "float",
      description: <>Executa <code>getLayerIntensity</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "setLayerIntensity(int, int, int, float)",
      returns: "void",
      description: <>Executa <code>setLayerIntensity</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getLayersCount()",
      returns: "int",
      description: <>Executa <code>getLayersCount</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getLayerAt(int)",
      returns: "Layer",
      description: <>Executa <code>getLayerAt</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "addLayer(Layer)",
      returns: "void",
      description: <>Executa <code>addLayer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "traceRay(Vector3, Vector3)",
      returns: "LaserHit",
      description: <>Executa <code>traceRay</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "traceRay(Vector3, Vector3, float)",
      returns: "LaserHit",
      description: <>Executa <code>traceRay</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "applyRaiseLower(Vector2, float, float, Brush)",
      returns: "void",
      description: <>Executa <code>applyRaiseLower</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "applyPaint(Vector2, float, float, float, int, Brush)",
      returns: "void",
      description: <>Executa <code>applyPaint</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
