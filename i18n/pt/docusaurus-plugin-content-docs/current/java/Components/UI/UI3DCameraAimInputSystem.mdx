import ApiDoc from '@site/src/components/ApiDoc';

# UI3DCameraAimInputSystem

Referência completa da API de `UI3DCameraAimInputSystem`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UI3DCameraAimInputSystem extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UI3DCameraAimInputSystem(UI3DCameraAimInputSystem)",
      description: <>Executa <code>UI3DCameraAimInputSystem</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UI3DCameraAimInputSystem(Engine)",
      description: <>Executa <code>UI3DCameraAimInputSystem</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UI3DCameraAimInputSystem()",
      description: <>Executa <code>UI3DCameraAimInputSystem</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UI3DCameraAimInputSystem",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "performClick()",
      returns: "void",
      description: <>Executa <code>performClick</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
