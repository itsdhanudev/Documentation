import ApiDoc from '@site/src/components/ApiDoc';

# UI3DInputSystem

Referência completa da API de `UI3DInputSystem`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UI3DInputSystem extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UI3DInputSystem(UI3DInputSystem)",
      description: <>Executa <code>UI3DInputSystem</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UI3DInputSystem(Engine)",
      description: <>Executa <code>UI3DInputSystem</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UI3DInputSystem()",
      description: <>Executa <code>UI3DInputSystem</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UI3DInputSystem",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "maxHitDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxHitDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
