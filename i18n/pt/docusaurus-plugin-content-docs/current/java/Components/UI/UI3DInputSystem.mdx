# UI3DInputSystem

Referência completa da API de `UI3DInputSystem`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UI3DInputSystem extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UI3DInputSystem([UI3DInputSystem]) | Executa `UI3DInputSystem` e retorna quando a chamada do motor termina. |
| UI3DInputSystem([Engine]) | Executa `UI3DInputSystem` e retorna quando a chamada do motor termina. |
| UI3DInputSystem() | Executa `UI3DInputSystem` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UI3DInputSystem] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `maxHitDistance` | [float] | `get/set` | Atributo virtual exposto por `getMaxHitDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
