# UIAlignment

Referência completa da API de `UIAlignment`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIAlignment extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIAlignment([UIAlignment]) | Executa `UIAlignment` e retorna quando a chamada do motor termina. |
| UIAlignment([Engine]) | Executa `UIAlignment` e retorna quando a chamada do motor termina. |
| UIAlignment() | Cria uma nova instância de UIAlignment. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIAlignment] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (9)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `bottomCenterEnabled` | [boolean] | `get/set` | Atributo virtual exposto por `isBottomCenterEnabled`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `bottomLeftEnabled` | [boolean] | `get/set` | Atributo virtual exposto por `isBottomLeftEnabled`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `bottomRightEnabled` | [boolean] | `get/set` | Atributo virtual exposto por `isBottomRightEnabled`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `centerEnabled` | [boolean] | `get/set` | Atributo virtual exposto por `isCenterEnabled`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `centerLeftEnabled` | [boolean] | `get/set` | Atributo virtual exposto por `isCenterLeftEnabled`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `centerRightEnabled` | [boolean] | `get/set` | Atributo virtual exposto por `isCenterRightEnabled`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `topCenterEnabled` | [boolean] | `get/set` | Atributo virtual exposto por `isTopCenterEnabled`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `topLeftEnabled` | [boolean] | `get/set` | Atributo virtual exposto por `isTopLeftEnabled`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `topRightEnabled` | [boolean] | `get/set` | Atributo virtual exposto por `isTopRightEnabled`. Acesse como um campo para ler ou escrever o valor subjacente. |
