import ApiDoc from '@site/src/components/ApiDoc';

# UIAspectRatio

Referência completa da API de `UIAspectRatio`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIAspectRatio extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIAspectRatio(UIAspectRatio)",
      description: <>Executa <code>UIAspectRatio</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIAspectRatio(Engine)",
      description: <>Executa <code>UIAspectRatio</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIAspectRatio()",
      description: <>Executa <code>UIAspectRatio</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIAspectRatio",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "mode",
      type: "Mode",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMode</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "proportion",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getProportion</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
