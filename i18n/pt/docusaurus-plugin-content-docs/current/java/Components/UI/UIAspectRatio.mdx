# UIAspectRatio

Referência completa da API de `UIAspectRatio`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIAspectRatio extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIAspectRatio([UIAspectRatio]) | Executa `UIAspectRatio` e retorna quando a chamada do motor termina. |
| UIAspectRatio([Engine]) | Executa `UIAspectRatio` e retorna quando a chamada do motor termina. |
| UIAspectRatio() | Executa `UIAspectRatio` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIAspectRatio] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `mode` | [Mode] | `get/set` | Atributo virtual exposto por `getMode`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `proportion` | [float] | `get/set` | Atributo virtual exposto por `getProportion`. Acesse como um campo para ler ou escrever o valor subjacente. |
