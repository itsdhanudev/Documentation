# UIAxisEventListener

Referência completa da API de `UIAxisEventListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIAxisEventListener extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIAxisEventListener([UIAxisEventListener]) | Executa `UIAxisEventListener` e retorna quando a chamada do motor termina. |
| UIAxisEventListener([Engine]) | Executa `UIAxisEventListener` e retorna quando a chamada do motor termina. |
| UIAxisEventListener() | Cria uma nova instância de UIAxisEventListener. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIAxisEventListener] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `axisName` | [String] | `get/set` | Atributo virtual exposto por `getAxisName`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `value` | [Vector2] | `get/set` | Atributo virtual exposto por `getValue`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `setValue(float, float)` | `void` | Define ou atualiza a propriedade `propriedade `value``. |
