import ApiDoc from '@site/src/components/ApiDoc';

# UIAxisEventListener

Referência completa da API de `UIAxisEventListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIAxisEventListener extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIAxisEventListener(UIAxisEventListener)",
      description: <>Executa <code>UIAxisEventListener</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIAxisEventListener(Engine)",
      description: <>Executa <code>UIAxisEventListener</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIAxisEventListener()",
      description: <>Cria uma nova instância de UIAxisEventListener.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIAxisEventListener",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "axisName",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAxisName</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "value",
      type: "Vector2",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getValue</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "setValue(float, float)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>value``.</>
    }
  ]}
/>
