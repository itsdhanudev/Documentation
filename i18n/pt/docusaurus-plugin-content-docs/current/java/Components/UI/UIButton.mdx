# UIButton

Referência completa da API de `UIButton`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIButton extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIButton([UIButton]) | Executa `UIButton` e retorna quando a chamada do motor termina. |
| UIButton([Engine]) | Executa `UIButton` e retorna quando a chamada do motor termina. |
| UIButton() | Cria uma nova instância de UIButton. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIButton] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (15)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `border` | [float] | `get/set` | Atributo virtual exposto por `getBorder`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `down` | [boolean] | `get` | Atributo virtual exposto por `isDown`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `fontFile` | [String] | `get` | Atributo virtual exposto por `getFontFile`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `normalColor` | [Color] | `get/set` | Atributo virtual exposto por `getNormalColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `normalTexture` | [Texture] | `get/set` | Atributo virtual exposto por `getNormalTexture`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `padding` | [Vector2] | `get/set` | Atributo virtual exposto por `getPadding`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `pressed` | [boolean] | `get` | Atributo virtual exposto por `isPressed`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `pressedColor` | [Color] | `get/set` | Atributo virtual exposto por `getPressedColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `pressedTexture` | [Texture] | `get/set` | Atributo virtual exposto por `getPressedTexture`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `resolution` | [int] | `get/set` | Atributo virtual exposto por `getResolution`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `text` | [String] | `get/set` | Atributo virtual exposto por `getText`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `textNormalColor` | [Color] | `get/set` | Atributo virtual exposto por `getTextNormalColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `textPressedColor` | [Color] | `get/set` | Atributo virtual exposto por `getTextPressedColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `textSize` | [float] | `get/set` | Atributo virtual exposto por `getTextSize`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `up` | [boolean] | `get` | Atributo virtual exposto por `isUp`. Acesse como um campo para ler ou escrever o valor subjacente. |
