# UICheckBox

Referencia completa da API de `UICheckBox`, incluindo construtores, atributos, atributos virtuais e metodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UICheckBox extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UICheckBox([UICheckBox]) | Executa `UICheckBox` e retorna quando a chamada do motor termina. |
| UICheckBox([Engine]) | Executa `UICheckBox` e retorna quando a chamada do motor termina. |
| UICheckBox() | Cria uma nova instancia de UICheckBox. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UICheckBox] | Representa o valor de `instance` armazenado nesta instancia. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (7)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `checked` | [boolean] | `get/set` | Atributo virtual exposto por `isChecked`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `pressed` | [boolean] | `get` | Atributo virtual exposto por `isPressed`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `down` | [boolean] | `get` | Atributo virtual exposto por `isDown`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `up` | [boolean] | `get` | Atributo virtual exposto por `isUp`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `color` | [Color] | `get/set` | Atributo virtual exposto por `getColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `checkedTexture` | [Texture] | `get/set` | Atributo virtual exposto por `getCheckedTexture`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `uncheckedTexture` | [Texture] | `get/set` | Atributo virtual exposto por `getUncheckedTexture`. Acesse como um campo para ler ou escrever o valor subjacente. |
