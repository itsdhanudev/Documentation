# UIDrivingWheel

Referencia completa da API de `UIDrivingWheel`, incluindo construtores, atributos, atributos virtuais e metodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIDrivingWheel extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIDrivingWheel([UIDrivingWheel]) | Executa `UIDrivingWheel` e retorna quando a chamada do motor termina. |
| UIDrivingWheel([Engine]) | Executa `UIDrivingWheel` e retorna quando a chamada do motor termina. |
| UIDrivingWheel() | Cria uma nova instancia de UIDrivingWheel. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIDrivingWheel] | Representa o valor de `instance` armazenado nesta instancia. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (9)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `value` | [float] | `get` | Atributo virtual exposto por `getValue`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `angle` | [float] | `get` | Atributo virtual exposto por `getAngle`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `lerp` | [float] | `get/set` | Atributo virtual exposto por `getLerp`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `maxAngle` | [float] | `get/set` | Atributo virtual exposto por `getMaxAngle`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `pressed` | [boolean] | `get` | Atributo virtual exposto por `isPressed`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `down` | [boolean] | `get` | Atributo virtual exposto por `isDown`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `up` | [boolean] | `get` | Atributo virtual exposto por `isUp`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `color` | [Color] | `get/set` | Atributo virtual exposto por `getColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `texture` | [Texture] | `get/set` | Atributo virtual exposto por `getTexture`. Acesse como um campo para ler ou escrever o valor subjacente. |
