import ApiDoc from '@site/src/components/ApiDoc';

# UIFitParent

Referência completa da API de `UIFitParent`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIFitParent extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIFitParent(UIFitParent)",
      description: <>Executa <code>UIFitParent</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIFitParent(Engine)",
      description: <>Executa <code>UIFitParent</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIFitParent()",
      description: <>Cria uma nova instância de UIFitParent.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIFitParent",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "fitHeight",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setFitHeight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "fitWidth",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setFitWidth</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isFitHeight()",
      returns: "boolean",
      description: <>Retorna se fit height é verdadeiro.</>
    },
    {
      signature: "isFitWidth()",
      returns: "boolean",
      description: <>Retorna se fit width é verdadeiro.</>
    }
  ]}
/>
