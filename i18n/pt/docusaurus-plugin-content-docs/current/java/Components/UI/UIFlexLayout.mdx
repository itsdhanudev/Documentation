import ApiDoc from '@site/src/components/ApiDoc';

# UIFlexLayout

Referência completa da API de `UIFlexLayout`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIFlexLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIFlexLayout(UIFlexLayout)",
      description: <>Executa <code>UIFlexLayout</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIFlexLayout(Engine)",
      description: <>Executa <code>UIFlexLayout</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIFlexLayout()",
      description: <>Executa <code>UIFlexLayout</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIFlexLayout",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "spacing",
      type: "int",
      access: "set",
      description: <>Atributo virtual exposto por <code>setSpacing</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getSpacing()",
      returns: "int",
      description: <>Executa <code>getSpacing</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
