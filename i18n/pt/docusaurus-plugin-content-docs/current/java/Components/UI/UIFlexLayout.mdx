# UIFlexLayout

Referência completa da API de `UIFlexLayout`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIFlexLayout extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIFlexLayout([UIFlexLayout]) | Executa `UIFlexLayout` e retorna quando a chamada do motor termina. |
| UIFlexLayout([Engine]) | Executa `UIFlexLayout` e retorna quando a chamada do motor termina. |
| UIFlexLayout() | Executa `UIFlexLayout` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIFlexLayout] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `spacing` | [int] | `set` | Atributo virtual exposto por `setSpacing`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `getSpacing()` | `int` | Executa `getSpacing` e retorna quando a chamada do motor termina. |
