# UIGridLayout

Referência completa da API de `UIGridLayout`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIGridLayout extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIGridLayout([UIGridLayout]) | Executa `UIGridLayout` e retorna quando a chamada do motor termina. |
| UIGridLayout([Engine]) | Executa `UIGridLayout` e retorna quando a chamada do motor termina. |
| UIGridLayout() | Executa `UIGridLayout` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIGridLayout] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (3)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `autoColumns` | [boolean] | `set` | Atributo virtual exposto por `setAutoColumns`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `columns` | [int] | `set` | Atributo virtual exposto por `setColumns`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `spacing` | [int] | `set` | Atributo virtual exposto por `setSpacing`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `getSpacing()` | `int` | Executa `getSpacing` e retorna quando a chamada do motor termina. |
| `getColumns()` | `int` | Executa `getColumns` e retorna quando a chamada do motor termina. |
| `isAutoColumns()` | `boolean` | Executa `isAutoColumns` e retorna quando a chamada do motor termina. |
