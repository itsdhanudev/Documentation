import ApiDoc from '@site/src/components/ApiDoc';

# UIHorizontalLayout

Referência completa da API de `UIHorizontalLayout`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIHorizontalLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIHorizontalLayout(UIHorizontalLayout)",
      description: <>Executa <code>UIHorizontalLayout</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIHorizontalLayout(Engine)",
      description: <>Executa <code>UIHorizontalLayout</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIHorizontalLayout()",
      description: <>Cria uma nova instância de UIHorizontalLayout.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIHorizontalLayout",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "spacing",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSpacing</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
