# UIHorizontalLayout

Referência completa da API de `UIHorizontalLayout`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIHorizontalLayout extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIHorizontalLayout([UIHorizontalLayout]) | Executa `UIHorizontalLayout` e retorna quando a chamada do motor termina. |
| UIHorizontalLayout([Engine]) | Executa `UIHorizontalLayout` e retorna quando a chamada do motor termina. |
| UIHorizontalLayout() | Cria uma nova instância de UIHorizontalLayout. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIHorizontalLayout] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `spacing` | [int] | `get/set` | Atributo virtual exposto por `getSpacing`. Acesse como um campo para ler ou escrever o valor subjacente. |
