# UIHorizontalScrollView

Referência completa da API de `UIHorizontalScrollView`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIHorizontalScrollView extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIHorizontalScrollView([UIHorizontalScrollView]) | Executa `UIHorizontalScrollView` e retorna quando a chamada do motor termina. |
| UIHorizontalScrollView([Engine]) | Executa `UIHorizontalScrollView` e retorna quando a chamada do motor termina. |
| UIHorizontalScrollView() | Cria uma nova instância de UIHorizontalScrollView. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIHorizontalScrollView] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `scroll` | [int] | `get/set` | Atributo virtual exposto por `getScroll`. Acesse como um campo para ler ou escrever o valor subjacente. |
