import ApiDoc from '@site/src/components/ApiDoc';

# UIHorizontalScrollView

Referência completa da API de `UIHorizontalScrollView`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIHorizontalScrollView extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIHorizontalScrollView(UIHorizontalScrollView)",
      description: <>Executa <code>UIHorizontalScrollView</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIHorizontalScrollView(Engine)",
      description: <>Executa <code>UIHorizontalScrollView</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIHorizontalScrollView()",
      description: <>Cria uma nova instância de UIHorizontalScrollView.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIHorizontalScrollView",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "scroll",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScroll</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
