import ApiDoc from '@site/src/components/ApiDoc';

# UIKeyEventListener

Referência completa da API de `UIKeyEventListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIKeyEventListener extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIKeyEventListener(UIKeyEventListener)",
      description: <>Executa <code>UIKeyEventListener</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIKeyEventListener(Engine)",
      description: <>Executa <code>UIKeyEventListener</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIKeyEventListener()",
      description: <>Cria uma nova instância de UIKeyEventListener.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIKeyEventListener",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "keyName",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getKeyName</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
