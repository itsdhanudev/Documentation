import ApiDoc from '@site/src/components/ApiDoc';

# UIRadioGroup

Referencia completa da API de `UIRadioGroup`, incluindo construtores, atributos, atributos virtuais e metodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIRadioGroup extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIRadioGroup(UIRadioGroup)",
      description: <>Executa <code>UIRadioGroup</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIRadioGroup(Engine)",
      description: <>Executa <code>UIRadioGroup</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIRadioGroup()",
      description: <>Cria uma nova instancia de UIRadioGroup.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIRadioGroup",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instancia. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
/>
