# UIRect

Referência completa da API de `UIRect`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIRect extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIRect([UIRect]) | Executa `UIRect` e retorna quando a chamada do motor termina. |
| UIRect([Engine]) | Executa `UIRect` e retorna quando a chamada do motor termina. |
| UIRect() | Cria uma nova instância de UIRect. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIRect] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (16)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `autoSH` | [boolean] | `set` | Atributo virtual exposto por `setAutoSH`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `autoSW` | [boolean] | `set` | Atributo virtual exposto por `setAutoSW`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `marginBottom` | [int] | `get/set` | Atributo virtual exposto por `getMarginBottom`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `marginLeft` | [int] | `get/set` | Atributo virtual exposto por `getMarginLeft`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `marginRight` | [int] | `get/set` | Atributo virtual exposto por `getMarginRight`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `marginTop` | [int] | `get/set` | Atributo virtual exposto por `getMarginTop`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `paddingBottom` | [int] | `get/set` | Atributo virtual exposto por `getPaddingBottom`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `paddingLeft` | [int] | `get/set` | Atributo virtual exposto por `getPaddingLeft`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `paddingRight` | [int] | `get/set` | Atributo virtual exposto por `getPaddingRight`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `paddingTop` | [int] | `get/set` | Atributo virtual exposto por `getPaddingTop`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `position` | [int] | `get/set` | Atributo virtual exposto por `getPosition`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `positionX` | [int] | `get/set` | Atributo virtual exposto por `getPositionX`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `positionY` | [int] | `get/set` | Atributo virtual exposto por `getPositionY`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `size` | [int] | `get/set` | Atributo virtual exposto por `getSize`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `sizeX` | [int] | `get/set` | Atributo virtual exposto por `getSizeX`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `sizeY` | [int] | `get/set` | Atributo virtual exposto por `getSizeY`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `isAutoSH()` | `boolean` | Retorna se auto sh é verdadeiro. |
| `isAutoSW()` | `boolean` | Retorna se auto sw é verdadeiro. |
