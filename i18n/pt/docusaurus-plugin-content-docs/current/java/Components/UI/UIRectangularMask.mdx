import ApiDoc from '@site/src/components/ApiDoc';

# UIRectangularMask

Referência completa da API de `UIRectangularMask`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIRectangularMask extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIRectangularMask(UIRectangularMask)",
      description: <>Executa <code>UIRectangularMask</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIRectangularMask(Engine)",
      description: <>Executa <code>UIRectangularMask</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIRectangularMask()",
      description: <>Cria uma nova instância de UIRectangularMask.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIRectangularMask",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
/>
