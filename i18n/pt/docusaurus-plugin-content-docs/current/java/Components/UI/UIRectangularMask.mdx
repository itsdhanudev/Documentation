# UIRectangularMask

Referência completa da API de `UIRectangularMask`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIRectangularMask extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIRectangularMask([UIRectangularMask]) | Executa `UIRectangularMask` e retorna quando a chamada do motor termina. |
| UIRectangularMask([Engine]) | Executa `UIRectangularMask` e retorna quando a chamada do motor termina. |
| UIRectangularMask() | Cria uma nova instância de UIRectangularMask. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIRectangularMask] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
