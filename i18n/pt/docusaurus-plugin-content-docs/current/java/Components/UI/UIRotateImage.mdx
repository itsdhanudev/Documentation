import ApiDoc from '@site/src/components/ApiDoc';

# UIRotateImage

Referência completa da API de `UIRotateImage`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIRotateImage extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIRotateImage(UIRotateImage)",
      description: <>Executa <code>UIRotateImage</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIRotateImage(Engine)",
      description: <>Executa <code>UIRotateImage</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIRotateImage()",
      description: <>Cria uma nova instância de UIRotateImage.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIRotateImage",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "angle",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getAngle</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "texture",
      type: "Texture",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTexture</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
