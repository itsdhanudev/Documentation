# UIRotateImage

Referência completa da API de `UIRotateImage`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIRotateImage extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIRotateImage([UIRotateImage]) | Executa `UIRotateImage` e retorna quando a chamada do motor termina. |
| UIRotateImage([Engine]) | Executa `UIRotateImage` e retorna quando a chamada do motor termina. |
| UIRotateImage() | Cria uma nova instância de UIRotateImage. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIRotateImage] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `angle` | [float] | `get/set` | Atributo virtual exposto por `getAngle`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `texture` | [Texture] | `get/set` | Atributo virtual exposto por `getTexture`. Acesse como um campo para ler ou escrever o valor subjacente. |
