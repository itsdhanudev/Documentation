import ApiDoc from '@site/src/components/ApiDoc';

# UISlideArea

Referência completa da API de `UISlideArea`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UISlideArea extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UISlideArea(UISlideArea)",
      description: <>Executa <code>UISlideArea</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UISlideArea(Engine)",
      description: <>Executa <code>UISlideArea</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UISlideArea()",
      description: <>Cria uma nova instância de UISlideArea.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UISlideArea",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "getValue()",
      returns: "Vector2",
      description: <>Retorna a propriedade <code>propriedade </code>valor value atual``.</>
    }
  ]}
/>
