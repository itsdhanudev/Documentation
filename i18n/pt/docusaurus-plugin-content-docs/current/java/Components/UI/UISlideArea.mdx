# UISlideArea

Referência completa da API de `UISlideArea`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UISlideArea extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UISlideArea([UISlideArea]) | Executa `UISlideArea` e retorna quando a chamada do motor termina. |
| UISlideArea([Engine]) | Executa `UISlideArea` e retorna quando a chamada do motor termina. |
| UISlideArea() | Cria uma nova instância de UISlideArea. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UISlideArea] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `getValue()` | `Vector2` | Retorna a propriedade `propriedade `valor value atual``. |
