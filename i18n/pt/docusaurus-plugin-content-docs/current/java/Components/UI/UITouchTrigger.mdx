# UITouchTrigger

Referência completa da API de `UITouchTrigger`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UITouchTrigger extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UITouchTrigger([UITouchTrigger]) | Executa `UITouchTrigger` e retorna quando a chamada do motor termina. |
| UITouchTrigger([Engine]) | Executa `UITouchTrigger` e retorna quando a chamada do motor termina. |
| UITouchTrigger() | Cria uma nova instância de UITouchTrigger. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UITouchTrigger] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `selectedTouch` | [Touch] | `get` | Atributo virtual exposto por `getSelectedTouch`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `isDown()` | `boolean` | Retorna se down é verdadeiro. |
| `isPressed()` | `boolean` | Retorna se pressed é verdadeiro. |
| `isUp()` | `boolean` | Retorna se up é verdadeiro. |
