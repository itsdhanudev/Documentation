import ApiDoc from '@site/src/components/ApiDoc';

# UIVerticalLayout

Referência completa da API de `UIVerticalLayout`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIVerticalLayout extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIVerticalLayout(UIVerticalLayout)",
      description: <>Executa <code>UIVerticalLayout</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIVerticalLayout(Engine)",
      description: <>Executa <code>UIVerticalLayout</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIVerticalLayout()",
      description: <>Cria uma nova instância de UIVerticalLayout.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIVerticalLayout",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "spacing",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSpacing</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
