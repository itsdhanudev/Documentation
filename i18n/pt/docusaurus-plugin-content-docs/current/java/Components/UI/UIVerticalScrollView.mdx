import ApiDoc from '@site/src/components/ApiDoc';

# UIVerticalScrollView

Referência completa da API de `UIVerticalScrollView`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIVerticalScrollView extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIVerticalScrollView(UIVerticalScrollView)",
      description: <>Executa <code>UIVerticalScrollView</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIVerticalScrollView(Engine)",
      description: <>Executa <code>UIVerticalScrollView</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIVerticalScrollView()",
      description: <>Cria uma nova instância de UIVerticalScrollView.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIVerticalScrollView",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "scroll",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScroll</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
