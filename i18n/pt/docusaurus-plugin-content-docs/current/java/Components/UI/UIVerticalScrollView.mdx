# UIVerticalScrollView

Referência completa da API de `UIVerticalScrollView`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIVerticalScrollView extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| UIVerticalScrollView([UIVerticalScrollView]) | Executa `UIVerticalScrollView` e retorna quando a chamada do motor termina. |
| UIVerticalScrollView([Engine]) | Executa `UIVerticalScrollView` e retorna quando a chamada do motor termina. |
| UIVerticalScrollView() | Cria uma nova instância de UIVerticalScrollView. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [UIVerticalScrollView] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `scroll` | [int] | `get/set` | Atributo virtual exposto por `getScroll`. Acesse como um campo para ler ou escrever o valor subjacente. |
