import ApiDoc from '@site/src/components/ApiDoc';

# UIWeight

Referência completa da API de `UIWeight`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** UI

**Componente:** sim

---

```java
public class UIWeight extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "UIWeight(UIWeight)",
      description: <>Executa <code>UIWeight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIWeight(Engine)",
      description: <>Executa <code>UIWeight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "UIWeight()",
      description: <>Executa <code>UIWeight</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "UIWeight",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "weight",
      type: "float",
      access: "set",
      description: <>Atributo virtual exposto por <code>setWeight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getWeight()",
      returns: "float",
      description: <>Executa <code>getWeight</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
