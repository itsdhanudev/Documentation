import ApiDoc from '@site/src/components/ApiDoc';

# VoxelChunkSpawner

Referência completa da API de `VoxelChunkSpawner`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Voxels

**Componente:** sim

---

```java
public class VoxelChunkSpawner extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelChunkSpawner(VoxelChunkSpawner)",
      description: <>Executa <code>VoxelChunkSpawner</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VoxelChunkSpawner(Engine)",
      description: <>Executa <code>VoxelChunkSpawner</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VoxelChunkSpawner()",
      description: <>Cria uma nova instância de VoxelChunkSpawner.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "VoxelChunkSpawner",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "camera",
      type: "SpatialObject",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCamera</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "chunkBlueprint",
      type: "VoxelChunk",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getChunkBlueprint</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "onlyVisibleChunks",
      type: "boolean",
      access: "set",
      description: <>Atributo virtual exposto por <code>setOnlyVisibleChunks</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "renderChunks",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRenderChunks</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "updateDelay",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getUpdateDelay</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "visibleDot",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getVisibleDot</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "isOnlyVisibleChunks()",
      returns: "boolean",
      description: <>Retorna se only visible chunks é verdadeiro.</>
    }
  ]}
/>
