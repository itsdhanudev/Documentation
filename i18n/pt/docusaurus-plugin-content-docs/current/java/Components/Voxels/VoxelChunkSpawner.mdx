# VoxelChunkSpawner

Referência completa da API de `VoxelChunkSpawner`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Voxels

**Componente:** sim

---

```java
public class VoxelChunkSpawner extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| VoxelChunkSpawner([VoxelChunkSpawner]) | Executa `VoxelChunkSpawner` e retorna quando a chamada do motor termina. |
| VoxelChunkSpawner([Engine]) | Executa `VoxelChunkSpawner` e retorna quando a chamada do motor termina. |
| VoxelChunkSpawner() | Cria uma nova instância de VoxelChunkSpawner. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [VoxelChunkSpawner] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (6)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `camera` | [SpatialObject] | `get/set` | Atributo virtual exposto por `getCamera`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `chunkBlueprint` | [VoxelChunk] | `get/set` | Atributo virtual exposto por `getChunkBlueprint`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `onlyVisibleChunks` | [boolean] | `set` | Atributo virtual exposto por `setOnlyVisibleChunks`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `renderChunks` | [int] | `get/set` | Atributo virtual exposto por `getRenderChunks`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `updateDelay` | [float] | `get/set` | Atributo virtual exposto por `getUpdateDelay`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `visibleDot` | [float] | `get/set` | Atributo virtual exposto por `getVisibleDot`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `isOnlyVisibleChunks()` | `boolean` | Retorna se only visible chunks é verdadeiro. |
