import ApiDoc from '@site/src/components/ApiDoc';

# VoxelCollider

Referência completa da API de `VoxelCollider`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Voxels

**Componente:** sim

---

```java
public class VoxelCollider extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelCollider(VoxelCollider)",
      description: <>Executa <code>VoxelCollider</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VoxelCollider(Engine)",
      description: <>Executa <code>VoxelCollider</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VoxelCollider()",
      description: <>Cria uma nova instância de VoxelCollider.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "VoxelCollider",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "horizontalSize",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHorizontalSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "positionOffset",
      type: "Vector3",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPositionOffset</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "verticalSize",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getVerticalSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
