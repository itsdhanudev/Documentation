# VoxelCollider

Referência completa da API de `VoxelCollider`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Voxels

**Componente:** sim

---

```java
public class VoxelCollider extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| VoxelCollider([VoxelCollider]) | Executa `VoxelCollider` e retorna quando a chamada do motor termina. |
| VoxelCollider([Engine]) | Executa `VoxelCollider` e retorna quando a chamada do motor termina. |
| VoxelCollider() | Cria uma nova instância de VoxelCollider. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [VoxelCollider] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (3)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `horizontalSize` | [float] | `get/set` | Atributo virtual exposto por `getHorizontalSize`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `positionOffset` | [Vector3] | `get/set` | Atributo virtual exposto por `getPositionOffset`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `verticalSize` | [float] | `get/set` | Atributo virtual exposto por `getVerticalSize`. Acesse como um campo para ler ou escrever o valor subjacente. |
