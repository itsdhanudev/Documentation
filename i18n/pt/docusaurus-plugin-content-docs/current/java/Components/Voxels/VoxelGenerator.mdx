import ApiDoc from '@site/src/components/ApiDoc';

# VoxelGenerator

Referência completa da API de `VoxelGenerator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Voxels

**Componente:** sim

---

```java
public class VoxelGenerator extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelGenerator(VoxelGenerator)",
      description: <>Executa <code>VoxelGenerator</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VoxelGenerator()",
      description: <>Executa <code>VoxelGenerator</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "VoxelGenerator",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "setListener(Component)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>listener``.</>
    },
    {
      signature: "removeListener()",
      returns: "void",
      description: <>Remove listener da coleção.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
