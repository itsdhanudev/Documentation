# VoxelHand

Referência completa da API de `VoxelHand`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Voxels

**Componente:** sim

---

```java
public class VoxelHand extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| VoxelHand([VoxelHand]) | Executa `VoxelHand` e retorna quando a chamada do motor termina. |
| VoxelHand([Engine]) | Executa `VoxelHand` e retorna quando a chamada do motor termina. |
| VoxelHand() | Cria uma nova instância de VoxelHand. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `component` | [VoxelHand] | Representa o valor de `component` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (10)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `blockBroken` | [boolean] | `get` | Atributo virtual exposto por `isBlockBroken`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `blockPlaced` | [boolean] | `get` | Atributo virtual exposto por `isBlockPlaced`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `crossHairColor` | [Color] | `get` | Atributo virtual exposto por `getCrossHairColor`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `crossHairSize` | [Color] | `get/set` | Atributo virtual exposto por `getCrossHairSize`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `crossHairTexture` | [Texture] | `get/set` | Atributo virtual exposto por `getCrossHairTexture`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `distance` | [float] | `get/set` | Atributo virtual exposto por `getDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `lastHitBlockID` | [int] | `get` | Atributo virtual exposto por `getLastHitBlockID`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `listener` | [Listener] | `set` | Atributo virtual exposto por `setListener`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `selectedBlockID` | [int] | `get/set` | Atributo virtual exposto por `getSelectedBlockID`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `showCrosshair` | [boolean] | `get/set` | Atributo virtual exposto por `isShowCrosshair`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (4)

| Signature | Returns | Description |
| --- | --- | --- |
| `requestPut()` | `void` | Executa a operação requestPut. |
| `requestBreak()` | `void` | Executa a operação requestBreak. |
| `componentClassMatch(Component, Component)` | `boolean` | Executa `componentClassMatch` e retorna quando a chamada do motor termina. |
| `getEngineComponent()` | `Component` | Executa `getEngineComponent` e retorna quando a chamada do motor termina. |
