import ApiDoc from '@site/src/components/ApiDoc';

# VoxelHand

Referência completa da API de `VoxelHand`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Voxels

**Componente:** sim

---

```java
public class VoxelHand extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelHand(VoxelHand)",
      description: <>Executa <code>VoxelHand</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VoxelHand(Engine)",
      description: <>Executa <code>VoxelHand</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VoxelHand()",
      description: <>Cria uma nova instância de VoxelHand.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "VoxelHand",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "blockBroken",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isBlockBroken</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "blockPlaced",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isBlockPlaced</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "crossHairColor",
      type: "Color",
      access: "get",
      description: <>Atributo virtual exposto por <code>getCrossHairColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "crossHairSize",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCrossHairSize</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "crossHairTexture",
      type: "Texture",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getCrossHairTexture</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "distance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "lastHitBlockID",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getLastHitBlockID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "listener",
      type: "Listener",
      access: "set",
      description: <>Atributo virtual exposto por <code>setListener</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "selectedBlockID",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSelectedBlockID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "showCrosshair",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isShowCrosshair</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "requestPut()",
      returns: "void",
      description: <>Executa a operação requestPut.</>
    },
    {
      signature: "requestBreak()",
      returns: "void",
      description: <>Executa a operação requestBreak.</>
    },
    {
      signature: "componentClassMatch(Component, Component)",
      returns: "boolean",
      description: <>Executa <code>componentClassMatch</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getEngineComponent()",
      returns: "Component",
      description: <>Executa <code>getEngineComponent</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
