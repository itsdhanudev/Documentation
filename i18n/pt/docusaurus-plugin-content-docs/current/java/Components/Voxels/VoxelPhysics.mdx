import ApiDoc from '@site/src/components/ApiDoc';

# VoxelPhysics

Referência completa da API de `VoxelPhysics`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Voxels

**Componente:** sim

---

```java
public class VoxelPhysics extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelPhysics(VoxelPhysics)",
      description: <>Executa <code>VoxelPhysics</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VoxelPhysics(Engine)",
      description: <>Executa <code>VoxelPhysics</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VoxelPhysics()",
      description: <>Cria uma nova instância de VoxelPhysics.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "VoxelPhysics",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "climbSpeed",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getClimbSpeed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "gravity",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getGravity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxClimbHeight",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxClimbHeight</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxGravitySpeed",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxGravitySpeed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "velocity",
      type: "Vector3",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getVelocity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
