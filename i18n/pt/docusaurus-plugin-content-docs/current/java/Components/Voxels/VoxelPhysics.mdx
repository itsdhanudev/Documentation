# VoxelPhysics

Referência completa da API de `VoxelPhysics`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Voxels

**Componente:** sim

---

```java
public class VoxelPhysics extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| VoxelPhysics([VoxelPhysics]) | Executa `VoxelPhysics` e retorna quando a chamada do motor termina. |
| VoxelPhysics([Engine]) | Executa `VoxelPhysics` e retorna quando a chamada do motor termina. |
| VoxelPhysics() | Cria uma nova instância de VoxelPhysics. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [VoxelPhysics] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (5)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `climbSpeed` | [float] | `get/set` | Atributo virtual exposto por `getClimbSpeed`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `gravity` | [float] | `get/set` | Atributo virtual exposto por `getGravity`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `maxClimbHeight` | [float] | `get/set` | Atributo virtual exposto por `getMaxClimbHeight`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `maxGravitySpeed` | [float] | `get/set` | Atributo virtual exposto por `getMaxGravitySpeed`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `velocity` | [Vector3] | `get/set` | Atributo virtual exposto por `getVelocity`. Acesse como um campo para ler ou escrever o valor subjacente. |
