import ApiDoc from '@site/src/components/ApiDoc';

# VoxelPlayerController

Referência completa da API de `VoxelPlayerController`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Voxels

**Componente:** sim

---

```java
public class VoxelPlayerController extends Component
```

<ApiDoc
  constructors={[
    {
      signature: "VoxelPlayerController(VoxelPlayerController)",
      description: <>Executa <code>VoxelPlayerController</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VoxelPlayerController(Engine)",
      description: <>Executa <code>VoxelPlayerController</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "VoxelPlayerController()",
      description: <>Cria uma nova instância de VoxelPlayerController.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "VoxelPlayerController",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "horizontalSlideName",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHorizontalSlideName</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "horizontalSlideSens",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getHorizontalSlideSens</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "joystickName",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getJoystickName</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "runSpeed",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRunSpeed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "walkSpeed",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getWalkSpeed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
