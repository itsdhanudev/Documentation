# VoxelPlayerController

Referência completa da API de `VoxelPlayerController`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Voxels

**Componente:** sim

---

```java
public class VoxelPlayerController extends Component
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| VoxelPlayerController([VoxelPlayerController]) | Executa `VoxelPlayerController` e retorna quando a chamada do motor termina. |
| VoxelPlayerController([Engine]) | Executa `VoxelPlayerController` e retorna quando a chamada do motor termina. |
| VoxelPlayerController() | Cria uma nova instância de VoxelPlayerController. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [VoxelPlayerController] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (5)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `horizontalSlideName` | [String] | `get/set` | Atributo virtual exposto por `getHorizontalSlideName`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `horizontalSlideSens` | [float] | `get/set` | Atributo virtual exposto por `getHorizontalSlideSens`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `joystickName` | [String] | `get/set` | Atributo virtual exposto por `getJoystickName`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `runSpeed` | [float] | `get/set` | Atributo virtual exposto por `getRunSpeed`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `walkSpeed` | [float] | `get/set` | Atributo virtual exposto por `getWalkSpeed`. Acesse como um campo para ler ou escrever o valor subjacente. |
