# Gizmo

Referência completa da API de `Gizmo`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Debug

**Componente:** não

---

```java
public class Gizmo
```

## Métodos Estáticos (33)

| Signature | Returns | Description |
| --- | --- | --- |
| `drawPoint(Vector3, float)` | `void` | Desenha o objeto no contexto atual. |
| `drawPoint(Vector3, float, Color)` | `void` | Desenha o objeto no contexto atual. |
| `drawSphere(Vector3, float)` | `void` | Desenha o objeto no contexto atual. |
| `drawSphere(Vector3, float, Color)` | `void` | Desenha o objeto no contexto atual. |
| `drawSphere(Vector3, Vector3, Color)` | `void` | Desenha o objeto no contexto atual. |
| `drawQuad(Vector3, float, Texture)` | `void` | Desenha o objeto no contexto atual. |
| `drawQuad(Vector3, Vector3, Texture)` | `void` | Desenha o objeto no contexto atual. |
| `drawQuad(Vector3, Vector3, Texture, Color)` | `void` | Desenha o objeto no contexto atual. |
| `drawTransparentQuad(Vector3, Vector3, Texture)` | `void` | Executa `drawTransparentQuad` e retorna quando a chamada do motor termina. |
| `drawTransparentQuad(Vector3, Vector3, Texture, boolean)` | `void` | Executa `drawTransparentQuad` e retorna quando a chamada do motor termina. |
| `drawTransparentQuad(Vector3, Vector3, Texture, Color)` | `void` | Executa `drawTransparentQuad` e retorna quando a chamada do motor termina. |
| `drawTransparentQuad(Vector3, Vector3, Texture, Color, boolean)` | `void` | Executa `drawTransparentQuad` e retorna quando a chamada do motor termina. |
| `drawCube(Vector3, float)` | `void` | Desenha o objeto no contexto atual. |
| `drawCube(Vector3, float, Color)` | `void` | Desenha o objeto no contexto atual. |
| `drawCube(Vector3, Vector3)` | `void` | Desenha o objeto no contexto atual. |
| `drawCube(Vector3, Vector3, Color)` | `void` | Desenha o objeto no contexto atual. |
| `drawCube(Vector3, Vector3, Color, Texture)` | `void` | Executa `drawCube` e retorna quando a chamada do motor termina. |
| `drawCube(Vector3, float, Texture)` | `void` | Executa `drawCube` e retorna quando a chamada do motor termina. |
| `drawTransparentCube(Vector3, Vector3, Color)` | `void` | Executa `drawTransparentCube` e retorna quando a chamada do motor termina. |
| `drawVertex(Vertex, Vector3)` | `void` | Desenha o objeto no contexto atual. |
| `drawVertex(Vertex, Vector3, Color)` | `void` | Desenha o objeto no contexto atual. |
| `drawVertex(Vertex, Vector3, Quaternion)` | `void` | Desenha o objeto no contexto atual. |
| `drawVertex(Vertex, Vector3, Vector3)` | `void` | Desenha o objeto no contexto atual. |
| `drawVertex(Vertex, Vector3, Quaternion, Vector3)` | `void` | Desenha o objeto no contexto atual. |
| `drawVertex(Vertex, Vector3, Quaternion, Vector3, Color)` | `void` | Desenha o objeto no contexto atual. |
| `drawWireframeVertex(Vertex, Vector3, Quaternion, Vector3)` | `void` | Executa `drawWireframeVertex` e retorna quando a chamada do motor termina. |
| `drawWireframeVertex(Vertex, Vector3, Quaternion, Vector3, Color)` | `void` | Executa `drawWireframeVertex` e retorna quando a chamada do motor termina. |
| `drawWireframeVertex(Vertex, Vector3, Quaternion, Vector3, Color, Texture)` | `void` | Executa `drawWireframeVertex` e retorna quando a chamada do motor termina. |
| `drawVertex(GizmoElement)` | `void` | Desenha o objeto no contexto atual. |
| `drawGizmo(GizmoElement)` | `void` | Desenha o objeto no contexto atual. |
| `drawElement(GizmoElement)` | `void` | Desenha o objeto no contexto atual. |
| `draw(GizmoElement)` | `void` | Desenha o objeto no contexto atual. |
| `drawEngine(GizmoElement)` | `void` | Executa `drawEngine` e retorna quando a chamada do motor termina. |
