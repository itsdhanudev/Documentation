# GizmoElement

Referência completa da API de `GizmoElement`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Debug

**Componente:** não

---

```java
public class GizmoElement
```

## Métodos (31)

| Signature | Returns | Description |
| --- | --- | --- |
| `getUserPointer()` | `Object` | Retorna a propriedade `propriedade `user pointer atual``. |
| `setUserPointer(Object)` | `void` | Define ou atualiza a propriedade `propriedade `user pointer``. |
| `getVertex()` | `Vertex` | Retorna a propriedade `propriedade `vertex atual``. |
| `getColor()` | `Color` | Retorna a propriedade `propriedade `color atual``. |
| `getTexture()` | `Texture` | Retorna a propriedade `propriedade `texture atual``. |
| `getRenderMode()` | `RenderMode` | Retorna a propriedade `propriedade `render mode atual``. |
| `getMatrix()` | [float][] | Retorna a propriedade `propriedade `matrix atual``. |
| `getWireFrameWidth()` | `int` | Retorna a propriedade `propriedade `wire frame width atual``. |
| `getOutlineScale()` | `float` | Retorna a propriedade `propriedade `outline scale atual``. |
| `isDualFaceRender()` | `boolean` | Executa `isDualFaceRender` e retorna quando a chamada do motor termina. |
| `isEnableTransparency()` | `boolean` | Retorna se enable transparency é verdadeiro. |
| `setEnableTransparency(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `enable transparency``. |
| `isBillboard()` | `boolean` | Executa `isBillboard` e retorna quando a chamada do motor termina. |
| `setBillboard(boolean)` | `void` | Executa `setBillboard` e retorna quando a chamada do motor termina. |
| `isDrawInFront()` | `boolean` | Retorna se draw in front é verdadeiro. |
| `setDrawInFront(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `draw in front``. |
| `isAdditiveMode()` | `boolean` | Retorna se additive mode é verdadeiro. |
| `setAdditiveMode(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `additive mode``. |
| `isEnableLight()` | `boolean` | Retorna se enable light é verdadeiro. |
| `setEnableLight(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `enable light``. |
| `getMaxScaleBasedCameraSize()` | `float` | Retorna a propriedade `propriedade `max scale based camera size atual``. |
| `setMaxScaleBasedCameraSize(float)` | `void` | Define ou atualiza a propriedade `propriedade `max scale based camera size``. |
| `getLightIntensity()` | `float` | Retorna a propriedade `propriedade `light intensity atual``. |
| `setLightIntensity(float)` | `void` | Define ou atualiza a propriedade `propriedade `light intensity``. |
| `getVisibleDistance()` | `float` | Executa `getVisibleDistance` e retorna quando a chamada do motor termina. |
| `setVisibleDistance(float)` | `void` | Executa `setVisibleDistance` e retorna quando a chamada do motor termina. |
| `getFadeOutRange()` | `float` | Executa `getFadeOutRange` e retorna quando a chamada do motor termina. |
| `setFadeOutRange(float)` | `void` | Executa `setFadeOutRange` e retorna quando a chamada do motor termina. |
| `isPooling()` | `boolean` | Executa `isPooling` e retorna quando a chamada do motor termina. |
| `setPooling(boolean)` | `void` | Executa `setPooling` e retorna quando a chamada do motor termina. |
| `isScaleBasedCamera()` | `boolean` | Retorna se scale based camera é verdadeiro. |
