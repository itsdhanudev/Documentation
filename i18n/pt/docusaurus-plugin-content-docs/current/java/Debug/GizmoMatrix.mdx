import ApiDoc from '@site/src/components/ApiDoc';

# GizmoMatrix

Referência completa da API de `GizmoMatrix`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Debug

**Componente:** não

---

```java
public class GizmoMatrix extends GizmoElement
```

<ApiDoc
  constructors={[
    {
      signature: "GizmoMatrix()",
      description: <>Cria uma nova instância de GizmoMatrix.</>
    }
  ]}
  attributes={[
    {
      name: "matrix",
      type: "float[]",
      description: <><code>get/set</code></>
    }
  ]}
  virtualAttributes={[
    {
      name: "matrix",
      type: "float[]",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMatrix</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
