import ApiDoc from '@site/src/components/ApiDoc';

# Log

Referência completa da API de `Log`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Debug

**Componente:** não

---

```java
public class Log
```

<ApiDoc
  constructors={[
    {
      signature: "Log(ILog)",
      description: <>Executa <code>Log</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Log()",
      description: <>Executa <code>Log</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Log(String)",
      description: <>Cria uma nova instância de Log.</>
    },
    {
      signature: "Log(String, String)",
      description: <>Cria uma nova instância de Log.</>
    },
    {
      signature: "Log(String, String, String)",
      description: <>Cria uma nova instância de Log.</>
    },
    {
      signature: "Log(String, String, String, Color)",
      description: <>Cria uma nova instância de Log.</>
    }
  ]}
  attributes={[
    {
      name: "log",
      type: "ILog",
      description: <>Representa o valor de <code>log</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "link",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLink</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "message",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMessage</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "quantity",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getQuantity</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "tag",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTag</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "tittle",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTittle</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
