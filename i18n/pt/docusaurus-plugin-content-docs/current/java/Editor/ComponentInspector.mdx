import ApiDoc from '@site/src/components/ApiDoc';

# ComponentInspector

Referência completa da API de `ComponentInspector`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Editor

**Componente:** não

---

```java
public class ComponentInspector
```

<ApiDoc
  constructors={[
    {
      signature: "ComponentInspector()",
      description: <>Cria uma nova instância de ComponentInspector.</>
    }
  ]}
  attributes={[
    {
      name: "myComponent",
      type: "T",
      description: <>Representa o valor de <code>myComponent</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "updateUIVisible()",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>uivisible``.</>
    },
    {
      signature: "onAttach()",
      returns: "View",
      description: <>Executa a operação onAttach.</>
    }
  ]}
  staticMethods={[
    {
      signature: "findViewByID(View, String)",
      returns: "<T extends View> T",
      description: <>Executa a operação findViewByID usando os parâmetros informados (view, id).</>
    },
    {
      signature: "toView(Object)",
      returns: "View",
      description: <>Executa a operação toView usando os parâmetros informados (o).</>
    },
    {
      signature: "getIdOf(View)",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>id of atual``.</>
    }
  ]}
/>
