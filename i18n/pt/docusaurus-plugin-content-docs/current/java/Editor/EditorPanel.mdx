import ApiDoc from '@site/src/components/ApiDoc';

# EditorPanel

Referência completa da API de `EditorPanel`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Editor

**Componente:** não

---

```java
public class EditorPanel
```

<ApiDoc
  constructors={[
    {
      signature: "EditorPanel(EditorPanel)",
      description: <>Executa <code>EditorPanel</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "EditorPanel(Engine)",
      description: <>Executa <code>EditorPanel</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "EditorPanel()",
      description: <>Executa <code>EditorPanel</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "panel",
      type: "EditorPanel",
      description: <>Representa o valor de <code>panel</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "contentHeightPixels",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getContentHeightPixels</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "contentWidthPixels",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getContentWidthPixels</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "contentXPixels",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getContentXPixels</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "contentYPixels",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getContentYPixels</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "floating",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isFloating</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "requestCloseConfirmation",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isRequestCloseConfirmation</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "tittle",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getTittle</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "totalHeightPixels",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getTotalHeightPixels</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "totalWidthPixels",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getTotalWidthPixels</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "totalXPixels",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getTotalXPixels</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "totalYPixels",
      type: "int",
      access: "get",
      description: <>Atributo virtual exposto por <code>getTotalYPixels</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "visible",
      type: "boolean",
      access: "get",
      description: <>Atributo virtual exposto por <code>isVisible</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "setPanel(EditorPanel)",
      returns: "void",
      description: <>Executa <code>setPanel</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "isTouchInside(Touch)",
      returns: "boolean",
      description: <>Retorna se touch inside é verdadeiro.</>
    },
    {
      signature: "determineTouch(GUIUtils.TouchFilter)",
      returns: "Touch",
      description: <>Executa a operação determineTouch usando os parâmetros informados (touchFilter).</>
    },
    {
      signature: "determineTouch(GUIUtils.TouchFilter, int)",
      returns: "Touch",
      description: <>Executa a operação determineTouch usando os parâmetros informados (touchFilter, offset).</>
    },
    {
      signature: "determineTouchPosition(Touch)",
      returns: "Vector2",
      description: <>Executa a operação determineTouchPosition usando os parâmetros informados (touch).</>
    },
    {
      signature: "determineTouchPosition(Touch, Vector2)",
      returns: "Vector2",
      description: <>Executa a operação determineTouchPosition usando os parâmetros informados (touch, out).</>
    },
    {
      signature: "requestDetach()",
      returns: "void",
      description: <>Executa a operação requestDetach.</>
    },
    {
      signature: "blink()",
      returns: "void",
      description: <>Executa a operação blink.</>
    },
    {
      signature: "isAllowCloseX()",
      returns: "boolean",
      description: <>Retorna se allow close x é verdadeiro.</>
    },
    {
      signature: "setAllowCloseX(boolean)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>allow close x``.</>
    },
    {
      signature: "copy()",
      returns: "EditorPanel",
      description: <>Retorna uma cópia da instância de EditorPanel.</>
    }
  ]}
/>
