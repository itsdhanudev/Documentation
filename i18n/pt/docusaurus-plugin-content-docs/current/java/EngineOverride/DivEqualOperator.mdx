# DivEqualOperator

Referência completa da API de `DivEqualOperator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** EngineOverride

**Componente:** não

---

```java
public interface DivEqualOperator
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"otherObject"}])` | `void` | Divide o valor atual pelo objeto fornecido e atribui o resultado. |
| `divEqual(Object)` | `void` | Executa `divEqual` e retorna quando a chamada do motor termina. |
