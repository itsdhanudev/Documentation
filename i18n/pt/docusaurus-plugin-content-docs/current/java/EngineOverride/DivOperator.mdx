# DivOperator

Referência completa da API de `DivOperator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** EngineOverride

**Componente:** não

---

```java
public interface DivOperator
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"otherObject"}])` | `void` | Divide o valor atual pelo objeto fornecido. |
| `div(Object)` | `T` | Executa `div` e retorna quando a chamada do motor termina. |
