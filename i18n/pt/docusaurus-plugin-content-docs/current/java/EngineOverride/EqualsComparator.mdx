# EqualsComparator

Referência completa da API de `EqualsComparator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** EngineOverride

**Componente:** não

---

```java
public interface EqualsComparator
```

## Métodos (6)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"otherObject"}])` | `void` | Verifica se o objeto atual é igual ao objeto fornecido. |
| `equals(Object)` | `boolean` | Executa `equals` e retorna quando a chamada do motor termina. |
| `@MethodArgs([{"otherObject"}])` | `void` | Verifica se o objeto atual é a mesma instância do objeto fornecido. |
| `pointerEquals(Object)` | `boolean` | Executa `pointerEquals` e retorna quando a chamada do motor termina. |
| `@MethodArgs([{"otherObject"}])` | `void` | Verifica se o objeto atual não é igual ao objeto fornecido. |
| `notEquals(Object)` | `boolean` | Executa `notEquals` e retorna quando a chamada do motor termina. |
