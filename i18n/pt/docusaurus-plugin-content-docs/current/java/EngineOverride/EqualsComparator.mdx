import ApiDoc from '@site/src/components/ApiDoc';

# EqualsComparator

Referência completa da API de `EqualsComparator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** EngineOverride

**Componente:** não

---

```java
public interface EqualsComparator
```

<ApiDoc
  methods={[
    {
      signature: "equals(Object)",
      returns: "boolean",
      description: <>Verifica se o objeto atual é igual ao objeto fornecido.</>
    },
    {
      signature: "pointerEquals(Object)",
      returns: "boolean",
      description: <>Verifica se o objeto atual é a mesma instância do objeto fornecido.</>
    },
    {
      signature: "notEquals(Object)",
      returns: "boolean",
      description: <>Verifica se o objeto atual não é igual ao objeto fornecido.</>
    }
  ]}
/>
