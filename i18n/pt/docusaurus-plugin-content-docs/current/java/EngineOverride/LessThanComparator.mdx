# LessThanComparator

Referência completa da API de `LessThanComparator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** EngineOverride

**Componente:** não

---

```java
public interface LessThanComparator
```

## Métodos (20)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"otherObject"}])` | `void` | Verifica se o objeto atual é menor que o objeto fornecido. |
| `lessThan(Object)` | `boolean` | Executa `lessThan` e retorna quando a chamada do motor termina. |
| `@MethodArgs([{"value"}])` | `void` | Verifica se o objeto atual é menor que o valor float fornecido. |
| `lessThan(float)` | `boolean` | Executa `lessThan` e retorna quando a chamada do motor termina. |
| `@MethodArgs([{"value"}])` | `void` | Verifica se o objeto atual é menor que o valor inteiro fornecido. |
| `lessThan(int)` | `boolean` | Executa `lessThan` e retorna quando a chamada do motor termina. |
| `@MethodArgs([{"value"}])` | `void` | Verifica se o objeto atual é menor que o valor long fornecido. |
| `lessThan(long)` | `boolean` | Executa `lessThan` e retorna quando a chamada do motor termina. |
| `@MethodArgs([{"value"}])` | `void` | Verifica se o objeto atual é menor que o valor double fornecido. |
| `lessThan(double)` | `boolean` | Executa `lessThan` e retorna quando a chamada do motor termina. |
| `@MethodArgs([{"otherObject"}])` | `void` | Verifica se o objeto atual é menor ou igual ao objeto fornecido. |
| `lessOrEqualsThan(Object)` | `boolean` | Executa `lessOrEqualsThan` e retorna quando a chamada do motor termina. |
| `@MethodArgs([{"value"}])` | `void` | Verifica se o objeto atual é menor ou igual ao valor float fornecido. |
| `lessOrEqualsThan(float)` | `boolean` | Executa `lessOrEqualsThan` e retorna quando a chamada do motor termina. |
| `@MethodArgs([{"value"}])` | `void` | Verifica se o objeto atual é menor ou igual ao valor inteiro fornecido. |
| `lessOrEqualsThan(int)` | `boolean` | Executa `lessOrEqualsThan` e retorna quando a chamada do motor termina. |
| `@MethodArgs([{"value"}])` | `void` | Verifica se o objeto atual é menor ou igual ao valor long fornecido. |
| `lessOrEqualsThan(long)` | `boolean` | Executa `lessOrEqualsThan` e retorna quando a chamada do motor termina. |
| `@MethodArgs([{"value"}])` | `void` | Verifica se o objeto atual é menor ou igual ao valor double fornecido. |
| `lessOrEqualsThan(double)` | `boolean` | Executa `lessOrEqualsThan` e retorna quando a chamada do motor termina. |
