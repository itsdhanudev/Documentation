# ModEqualOperator

Referência completa da API de `ModEqualOperator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** EngineOverride

**Componente:** não

---

```java
public interface ModEqualOperator
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"otherObject"}])` | `void` | Aplica o módulo e atribui o resultado ao valor atual. |
| `modEqual(Object)` | `void` | Executa `modEqual` e retorna quando a chamada do motor termina. |
