# MulEqualOperator

Referência completa da API de `MulEqualOperator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** EngineOverride

**Componente:** não

---

```java
public interface MulEqualOperator
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"otherObject"}])` | `void` | Multiplica o valor atual pelo objeto fornecido e atribui o resultado. |
| `mulEqual(Object)` | `void` | Executa `mulEqual` e retorna quando a chamada do motor termina. |
