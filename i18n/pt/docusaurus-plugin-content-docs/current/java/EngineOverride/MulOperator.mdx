import ApiDoc from '@site/src/components/ApiDoc';

# MulOperator

Referência completa da API de `MulOperator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** EngineOverride

**Componente:** não

---

```java
public interface MulOperator
```

<ApiDoc
  methods={[
    {
      signature: "mul(Object)",
      returns: "T",
      description: <>Multiplica o valor atual pelo objeto fornecido.</>
    }
  ]}
/>
