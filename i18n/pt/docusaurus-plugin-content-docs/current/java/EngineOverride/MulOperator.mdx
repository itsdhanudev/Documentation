# MulOperator

Referência completa da API de `MulOperator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** EngineOverride

**Componente:** não

---

```java
public interface MulOperator
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"otherObject"}])` | `void` | Multiplica o valor atual pelo objeto fornecido. |
| `mul(Object)` | `T` | Executa `mul` e retorna quando a chamada do motor termina. |
