# SubOperator

Referência completa da API de `SubOperator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** EngineOverride

**Componente:** não

---

```java
public interface SubOperator
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"otherObject"}])` | `void` | Subtrai o objeto fornecido do valor atual. |
| `sub(Object)` | `T` | Executa `sub` e retorna quando a chamada do motor termina. |
