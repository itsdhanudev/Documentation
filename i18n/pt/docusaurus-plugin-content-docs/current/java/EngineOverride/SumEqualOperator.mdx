# SumEqualOperator

Referência completa da API de `SumEqualOperator`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** EngineOverride

**Componente:** não

---

```java
public interface SumEqualOperator
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"otherObject"}])` | `void` | Adiciona o objeto fornecido ao valor atual e atribui o resultado. |
| `sumEqual(Object)` | `void` | Executa `sumEqual` e retorna quando a chamada do motor termina. |
