import ApiDoc from '@site/src/components/ApiDoc';

# FileLoader

Referência completa da API de `FileLoader`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class FileLoader
```

<ApiDoc
  methods={[
    {
      signature: "exportTextToFile(String, String)",
      returns: "void",
      description: <>Executa <code>exportTextToFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "exportTextToFile(String, File)",
      returns: "void",
      description: <>Executa <code>exportTextToFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "loadTextFromFile(InputStream)",
      returns: "String",
      description: <>Executa <code>loadTextFromFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "loadTextFromFile(File)",
      returns: "String",
      description: <>Executa <code>loadTextFromFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "loadTextFromFile(ProjectFile)",
      returns: "String",
      description: <>Carrega text from file no objeto.</>
    }
  ]}
  staticMethods={[
    {
      signature: "exportTextToFile(String, String)",
      returns: "void",
      description: <>Executa <code>exportTextToFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "exportTextToFile(String, File)",
      returns: "void",
      description: <>Executa <code>exportTextToFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "loadTextFromFile(InputStream)",
      returns: "String",
      description: <>Executa <code>loadTextFromFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "loadTextFromFile(File)",
      returns: "String",
      description: <>Executa <code>loadTextFromFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "loadTextFromFile(ProjectFile)",
      returns: "String",
      description: <>Carrega text from file no objeto.</>
    }
  ]}
/>
