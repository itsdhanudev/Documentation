# FileUtils

Referência completa da API de `FileUtils`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class FileUtils
```

## Métodos Estáticos (9)

| Signature | Returns | Description |
| --- | --- | --- |
| `determineClassFolder(Class)` | `String` | Executa a operação determineClassFolder usando os parâmetros informados (classType). |
| `determineClassPath(Class)` | `String` | Executa a operação determineClassPath usando os parâmetros informados (classType). |
| `commonPath(List)` | `String` | Executa a operação commonPath usando os parâmetros informados (paths). |
| `getFileName(String)` | `String` | Retorna a propriedade `propriedade `file name atual``. |
| `getFileName(String, boolean)` | `String` | Retorna a propriedade `propriedade `file name atual``. |
| `getFileFolder(String)` | `String` | Retorna a propriedade `propriedade `file folder atual``. |
| `getExtensionName(String)` | `String` | Retorna a propriedade `propriedade `extension name atual``. |
| `removeExtension(String)` | `String` | Remove extension da coleção. |
| `getLastFolder(String)` | `String` | Retorna a propriedade `propriedade `last folder atual``. |
