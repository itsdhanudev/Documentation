import ApiDoc from '@site/src/components/ApiDoc';

# FileUtils

Referência completa da API de `FileUtils`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class FileUtils
```

<ApiDoc
  methods={[
    {
      signature: "determineClassFolder(Class)",
      returns: "String",
      description: <>Executa a operação determineClassFolder usando os parâmetros informados (classType).</>
    },
    {
      signature: "determineClassPath(Class)",
      returns: "String",
      description: <>Executa a operação determineClassPath usando os parâmetros informados (classType).</>
    },
    {
      signature: "commonPath(List)",
      returns: "String",
      description: <>Executa a operação commonPath usando os parâmetros informados (paths).</>
    },
    {
      signature: "getFileName(String)",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>file name atual``.</>
    },
    {
      signature: "getFileName(String, boolean)",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>file name atual``.</>
    },
    {
      signature: "getFileFolder(String)",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>file folder atual``.</>
    },
    {
      signature: "getExtensionName(String)",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>extension name atual``.</>
    },
    {
      signature: "removeExtension(String)",
      returns: "String",
      description: <>Remove extension da coleção.</>
    },
    {
      signature: "getLastFolder(String)",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>last folder atual``.</>
    }
  ]}
  staticMethods={[
    {
      signature: "determineClassFolder(Class)",
      returns: "String",
      description: <>Executa a operação determineClassFolder usando os parâmetros informados (classType).</>
    },
    {
      signature: "determineClassPath(Class)",
      returns: "String",
      description: <>Executa a operação determineClassPath usando os parâmetros informados (classType).</>
    },
    {
      signature: "commonPath(List)",
      returns: "String",
      description: <>Executa a operação commonPath usando os parâmetros informados (paths).</>
    },
    {
      signature: "getFileName(String)",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>file name atual``.</>
    },
    {
      signature: "getFileName(String, boolean)",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>file name atual``.</>
    },
    {
      signature: "getFileFolder(String)",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>file folder atual``.</>
    },
    {
      signature: "getExtensionName(String)",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>extension name atual``.</>
    },
    {
      signature: "removeExtension(String)",
      returns: "String",
      description: <>Remove extension da coleção.</>
    },
    {
      signature: "getLastFolder(String)",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>last folder atual``.</>
    }
  ]}
/>
