# FilesPanelCustomIcon

Referência completa da API de `FilesPanelCustomIcon`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class FilesPanelCustomIcon
```

## Construtores (1)

| Signature | Description |
| --- | --- |
| FilesPanelCustomIcon() | Cria uma nova instância de FilesPanelCustomIcon. |

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `getIconForFile(File)` | `File` | Retorna a propriedade `propriedade `icon for file atual``. |
| `applyIcon(File, ImageView)` | `boolean` | Executa a operação applyIcon usando os parâmetros informados (file, imageView). |
| `supportFile(File)` | `boolean` | Executa a operação supportFile usando os parâmetros informados (file). |
