import ApiDoc from '@site/src/components/ApiDoc';

# FilesPanelCustomIcon

Referência completa da API de `FilesPanelCustomIcon`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class FilesPanelCustomIcon
```

<ApiDoc
  constructors={[
    {
      signature: "FilesPanelCustomIcon()",
      description: <>Cria uma nova instância de FilesPanelCustomIcon.</>
    }
  ]}
  methods={[
    {
      signature: "getIconForFile(File)",
      returns: "File",
      description: <>Retorna a propriedade <code>propriedade </code>icon for file atual``.</>
    },
    {
      signature: "applyIcon(File, ImageView)",
      returns: "boolean",
      description: <>Executa a operação applyIcon usando os parâmetros informados (file, imageView).</>
    },
    {
      signature: "supportFile(File)",
      returns: "boolean",
      description: <>Executa a operação supportFile usando os parâmetros informados (file).</>
    }
  ]}
/>
