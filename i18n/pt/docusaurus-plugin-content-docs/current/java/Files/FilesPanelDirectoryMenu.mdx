import ApiDoc from '@site/src/components/ApiDoc';

# FilesPanelDirectoryMenu

Referência completa da API de `FilesPanelDirectoryMenu`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class FilesPanelDirectoryMenu
```

<ApiDoc
  constructors={[
    {
      signature: "FilesPanelDirectoryMenu(String)",
      description: <>Cria uma nova instância de FilesPanelDirectoryMenu.</>
    }
  ]}
  methods={[
    {
      signature: "getMenu()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>menu atual``.</>
    },
    {
      signature: "setMenu(String)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>menu``.</>
    },
    {
      signature: "onClick(File)",
      returns: "void",
      description: <>Executa a operação onClick usando os parâmetros informados (folder).</>
    },
    {
      signature: "onClick(File, View)",
      returns: "void",
      description: <>Executa a operação onClick usando os parâmetros informados (folder, view).</>
    }
  ]}
/>
