# FilesPanelFileMenu

Referência completa da API de `FilesPanelFileMenu`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class FilesPanelFileMenu
```

## Construtores (1)

| Signature | Description |
| --- | --- |
| FilesPanelFileMenu([String]) | Cria uma nova instância de FilesPanelFileMenu. |

## Métodos (5)

| Signature | Returns | Description |
| --- | --- | --- |
| `getMenu()` | `String` | Retorna a propriedade `propriedade `menu atual``. |
| `setMenu(String)` | `void` | Define ou atualiza a propriedade `propriedade `menu``. |
| `onClick(File)` | `void` | Executa a operação onClick usando os parâmetros informados (file). |
| `onClick(File, View)` | `void` | Executa a operação onClick usando os parâmetros informados (file, view). |
| `filterFormat(String)` | `boolean` | Executa a operação filterFormat usando os parâmetros informados (format). |
