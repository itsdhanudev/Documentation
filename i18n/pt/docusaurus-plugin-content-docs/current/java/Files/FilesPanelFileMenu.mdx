import ApiDoc from '@site/src/components/ApiDoc';

# FilesPanelFileMenu

Referência completa da API de `FilesPanelFileMenu`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class FilesPanelFileMenu
```

<ApiDoc
  constructors={[
    {
      signature: "FilesPanelFileMenu(String)",
      description: <>Cria uma nova instância de FilesPanelFileMenu.</>
    }
  ]}
  methods={[
    {
      signature: "getMenu()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>menu atual``.</>
    },
    {
      signature: "setMenu(String)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>menu``.</>
    },
    {
      signature: "onClick(File)",
      returns: "void",
      description: <>Executa a operação onClick usando os parâmetros informados (file).</>
    },
    {
      signature: "onClick(File, View)",
      returns: "void",
      description: <>Executa a operação onClick usando os parâmetros informados (file, view).</>
    },
    {
      signature: "filterFormat(String)",
      returns: "boolean",
      description: <>Executa a operação filterFormat usando os parâmetros informados (format).</>
    }
  ]}
/>
