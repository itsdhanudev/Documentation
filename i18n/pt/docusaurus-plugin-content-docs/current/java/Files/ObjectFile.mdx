import ApiDoc from '@site/src/components/ApiDoc';

# ObjectFile

Referência completa da API de `ObjectFile`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class ObjectFile
```

<ApiDoc
  constructors={[
    {
      signature: "ObjectFile(String)",
      description: <>Executa <code>ObjectFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ObjectFile()",
      description: <>Cria uma nova instância de ObjectFile.</>
    }
  ]}
  methods={[
    {
      signature: "instantiate()",
      returns: "GameObject",
      description: <>Executa <code>instantiate</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "setFilePath(String)",
      returns: "void",
      description: <>Executa <code>setFilePath</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "toString()",
      returns: "String",
      description: <>Retorna a propriedade <code>representação em string</code>.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executa <code>genericInterface</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
