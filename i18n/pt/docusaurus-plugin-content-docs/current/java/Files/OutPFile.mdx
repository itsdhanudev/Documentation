import ApiDoc from '@site/src/components/ApiDoc';

# OutPFile

Referência completa da API de `OutPFile`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class OutPFile
```

<ApiDoc
  constructors={[
    {
      signature: "OutPFile()",
      description: <>Cria uma nova instância de OutPFile.</>
    },
    {
      signature: "OutPFile(String)",
      description: <>Cria uma nova instância de OutPFile.</>
    }
  ]}
  methods={[
    {
      signature: "getFilePath()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>file path atual``.</>
    },
    {
      signature: "setFilePath(String)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>file path``.</>
    },
    {
      signature: "getAbsolutePath()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>absolute path atual``.</>
    },
    {
      signature: "toString()",
      returns: "String",
      description: <>Retorna a propriedade <code>representação em string</code>.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executa <code>genericInterface</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
