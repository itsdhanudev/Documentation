import ApiDoc from '@site/src/components/ApiDoc';

# OutputVideoFile

Referência completa da API de `OutputVideoFile`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class OutputVideoFile
```

<ApiDoc
  constructors={[
    {
      signature: "OutputVideoFile(String)",
      description: <>Executa <code>OutputVideoFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "OutputVideoFile()",
      description: <>Executa <code>OutputVideoFile</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  methods={[
    {
      signature: "toString()",
      returns: "String",
      description: <>Retorna a propriedade <code>representação em string</code>.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executa <code>genericInterface</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
