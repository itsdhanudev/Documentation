import ApiDoc from '@site/src/components/ApiDoc';

# ProjectFile

Referência completa da API de `ProjectFile`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class ProjectFile
```

<ApiDoc
  constructors={[
    {
      signature: "ProjectFile(String, String)",
      description: <>Executa <code>ProjectFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ProjectFile(String)",
      description: <>Cria uma nova instância de ProjectFile.</>
    }
  ]}
  methods={[
    {
      signature: "getFormat()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>format atual``.</>
    },
    {
      signature: "getInputStream()",
      returns: "InputStream",
      description: <>Retorna a propriedade <code>propriedade </code>input stream atual``.</>
    },
    {
      signature: "loadText()",
      returns: "String",
      description: <>Carrega text no objeto.</>
    },
    {
      signature: "toString()",
      returns: "String",
      description: <>Retorna a propriedade <code>representação em string</code>.</>
    },
    {
      signature: "copyTo(File)",
      returns: "boolean",
      description: <>Retorna uma cópia da instância de ProjectFile.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executa <code>genericInterface</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
