# ProjectFile

Referência completa da API de `ProjectFile`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Files

**Componente:** não

---

```java
public class ProjectFile
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| ProjectFile([String], [String]) | Executa `ProjectFile` e retorna quando a chamada do motor termina. |
| ProjectFile([String]) | Cria uma nova instância de ProjectFile. |

## Métodos (5)

| Signature | Returns | Description |
| --- | --- | --- |
| `getFormat()` | `String` | Retorna a propriedade `propriedade `format atual``. |
| `getInputStream()` | `InputStream` | Retorna a propriedade `propriedade `input stream atual``. |
| `loadText()` | `String` | Carrega text no objeto. |
| `toString()` | `String` | Retorna a propriedade `representação em string`. |
| `copyTo(File)` | `boolean` | Retorna uma cópia da instância de ProjectFile. |

## Métodos Estáticos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `genericInterface()` | `GenericClassInspectorEntry` | Executa `genericInterface` e retorna quando a chamada do motor termina. |
| `inspectorController()` | `ClassInspectorEntry` | Executa `inspectorController` e retorna quando a chamada do motor termina. |
