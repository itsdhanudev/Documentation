# Font

Referência completa da API de `Font`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Fonts

**Componente:** não

---

```java
public class Font
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| Font([Font]) | Executa `Font` e retorna quando a chamada do motor termina. |
| Font([Engine]) | Executa `Font` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `font` | [Font] | Representa o valor de `font` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `getResolution()` | `int` | Retorna a propriedade `resolução da fonte`. |
