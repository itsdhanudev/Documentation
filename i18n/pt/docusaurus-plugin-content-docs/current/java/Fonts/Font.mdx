import ApiDoc from '@site/src/components/ApiDoc';

# Font

Referência completa da API de `Font`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Fonts

**Componente:** não

---

```java
public class Font
```

<ApiDoc
  constructors={[
    {
      signature: "Font(Font)",
      description: <>Executa <code>Font</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Font(Engine)",
      description: <>Executa <code>Font</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "font",
      type: "Font",
      description: <>Representa o valor de <code>font</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "getResolution()",
      returns: "int",
      description: <>Retorna a propriedade <code>resolução da fonte</code>.</>
    }
  ]}
/>
