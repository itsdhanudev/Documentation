import ApiDoc from '@site/src/components/ApiDoc';

# FontFile

Referência completa da API de `FontFile`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Fonts

**Componente:** não

---

```java
public class FontFile
```

<ApiDoc
  constructors={[
    {
      signature: "FontFile(String)",
      description: <>Executa <code>FontFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "FontFile()",
      description: <>Executa <code>FontFile</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  methods={[
    {
      signature: "toString()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>nome da classe``.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executa <code>genericInterface</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
