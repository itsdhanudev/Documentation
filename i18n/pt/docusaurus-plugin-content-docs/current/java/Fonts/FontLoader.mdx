import ApiDoc from '@site/src/components/ApiDoc';

# FontLoader

Referência completa da API de `FontLoader`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Fonts

**Componente:** não

---

```java
public class FontLoader
```

<ApiDoc
  constructors={[
    {
      signature: "FontLoader(Font)",
      description: <>Executa <code>FontLoader</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "FontLoader(Engine)",
      description: <>Executa <code>FontLoader</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "font",
      type: "Font",
      description: <>Representa o valor de <code>font</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "load(int)",
      returns: "Font",
      description: <>Carrega data no objeto.</>
    },
    {
      signature: "load(FontFile, int)",
      returns: "Font",
      description: <>Carrega data no objeto.</>
    },
    {
      signature: "load(File, int)",
      returns: "Font",
      description: <>Carrega data no objeto.</>
    }
  ]}
  staticMethods={[
    {
      signature: "load(int)",
      returns: "Font",
      description: <>Carrega data no objeto.</>
    },
    {
      signature: "load(FontFile, int)",
      returns: "Font",
      description: <>Carrega data no objeto.</>
    },
    {
      signature: "load(File, int)",
      returns: "Font",
      description: <>Carrega data no objeto.</>
    }
  ]}
/>
