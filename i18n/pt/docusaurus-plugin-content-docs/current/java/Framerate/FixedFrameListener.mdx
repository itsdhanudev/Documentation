# FixedFrameListener

Referência completa da API de `FixedFrameListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Framerate

**Componente:** não

---

```java
public interface FixedFrameListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"deltaTime"}])` | `void` | Executa a operação @MethodArgs. |
| `repeat(float)` | `void` | Executa `repeat` e retorna quando a chamada do motor termina. |
