import ApiDoc from '@site/src/components/ApiDoc';

# FixedFrameListener

Referência completa da API de `FixedFrameListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Framerate

**Componente:** não

---

```java
public interface FixedFrameListener
```

<ApiDoc
  methods={[
    {
      signature: "repeat(float)",
      returns: "void",
      description: <>Executa <code>repeat</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
