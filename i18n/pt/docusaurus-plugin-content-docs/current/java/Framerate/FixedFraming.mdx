import ApiDoc from '@site/src/components/ApiDoc';

# FixedFraming

Referência completa da API de `FixedFraming`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Framerate

**Componente:** não

---

```java
public class FixedFraming
```

<ApiDoc
  constructors={[
    {
      signature: "FixedFraming(int, FixedFrameListener)",
      description: <>Executa <code>FixedFraming</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "fixedFrames",
      type: "int",
      description: <><code>get/set</code></>
    }
  ]}
  virtualAttributes={[
    {
      name: "fixedFrames",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFixedFrames</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "engineRepeat()",
      returns: "void",
      description: <>Executa a operação engineRepeat.</>
    },
    {
      signature: "setAllowTimeScale(boolean)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>allow time scale``.</>
    },
    {
      signature: "setDesyncLowFrameRate(boolean)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>desync low frame rate``.</>
    },
    {
      signature: "setDesyncHighFrameRate(boolean)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>desync high frame rate``.</>
    },
    {
      signature: "setFixedFrameListener(FixedFrameListener)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>fixed frame listener``.</>
    }
  ]}
/>
