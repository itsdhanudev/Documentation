import ApiDoc from '@site/src/components/ApiDoc';

# GUIUtils

Referência completa da API de `GUIUtils`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** GUI

**Componente:** não

---

```java
public class GUIUtils
```

<ApiDoc
  methods={[
    {
      signature: "applyFilter(Touch, TouchFilter)",
      returns: "boolean",
      description: <>Executa <code>applyFilter</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "isTouchInside(Touch, int, int, int, int)",
      returns: "boolean",
      description: <>Retorna se touch inside é verdadeiro.</>
    },
    {
      signature: "determineTouch(int, int, int, int)",
      returns: "Touch",
      description: <>Executa a operação determineTouch usando os parâmetros informados (x, y, width, height).</>
    },
    {
      signature: "determineTouch(int, int, int, int, TouchFilter)",
      returns: "Touch",
      description: <>Executa a operação determineTouch usando os parâmetros informados (x, y, width, height, touchFilter).</>
    },
    {
      signature: "determineTouches(int, int, int, int)",
      returns: "Touch[]",
      description: <>Executa a operação determineTouches usando os parâmetros informados (x, y, width, height).</>
    },
    {
      signature: "determineTouches(int, int, int, int, TouchFilter)",
      returns: "Touch[]",
      description: <>Executa a operação determineTouches usando os parâmetros informados (x, y, width, height, touchFilter).</>
    },
    {
      signature: "determineTouchList(int, int, int, int)",
      returns: "List&lt;Touch&gt;",
      description: <>Executa a operação determineTouchList usando os parâmetros informados (x, y, width, height).</>
    },
    {
      signature: "determineTouchList(int, int, int, int, TouchFilter)",
      returns: "List&lt;Touch&gt;",
      description: <>Executa a operação determineTouchList usando os parâmetros informados (x, y, width, height, touchFilter).</>
    }
  ]}
  staticMethods={[
    {
      signature: "applyFilter(Touch, TouchFilter)",
      returns: "boolean",
      description: <>Executa <code>applyFilter</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "isTouchInside(Touch, int, int, int, int)",
      returns: "boolean",
      description: <>Retorna se touch inside é verdadeiro.</>
    },
    {
      signature: "determineTouch(int, int, int, int)",
      returns: "Touch",
      description: <>Executa a operação determineTouch usando os parâmetros informados (x, y, width, height).</>
    },
    {
      signature: "determineTouch(int, int, int, int, TouchFilter)",
      returns: "Touch",
      description: <>Executa a operação determineTouch usando os parâmetros informados (x, y, width, height, touchFilter).</>
    },
    {
      signature: "determineTouches(int, int, int, int)",
      returns: "Touch[]",
      description: <>Executa a operação determineTouches usando os parâmetros informados (x, y, width, height).</>
    },
    {
      signature: "determineTouches(int, int, int, int, TouchFilter)",
      returns: "Touch[]",
      description: <>Executa a operação determineTouches usando os parâmetros informados (x, y, width, height, touchFilter).</>
    },
    {
      signature: "determineTouchList(int, int, int, int)",
      returns: "List&lt;Touch&gt;",
      description: <>Executa a operação determineTouchList usando os parâmetros informados (x, y, width, height).</>
    },
    {
      signature: "determineTouchList(int, int, int, int, TouchFilter)",
      returns: "List&lt;Touch&gt;",
      description: <>Executa a operação determineTouchList usando os parâmetros informados (x, y, width, height, touchFilter).</>
    }
  ]}
/>
