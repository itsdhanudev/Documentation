# GUIUtils

Referência completa da API de `GUIUtils`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** GUI

**Componente:** não

---

```java
public class GUIUtils
```

## Métodos Estáticos (8)

| Signature | Returns | Description |
| --- | --- | --- |
| `applyFilter(Touch, TouchFilter)` | `boolean` | Executa `applyFilter` e retorna quando a chamada do motor termina. |
| `isTouchInside(Touch, int, int, int, int)` | `boolean` | Retorna se touch inside é verdadeiro. |
| `determineTouch(int, int, int, int)` | `Touch` | Executa a operação determineTouch usando os parâmetros informados (x, y, width, height). |
| `determineTouch(int, int, int, int, TouchFilter)` | `Touch` | Executa a operação determineTouch usando os parâmetros informados (x, y, width, height, touchFilter). |
| `determineTouches(int, int, int, int)` | [Touch][] | Executa a operação determineTouches usando os parâmetros informados (x, y, width, height). |
| `determineTouches(int, int, int, int, TouchFilter)` | [Touch][] | Executa a operação determineTouches usando os parâmetros informados (x, y, width, height, touchFilter). |
| `determineTouchList(int, int, int, int)` | [List]&[lt];[Touch]&[gt]; | Executa a operação determineTouchList usando os parâmetros informados (x, y, width, height). |
| `determineTouchList(int, int, int, int, TouchFilter)` | [List]&[lt];[Touch]&[gt]; | Executa a operação determineTouchList usando os parâmetros informados (x, y, width, height, touchFilter). |
