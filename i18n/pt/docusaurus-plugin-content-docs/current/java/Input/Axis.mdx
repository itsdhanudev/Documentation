import ApiDoc from '@site/src/components/ApiDoc';

# Axis

Referência completa da API de `Axis`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Input

**Componente:** não

---

```java
public class Axis
```

<ApiDoc
  constructors={[
    {
      signature: "Axis(Axis)",
      description: <>Executa <code>Axis</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Axis(String)",
      description: <>Executa <code>Axis</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "axis",
      type: "Axis",
      description: <>Representa o valor de <code>axis</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "name",
      type: "String",
      access: "get",
      description: <>Retorna a propriedade <code>propriedade </code>nome do Axis``.</>
    },
    {
      name: "value",
      type: "Vector2",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>valor do Axis``.</>
    }
  ]}
/>
