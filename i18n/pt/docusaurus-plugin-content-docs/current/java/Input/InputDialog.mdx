# InputDialog

Referência completa da API de `InputDialog`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Input

**Componente:** não

---

```java
public class InputDialog
```

## Construtores (7)

| Signature | Description |
| --- | --- |
| InputDialog([String], [InputDialogListener]) | Cria uma nova instância de InputDialog. |
| InputDialog([String], [Type], [InputDialogListener]) | Cria uma nova instância de InputDialog. |
| InputDialog([String], [String], [InputDialogListener]) | Cria uma nova instância de InputDialog. |
| InputDialog([String], [String], [Type], [InputDialogListener]) | Cria uma nova instância de InputDialog. |
| InputDialog([String], [String], [String], [InputDialogListener]) | Cria uma nova instância de InputDialog. |
| InputDialog([String], [String], [String], [String], [InputDialogListener]) | Cria uma nova instância de InputDialog. |
| InputDialog([String], [String], [String], [String], [Type], [InputDialogListener]) | Cria uma nova instância de InputDialog. |
