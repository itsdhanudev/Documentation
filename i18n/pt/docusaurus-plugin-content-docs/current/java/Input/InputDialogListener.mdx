# InputDialogListener

Referência completa da API de `InputDialogListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Input

**Componente:** não

---

```java
public interface InputDialogListener
```

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"text"}])` | `void` | Executa a operação @MethodArgs. |
| `onFinish(String)` | `void` | Executa `onFinish` e retorna quando a chamada do motor termina. |
| `onCancel()` | `void` | Executa `onCancel` e retorna quando a chamada do motor termina. |
