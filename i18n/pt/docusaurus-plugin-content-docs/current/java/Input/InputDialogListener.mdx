import ApiDoc from '@site/src/components/ApiDoc';

# InputDialogListener

Referência completa da API de `InputDialogListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Input

**Componente:** não

---

```java
public interface InputDialogListener
```

<ApiDoc
  methods={[
    {
      signature: "onFinish(String)",
      returns: "void",
      description: <>Executa <code>onFinish</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "onCancel()",
      returns: "void",
      description: <>Executa <code>onCancel</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
