# Key

Referência completa da API de `Key`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Input

**Componente:** não

---

```java
public class Key
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| Key([Key]) | Executa `Key` e retorna quando a chamada do motor termina. |
| Key([String]) | Executa `Key` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `key` | [Key] | Representa o valor de `key` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (4)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `down` | [boolean] | `get` | Verifica se a Key acabou de ser pressionada. Retorna verdadeiro apenas no primeiro quadro em que ela foi pressionada. |
| `name` | [String] | `get` | Retorna a propriedade `propriedade `nome da Key``. |
| `pressed` | [boolean] | `get` | Verifica se a Key está sendo mantida pressionada. Retorna verdadeiro em todos os quadros enquanto ela permanecer pressionada. |
| `up` | [boolean] | `get` | Verifica se a Key acabou de ser liberada. Retorna verdadeiro apenas no primeiro quadro após ela ser liberada. |

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `requestPress(Object)` | `void` | Registra que a tecla está sendo mantida pressionada pela referência especificada. |
| `releasePress(Object)` | `void` | Cancela o registro da tecla pressionada pela referência especificada. |
