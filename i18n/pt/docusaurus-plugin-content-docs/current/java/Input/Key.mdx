import ApiDoc from '@site/src/components/ApiDoc';

# Key

Referência completa da API de `Key`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Input

**Componente:** não

---

```java
public class Key
```

<ApiDoc
  constructors={[
    {
      signature: "Key(Key)",
      description: <>Executa <code>Key</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Key(String)",
      description: <>Executa <code>Key</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "key",
      type: "Key",
      description: <>Representa o valor de <code>key</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "down",
      type: "boolean",
      access: "get",
      description: <>Verifica se a Key acabou de ser pressionada. Retorna verdadeiro apenas no primeiro quadro em que ela foi pressionada.</>
    },
    {
      name: "name",
      type: "String",
      access: "get",
      description: <>Retorna a propriedade <code>propriedade </code>nome da Key``.</>
    },
    {
      name: "pressed",
      type: "boolean",
      access: "get",
      description: <>Verifica se a Key está sendo mantida pressionada. Retorna verdadeiro em todos os quadros enquanto ela permanecer pressionada.</>
    },
    {
      name: "up",
      type: "boolean",
      access: "get",
      description: <>Verifica se a Key acabou de ser liberada. Retorna verdadeiro apenas no primeiro quadro após ela ser liberada.</>
    }
  ]}
  methods={[
    {
      signature: "requestPress(Object)",
      returns: "void",
      description: <>Registra que a tecla está sendo mantida pressionada pela referência especificada.</>
    },
    {
      signature: "releasePress(Object)",
      returns: "void",
      description: <>Cancela o registro da tecla pressionada pela referência especificada.</>
    }
  ]}
/>
