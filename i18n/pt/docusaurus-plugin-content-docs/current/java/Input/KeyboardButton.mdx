# KeyboardButton

Referência completa da API de `KeyboardButton`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Input

**Componente:** não

---

```java
public class KeyboardButton
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| KeyboardButton([KeyboardButton]) | Executa `KeyboardButton` e retorna quando a chamada do motor termina. |
| KeyboardButton() | Executa `KeyboardButton` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `gamePadButton` | [KeyboardButton] | Representa o valor de `gamePadButton` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (8)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `down` | [boolean] | `get/set` | Verifica se o botão acabou de ser pressionado. Retorna verdadeiro apenas no primeiro quadro em que ele foi pressionado. |
| `longDown` | [boolean] | `get/set` | Verifica se o botão acabou de ser pressionado por muito tempo. Retorna verdadeiro apenas no primeiro quadro em que ele foi pressionado por muito tempo. |
| `longPressed` | [boolean] | `get/set` | Verifica se o botão está sendo mantido pressionado por um longo período. Retorna verdadeiro em todos os quadros enquanto ele permanecer pressionado por muito tempo. |
| `name` | [String] | `get` | Retorna a propriedade `propriedade `nome do botão do teclado``. |
| `pressed` | [boolean] | `get/set` | Verifica se o botão está sendo mantido pressionado. Retorna verdadeiro em todos os quadros enquanto ele permanecer pressionado. |
| `printableName` | [String] | `get` | Retorna a propriedade `propriedade `nome imprimível do botão do teclado``. |
| `up` | [boolean] | `get/set` | Verifica se o botão acabou de ser liberado. Retorna verdadeiro apenas no primeiro quadro após ele ser liberado. |
| `upperCase` | [boolean] | `get` | Verifica se o botão está em maiúsculo. |
