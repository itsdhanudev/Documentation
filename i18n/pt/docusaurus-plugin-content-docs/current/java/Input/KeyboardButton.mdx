import ApiDoc from '@site/src/components/ApiDoc';

# KeyboardButton

Referência completa da API de `KeyboardButton`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Input

**Componente:** não

---

```java
public class KeyboardButton
```

<ApiDoc
  constructors={[
    {
      signature: "KeyboardButton(KeyboardButton)",
      description: <>Executa <code>KeyboardButton</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "KeyboardButton()",
      description: <>Executa <code>KeyboardButton</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "gamePadButton",
      type: "KeyboardButton",
      description: <>Representa o valor de <code>gamePadButton</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "down",
      type: "boolean",
      access: "get/set",
      description: <>Verifica se o botão acabou de ser pressionado. Retorna verdadeiro apenas no primeiro quadro em que ele foi pressionado.</>
    },
    {
      name: "longDown",
      type: "boolean",
      access: "get/set",
      description: <>Verifica se o botão acabou de ser pressionado por muito tempo. Retorna verdadeiro apenas no primeiro quadro em que ele foi pressionado por muito tempo.</>
    },
    {
      name: "longPressed",
      type: "boolean",
      access: "get/set",
      description: <>Verifica se o botão está sendo mantido pressionado por um longo período. Retorna verdadeiro em todos os quadros enquanto ele permanecer pressionado por muito tempo.</>
    },
    {
      name: "name",
      type: "String",
      access: "get",
      description: <>Retorna a propriedade <code>propriedade </code>nome do botão do teclado``.</>
    },
    {
      name: "pressed",
      type: "boolean",
      access: "get/set",
      description: <>Verifica se o botão está sendo mantido pressionado. Retorna verdadeiro em todos os quadros enquanto ele permanecer pressionado.</>
    },
    {
      name: "printableName",
      type: "String",
      access: "get",
      description: <>Retorna a propriedade <code>propriedade </code>nome imprimível do botão do teclado``.</>
    },
    {
      name: "up",
      type: "boolean",
      access: "get/set",
      description: <>Verifica se o botão acabou de ser liberado. Retorna verdadeiro apenas no primeiro quadro após ele ser liberado.</>
    },
    {
      name: "upperCase",
      type: "boolean",
      access: "get",
      description: <>Verifica se o botão está em maiúsculo.</>
    }
  ]}
/>
