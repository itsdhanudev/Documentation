# PopupDialog

Referência completa da API de `PopupDialog`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Input

**Componente:** não

---

```java
public class PopupDialog
```

## Construtores (4)

| Signature | Description |
| --- | --- |
| PopupDialog([SweetAlertDialog]) | Executa `PopupDialog` e retorna quando a chamada do motor termina. |
| PopupDialog([int]) | Executa `PopupDialog` e retorna quando a chamada do motor termina. |
| PopupDialog([String], [String]) | Cria uma nova instância de PopupDialog. |
| PopupDialog([int], [String], [String]) | Cria uma nova instância de PopupDialog. |

## Atributos (5)

| Name | Type | Description |
| --- | --- | --- |
| `SUCCESS` | [int] | Representa o valor de `SUCCESS` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `ERROR` | [int] | Representa o valor de `ERROR` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `ALERT` | [int] | Representa o valor de `ALERT` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `PROGRESS` | [int] | Representa o valor de `PROGRESS` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `dialog` | [SweetAlertDialog] | Representa o valor de `dialog` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (13)

| Signature | Returns | Description |
| --- | --- | --- |
| `setConfirmButton(String, PopupDialogListener)` | `void` | Define ou atualiza a propriedade `propriedade `confirm button``. |
| `setCancelButton(String, PopupDialogListener)` | `void` | Define ou atualiza a propriedade `propriedade `cancel button``. |
| `setMidButton(String, PopupDialogListener)` | `void` | Define ou atualiza a propriedade `propriedade `mid button``. |
| `setConfirmButton(String, Listener)` | `void` | Define ou atualiza a propriedade `propriedade `confirm button``. |
| `setCancelButton(String, Listener)` | `void` | Define ou atualiza a propriedade `propriedade `cancel button``. |
| `setMidButton(String, Listener)` | `void` | Define ou atualiza a propriedade `propriedade `mid button``. |
| `getTittle()` | `String` | Retorna a propriedade `propriedade `tittle atual``. |
| `setName(String)` | `void` | Define ou atualiza a propriedade `propriedade `name``. |
| `getMessage()` | `String` | Retorna a propriedade `propriedade `message atual``. |
| `setMessage(String)` | `void` | Define ou atualiza a propriedade `propriedade `message``. |
| `changeType(int)` | `void` | Executa a operação changeType usando os parâmetros informados (type). |
| `show()` | `void` | Executa a operação show. |
| `dismiss()` | `void` | Executa a operação dismiss. |
