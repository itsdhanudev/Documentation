import ApiDoc from '@site/src/components/ApiDoc';

# Toast

Referência completa da API de `Toast`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Input

**Componente:** não

---

```java
public class Toast
```

<ApiDoc
  attributes={[
    {
      name: "LENGTH_LONG",
      type: "int",
      description: <>Representa o valor de <code>LENGTH_LONG</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    },
    {
      name: "LENGTH_SHORT",
      type: "int",
      description: <>Representa o valor de <code>LENGTH_SHORT</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "showText(String, int)",
      returns: "void",
      description: <>Exibe uma mensagem toast na tela com o texto e duração informados (use Toast.LENGTH_SHORT ou Toast.LENGTH_LONG para o length).</>
    }
  ]}
  staticMethods={[
    {
      signature: "showText(String, int)",
      returns: "void",
      description: <>Exibe uma mensagem toast na tela com o texto e duração informados (use Toast.LENGTH_SHORT ou Toast.LENGTH_LONG para o length).</>
    }
  ]}
/>
