import ApiDoc from '@site/src/components/ApiDoc';

# PropertiesButton

Referência completa da API de `PropertiesButton`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Inspector

**Componente:** não

---

```java
public class PropertiesButton
```

<ApiDoc
  constructors={[
    {
      signature: "PropertiesButton(Engine)",
      description: <>Executa <code>PropertiesButton</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "PropertiesButton(PropertiesButtonListener)",
      description: <>Cria uma nova instância de PropertiesButton.</>
    }
  ]}
  attributes={[
    {
      name: "runOnEngineThread",
      type: "boolean",
      description: <>Representa o valor de <code>runOnEngineThread</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  staticMethods={[
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
