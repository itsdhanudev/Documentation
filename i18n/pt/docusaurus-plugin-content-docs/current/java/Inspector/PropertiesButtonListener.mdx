import ApiDoc from '@site/src/components/ApiDoc';

# PropertiesButtonListener

Referência completa da API de `PropertiesButtonListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Inspector

**Componente:** não

---

```java
public interface PropertiesButtonListener
```

<ApiDoc
  methods={[
    {
      signature: "onClicked()",
      returns: "void",
      description: <>Executa <code>onClicked</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
