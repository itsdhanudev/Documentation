import ApiDoc from '@site/src/components/ApiDoc';

# JClass

Referência completa da API de `JClass`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Java

**Componente:** não

---

```java
public class JClass
```

<ApiDoc
  constructors={[
    {
      signature: "JClass(Class, String, boolean)",
      description: <>Cria uma nova instância de JClass.</>
    }
  ]}
  attributes={[
    {
      name: "name",
      type: "String",
      description: <>Representa o valor de <code>name</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    },
    {
      name: "isEngineNative",
      type: "boolean",
      description: <>Representa o valor de <code>isEngineNative</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "getClassAddress()",
      returns: "Class",
      description: <>Retorna a propriedade <code>propriedade </code>class address atual``.</>
    },
    {
      signature: "getName()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>name atual``.</>
    },
    {
      signature: "isEngineNative()",
      returns: "boolean",
      description: <>Retorna se engine native é verdadeiro.</>
    }
  ]}
/>
