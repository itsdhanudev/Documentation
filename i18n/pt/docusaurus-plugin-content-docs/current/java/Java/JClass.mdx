# JClass

Referência completa da API de `JClass`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Java

**Componente:** não

---

```java
public class JClass
```

## Construtores (1)

| Signature | Description |
| --- | --- |
| JClass([Class], [String], [boolean]) | Cria uma nova instância de JClass. |

## Atributos (2)

| Name | Type | Description |
| --- | --- | --- |
| `name` | [String] | Representa o valor de `name` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `isEngineNative` | [boolean] | Representa o valor de `isEngineNative` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `getClassAddress()` | `Class` | Retorna a propriedade `propriedade `class address atual``. |
| `getName()` | `String` | Retorna a propriedade `propriedade `name atual``. |
| `isEngineNative()` | `boolean` | Retorna se engine native é verdadeiro. |
