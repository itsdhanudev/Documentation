# JCompiler

Referência completa da API de `JCompiler`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Java

**Componente:** não

---

```java
public class JCompiler
```

## Métodos Estáticos (12)

| Signature | Returns | Description |
| --- | --- | --- |
| `getAllClasses()` | [List]&[lt];[JClass]&[gt]; | Retorna a propriedade `propriedade `all classes atual``. |
| `getEngineClasses()` | [List]&[lt];[JClass]&[gt]; | Retorna a propriedade `ITsMagic classes atual`. |
| `engineClassCount()` | `int` | Executa a operação engineClassCount. |
| `engineClassAt(int)` | `JClass` | Executa a operação engineClassAt usando os parâmetros informados (idx). |
| `getUsersClasses()` | [List]&[lt];[JClass]&[gt]; | Retorna a propriedade `propriedade `users classes atual``. |
| `userClassCount()` | `int` | Executa a operação userClassCount. |
| `userClassAt(int)` | `JClass` | Executa a operação userClassAt usando os parâmetros informados (idx). |
| `findOfficialClass(String)` | `JClass` | Executa a operação findOfficialClass usando os parâmetros informados (fullName). |
| `findUserClass(String)` | `JClass` | Executa a operação findUserClass usando os parâmetros informados (fullName). |
| `isCompiling()` | `boolean` | Retorna se compiling é verdadeiro. |
| `addListener(Object)` | `void` | Adiciona listener à coleção. |
| `removeListener(Object)` | `void` | Remove listener da coleção. |
