import ApiDoc from '@site/src/components/ApiDoc';

# JavaUtils

Referência completa da API de `JavaUtils`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Java

**Componente:** não

---

```java
public class JavaUtils
```

<ApiDoc
  methods={[
    {
      signature: "isNull(Object)",
      returns: "boolean",
      description: <>Retorna se null é verdadeiro.</>
    }
  ]}
  staticMethods={[
    {
      signature: "isNull(Object)",
      returns: "boolean",
      description: <>Retorna se null é verdadeiro.</>
    }
  ]}
/>
