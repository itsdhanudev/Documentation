# LaserHit

Referência completa da API de `LaserHit`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Laser

**Componente:** não

---

```java
public class LaserHit
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| LaserHit([LaserHit]) | Executa `LaserHit` e retorna quando a chamada do motor termina. |
| LaserHit() | Executa `LaserHit` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `laserHit` | [LaserHit] | Representa o valor de `laserHit` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (8)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `collider` | [Collider] | `get/set` | Retorna a propriedade `propriedade `Collider que o Laser colidiu``. |
| `colliderObject` | [SpatialObject] | `get/set` | Retorna a propriedade `propriedade `objeto que o Laser colidiu``. |
| `distance` | [float] | `get/set` | Retorna a propriedade `distância entre a origem do Laser e o ponto que o Laser colidiu`. |
| `faceNormal` | [Vector3] | `get/set` | Retorna a propriedade `propriedade `vetor normal da face que o Laser colidiu``. |
| `normal` | [Vector3] | `get/set` | Retorna a propriedade `propriedade `vetor normal da direção do Laser``. |
| `object` | [SpatialObject] | `get/set` | Retorna a propriedade `propriedade `objeto que o Laser colidiu``. |
| `point` | [Vector3] | `get/set` | Retorna a propriedade `propriedade `ponto onde o Laser atingiu``. |
| `uVCoord` | [Vector2] | `get/set` | Retorna as coordenadas UV do ponto que o Laser colidiu. |
