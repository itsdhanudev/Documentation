import ApiDoc from '@site/src/components/ApiDoc';

# LaserHit

Referência completa da API de `LaserHit`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Laser

**Componente:** não

---

```java
public class LaserHit
```

<ApiDoc
  constructors={[
    {
      signature: "LaserHit(LaserHit)",
      description: <>Executa <code>LaserHit</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "LaserHit()",
      description: <>Executa <code>LaserHit</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "laserHit",
      type: "LaserHit",
      description: <>Representa o valor de <code>laserHit</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "collider",
      type: "Collider",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>Collider que o Laser colidiu``.</>
    },
    {
      name: "colliderObject",
      type: "SpatialObject",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>objeto que o Laser colidiu``.</>
    },
    {
      name: "distance",
      type: "float",
      access: "get/set",
      description: <>Retorna a propriedade <code>distância entre a origem do Laser e o ponto que o Laser colidiu</code>.</>
    },
    {
      name: "faceNormal",
      type: "Vector3",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>vetor normal da face que o Laser colidiu``.</>
    },
    {
      name: "normal",
      type: "Vector3",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>vetor normal da direção do Laser``.</>
    },
    {
      name: "object",
      type: "SpatialObject",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>objeto que o Laser colidiu``.</>
    },
    {
      name: "point",
      type: "Vector3",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>ponto onde o Laser atingiu``.</>
    },
    {
      name: "uVCoord",
      type: "Vector2",
      access: "get/set",
      description: <>Retorna as coordenadas UV do ponto que o Laser colidiu.</>
    }
  ]}
/>
