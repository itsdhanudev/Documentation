import ApiDoc from '@site/src/components/ApiDoc';

# Ray

Referência completa da API de `Ray`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Laser

**Componente:** não

---

```java
public class Ray
```

<ApiDoc
  constructors={[
    {
      signature: "Ray(Ray)",
      description: <>Executa <code>Ray</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Ray()",
      description: <>Executa <code>Ray</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Ray(JAVARuntime.RayDirection, float)",
      description: <>Cria uma nova instância de Ray.</>
    },
    {
      signature: "Ray(Vector3, Vector3, float)",
      description: <>Cria uma nova instância de Ray.</>
    }
  ]}
  attributes={[
    {
      name: "ray",
      type: "Ray",
      description: <>Representa o valor de <code>ray</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "physicsLayer",
      type: "PhysicsLayer",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>PhysicsLayer deste Ray``.</>
    }
  ]}
  methods={[
    {
      signature: "getRayDirection()",
      returns: "JAVARuntime.RayDirection",
      description: <>Retorna a propriedade <code>RayDirection deste Ray</code>.</>
    },
    {
      signature: "setRayDirection(JAVARuntime.RayDirection)",
      returns: "void",
      description: <>Define a propriedade <code>RayDirection deste Ray</code>.</>
    },
    {
      signature: "getDistance()",
      returns: "float",
      description: <>Retorna a propriedade <code>distância deste Ray</code>.</>
    },
    {
      signature: "setDistance(float)",
      returns: "void",
      description: <>Define a propriedade <code>distância deste Ray</code>.</>
    }
  ]}
/>
