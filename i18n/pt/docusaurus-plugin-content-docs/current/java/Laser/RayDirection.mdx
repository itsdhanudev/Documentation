import ApiDoc from '@site/src/components/ApiDoc';

# RayDirection

Referência completa da API de `RayDirection`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Laser

**Componente:** não

---

```java
public class RayDirection
```

<ApiDoc
  constructors={[
    {
      signature: "RayDirection(RayDirection)",
      description: <>Executa <code>RayDirection</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "RayDirection()",
      description: <>Executa <code>RayDirection</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "RayDirection(Vector3, Vector3)",
      description: <>Cria uma nova instância de RayDirection.</>
    }
  ]}
  attributes={[
    {
      name: "rayDirection",
      type: "RayDirection",
      description: <>Representa o valor de <code>rayDirection</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "getOrigin()",
      returns: "Vector3",
      description: <>Retorna a propriedade <code>posição de origem deste RayDirection</code>.</>
    },
    {
      signature: "setOrigin(Vector3)",
      returns: "void",
      description: <>Define a propriedade <code>posição de origem deste RayDirection</code>.</>
    },
    {
      signature: "getDirection()",
      returns: "Vector3",
      description: <>Retorna a propriedade <code>direção deste RayDirection</code>.</>
    },
    {
      signature: "setDirection(Vector3)",
      returns: "void",
      description: <>Define a propriedade <code>direção deste RayDirection</code>.</>
    }
  ]}
/>
