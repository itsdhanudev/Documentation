# RayDirection

Referência completa da API de `RayDirection`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Laser

**Componente:** não

---

```java
public class RayDirection
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| RayDirection([RayDirection]) | Executa `RayDirection` e retorna quando a chamada do motor termina. |
| RayDirection() | Executa `RayDirection` e retorna quando a chamada do motor termina. |
| RayDirection([Vector3], [Vector3]) | Cria uma nova instância de RayDirection. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `rayDirection` | [RayDirection] | Representa o valor de `rayDirection` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (4)

| Signature | Returns | Description |
| --- | --- | --- |
| `getOrigin()` | `Vector3` | Retorna a propriedade `posição de origem deste RayDirection`. |
| `setOrigin(Vector3)` | `void` | Define a propriedade `posição de origem deste RayDirection`. |
| `getDirection()` | `Vector3` | Retorna a propriedade `direção deste RayDirection`. |
| `setDirection(Vector3)` | `void` | Define a propriedade `direção deste RayDirection`. |
