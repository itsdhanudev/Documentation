import ApiDoc from '@site/src/components/ApiDoc';

# ListStream

Referência completa da API de `ListStream`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Lists

**Componente:** não

---

```java
public class ListStream
```

<ApiDoc
  constructors={[
    {
      signature: "ListStream(List)",
      description: <>Executa <code>ListStream</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ListStream(List, int, ListStreamListener)",
      description: <>Cria uma nova instância de ListStream.</>
    },
    {
      signature: "ListStream(Object, List, int, ListStreamListener)",
      description: <>Cria uma nova instância de ListStream.</>
    },
    {
      signature: "ListStream(List, int, int, ListStreamListener)",
      description: <>Cria uma nova instância de ListStream.</>
    },
    {
      signature: "ListStream(Object, List, int, int, ListStreamListener)",
      description: <>Cria uma nova instância de ListStream.</>
    }
  ]}
  attributes={[
    {
      name: "HALF_THREADS",
      type: "int",
      description: <>Representa o valor de <code>HALF_THREADS</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    },
    {
      name: "MAX_THREADS",
      type: "int",
      description: <>Representa o valor de <code>MAX_THREADS</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    },
    {
      name: "FIXED_THREADS",
      type: "int",
      description: <>Representa o valor de <code>FIXED_THREADS</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "setThreadsSequence(int)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>threads sequence``.</>
    },
    {
      signature: "setThreadsCount(int)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>threads count``.</>
    },
    {
      signature: "setListStreamListener(ListStreamListener)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>list stream listener``.</>
    }
  ]}
/>
