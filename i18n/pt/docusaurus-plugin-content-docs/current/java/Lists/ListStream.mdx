# ListStream

Referência completa da API de `ListStream`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Lists

**Componente:** não

---

```java
public class ListStream
```

## Construtores (5)

| Signature | Description |
| --- | --- |
| ListStream([List]) | Executa `ListStream` e retorna quando a chamada do motor termina. |
| ListStream([List], [int], [ListStreamListener]) | Cria uma nova instância de ListStream. |
| ListStream([Object], [List], [int], [ListStreamListener]) | Cria uma nova instância de ListStream. |
| ListStream([List], [int], [int], [ListStreamListener]) | Cria uma nova instância de ListStream. |
| ListStream([Object], [List], [int], [int], [ListStreamListener]) | Cria uma nova instância de ListStream. |

## Atributos (3)

| Name | Type | Description |
| --- | --- | --- |
| `HALF_THREADS` | [int] | Representa o valor de `HALF_THREADS` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `MAX_THREADS` | [int] | Representa o valor de `MAX_THREADS` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `FIXED_THREADS` | [int] | Representa o valor de `FIXED_THREADS` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `setThreadsSequence(int)` | `void` | Define ou atualiza a propriedade `propriedade `threads sequence``. |
| `setThreadsCount(int)` | `void` | Define ou atualiza a propriedade `propriedade `threads count``. |
| `setListStreamListener(ListStreamListener)` | `void` | Define ou atualiza a propriedade `propriedade `list stream listener``. |
