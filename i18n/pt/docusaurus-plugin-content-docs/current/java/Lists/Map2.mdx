# Map2

Referência completa da API de `Map2`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Lists

**Componente:** não

---

```java
public class Map2
```

## Construtores (1)

| Signature | Description |
| --- | --- |
| Map2([Class], [Class]) | Cria uma nova instância de Map2. |

## Métodos (13)

| Signature | Returns | Description |
| --- | --- | --- |
| `put(Object, Object)` | `void` | Define ou atualiza the value. |
| `get(Object)` | `Object` | Retorna a propriedade `propriedade `valor value atual``. |
| `replace(Object, Object)` | `void` | Executa a operação replace usando os parâmetros informados (key, newValue). |
| `remove(Object)` | `void` | Remove an item da coleção. |
| `getKeyClass()` | `Class` | Retorna a propriedade `propriedade `key class atual``. |
| `getValueClass()` | `Class` | Retorna a propriedade `propriedade `value class atual``. |
| `getKeyList()` | `List` | Retorna a propriedade `propriedade `key list atual``. |
| `getDirectKeyList()` | `List` | Retorna a propriedade `propriedade `direct key list atual``. |
| `getValueList()` | `List` | Retorna a propriedade `propriedade `value list atual``. |
| `getDirectValueList()` | `List` | Retorna a propriedade `propriedade `direct value list atual``. |
| `toJson()` | `String` | Executa a operação toJson. |
| `clear()` | `void` | Limpa ou redefine o estado do objeto. |
| `getSize()` | `int` | Retorna a propriedade `propriedade `size atual``. |

## Métodos Estáticos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `inspectorController()` | `ClassInspectorEntry` | Executa `inspectorController` e retorna quando a chamada do motor termina. |
| `fromJson(String)` | `Map2` | Executa a operação fromJson usando os parâmetros informados (json). |
