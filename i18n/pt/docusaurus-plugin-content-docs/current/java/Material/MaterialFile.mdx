import ApiDoc from '@site/src/components/ApiDoc';

# MaterialFile

Referência completa da API de `MaterialFile`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Material

**Componente:** não

---

```java
public class MaterialFile
```

<ApiDoc
  constructors={[
    {
      signature: "MaterialFile(String)",
      description: <>Executa <code>MaterialFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "MaterialFile()",
      description: <>Executa <code>MaterialFile</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "cache",
      type: "Material",
      description: <>Representa o valor de <code>cache</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "toString()",
      returns: "String",
      description: <>Retorna a propriedade <code>representação em string</code>.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executa <code>genericInterface</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
