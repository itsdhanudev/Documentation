# Texture

Referência completa da API de `Texture`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Material

**Componente:** não

---

```java
public class Texture
```

## Construtores (8)

| Signature | Description |
| --- | --- |
| Texture([TextureInstance]) | Executa `Texture` e retorna quando a chamada do motor termina. |
| Texture([Engine]) | Executa `Texture` e retorna quando a chamada do motor termina. |
| Texture([int], [int]) | Executa `Texture` e retorna quando a chamada do motor termina. |
| Texture([int], [int], [boolean]) | Cria uma nova instância de Texture. |
| Texture([int], [int], [boolean], [boolean]) | Executa `Texture` e retorna quando a chamada do motor termina. |
| Texture([int], [int], [Format]) | Cria uma nova instância de Texture. |
| Texture([int], [int], [boolean], [Format]) | Cria uma nova instância de Texture. |
| Texture([int], [int], [boolean], [boolean], [Format]) | Executa `Texture` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [TextureInstance] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (4)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `height` | [int] | `get` | Atributo virtual exposto por `getHeight`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `mipmapEnabled` | [boolean] | `get/set` | Atributo virtual exposto por `isMipmapEnabled`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `width` | [int] | `get` | Atributo virtual exposto por `getWidth`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `writable` | [boolean] | `get` | Atributo virtual exposto por `isWritable`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (35)

| Signature | Returns | Description |
| --- | --- | --- |
| `exportToFile(JAVARuntime.File)` | `void` | Executa a operação exportToFile usando os parâmetros informados (file). |
| `exportToFile(JAVARuntime.File, int)` | `void` | Executa a operação exportToFile usando os parâmetros informados (file, quality). |
| `exportToFile(JAVARuntime.File, int, ExportFormat)` | `void` | Executa `exportToFile` e retorna quando a chamada do motor termina. |
| `apply()` | `void` | Executa a operação apply. |
| `getPixel(int, int)` | `Color` | Retorna a propriedade `propriedade `pixel atual``. |
| `getPixel(Vector2)` | `Color` | Retorna a propriedade `propriedade `pixel atual``. |
| `setPixel(int, int, Color)` | `void` | Define ou atualiza a propriedade `propriedade `pixel``. |
| `setPixel(Vector2, Color)` | `void` | Define ou atualiza a propriedade `propriedade `pixel``. |
| `get(int, int)` | `Color` | Retorna a propriedade `propriedade `valor value atual``. |
| `get(int, int, Color)` | `Color` | Retorna a propriedade `propriedade `valor value atual``. |
| `getFloatRed(int, int)` | `float` | Retorna a propriedade `propriedade `float red atual``. |
| `getFloatGreen(int, int)` | `float` | Retorna a propriedade `propriedade `float green atual``. |
| `getFloatBlue(int, int)` | `float` | Retorna a propriedade `propriedade `float blue atual``. |
| `getFloatAlpha(int, int)` | `float` | Retorna a propriedade `propriedade `float alpha atual``. |
| `getIntRed(int, int)` | `int` | Retorna a propriedade `propriedade `int red atual``. |
| `getIntGreen(int, int)` | `int` | Retorna a propriedade `propriedade `int green atual``. |
| `getIntBlue(int, int)` | `int` | Retorna a propriedade `propriedade `int blue atual``. |
| `getIntAlpha(int, int)` | `int` | Retorna a propriedade `propriedade `int alpha atual``. |
| `set(int, int, Color)` | `void` | Define ou atualiza the value. |
| `set(int, int, float, float, float, float)` | `void` | Define ou atualiza the value. |
| `set(int, int, float, float, float)` | `void` | Define ou atualiza the value. |
| `setR(int, int, float)` | `void` | Define ou atualiza a propriedade `propriedade `r``. |
| `setG(int, int, float)` | `void` | Define ou atualiza a propriedade `propriedade `g``. |
| `setB(int, int, float)` | `void` | Define ou atualiza a propriedade `propriedade `b``. |
| `setA(int, int, float)` | `void` | Define ou atualiza a propriedade `propriedade `a``. |
| `set(int, int, int, int, int, int)` | `void` | Define ou atualiza the value. |
| `set(int, int, int, int, int)` | `void` | Define ou atualiza the value. |
| `setR(int, int, int)` | `void` | Define ou atualiza a propriedade `propriedade `r``. |
| `setG(int, int, int)` | `void` | Define ou atualiza a propriedade `propriedade `g``. |
| `setB(int, int, int)` | `void` | Define ou atualiza a propriedade `propriedade `b``. |
| `setA(int, int, int)` | `void` | Define ou atualiza a propriedade `propriedade `a``. |
| `copyPixels(Texture)` | `void` | Retorna uma cópia da instância de Texture. |
| `extractGPUImageToBtm(ExtractCallback)` | `void` | Executa `extractGPUImageToBtm` e retorna quando a chamada do motor termina. |
| `saveGPUImageToFile(File)` | `void` | Executa `saveGPUImageToFile` e retorna quando a chamada do motor termina. |
| `saveGPUImageToFile(File, Runnable)` | `void` | Executa `saveGPUImageToFile` e retorna quando a chamada do motor termina. |

## Métodos Estáticos (12)

| Signature | Returns | Description |
| --- | --- | --- |
| `genericInterface()` | `GenericClassInspectorEntry` | Executa `genericInterface` e retorna quando a chamada do motor termina. |
| `inspectorController()` | `ClassInspectorEntry` | Executa `inspectorController` e retorna quando a chamada do motor termina. |
| `loadFile(TextureFile)` | `Texture` | Carrega file no objeto. |
| `loadFile(JAVARuntime.File)` | `Texture` | Carrega file no objeto. |
| `loadInputStream(InputStream)` | `Texture` | Carrega input stream no objeto. |
| `loadURL(String)` | `Texture` | Executa `loadURL` e retorna quando a chamada do motor termina. |
| `newDepth(int, int)` | `Texture` | Cria um novo depth. |
| `white()` | `Texture` | Executa a operação white. |
| `empty()` | `Texture` | Executa a operação empty. |
| `black()` | `Texture` | Executa a operação black. |
| `isRenderable(Texture)` | `boolean` | Retorna se renderable é verdadeiro. |
| `isRenderable(TextureInstance)` | `boolean` | Retorna se renderable é verdadeiro. |
