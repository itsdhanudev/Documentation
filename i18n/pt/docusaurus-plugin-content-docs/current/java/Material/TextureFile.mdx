import ApiDoc from '@site/src/components/ApiDoc';

# TextureFile

Referência completa da API de `TextureFile`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Material

**Componente:** não

---

```java
public class TextureFile
```

<ApiDoc
  constructors={[
    {
      signature: "TextureFile(String)",
      description: <>Executa <code>TextureFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "TextureFile()",
      description: <>Executa <code>TextureFile</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  methods={[
    {
      signature: "toString()",
      returns: "String",
      description: <>Retorna a propriedade <code>representação em string</code>.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executa <code>genericInterface</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
