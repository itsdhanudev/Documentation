# Easings

Referência completa da API de `Easings`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Math

**Componente:** não

---

```java
public class Easings
```

## Métodos Estáticos (30)

| Signature | Returns | Description |
| --- | --- | --- |
| `easeInSine(float)` | `float` | Executa a operação easeInSine usando os parâmetros informados (x). |
| `easeOutSine(float)` | `float` | Executa a operação easeOutSine usando os parâmetros informados (x). |
| `easeInOutSine(float)` | `float` | Executa a operação easeInOutSine usando os parâmetros informados (x). |
| `easeInQuad(float)` | `float` | Executa a operação easeInQuad usando os parâmetros informados (x). |
| `easeOutQuad(float)` | `float` | Executa a operação easeOutQuad usando os parâmetros informados (x). |
| `easeInOutQuad(float)` | `float` | Executa a operação easeInOutQuad usando os parâmetros informados (x). |
| `easeInCubic(float)` | `float` | Executa a operação easeInCubic usando os parâmetros informados (x). |
| `easeOutCubic(float)` | `float` | Executa a operação easeOutCubic usando os parâmetros informados (x). |
| `easeInOutCubic(float)` | `float` | Executa a operação easeInOutCubic usando os parâmetros informados (x). |
| `easeInQuart(float)` | `float` | Executa a operação easeInQuart usando os parâmetros informados (x). |
| `easeOutQuart(float)` | `float` | Executa a operação easeOutQuart usando os parâmetros informados (x). |
| `easeInOutQuart(float)` | `float` | Executa a operação easeInOutQuart usando os parâmetros informados (x). |
| `easeInQuint(float)` | `float` | Executa a operação easeInQuint usando os parâmetros informados (x). |
| `easeOutQuint(float)` | `float` | Executa a operação easeOutQuint usando os parâmetros informados (x). |
| `easeInOutQuint(float)` | `float` | Executa a operação easeInOutQuint usando os parâmetros informados (x). |
| `easeInExpo(float)` | `float` | Executa a operação easeInExpo usando os parâmetros informados (x). |
| `easeOutExpo(float)` | `float` | Executa a operação easeOutExpo usando os parâmetros informados (x). |
| `easeInOutExpo(float)` | `float` | Executa a operação easeInOutExpo usando os parâmetros informados (x). |
| `easeInCirc(float)` | `float` | Executa a operação easeInCirc usando os parâmetros informados (x). |
| `easeOutCirc(float)` | `float` | Executa a operação easeOutCirc usando os parâmetros informados (x). |
| `easeInOutCirc(float)` | `float` | Executa a operação easeInOutCirc usando os parâmetros informados (x). |
| `easeInBack(float)` | `float` | Executa a operação easeInBack usando os parâmetros informados (x). |
| `easeOutBack(float)` | `float` | Executa a operação easeOutBack usando os parâmetros informados (x). |
| `easeInOutBack(float)` | `float` | Executa a operação easeInOutBack usando os parâmetros informados (x). |
| `easeInElastic(float)` | `float` | Executa a operação easeInElastic usando os parâmetros informados (x). |
| `easeOutElastic(float)` | `float` | Executa a operação easeOutElastic usando os parâmetros informados (x). |
| `easeInOutElastic(float)` | `float` | Executa a operação easeInOutElastic usando os parâmetros informados (x). |
| `easeInBounce(float)` | `float` | Executa a operação easeInBounce usando os parâmetros informados (x). |
| `easeOutBounce(float)` | `float` | Executa a operação easeOutBounce usando os parâmetros informados (x). |
| `easeInOutBounce(float)` | `float` | Executa a operação easeInOutBounce usando os parâmetros informados (x). |
