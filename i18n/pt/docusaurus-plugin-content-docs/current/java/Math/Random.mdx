import ApiDoc from '@site/src/components/ApiDoc';

# Random

Referência completa da API de `Random`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Math

**Componente:** não

---

```java
public class Random
```

<ApiDoc
  methods={[
    {
      signature: "range(int, int)",
      returns: "int",
      description: <>Retorna um valor inteiro aleatório entre o valor min e max.</>
    },
    {
      signature: "range(float, float)",
      returns: "float",
      description: <>Retorna um valor float aleatório entre o valor min e max.</>
    }
  ]}
  staticMethods={[
    {
      signature: "range(int, int)",
      returns: "int",
      description: <>Retorna um valor inteiro aleatório entre o valor min e max.</>
    },
    {
      signature: "range(float, float)",
      returns: "float",
      description: <>Retorna um valor float aleatório entre o valor min e max.</>
    }
  ]}
/>
