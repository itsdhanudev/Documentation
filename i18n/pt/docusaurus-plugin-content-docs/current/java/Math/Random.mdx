# Random

Referência completa da API de `Random`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Math

**Componente:** não

---

```java
public class Random
```

## Métodos Estáticos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `range(int, int)` | `int` | Retorna um valor inteiro aleatório entre o valor min e max. |
| `range(float, float)` | `float` | Retorna um valor float aleatório entre o valor min e max. |
