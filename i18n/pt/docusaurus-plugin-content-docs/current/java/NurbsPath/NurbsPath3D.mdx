# NurbsPath3D

Referência completa da API de `NurbsPath3D`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** NurbsPath

**Componente:** não

---

```java
public class NurbsPath3D
```

## Construtores (5)

| Signature | Description |
| --- | --- |
| NurbsPath3D([NurbsPath3D]) | Executa `NurbsPath3D` e retorna quando a chamada do motor termina. |
| NurbsPath3D() | Executa `NurbsPath3D` e retorna quando a chamada do motor termina. |
| NurbsPath3D([[List]&lt;Vector3&gt;]) | Cria uma nova instância de NurbsPath3D. |
| NurbsPath3D([boolean]) | Cria uma nova instância de NurbsPath3D. |
| NurbsPath3D([[List]&lt;Vector3&gt;], [boolean]) | Cria uma nova instância de NurbsPath3D. |

## Métodos (12)

| Signature | Returns | Description |
| --- | --- | --- |
| `evaluate(float)` | `Vector3` | Executa a operação evaluate usando os parâmetros informados (u). |
| `evaluate(float, Vector3)` | `Vector3` | Executa a operação evaluate usando os parâmetros informados (u, out). |
| `evaluatePath(int)` | [List]&[lt];[Vector3]&[gt]; | Executa a operação evaluatePath usando os parâmetros informados (resolution). |
| `evaluatePath(int, [List&lt;Vector3&gt;])` | [List]&[lt];[Vector3]&[gt]; | Executa a operação evaluatePath usando os parâmetros informados (resolution, list). |
| `addPoint(Vector3)` | `void` | Adiciona point à coleção. |
| `addPath([List&lt;Vector3&gt;])` | `void` | Adiciona path à coleção. |
| `getPoint(int)` | `Vector3` | Retorna a propriedade `propriedade `point atual``. |
| `removePoint(Vector3)` | `void` | Remove point da coleção. |
| `removePoint(int)` | `void` | Remove point da coleção. |
| `pointsCount()` | `int` | Executa a operação pointsCount. |
| `isContinuous()` | `boolean` | Retorna se continuous é verdadeiro. |
| `setContinuous(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `continuous``. |
