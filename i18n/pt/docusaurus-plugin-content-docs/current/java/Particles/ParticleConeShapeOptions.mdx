import ApiDoc from '@site/src/components/ApiDoc';

# ParticleConeShapeOptions

Referência completa da API de `ParticleConeShapeOptions`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Particles

**Componente:** não

---

```java
public class ParticleConeShapeOptions
```

<ApiDoc
  constructors={[
    {
      signature: "ParticleConeShapeOptions(ParticleConeShapeOptions)",
      description: <>Executa <code>ParticleConeShapeOptions</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ParticleConeShapeOptions(Engine)",
      description: <>Executa <code>ParticleConeShapeOptions</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ParticleConeShapeOptions()",
      description: <>Cria uma nova instância de ParticleConeShapeOptions.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ParticleConeShapeOptions",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "maxAngle",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxAngle</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "minAngle",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMinAngle</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "radius",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRadius</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
