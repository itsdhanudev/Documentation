import ApiDoc from '@site/src/components/ApiDoc';

# ParticleSphereShapeOptions

Referência completa da API de `ParticleSphereShapeOptions`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Particles

**Componente:** não

---

```java
public class ParticleSphereShapeOptions
```

<ApiDoc
  constructors={[
    {
      signature: "ParticleSphereShapeOptions(ParticleSphereShapeOptions)",
      description: <>Executa <code>ParticleSphereShapeOptions</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ParticleSphereShapeOptions(Engine)",
      description: <>Executa <code>ParticleSphereShapeOptions</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ParticleSphereShapeOptions()",
      description: <>Cria uma nova instância de ParticleSphereShapeOptions.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "ParticleSphereShapeOptions",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "radius",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRadius</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "randomizeRotation",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRandomizeRotation</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "spherizeRotation",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSpherizeRotation</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "volume",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getVolume</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
