# ParticleSphereShapeOptions

Referência completa da API de `ParticleSphereShapeOptions`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Particles

**Componente:** não

---

```java
public class ParticleSphereShapeOptions
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| ParticleSphereShapeOptions([ParticleSphereShapeOptions]) | Executa `ParticleSphereShapeOptions` e retorna quando a chamada do motor termina. |
| ParticleSphereShapeOptions([Engine]) | Executa `ParticleSphereShapeOptions` e retorna quando a chamada do motor termina. |
| ParticleSphereShapeOptions() | Cria uma nova instância de ParticleSphereShapeOptions. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [ParticleSphereShapeOptions] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (4)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `radius` | [float] | `get/set` | Atributo virtual exposto por `getRadius`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `randomizeRotation` | [float] | `get/set` | Atributo virtual exposto por `getRandomizeRotation`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `spherizeRotation` | [float] | `get/set` | Atributo virtual exposto por `getSpherizeRotation`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `volume` | [float] | `get/set` | Atributo virtual exposto por `getVolume`. Acesse como um campo para ler ou escrever o valor subjacente. |
