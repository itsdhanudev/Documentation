import ApiDoc from '@site/src/components/ApiDoc';

# AreaTrigger

Referência completa da API de `AreaTrigger`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Physics

**Componente:** não

---

```java
public class AreaTrigger extends PhysicsComponent
```

<ApiDoc
  constructors={[
    {
      signature: "AreaTrigger(AreaTrigger)",
      description: <>Executa <code>AreaTrigger</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "AreaTrigger()",
      description: <>Executa <code>AreaTrigger</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "component",
      type: "AreaTrigger",
      description: <>Representa o valor de <code>component</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
/>
