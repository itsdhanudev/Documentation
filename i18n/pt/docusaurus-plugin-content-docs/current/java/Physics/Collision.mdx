import ApiDoc from '@site/src/components/ApiDoc';

# Collision

Referência completa da API de `Collision`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Physics

**Componente:** não

---

```java
public class Collision
```

<ApiDoc
  constructors={[
    {
      signature: "Collision(Collision)",
      description: <>Executa <code>Collision</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Collision()",
      description: <>Executa <code>Collision</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Collision",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "collider",
      type: "Collider",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>Collider associado a esta colisão``.</>
    },
    {
      name: "colliderObject",
      type: "SpatialObject",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>objeto associado ao Collider nesta colisão``.</>
    },
    {
      name: "contactCount",
      type: "int",
      access: "get",
      description: <>Retorna a propriedade <code>propriedade </code>número de pontos de contato na colisão``.</>
    },
    {
      name: "contactPoint",
      type: "Vector3",
      access: "get/set",
      description: <>Retorna a propriedade <code>posição do primeiro ponto de contato (obsoleto, pegue um Contact com contactAt() ou getContactList() e use getContactPoint() nele)</code>.</>
    },
    {
      name: "distance",
      type: "float",
      access: "get/set",
      description: <>Retorna a propriedade <code>distância de penetração no primeiro ponto de contato (obsoleto, pegue um Contact com contactAt() ou getContactList() e use getDistance() nele)</code>.</>
    },
    {
      name: "impulse",
      type: "float",
      access: "get/set",
      description: <>Retorna a propriedade <code>magnitude do impulso no primeiro ponto de contato (obsoleto, pegue um Contact com contactAt() ou getContactList() e use getImpulse() nele)</code>.</>
    },
    {
      name: "normal",
      type: "Vector3",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>vetor normal do primeiro ponto de contato (obsoleto, pegue um Contact com contactAt() ou getContactList() e use getNormal() nele)``.</>
    },
    {
      name: "object",
      type: "SpatialObject",
      access: "get/set",
      description: <>Retorna a propriedade <code>propriedade </code>outro objeto envolvido na colisão``.</>
    },
    {
      name: "physicsComponent",
      type: "PhysicsComponent",
      access: "get/set",
      description: <>Retorna a propriedade <code>física do outro objeto envolvido na colisão</code>.</>
    }
  ]}
  methods={[
    {
      signature: "contactCount()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>número de pontos de contato na colisão``.</>
    },
    {
      signature: "contactAt(int)",
      returns: "Contact",
      description: <>Retorna a propriedade <code>propriedade </code>contato no índice especificado``.</>
    },
    {
      signature: "getContactList()",
      returns: "List&lt;Contact&gt;",
      description: <>Retorna a propriedade <code>lista de pontos de contato na colisão</code>.</>
    }
  ]}
/>
