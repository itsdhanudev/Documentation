# PhysicsSettings

Referência completa da API de `PhysicsSettings`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Physics

**Componente:** não

---

```java
public class PhysicsSettings
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| PhysicsSettings([PhysicsSettings]) | Executa `PhysicsSettings` e retorna quando a chamada do motor termina. |
| PhysicsSettings() | Executa `PhysicsSettings` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `physicsSettings` | [PhysicsSettings] | Representa o valor de `physicsSettings` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (1)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `gravity` | [Vector3] | `get/set` | Retorna a propriedade `gravidade do mundo`. |

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `setGravity(float, float, float)` | `void` | Define a propriedade `gravidade do mundo com os valores x, y e z especificados`. |
