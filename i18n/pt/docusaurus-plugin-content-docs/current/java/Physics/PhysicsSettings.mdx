import ApiDoc from '@site/src/components/ApiDoc';

# PhysicsSettings

Referência completa da API de `PhysicsSettings`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Physics

**Componente:** não

---

```java
public class PhysicsSettings
```

<ApiDoc
  constructors={[
    {
      signature: "PhysicsSettings(PhysicsSettings)",
      description: <>Executa <code>PhysicsSettings</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "PhysicsSettings()",
      description: <>Executa <code>PhysicsSettings</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "physicsSettings",
      type: "PhysicsSettings",
      description: <>Representa o valor de <code>physicsSettings</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "gravity",
      type: "Vector3",
      access: "get/set",
      description: <>Retorna a propriedade <code>gravidade do mundo</code>.</>
    }
  ]}
  methods={[
    {
      signature: "setGravity(float, float, float)",
      returns: "void",
      description: <>Define a propriedade <code>gravidade do mundo com os valores x, y e z especificados</code>.</>
    }
  ]}
/>
