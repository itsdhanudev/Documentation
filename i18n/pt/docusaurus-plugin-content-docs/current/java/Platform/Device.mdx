# Device

Referência completa da API de `Device`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Platform

**Componente:** não

---

```java
public class Device
```

## Métodos Estáticos (10)

| Signature | Returns | Description |
| --- | --- | --- |
| `getBatteryPercentage()` | `int` | Retorna a propriedade `porcentagem de bateria do dispositivo`. |
| `availableProcessors()` | `int` | Retorna a propriedade `propriedade `número de processadores disponíveis no dispositivo``. |
| `openURL(String)` | `void` | Abre a URL especificada no navegador padrão. |
| `openLink(String)` | `void` | Abre o link especificada no navegador padrão. |
| `vibrateOneShot(long)` | `void` | Faz o dispositivo vibrar pela duração especificada (em milissegundos). |
| `vibratePattern(long[], int)` | `void` | Faz o dispositivo vibrar seguindo as durações de tempo (em milissegundos) definidas no array, alternando entre vibração e pausa (iniciando com pausa). Após percorrer todo o array, a repetição começa no índice especificado (-1 para não repetir). |
| `stopVibrate()` | `void` | Para qualquer vibração em andamento no dispositivo. |
| `setMouseVisible(boolean)` | `void` | Define a propriedade `visibilidade do cursor do mouse no dispositivo`. |
| `setClipboard(String)` | `void` | Copia o texto fornecido para a área de transferência do dispositivo. |
| `getClipboard()` | `String` | Retorna a propriedade `propriedade `texto da área de transferência do dispositivo``. |
