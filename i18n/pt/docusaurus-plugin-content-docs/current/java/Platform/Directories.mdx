# Directories

Referência completa da API de `Directories`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Platform

**Componente:** não

---

```java
public class Directories
```

## Métodos Estáticos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `internal()` | `String` | Executa a operação internal. |
| `getProjectFolder()` | `String` | Retorna a propriedade `propriedade `project folder atual``. |
