import ApiDoc from '@site/src/components/ApiDoc';

# Directories

Referência completa da API de `Directories`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Platform

**Componente:** não

---

```java
public class Directories
```

<ApiDoc
  methods={[
    {
      signature: "internal()",
      returns: "String",
      description: <>Executa a operação internal.</>
    },
    {
      signature: "getProjectFolder()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>project folder atual``.</>
    }
  ]}
  staticMethods={[
    {
      signature: "internal()",
      returns: "String",
      description: <>Executa a operação internal.</>
    },
    {
      signature: "getProjectFolder()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>project folder atual``.</>
    }
  ]}
/>
