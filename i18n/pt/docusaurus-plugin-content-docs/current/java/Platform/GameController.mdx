# GameController

Referência completa da API de `GameController`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Platform

**Componente:** não

---

```java
public class GameController
```

## Atributos Virtuais (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `enableFrameLimit` | [boolean] | `get/set` | Verifica se o limite de frames está ativado. |
| `limitFrames` | [int] | `get/set` | Retorna a propriedade `propriedade `número máximo de frames por segundo permitido``. |

## Métodos Estáticos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `quit()` | `void` | Encerra o jogo e fecha a aplicação (Somente no APK). |
| `getState()` | `State` | Retorna a propriedade `propriedade `estado atual do jogo (Parado, Pausado ou Rodando)``. |
