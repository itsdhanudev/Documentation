import ApiDoc from '@site/src/components/ApiDoc';

# InspectorMemory

Referência completa da API de `InspectorMemory`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Platform

**Componente:** não

---

```java
public class InspectorMemory
```

<ApiDoc
  methods={[
    {
      signature: "HashMap&lt;&gt;()",
      returns: "Map&lt;String, String&gt; dataBase = new",
      description: <>Executa a operação HashMap&lt;&gt;.</>
    },
    {
      signature: "store(String, String)",
      returns: "void",
      description: <>Executa <code>store</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "load(String)",
      returns: "String",
      description: <>Executa <code>load</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "clear()",
      returns: "void",
      description: <>Executa <code>clear</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
