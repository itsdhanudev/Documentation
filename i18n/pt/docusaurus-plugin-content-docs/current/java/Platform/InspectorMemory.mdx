# InspectorMemory

Referência completa da API de `InspectorMemory`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Platform

**Componente:** não

---

```java
public class InspectorMemory
```

## Métodos (4)

| Signature | Returns | Description |
| --- | --- | --- |
| HashMap&lt;&gt;() | [Map]&[lt];[String], [String]&[gt]; [dataBase] = [new] | Executa a operação HashMap&lt;&gt;. |
| `store(String, String)` | `void` | Executa `store` e retorna quando a chamada do motor termina. |
| `load(String)` | `String` | Executa `load` e retorna quando a chamada do motor termina. |
| `clear()` | `void` | Executa `clear` e retorna quando a chamada do motor termina. |
