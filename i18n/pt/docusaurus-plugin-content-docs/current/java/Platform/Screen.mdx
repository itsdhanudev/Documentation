import ApiDoc from '@site/src/components/ApiDoc';

# Screen

Referência completa da API de `Screen`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Platform

**Componente:** não

---

```java
public class Screen
```

<ApiDoc
  methods={[
    {
      signature: "getWidth()",
      returns: "int",
      description: <>Retorna a propriedade <code>largura da tela em pixels</code>.</>
    },
    {
      signature: "getHeight()",
      returns: "int",
      description: <>Retorna a propriedade <code>altura da tela em pixels</code>.</>
    },
    {
      signature: "width()",
      returns: "int",
      description: <>Retorna a propriedade <code>largura da tela em pixels</code>.</>
    },
    {
      signature: "height()",
      returns: "int",
      description: <>Retorna a propriedade <code>altura da tela em pixels</code>.</>
    },
    {
      signature: "ratio()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>aspecto da tela (largura dividida pela altura)``.</>
    }
  ]}
  staticMethods={[
    {
      signature: "getWidth()",
      returns: "int",
      description: <>Retorna a propriedade <code>largura da tela em pixels</code>.</>
    },
    {
      signature: "getHeight()",
      returns: "int",
      description: <>Retorna a propriedade <code>altura da tela em pixels</code>.</>
    },
    {
      signature: "width()",
      returns: "int",
      description: <>Retorna a propriedade <code>largura da tela em pixels</code>.</>
    },
    {
      signature: "height()",
      returns: "int",
      description: <>Retorna a propriedade <code>altura da tela em pixels</code>.</>
    },
    {
      signature: "ratio()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>aspecto da tela (largura dividida pela altura)``.</>
    }
  ]}
/>
