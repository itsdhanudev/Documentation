import ApiDoc from '@site/src/components/ApiDoc';

# TagEnemyFieldOfView

Referência completa da API de `TagEnemyFieldOfView`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Prototyping

**Componente:** não

---

```java
public class TagEnemyFieldOfView
```

<ApiDoc
  constructors={[
    {
      signature: "TagEnemyFieldOfView(TagEnemyFieldOfView)",
      description: <>Executa <code>TagEnemyFieldOfView</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "TagEnemyFieldOfView(Engine)",
      description: <>Executa <code>TagEnemyFieldOfView</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "TagEnemyFieldOfView()",
      description: <>Cria uma nova instância de TagEnemyFieldOfView.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "TagEnemyFieldOfView",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "active",
      type: "boolean",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>isActive</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "fov",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getFov</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "maxDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMaxDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "minDistance",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMinDistance</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "targetTag",
      type: "String",
      access: "get",
      description: <>Atributo virtual exposto por <code>getTargetTag</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "targetsTag",
      type: "String",
      access: "set",
      description: <>Atributo virtual exposto por <code>setTargetsTag</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
