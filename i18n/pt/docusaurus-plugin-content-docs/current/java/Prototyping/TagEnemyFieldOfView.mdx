# TagEnemyFieldOfView

Referência completa da API de `TagEnemyFieldOfView`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Prototyping

**Componente:** não

---

```java
public class TagEnemyFieldOfView
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| TagEnemyFieldOfView([TagEnemyFieldOfView]) | Executa `TagEnemyFieldOfView` e retorna quando a chamada do motor termina. |
| TagEnemyFieldOfView([Engine]) | Executa `TagEnemyFieldOfView` e retorna quando a chamada do motor termina. |
| TagEnemyFieldOfView() | Cria uma nova instância de TagEnemyFieldOfView. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [TagEnemyFieldOfView] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (6)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `active` | [boolean] | `get/set` | Atributo virtual exposto por `isActive`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `fov` | [float] | `get/set` | Atributo virtual exposto por `getFov`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `maxDistance` | [float] | `get/set` | Atributo virtual exposto por `getMaxDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `minDistance` | [float] | `get/set` | Atributo virtual exposto por `getMinDistance`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `targetTag` | [String] | `get` | Atributo virtual exposto por `getTargetTag`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `targetsTag` | [String] | `set` | Atributo virtual exposto por `setTargetsTag`. Acesse como um campo para ler ou escrever o valor subjacente. |
