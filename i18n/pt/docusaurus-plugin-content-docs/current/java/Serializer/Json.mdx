# Json

Referência completa da API de `Json`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Serializer

**Componente:** não

---

```java
public class Json
```

## Construtores (1)

| Signature | Description |
| --- | --- |
| Json() | Cria uma nova instância de Json. |

## Atributos Virtuais (5)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `ignorePrivateFields` | [boolean] | `get/set` | Atributo virtual exposto por `isIgnorePrivateFields`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ignoreProtectedFields` | [boolean] | `get/set` | Atributo virtual exposto por `isIgnoreProtectedFields`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `ignorePublicFields` | [boolean] | `get/set` | Atributo virtual exposto por `isIgnorePublicFields`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `lenient` | [boolean] | `get/set` | Atributo virtual exposto por `isLenient`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `prettyPrinting` | [boolean] | `get/set` | Atributo virtual exposto por `isPrettyPrinting`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `toJsonString(Object)` | `String` | Retorna a propriedade `representação em string`. |
| `fromJsonString(String, Class)` | `Object` | Executa a operação fromJsonString usando os parâmetros informados (json, classType). |

## Métodos Estáticos (8)

| Signature | Returns | Description |
| --- | --- | --- |
| `toJson(Object)` | `String` | Executa a operação toJson usando os parâmetros informados (object). |
| `toJson(Object, boolean)` | `String` | Executa a operação toJson usando os parâmetros informados (object, prettyPrinting). |
| `toJson(Object, boolean, boolean)` | `String` | Executa a operação toJson usando os parâmetros informados (object, prettyPrinting, lenient). |
| `toJson(Object, boolean, boolean, boolean)` | `String` | Executa a operação toJson usando os parâmetros informados (object, prettyPrinting, lenient, ignorePrivateFields). |
| `toJson(Object, boolean, boolean, boolean, boolean)` | `String` | Executa a operação toJson usando os parâmetros informados (object, prettyPrinting, lenient, ignorePrivateFields, ignoreProtectedFields). |
| `toJson(Object, boolean, boolean, boolean, boolean, boolean)` | `String` | Executa a operação toJson usando os parâmetros informados (object, prettyPrinting, lenient, ignorePrivateFields, ignoreProtectedFields, ignorePublicFields). |
| `fromJson(String, Class)` | `Object` | Executa a operação fromJson usando os parâmetros informados (json, classType). |
| `fromJson(String, Class, boolean)` | `Object` | Executa a operação fromJson usando os parâmetros informados (json, classType, lenient). |
