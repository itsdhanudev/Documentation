import ApiDoc from '@site/src/components/ApiDoc';

# BassBoostEffect

Referência completa da API de `BassBoostEffect`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** SoundEffects

**Componente:** não

---

```java
public class BassBoostEffect extends AudioEffect
```

<ApiDoc
  constructors={[
    {
      signature: "BassBoostEffect(BassBoost)",
      description: <>Executa <code>BassBoostEffect</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "BassBoostEffect()",
      description: <>Executa <code>BassBoostEffect</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "effect",
      type: "BassBoost",
      description: <>Representa o valor de <code>effect</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "strength",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getStrength</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
