import ApiDoc from '@site/src/components/ApiDoc';

# EqualizerEffect

Referência completa da API de `EqualizerEffect`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** SoundEffects

**Componente:** não

---

```java
public class EqualizerEffect extends AudioEffect
```

<ApiDoc
  constructors={[
    {
      signature: "EqualizerEffect(Equalizer)",
      description: <>Executa <code>EqualizerEffect</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "EqualizerEffect()",
      description: <>Executa <code>EqualizerEffect</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "effect",
      type: "Equalizer",
      description: <>Representa o valor de <code>effect</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "getBandGain(int)",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>band gain atual``.</>
    },
    {
      signature: "setBandGain(int, float)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>band gain``.</>
    }
  ]}
/>
