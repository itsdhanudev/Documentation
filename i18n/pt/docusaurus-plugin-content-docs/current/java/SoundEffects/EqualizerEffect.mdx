# EqualizerEffect

Referência completa da API de `EqualizerEffect`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** SoundEffects

**Componente:** não

---

```java
public class EqualizerEffect extends AudioEffect
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| EqualizerEffect([Equalizer]) | Executa `EqualizerEffect` e retorna quando a chamada do motor termina. |
| EqualizerEffect() | Executa `EqualizerEffect` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `effect` | [Equalizer] | Representa o valor de `effect` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `getBandGain(int)` | `float` | Retorna a propriedade `propriedade `band gain atual``. |
| `setBandGain(int, float)` | `void` | Define ou atualiza a propriedade `propriedade `band gain``. |
