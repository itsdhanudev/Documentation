# Runnable

Referência completa da API de `Runnable`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Thread

**Componente:** não

---

```java
public interface Runnable
```

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `run()` | `void` | Executa `run` e retorna quando a chamada do motor termina. |
