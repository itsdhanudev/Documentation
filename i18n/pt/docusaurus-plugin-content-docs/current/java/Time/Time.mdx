import ApiDoc from '@site/src/components/ApiDoc';

# Time

Referência completa da API de `Time`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Time

**Componente:** não

---

```java
public class Time
```

<ApiDoc
  attributes={[
    {
      name: "deltaTime",
      type: "float",
      description: <><code>get</code></>
    },
    {
      name: "frameTime",
      type: "float",
      description: <><code>get</code></>
    },
    {
      name: "timeScale",
      type: "float",
      description: <><code>get/set</code></>
    },
    {
      name: "timeSpeed",
      type: "float",
      description: <><code>get/set</code></>
    },
    {
      name: "unscaledDeltaTime",
      type: "float",
      description: <><code>get</code></>
    },
    {
      name: "unscaledFrameTime",
      type: "float",
      description: <><code>get</code></>
    }
  ]}
  virtualAttributes={[
    {
      name: "deltaTime",
      type: "float",
      access: "get",
      description: <>Retorna a propriedade <code>propriedade </code>tempo (em segundos) que passou entre o último frame e o atual``.</>
    },
    {
      name: "frameTime",
      type: "float",
      access: "get",
      description: <>Retorna a propriedade <code>propriedade </code>tempo (em segundos) que passou entre o último frame e o atual``.</>
    },
    {
      name: "timeScale",
      type: "float",
      access: "get/set",
      description: <>Retorna a propriedade <code>escala de tempo atual</code>.</>
    },
    {
      name: "timeSpeed",
      type: "float",
      access: "get/set",
      description: <>Retorna a propriedade <code>velocidade atual da escala de tempo</code>.</>
    },
    {
      name: "unscaledDeltaTime",
      type: "float",
      access: "get",
      description: <>Retorna a propriedade <code>propriedade </code>tempo (em segundos) que passou entre o último frame e o atual sem escala de tempo (tempo real)``.</>
    },
    {
      name: "unscaledFrameTime",
      type: "float",
      access: "get",
      description: <>Retorna a propriedade <code>propriedade </code>tempo (em segundos) que passou entre o último frame e o atual sem escala de tempo (tempo real)``.</>
    }
  ]}
  methods={[
    {
      signature: "deltaTime()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>tempo (em segundos) que passou entre o último frame e o atual``.</>
    },
    {
      signature: "frameTime()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>tempo (em segundos) que passou entre o último frame e o atual``.</>
    },
    {
      signature: "unscaledDeltaTime()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>tempo (em segundos) que passou entre o último frame e o atual sem escala de tempo (tempo real)``.</>
    }
  ]}
  staticMethods={[
    {
      signature: "deltaTime()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>tempo (em segundos) que passou entre o último frame e o atual``.</>
    },
    {
      signature: "frameTime()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>tempo (em segundos) que passou entre o último frame e o atual``.</>
    },
    {
      signature: "unscaledDeltaTime()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>tempo (em segundos) que passou entre o último frame e o atual sem escala de tempo (tempo real)``.</>
    }
  ]}
/>
