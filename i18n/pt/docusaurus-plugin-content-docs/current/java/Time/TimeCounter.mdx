# TimeCounter

Referência completa da API de `TimeCounter`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Time

**Componente:** não

---

```java
public class TimeCounter
```

## Construtores (1)

| Signature | Description |
| --- | --- |
| TimeCounter() | Executa `TimeCounter` e retorna quando a chamada do motor termina. |

## Atributos Virtuais (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `elapsedMilliseconds` | [float] | `get` | Retorna a propriedade `propriedade `tempo decorrido em milissegundos``. |
| `elapsedSeconds` | [float] | `get` | Retorna a propriedade `propriedade `tempo decorrido em segundos``. |

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `start()` | `void` | Inicia o contador de tempo. |
| `finish()` | `void` | Finaliza o contador de tempo e calcula o tempo decorrido. |
