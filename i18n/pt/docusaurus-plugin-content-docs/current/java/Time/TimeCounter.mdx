import ApiDoc from '@site/src/components/ApiDoc';

# TimeCounter

Referência completa da API de `TimeCounter`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Time

**Componente:** não

---

```java
public class TimeCounter
```

<ApiDoc
  constructors={[
    {
      signature: "TimeCounter()",
      description: <>Executa <code>TimeCounter</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "elapsedMilliseconds",
      type: "float",
      description: <><code>get</code></>
    },
    {
      name: "elapsedSeconds",
      type: "float",
      description: <><code>get</code></>
    }
  ]}
  virtualAttributes={[
    {
      name: "elapsedMilliseconds",
      type: "float",
      access: "get",
      description: <>Retorna a propriedade <code>propriedade </code>tempo decorrido em milissegundos``.</>
    },
    {
      name: "elapsedSeconds",
      type: "float",
      access: "get",
      description: <>Retorna a propriedade <code>propriedade </code>tempo decorrido em segundos``.</>
    }
  ]}
  methods={[
    {
      signature: "start()",
      returns: "void",
      description: <>Inicia o contador de tempo.</>
    },
    {
      signature: "finish()",
      returns: "void",
      description: <>Finaliza o contador de tempo e calcula o tempo decorrido.</>
    }
  ]}
/>
