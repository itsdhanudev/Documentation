import ApiDoc from '@site/src/components/ApiDoc';

# ClassCategory

Referência completa da API de `ClassCategory`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public interface ClassCategory
```

<ApiDoc
  methods={[
    {
      signature: "cat()",
      returns: "String[]",
      description: <>Executa <code>cat</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
