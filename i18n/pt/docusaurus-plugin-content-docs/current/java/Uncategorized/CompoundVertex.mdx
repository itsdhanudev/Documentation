import ApiDoc from '@site/src/components/ApiDoc';

# CompoundVertex

Referência completa da API de `CompoundVertex`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class CompoundVertex
```

<ApiDoc
  constructors={[
    {
      signature: "CompoundVertex(CompoundVertex)",
      description: <>Executa <code>CompoundVertex</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "CompoundVertex()",
      description: <>Executa <code>CompoundVertex</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "CompoundVertex",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "addVertex(Vertex, Transform)",
      returns: "Part",
      description: <>Adiciona vertex à coleção.</>
    },
    {
      signature: "refreshMatrix(Part)",
      returns: "void",
      description: <>Executa a operação refreshMatrix usando os parâmetros informados (part).</>
    },
    {
      signature: "remove(Part)",
      returns: "void",
      description: <>Remove an item da coleção.</>
    },
    {
      signature: "removeAll(List&lt;Part&gt;)",
      returns: "void",
      description: <>Remove all da coleção.</>
    },
    {
      signature: "getBake()",
      returns: "Vertex",
      description: <>Retorna a propriedade <code>propriedade </code>bake atual``.</>
    }
  ]}
/>
