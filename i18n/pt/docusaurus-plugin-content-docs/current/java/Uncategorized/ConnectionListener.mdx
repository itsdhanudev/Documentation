import ApiDoc from '@site/src/components/ApiDoc';

# ConnectionListener

Referência completa da API de `ConnectionListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public interface ConnectionListener
```

<ApiDoc
  methods={[
    {
      signature: "onConnected()",
      returns: "void",
      description: <>Executa <code>onConnected</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "onDisconnected()",
      returns: "void",
      description: <>Executa <code>onDisconnected</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "onError(String, int)",
      returns: "void",
      description: <>Executa <code>onError</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
