# ConnectionListener

Referência completa da API de `ConnectionListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public interface ConnectionListener
```

## Métodos (4)

| Signature | Returns | Description |
| --- | --- | --- |
| `onConnected()` | `void` | Executa `onConnected` e retorna quando a chamada do motor termina. |
| `onDisconnected()` | `void` | Executa `onDisconnected` e retorna quando a chamada do motor termina. |
| `@MethodArgs([\{"error"], ["code"\}])` | `void` | Executa a operação @MethodArgs. |
| `onError(String, int)` | `void` | Executa `onError` e retorna quando a chamada do motor termina. |
