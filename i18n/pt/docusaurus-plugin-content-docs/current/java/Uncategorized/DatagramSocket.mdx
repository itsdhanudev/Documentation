import ApiDoc from '@site/src/components/ApiDoc';

# DatagramSocket

Referência completa da API de `DatagramSocket`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class DatagramSocket extends DatagramSocket
```

<ApiDoc
  constructors={[
    {
      signature: "DatagramSocket()",
      description: <>Executa <code>DatagramSocket</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "DatagramSocket(DatagramSocketImpl)",
      description: <>Cria uma nova instância de DatagramSocket.</>
    },
    {
      signature: "DatagramSocket(SocketAddress)",
      description: <>Executa <code>DatagramSocket</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "DatagramSocket(int)",
      description: <>Executa <code>DatagramSocket</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "DatagramSocket(int, InetAddress)",
      description: <>Executa <code>DatagramSocket</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  methods={[
    {
      signature: "bind(SocketAddress)",
      returns: "void",
      description: <>Executa <code>bind</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "connect(InetAddress, int)",
      returns: "void",
      description: <>Executa a operação connect usando os parâmetros informados (address, port).</>
    },
    {
      signature: "connect(SocketAddress)",
      returns: "void",
      description: <>Executa <code>connect</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
