# DatagramSocket

Referência completa da API de `DatagramSocket`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class DatagramSocket extends DatagramSocket
```

## Construtores (5)

| Signature | Description |
| --- | --- |
| DatagramSocket() | Executa `DatagramSocket` e retorna quando a chamada do motor termina. |
| DatagramSocket([DatagramSocketImpl]) | Cria uma nova instância de DatagramSocket. |
| DatagramSocket([SocketAddress]) | Executa `DatagramSocket` e retorna quando a chamada do motor termina. |
| DatagramSocket([int]) | Executa `DatagramSocket` e retorna quando a chamada do motor termina. |
| DatagramSocket([int], [InetAddress]) | Executa `DatagramSocket` e retorna quando a chamada do motor termina. |

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `bind(SocketAddress)` | `void` | Executa `bind` e retorna quando a chamada do motor termina. |
| `connect(InetAddress, int)` | `void` | Executa a operação connect usando os parâmetros informados (address, port). |
| `connect(SocketAddress)` | `void` | Executa `connect` e retorna quando a chamada do motor termina. |
