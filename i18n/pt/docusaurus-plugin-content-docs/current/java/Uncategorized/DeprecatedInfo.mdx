import ApiDoc from '@site/src/components/ApiDoc';

# DeprecatedInfo

Referência completa da API de `DeprecatedInfo`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public interface DeprecatedInfo
```

<ApiDoc
  methods={[
    {
      signature: "info()",
      returns: "String[]",
      description: <>Executa <code>info</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
