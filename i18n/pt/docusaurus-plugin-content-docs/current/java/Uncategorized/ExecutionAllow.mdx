import ApiDoc from '@site/src/components/ApiDoc';

# ExecutionAllow

Referência completa da API de `ExecutionAllow`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class ExecutionAllow
```

<ApiDoc
  constructors={[
    {
      signature: "ExecutionAllow(boolean, boolean)",
      description: <>Cria uma nova instância de ExecutionAllow.</>
    }
  ]}
  methods={[
    {
      signature: "onGameStopped()",
      returns: "boolean",
      description: <>Executa a operação onGameStopped.</>
    },
    {
      signature: "setOnGameStopped(boolean)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>on game stopped``.</>
    },
    {
      signature: "onGameRunning()",
      returns: "boolean",
      description: <>Executa a operação onGameRunning.</>
    },
    {
      signature: "setOnGameRunning(boolean)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>on game running``.</>
    }
  ]}
/>
