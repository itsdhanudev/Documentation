# ExecutionAllow

Referência completa da API de `ExecutionAllow`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class ExecutionAllow
```

## Construtores (1)

| Signature | Description |
| --- | --- |
| ExecutionAllow([boolean], [boolean]) | Cria uma nova instância de ExecutionAllow. |

## Métodos (4)

| Signature | Returns | Description |
| --- | --- | --- |
| `onGameStopped()` | `boolean` | Executa a operação onGameStopped. |
| `setOnGameStopped(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `on game stopped``. |
| `onGameRunning()` | `boolean` | Executa a operação onGameRunning. |
| `setOnGameRunning(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `on game running``. |
