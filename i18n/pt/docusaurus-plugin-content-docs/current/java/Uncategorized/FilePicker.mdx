# FilePicker

Referência completa da API de `FilePicker`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class FilePicker
```

## Métodos Estáticos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `open(Listener)` | `void` | Executa a operação open usando os parâmetros informados (listener). |
| `importFile(DFile, File)` | `boolean` | Executa a operação importFile usando os parâmetros informados (documentFile, destination). |
