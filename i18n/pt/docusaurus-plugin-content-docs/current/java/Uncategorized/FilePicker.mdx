import ApiDoc from '@site/src/components/ApiDoc';

# FilePicker

Referência completa da API de `FilePicker`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class FilePicker
```

<ApiDoc
  methods={[
    {
      signature: "open(Listener)",
      returns: "void",
      description: <>Executa a operação open usando os parâmetros informados (listener).</>
    },
    {
      signature: "importFile(DFile, File)",
      returns: "boolean",
      description: <>Executa a operação importFile usando os parâmetros informados (documentFile, destination).</>
    }
  ]}
  staticMethods={[
    {
      signature: "open(Listener)",
      returns: "void",
      description: <>Executa a operação open usando os parâmetros informados (listener).</>
    },
    {
      signature: "importFile(DFile, File)",
      returns: "boolean",
      description: <>Executa a operação importFile usando os parâmetros informados (documentFile, destination).</>
    }
  ]}
/>
