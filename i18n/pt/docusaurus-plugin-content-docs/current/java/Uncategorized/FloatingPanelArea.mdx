# FloatingPanelArea

Referência completa da API de `FloatingPanelArea`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class FloatingPanelArea extends PanelArea
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| FloatingPanelArea([PanelArea]) | Executa `FloatingPanelArea` e retorna quando a chamada do motor termina. |
| FloatingPanelArea() | Cria uma nova instância de FloatingPanelArea. |

## Métodos (20)

| Signature | Returns | Description |
| --- | --- | --- |
| `isOpen()` | `boolean` | Retorna se open é verdadeiro. |
| `isCloseWhenClickOutside()` | `boolean` | Retorna se close when click outside é verdadeiro. |
| `setCloseWhenClickOutside(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `close when click outside``. |
| `isCaptureTouchWhenClickOutside()` | `boolean` | Retorna se capture touch when click outside é verdadeiro. |
| `setCaptureTouchWhenClickOutside(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `capture touch when click outside``. |
| `isResizeable()` | `boolean` | Retorna se resizeable é verdadeiro. |
| `setResizeable(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `resizeable``. |
| `isAllowCloseX()` | `boolean` | Retorna se allow close x é verdadeiro. |
| `setAllowCloseX(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `allow close x``. |
| `getFloatingX()` | `float` | Retorna a propriedade `propriedade `floating x atual``. |
| `setFloatingX(float)` | `void` | Define ou atualiza a propriedade `propriedade `floating x``. |
| `getFloatingY()` | `float` | Retorna a propriedade `propriedade `floating y atual``. |
| `setFloatingY(float)` | `void` | Define ou atualiza a propriedade `propriedade `floating y``. |
| `getFloatingW()` | `float` | Retorna a propriedade `propriedade `floating w atual``. |
| `setFloatingW(float)` | `void` | Define ou atualiza a propriedade `propriedade `floating w``. |
| `getFloatingH()` | `float` | Retorna a propriedade `propriedade `floating h atual``. |
| `setFloatingH(float)` | `void` | Define ou atualiza a propriedade `propriedade `floating h``. |
| `isCloseWhenPressBack()` | `boolean` | Retorna se close when press back é verdadeiro. |
| `setCloseWhenPressBack(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `close when press back``. |
| `closeWhenPressBack()` | `void` | Executa a operação closeWhenPressBack. |
