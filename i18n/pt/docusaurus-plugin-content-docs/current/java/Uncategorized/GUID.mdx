# GUID

Referência completa da API de `GUID`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class GUID
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| GUID([GUID]) | Executa `GUID` e retorna quando a chamada do motor termina. |
| GUID() | Executa `GUID` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `guid` | [GUID] | Representa o valor de `guid` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `duplicableGUID` | [String] | `get/set` | Atributo virtual exposto por `getDuplicableGUID`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `metaID` | [String] | `get/set` | Atributo virtual exposto por `getMetaID`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (1)

| Signature | Returns | Description |
| --- | --- | --- |
| `getUniqueGUID()` | `String` | Retorna a propriedade `propriedade `unique guid atual``. |
