import ApiDoc from '@site/src/components/ApiDoc';

# GUID

Referência completa da API de `GUID`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class GUID
```

<ApiDoc
  constructors={[
    {
      signature: "GUID(GUID)",
      description: <>Executa <code>GUID</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "GUID()",
      description: <>Executa <code>GUID</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "guid",
      type: "GUID",
      description: <>Representa o valor de <code>guid</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "duplicableGUID",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getDuplicableGUID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "metaID",
      type: "String",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMetaID</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "getUniqueGUID()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>unique guid atual``.</>
    }
  ]}
/>
