import ApiDoc from '@site/src/components/ApiDoc';

# InaccessibleFilePathException

Referência completa da API de `InaccessibleFilePathException`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class InaccessibleFilePathException extends RuntimeException
```

<ApiDoc
  constructors={[
    {
      signature: "InaccessibleFilePathException(String)",
      description: <>Cria uma nova instância de InaccessibleFilePathException.</>
    }
  ]}
/>
