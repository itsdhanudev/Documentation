import ApiDoc from '@site/src/components/ApiDoc';

# InvokeListener

Referência completa da API de `InvokeListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public interface InvokeListener
```

<ApiDoc
  methods={[
    {
      signature: "run()",
      returns: "void",
      description: <>Executa <code>run</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
