import ApiDoc from '@site/src/components/ApiDoc';

# JRDoc_EN

Referência completa da API de `JRDoc_EN`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public interface JRDoc_EN
```

<ApiDoc
  methods={[
    {
      signature: "value()",
      returns: "String",
      description: <>Executa <code>value</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
