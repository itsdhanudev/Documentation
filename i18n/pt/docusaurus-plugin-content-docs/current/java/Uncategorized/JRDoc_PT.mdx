import ApiDoc from '@site/src/components/ApiDoc';

# JRDoc_PT

Referência completa da API de `JRDoc_PT`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public interface JRDoc_PT
```

<ApiDoc
  methods={[
    {
      signature: "value()",
      returns: "String",
      description: <>Executa <code>value</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
