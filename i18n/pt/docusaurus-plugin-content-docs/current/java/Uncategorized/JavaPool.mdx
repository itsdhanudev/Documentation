# JavaPool

Referência completa da API de `JavaPool`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class JavaPool
```

## Métodos Estáticos (5)

| Signature | Returns | Description |
| --- | --- | --- |
| `acquire(Class)` | `T` | Executa `acquire` e retorna quando a chamada do motor termina. |
| `release(Object)` | `void` | Executa `release` e retorna quando a chamada do motor termina. |
| `acquireFloatArray(int)` | [float][] | Executa `acquireFloatArray` e retorna quando a chamada do motor termina. |
| `acquireFloatArray(int, boolean)` | [float][] | Executa `acquireFloatArray` e retorna quando a chamada do motor termina. |
| `releaseFloatArray(float[])` | `void` | Executa `releaseFloatArray` e retorna quando a chamada do motor termina. |
