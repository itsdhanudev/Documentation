import ApiDoc from '@site/src/components/ApiDoc';

# JavaPool

Referência completa da API de `JavaPool`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class JavaPool
```

<ApiDoc
  methods={[
    {
      signature: "acquire(Class)",
      returns: "T",
      description: <>Executa <code>acquire</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "release(Object)",
      returns: "void",
      description: <>Executa <code>release</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "acquireFloatArray(int)",
      returns: "float[]",
      description: <>Executa <code>acquireFloatArray</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "acquireFloatArray(int, boolean)",
      returns: "float[]",
      description: <>Executa <code>acquireFloatArray</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "releaseFloatArray(float[])",
      returns: "void",
      description: <>Executa <code>releaseFloatArray</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  staticMethods={[
    {
      signature: "acquire(Class)",
      returns: "T",
      description: <>Executa <code>acquire</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "release(Object)",
      returns: "void",
      description: <>Executa <code>release</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "acquireFloatArray(int)",
      returns: "float[]",
      description: <>Executa <code>acquireFloatArray</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "acquireFloatArray(int, boolean)",
      returns: "float[]",
      description: <>Executa <code>acquireFloatArray</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "releaseFloatArray(float[])",
      returns: "void",
      description: <>Executa <code>releaseFloatArray</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
