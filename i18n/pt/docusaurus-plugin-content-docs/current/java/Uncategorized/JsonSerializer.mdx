# JsonSerializer

Referência completa da API de `JsonSerializer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public interface JsonSerializer
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `serializeToString()` | `String` | Executa `serializeToString` e retorna quando a chamada do motor termina. |
| `deserializeLocal(String)` | `void` | Executa `deserializeLocal` e retorna quando a chamada do motor termina. |
