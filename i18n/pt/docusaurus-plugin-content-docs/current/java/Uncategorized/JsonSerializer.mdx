import ApiDoc from '@site/src/components/ApiDoc';

# JsonSerializer

Referência completa da API de `JsonSerializer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public interface JsonSerializer
```

<ApiDoc
  methods={[
    {
      signature: "serializeToString()",
      returns: "String",
      description: <>Executa <code>serializeToString</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "deserializeLocal(String)",
      returns: "void",
      description: <>Executa <code>deserializeLocal</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
