import ApiDoc from '@site/src/components/ApiDoc';

# LooperThread

Referência completa da API de `LooperThread`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class LooperThread
```

<ApiDoc
  constructors={[
    {
      signature: "LooperThread()",
      description: <>Cria uma nova instância de LooperThread.</>
    }
  ]}
  methods={[
    {
      signature: "start()",
      returns: "void",
      description: <>Executa a operação start.</>
    },
    {
      signature: "stop()",
      returns: "void",
      description: <>Executa a operação stop.</>
    },
    {
      signature: "execute(Runnable)",
      returns: "void",
      description: <>Executa <code>execute</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
