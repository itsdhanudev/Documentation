# LooperThread

Referência completa da API de `LooperThread`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class LooperThread
```

## Construtores (1)

| Signature | Description |
| --- | --- |
| LooperThread() | Cria uma nova instância de LooperThread. |

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `start()` | `void` | Executa a operação start. |
| `stop()` | `void` | Executa a operação stop. |
| `execute(Runnable)` | `void` | Executa `execute` e retorna quando a chamada do motor termina. |
