import ApiDoc from '@site/src/components/ApiDoc';

# Matcap

Referência completa da API de `Matcap`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class Matcap
```

<ApiDoc
  constructors={[
    {
      signature: "Matcap(Engine)",
      description: <>Cria uma nova instância de Matcap.</>
    },
    {
      signature: "Matcap(String)",
      description: <>Cria uma nova instância de Matcap.</>
    },
    {
      signature: "Matcap()",
      description: <>Cria uma nova instância de Matcap.</>
    }
  ]}
  methods={[
    {
      signature: "getFile()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>file atual``.</>
    },
    {
      signature: "getTextureFile()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>texture file atual``.</>
    },
    {
      signature: "setTextureFile(String)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>texture file``.</>
    },
    {
      signature: "toJson()",
      returns: "String",
      description: <>Executa a operação toJson.</>
    },
    {
      signature: "getTexture()",
      returns: "Texture",
      description: <>Retorna a propriedade <code>propriedade </code>texture atual``.</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executa <code>genericInterface</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "fromJson(String)",
      returns: "Matcap",
      description: <>Executa a operação fromJson usando os parâmetros informados (json).</>
    }
  ]}
/>
