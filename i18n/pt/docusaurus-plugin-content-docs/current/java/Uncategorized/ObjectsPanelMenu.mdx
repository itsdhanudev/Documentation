import ApiDoc from '@site/src/components/ApiDoc';

# ObjectsPanelMenu

Referência completa da API de `ObjectsPanelMenu`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class ObjectsPanelMenu
```

<ApiDoc
  constructors={[
    {
      signature: "ObjectsPanelMenu(String)",
      description: <>Cria uma nova instância de ObjectsPanelMenu.</>
    }
  ]}
  methods={[
    {
      signature: "getMenu()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>menu atual``.</>
    },
    {
      signature: "setMenu(String)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>menu``.</>
    },
    {
      signature: "onClick(SpatialObject)",
      returns: "void",
      description: <>Executa a operação onClick usando os parâmetros informados (object).</>
    }
  ]}
/>
