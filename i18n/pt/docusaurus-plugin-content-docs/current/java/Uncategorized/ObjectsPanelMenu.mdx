# ObjectsPanelMenu

Referência completa da API de `ObjectsPanelMenu`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class ObjectsPanelMenu
```

## Construtores (1)

| Signature | Description |
| --- | --- |
| ObjectsPanelMenu([String]) | Cria uma nova instância de ObjectsPanelMenu. |

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `getMenu()` | `String` | Retorna a propriedade `propriedade `menu atual``. |
| `setMenu(String)` | `void` | Define ou atualiza a propriedade `propriedade `menu``. |
| `onClick(SpatialObject)` | `void` | Executa a operação onClick usando os parâmetros informados (object). |
