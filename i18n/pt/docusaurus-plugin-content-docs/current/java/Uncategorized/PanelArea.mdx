# PanelArea

Referência completa da API de `PanelArea`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class PanelArea
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| PanelArea([PanelArea]) | Executa `PanelArea` e retorna quando a chamada do motor termina. |
| PanelArea() | Executa `PanelArea` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [PanelArea] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (12)

| Signature | Returns | Description |
| --- | --- | --- |
| `setInstance(PanelArea)` | `void` | Executa `setInstance` e retorna quando a chamada do motor termina. |
| `addPanel(EditorPanel)` | `void` | Adiciona panel à coleção. |
| `removePanel(EditorPanel)` | `void` | Remove panel da coleção. |
| `requestDetach()` | `void` | Executa a operação requestDetach. |
| `getX()` | `float` | Retorna a propriedade `propriedade `x atual``. |
| `setX(float)` | `void` | Define ou atualiza a propriedade `propriedade `x``. |
| `getY()` | `float` | Retorna a propriedade `propriedade `y atual``. |
| `setY(float)` | `void` | Define ou atualiza a propriedade `propriedade `y``. |
| `getWidth()` | `float` | Retorna a propriedade `propriedade `width atual``. |
| `setWidth(float)` | `void` | Define ou atualiza a propriedade `propriedade `width``. |
| `getHeight()` | `float` | Retorna a propriedade `propriedade `height atual``. |
| `setHeight(float)` | `void` | Define ou atualiza a propriedade `propriedade `height``. |
