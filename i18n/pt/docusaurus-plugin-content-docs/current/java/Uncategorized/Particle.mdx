import ApiDoc from '@site/src/components/ApiDoc';

# Particle

Referência completa da API de `Particle`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class Particle
```

<ApiDoc
  constructors={[
    {
      signature: "Particle(Particle)",
      description: <>Executa <code>Particle</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Particle()",
      description: <>Executa <code>Particle</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Particle",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "color",
      type: "Color",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getColor</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "liteTime",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLiteTime</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "movement",
      type: "Vector3",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getMovement</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "position",
      type: "Vector3",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPosition</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "rotation",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRotation</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "scale",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getScale</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
