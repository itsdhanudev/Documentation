# PhysicsLayer

Referência completa da API de `PhysicsLayer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class PhysicsLayer
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| PhysicsLayer([Layer]) | Executa `PhysicsLayer` e retorna quando a chamada do motor termina. |
| PhysicsLayer([String]) | Executa `PhysicsLayer` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `layer` | [Layer] | Representa o valor de `layer` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (4)

| Signature | Returns | Description |
| --- | --- | --- |
| `addIgnoreLayer(PhysicsLayer)` | `void` | Adiciona a PhysicsLayer especificada à lista de ignorados desta layer. |
| `removeIgnoreLayer(PhysicsLayer)` | `void` | Remove a PhysicsLayer especificada da lista de ignorados desta layer. |
| `getIgnoreAt(int)` | `PhysicsLayer` | Retorna a propriedade `PhysicsLayer no índice especificado da lista de ignorados`. |
| `ignoreCount()` | `int` | Retorna a propriedade `propriedade `número de PhysicsLayers na lista de ignorados``. |
