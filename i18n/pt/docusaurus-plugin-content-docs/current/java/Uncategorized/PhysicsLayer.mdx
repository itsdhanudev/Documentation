import ApiDoc from '@site/src/components/ApiDoc';

# PhysicsLayer

Referência completa da API de `PhysicsLayer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class PhysicsLayer
```

<ApiDoc
  constructors={[
    {
      signature: "PhysicsLayer(Layer)",
      description: <>Executa <code>PhysicsLayer</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "PhysicsLayer(String)",
      description: <>Executa <code>PhysicsLayer</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "layer",
      type: "Layer",
      description: <>Representa o valor de <code>layer</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "addIgnoreLayer(PhysicsLayer)",
      returns: "void",
      description: <>Adiciona a PhysicsLayer especificada à lista de ignorados desta layer.</>
    },
    {
      signature: "removeIgnoreLayer(PhysicsLayer)",
      returns: "void",
      description: <>Remove a PhysicsLayer especificada da lista de ignorados desta layer.</>
    },
    {
      signature: "getIgnoreAt(int)",
      returns: "PhysicsLayer",
      description: <>Retorna a propriedade <code>PhysicsLayer no índice especificado da lista de ignorados</code>.</>
    },
    {
      signature: "ignoreCount()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>número de PhysicsLayers na lista de ignorados``.</>
    }
  ]}
/>
