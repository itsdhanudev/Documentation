# Resources

Referência completa da API de `Resources`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class Resources
```

## Métodos Estáticos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `getFile(String)` | `File` | Retorna a propriedade `propriedade `file atual``. |
| `isFolder(String)` | `boolean` | Retorna se folder é verdadeiro. |
