import ApiDoc from '@site/src/components/ApiDoc';

# Resources

Referência completa da API de `Resources`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class Resources
```

<ApiDoc
  methods={[
    {
      signature: "getFile(String)",
      returns: "File",
      description: <>Retorna a propriedade <code>propriedade </code>file atual``.</>
    },
    {
      signature: "isFolder(String)",
      returns: "boolean",
      description: <>Retorna se folder é verdadeiro.</>
    }
  ]}
  staticMethods={[
    {
      signature: "getFile(String)",
      returns: "File",
      description: <>Retorna a propriedade <code>propriedade </code>file atual``.</>
    },
    {
      signature: "isFolder(String)",
      returns: "boolean",
      description: <>Retorna se folder é verdadeiro.</>
    }
  ]}
/>
