import ApiDoc from '@site/src/components/ApiDoc';

# ServerSocket

Referência completa da API de `ServerSocket`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class ServerSocket extends ServerSocket
```

<ApiDoc
  constructors={[
    {
      signature: "ServerSocket()",
      description: <>Executa <code>ServerSocket</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ServerSocket(int)",
      description: <>Executa <code>ServerSocket</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ServerSocket(int, int)",
      description: <>Executa <code>ServerSocket</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "ServerSocket(int, int, InetAddress)",
      description: <>Executa <code>ServerSocket</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  methods={[
    {
      signature: "bind(SocketAddress)",
      returns: "void",
      description: <>Executa <code>bind</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "bind(SocketAddress, int)",
      returns: "void",
      description: <>Executa <code>bind</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "accept()",
      returns: "Socket",
      description: <>Executa <code>accept</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
