# SoundData

Referência completa da API de `SoundData`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class SoundData
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| SoundData() | Cria uma nova instância de SoundData. |
| SoundData([NativeFloatBuffer], [int]) | Cria uma nova instância de SoundData. |
| SoundData([float][], [int]) | Cria uma nova instância de SoundData. |

## Atributos Virtuais (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `dataBuffer` | [NativeFloatBuffer] | `get/set` | Atributo virtual exposto por `getDataBuffer`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `sampleRate` | [int] | `get/set` | Atributo virtual exposto por `getSampleRate`. Acesse como um campo para ler ou escrever o valor subjacente. |
