import ApiDoc from '@site/src/components/ApiDoc';

# SoundData

Referência completa da API de `SoundData`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class SoundData
```

<ApiDoc
  constructors={[
    {
      signature: "SoundData()",
      description: <>Cria uma nova instância de SoundData.</>
    },
    {
      signature: "SoundData(NativeFloatBuffer, int)",
      description: <>Cria uma nova instância de SoundData.</>
    },
    {
      signature: "SoundData(float[], int)",
      description: <>Cria uma nova instância de SoundData.</>
    }
  ]}
  attributes={[
    {
      name: "dataBuffer",
      type: "NativeFloatBuffer",
      description: <><code>get/set</code></>
    },
    {
      name: "sampleRate",
      type: "int",
      description: <><code>get/set</code></>
    }
  ]}
  virtualAttributes={[
    {
      name: "dataBuffer",
      type: "NativeFloatBuffer",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getDataBuffer</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "sampleRate",
      type: "int",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSampleRate</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
/>
