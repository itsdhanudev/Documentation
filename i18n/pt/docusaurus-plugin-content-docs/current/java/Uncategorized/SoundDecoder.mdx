# SoundDecoder

Referência completa da API de `SoundDecoder`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class SoundDecoder
```

## Métodos Estáticos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `decodeFile(File)` | `SoundData` | Executa `decodeFile` e retorna quando a chamada do motor termina. |
| `streamDecodeFile(File)` | `SoundEmitter` | Executa a operação streamDecodeFile usando os parâmetros informados (soundFile). |
