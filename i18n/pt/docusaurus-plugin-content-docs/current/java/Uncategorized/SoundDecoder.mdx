import ApiDoc from '@site/src/components/ApiDoc';

# SoundDecoder

Referência completa da API de `SoundDecoder`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class SoundDecoder
```

<ApiDoc
  methods={[
    {
      signature: "decodeFile(File)",
      returns: "SoundData",
      description: <>Executa <code>decodeFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "streamDecodeFile(File)",
      returns: "SoundEmitter",
      description: <>Executa a operação streamDecodeFile usando os parâmetros informados (soundFile).</>
    }
  ]}
  staticMethods={[
    {
      signature: "decodeFile(File)",
      returns: "SoundData",
      description: <>Executa <code>decodeFile</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "streamDecodeFile(File)",
      returns: "SoundEmitter",
      description: <>Executa a operação streamDecodeFile usando os parâmetros informados (soundFile).</>
    }
  ]}
/>
