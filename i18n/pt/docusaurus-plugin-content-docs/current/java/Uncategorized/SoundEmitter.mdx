# SoundEmitter

Referência completa da API de `SoundEmitter`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class SoundEmitter
```

## Construtores (5)

| Signature | Description |
| --- | --- |
| SoundEmitter([NativeSoundEmitter]) | Executa `SoundEmitter` e retorna quando a chamada do motor termina. |
| SoundEmitter([NativeFloatBuffer], [int]) | Executa `SoundEmitter` e retorna quando a chamada do motor termina. |
| SoundEmitter([float][], [int]) | Cria uma nova instância de SoundEmitter. |
| SoundEmitter([SoundData]) | Cria uma nova instância de SoundEmitter. |
| SoundEmitter([File]) | Executa `SoundEmitter` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [NativeSoundEmitter] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (5)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `leftVolume` | [float] | `get/set` | Atributo virtual exposto por `getLeftVolume`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `pitch` | [float] | `get/set` | Atributo virtual exposto por `getPitch`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `rightVolume` | [float] | `get/set` | Atributo virtual exposto por `getRightVolume`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `speed` | [float] | `get/set` | Atributo virtual exposto por `getSpeed`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `volume` | [float] | `get/set` | Atributo virtual exposto por `getVolume`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (13)

| Signature | Returns | Description |
| --- | --- | --- |
| `setVolumes(float, float)` | `void` | Define ou atualiza a propriedade `propriedade `volumes``. |
| `setVolumes(float, float, float)` | `void` | Define ou atualiza a propriedade `propriedade `volumes``. |
| `getCurrentSecond()` | `float` | Retorna a propriedade `propriedade `current second atual``. |
| `getTotalSeconds()` | `float` | Retorna a propriedade `propriedade `total seconds atual``. |
| `seekToSecond(float)` | `void` | Executa a operação seekToSecond usando os parâmetros informados (second). |
| `isLoop()` | `boolean` | Retorna se loop é verdadeiro. |
| `setLoop(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `loop``. |
| `play()` | `void` | Executa a operação play. |
| `pause()` | `void` | Executa a operação pause. |
| `stop()` | `void` | Executa a operação stop. |
| `isPaused()` | `boolean` | Retorna se paused é verdadeiro. |
| `isPlaying()` | `boolean` | Retorna se playing é verdadeiro. |
| `isStopped()` | `boolean` | Retorna se stopped é verdadeiro. |
