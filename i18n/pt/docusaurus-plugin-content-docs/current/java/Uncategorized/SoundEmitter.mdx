import ApiDoc from '@site/src/components/ApiDoc';

# SoundEmitter

Referência completa da API de `SoundEmitter`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class SoundEmitter
```

<ApiDoc
  constructors={[
    {
      signature: "SoundEmitter(NativeSoundEmitter)",
      description: <>Executa <code>SoundEmitter</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SoundEmitter(NativeFloatBuffer, int)",
      description: <>Executa <code>SoundEmitter</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "SoundEmitter(float[], int)",
      description: <>Cria uma nova instância de SoundEmitter.</>
    },
    {
      signature: "SoundEmitter(SoundData)",
      description: <>Cria uma nova instância de SoundEmitter.</>
    },
    {
      signature: "SoundEmitter(File)",
      description: <>Executa <code>SoundEmitter</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "NativeSoundEmitter",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  virtualAttributes={[
    {
      name: "leftVolume",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getLeftVolume</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "pitch",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getPitch</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "rightVolume",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getRightVolume</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "speed",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getSpeed</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    },
    {
      name: "volume",
      type: "float",
      access: "get/set",
      description: <>Atributo virtual exposto por <code>getVolume</code>. Acesse como um campo para ler ou escrever o valor subjacente.</>
    }
  ]}
  methods={[
    {
      signature: "setVolumes(float, float)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>volumes``.</>
    },
    {
      signature: "setVolumes(float, float, float)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>volumes``.</>
    },
    {
      signature: "getCurrentSecond()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>current second atual``.</>
    },
    {
      signature: "getTotalSeconds()",
      returns: "float",
      description: <>Retorna a propriedade <code>propriedade </code>total seconds atual``.</>
    },
    {
      signature: "seekToSecond(float)",
      returns: "void",
      description: <>Executa a operação seekToSecond usando os parâmetros informados (second).</>
    },
    {
      signature: "isLoop()",
      returns: "boolean",
      description: <>Retorna se loop é verdadeiro.</>
    },
    {
      signature: "setLoop(boolean)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>loop``.</>
    },
    {
      signature: "play()",
      returns: "void",
      description: <>Executa a operação play.</>
    },
    {
      signature: "pause()",
      returns: "void",
      description: <>Executa a operação pause.</>
    },
    {
      signature: "stop()",
      returns: "void",
      description: <>Executa a operação stop.</>
    },
    {
      signature: "isPaused()",
      returns: "boolean",
      description: <>Retorna se paused é verdadeiro.</>
    },
    {
      signature: "isPlaying()",
      returns: "boolean",
      description: <>Retorna se playing é verdadeiro.</>
    },
    {
      signature: "isStopped()",
      returns: "boolean",
      description: <>Retorna se stopped é verdadeiro.</>
    }
  ]}
/>
