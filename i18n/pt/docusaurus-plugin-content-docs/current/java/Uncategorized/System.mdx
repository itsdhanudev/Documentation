# System

Referência completa da API de `System`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class System
```

## Métodos Estáticos (10)

| Signature | Returns | Description |
| --- | --- | --- |
| `gc()` | `void` | Executa o coletor de lixo do Java. |
| `exit()` | `void` | Encerra o jogo chamando GameController.quit(). |
| `exit(int)` | `void` | Encerra o jogo chamando GameController.quit(). |
| `load(String)` | `void` | Não suportado. |
| `loadLibrary(String)` | `void` | Não suportado. |
| `mapLibraryName(String)` | `String` | Não suportado. |
| `lineSeparator()` | `String` | Retorna a propriedade `propriedade `separador de linha do sistema operacional atual``. |
| `arraycopy(Object, int, Object, int, int)` | `void` | Copia um array da origem especificada para o destino especificado. |
| `currentTimeMillis()` | `long` | Retorna a propriedade `propriedade `tempo atual em milissegundos``. |
| `nanoTime()` | `long` | Retorna a propriedade `propriedade `tempo atual em nanossegundos``. |
