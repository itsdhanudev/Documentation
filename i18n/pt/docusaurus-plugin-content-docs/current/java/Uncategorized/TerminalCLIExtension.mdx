# TerminalCLIExtension

Referência completa da API de `TerminalCLIExtension`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class TerminalCLIExtension
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| TerminalCLIExtension([JavaTerminalCLIExtension]) | Executa `TerminalCLIExtension` e retorna quando a chamada do motor termina. |
| TerminalCLIExtension() | Executa `TerminalCLIExtension` e retorna quando a chamada do motor termina. |

## Métodos (23)

| Signature | Returns | Description |
| --- | --- | --- |
| `setInstance(JavaTerminalCLIExtension)` | `void` | Executa `setInstance` e retorna quando a chamada do motor termina. |
| `getCommand()` | `String` | Executa `getCommand` e retorna quando a chamada do motor termina. |
| `getInfo()` | `String` | Retorna a propriedade `propriedade `info atual``. |
| `start(String[])` | `void` | Executa `start` e retorna quando a chamada do motor termina. |
| `run(String)` | `void` | Executa `run` e retorna quando a chamada do motor termina. |
| `exit()` | `void` | Executa a operação exit. |
| `log(Object)` | `void` | Executa a operação log usando os parâmetros informados (message). |
| `log(String)` | `void` | Executa a operação log usando os parâmetros informados (message). |
| `log(Vector3)` | `void` | Executa a operação log usando os parâmetros informados (message). |
| `log(Quaternion)` | `void` | Executa a operação log usando os parâmetros informados (message). |
| `log(Vector2)` | `void` | Executa a operação log usando os parâmetros informados (message). |
| `log(float)` | `void` | Executa a operação log usando os parâmetros informados (message). |
| `log(int)` | `void` | Executa a operação log usando os parâmetros informados (message). |
| `log(long)` | `void` | Executa a operação log usando os parâmetros informados (message). |
| `log(double)` | `void` | Executa a operação log usando os parâmetros informados (message). |
| `log(char)` | `void` | Executa a operação log usando os parâmetros informados (message). |
| `log(byte)` | `void` | Executa a operação log usando os parâmetros informados (message). |
| `log(Error)` | `void` | Executa a operação log usando os parâmetros informados (e). |
| `log(Exception)` | `void` | Executa a operação log usando os parâmetros informados (e). |
| `log(Throwable)` | `void` | Executa a operação log usando os parâmetros informados (e). |
| `log(Log)` | `void` | Executa a operação log usando os parâmetros informados (log). |
| `log(String[])` | `void` | Executa a operação log usando os parâmetros informados (log). |
| `log([List&lt;String&gt;])` | `void` | Executa a operação log usando os parâmetros informados (log). |
