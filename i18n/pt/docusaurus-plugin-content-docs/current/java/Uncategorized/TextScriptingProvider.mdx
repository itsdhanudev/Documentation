# TextScriptingProvider

Referência completa da API de `TextScriptingProvider`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class TextScriptingProvider
```

## Métodos (4)

| Signature | Returns | Description |
| --- | --- | --- |
| `getAll()` | [List]&[lt];[TextScriptingSuggestion]&[gt]; | Retorna a propriedade `propriedade `all atual``. |
| `processLine(int, String)` | `void` | Executa a operação processLine usando os parâmetros informados (lineNumber, text). |
| `deleteLine(int)` | `void` | Remove line da coleção. |
| `clearLines()` | `void` | Limpa ou redefine o estado do objeto. |
