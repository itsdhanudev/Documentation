# TextScriptingStyleSpan

Referência completa da API de `TextScriptingStyleSpan`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class TextScriptingStyleSpan
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| TextScriptingStyleSpan([Color]) | Cria uma nova instância de TextScriptingStyleSpan. |
| TextScriptingStyleSpan([Color], [boolean], [boolean], [boolean], [boolean]) | Cria uma nova instância de TextScriptingStyleSpan. |

## Atributos (5)

| Name | Type | Description |
| --- | --- | --- |
| `color` | [Color] | Representa o valor de `color` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `bold` | [boolean] | Representa o valor de `bold` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `italic` | [boolean] | Representa o valor de `italic` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `underline` | [boolean] | Representa o valor de `underline` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `strikethrough` | [boolean] | Representa o valor de `strikethrough` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (11)

| Signature | Returns | Description |
| --- | --- | --- |
| `getColor()` | `Color` | Retorna a propriedade `propriedade `color atual``. |
| `setColor(Color)` | `void` | Define ou atualiza a propriedade `propriedade `color``. |
| `isBold()` | `boolean` | Retorna se bold é verdadeiro. |
| `setBold(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `bold``. |
| `isItalic()` | `boolean` | Retorna se italic é verdadeiro. |
| `setItalic(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `italic``. |
| `isUnderline()` | `boolean` | Retorna se underline é verdadeiro. |
| `setUnderline(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `underline``. |
| `isStrikethrough()` | `boolean` | Retorna se strikethrough é verdadeiro. |
| `setStrikethrough(boolean)` | `void` | Define ou atualiza a propriedade `propriedade `strikethrough``. |
| `convertInternal()` | `StyleSpan` | Executa `convertInternal` e retorna quando a chamada do motor termina. |
