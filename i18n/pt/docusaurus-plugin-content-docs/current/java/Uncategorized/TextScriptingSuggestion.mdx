# TextScriptingSuggestion

Referência completa da API de `TextScriptingSuggestion`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class TextScriptingSuggestion
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| TextScriptingSuggestion([String]) | Cria uma nova instância de TextScriptingSuggestion. |
| TextScriptingSuggestion([String], [String]) | Cria uma nova instância de TextScriptingSuggestion. |

## Atributos (2)

| Name | Type | Description |
| --- | --- | --- |
| `text` | [String] | Representa o valor de `text` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `returnType` | [String] | Representa o valor de `returnType` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (5)

| Signature | Returns | Description |
| --- | --- | --- |
| `getText()` | `String` | Retorna a propriedade `propriedade `text atual``. |
| `setText(String)` | `void` | Define ou atualiza a propriedade `propriedade `text``. |
| `getReturnType()` | `String` | Retorna a propriedade `propriedade `return type atual``. |
| `setReturnType(String)` | `void` | Define ou atualiza a propriedade `propriedade `return type``. |
| `convertInternal()` | `Suggestion` | Executa `convertInternal` e retorna quando a chamada do motor termina. |
