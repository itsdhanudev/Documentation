# TextScriptingSyntaxHighlightSpan

Referência completa da API de `TextScriptingSyntaxHighlightSpan`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class TextScriptingSyntaxHighlightSpan
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| TextScriptingSyntaxHighlightSpan() | Cria uma nova instância de TextScriptingSyntaxHighlightSpan. |
| TextScriptingSyntaxHighlightSpan([TextScriptingStyleSpan], [int], [int]) | Cria uma nova instância de TextScriptingSyntaxHighlightSpan. |

## Atributos (3)

| Name | Type | Description |
| --- | --- | --- |
| `span` | [TextScriptingStyleSpan] | Representa o valor de `span` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `start` | [int] | Representa o valor de `start` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |
| `end` | [int] | Representa o valor de `end` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (6)

| Signature | Returns | Description |
| --- | --- | --- |
| `getSpan()` | `TextScriptingStyleSpan` | Retorna a propriedade `propriedade `span atual``. |
| `setSpan(TextScriptingStyleSpan)` | `void` | Define ou atualiza a propriedade `propriedade `span``. |
| `getStart()` | `int` | Retorna a propriedade `propriedade `start atual``. |
| `setStart(int)` | `void` | Define ou atualiza a propriedade `propriedade `start``. |
| `getEnd()` | `int` | Retorna a propriedade `propriedade `end atual``. |
| `setEnd(int)` | `void` | Define ou atualiza a propriedade `propriedade `end``. |
