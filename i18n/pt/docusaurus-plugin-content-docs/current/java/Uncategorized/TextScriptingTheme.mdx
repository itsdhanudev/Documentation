# TextScriptingTheme

Referência completa da API de `TextScriptingTheme`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class TextScriptingTheme
```

## Métodos (29)

| Signature | Returns | Description |
| --- | --- | --- |
| `Color(["#ABB7C5"])` | [Color] [textColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#1f1f1f"])` | [Color] [backgroundColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#2e2e2e"])` | [Color] [gutterColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#555555"])` | [Color] [gutterDividerColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#A4A3A3"])` | [Color] [gutterCurrentLineNumberColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#616366"])` | [Color] [gutterTextColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#3A3A3A"])` | [Color] [selectedLineColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#28427F"])` | [Color] [selectionColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#987DAC"])` | [Color] [suggestionQueryColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#33654B"])` | [Color] [findResultBackgroundColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#33654B"])` | [Color] [delimiterBackgroundColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#6897BB"])` | [Color] [numberColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#E8E2B7"])` | [Color] [operatorColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#EC7600"])` | [Color] [keywordColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#EC7600"])` | [Color] [typeColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#EC7600"])` | [Color] [langConstColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#C9C54E"])` | [Color] [preprocessorColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#9378A7"])` | [Color] [variableColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#FEC76C"])` | [Color] [methodColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#6E875A"])` | [Color] [stringColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#66747B"])` | [Color] [commentColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#E2C077"])` | [Color] [tagColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#E2C077"])` | [Color] [tagNameColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#BABABA"])` | [Color] [attrNameColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#ABC16D"])` | [Color] [attrValueColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#6897BB"])` | [Color] [entityRefColor] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#ff0000"])` | [Color] [gutterLineError] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `Color(["#f1c40f"])` | [Color] [gutterLineAlert] = [new] | Executa `Color` e retorna quando a chamada do motor termina. |
| `convertInternal()` | `ColorScheme` | Executa `convertInternal` e retorna quando a chamada do motor termina. |
