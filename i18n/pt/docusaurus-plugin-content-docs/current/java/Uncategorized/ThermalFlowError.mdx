import ApiDoc from '@site/src/components/ApiDoc';

# ThermalFlowError

Referência completa da API de `ThermalFlowError`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class ThermalFlowError extends RuntimeException
```

<ApiDoc
  constructors={[
    {
      signature: "ThermalFlowError(String, String)",
      description: <>Cria uma nova instância de ThermalFlowError.</>
    },
    {
      signature: "ThermalFlowError(String, String, Exception)",
      description: <>Cria uma nova instância de ThermalFlowError.</>
    },
    {
      signature: "ThermalFlowError(String, String, Error)",
      description: <>Cria uma nova instância de ThermalFlowError.</>
    }
  ]}
  attributes={[
    {
      name: "TYPE_SIMPLE",
      type: "int",
      description: <>Representa o valor de <code>TYPE_SIMPLE</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    },
    {
      name: "TYPE_EXCEPTION",
      type: "int",
      description: <>Representa o valor de <code>TYPE_EXCEPTION</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    },
    {
      name: "TYPE_ERROR",
      type: "int",
      description: <>Representa o valor de <code>TYPE_ERROR</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "getMessage()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>message atual``.</>
    },
    {
      signature: "getCallStack()",
      returns: "String",
      description: <>Retorna a propriedade <code>propriedade </code>call stack atual``.</>
    },
    {
      signature: "getException()",
      returns: "Exception",
      description: <>Retorna a propriedade <code>propriedade </code>exception atual``.</>
    },
    {
      signature: "getError()",
      returns: "Error",
      description: <>Retorna a propriedade <code>propriedade </code>error atual``.</>
    },
    {
      signature: "getType()",
      returns: "int",
      description: <>Retorna a propriedade <code>propriedade </code>type atual``.</>
    }
  ]}
/>
