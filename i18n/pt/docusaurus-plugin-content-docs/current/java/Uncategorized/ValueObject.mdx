# ValueObject

Referência completa da API de `ValueObject`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public class ValueObject
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| ValueObject() | Cria uma nova instância de ValueObject. |
| ValueObject([LuaValue]) | Cria uma nova instância de ValueObject. |

## Métodos (18)

| Signature | Returns | Description |
| --- | --- | --- |
| `getCriticalValue()` | `LuaValue` | Retorna a propriedade `propriedade `critical value atual``. |
| `typename()` | `String` | Executa a operação typename. |
| `get(String)` | `ValueObject` | Retorna a propriedade `propriedade `valor value atual``. |
| `put(String, ValueObject)` | `void` | Define ou atualiza the value. |
| `put(String, Object)` | `void` | Define ou atualiza the value. |
| `toJava(Class)` | `Object` | Executa a operação toJava usando os parâmetros informados (var1). |
| `toBoolean()` | `boolean` | Executa a operação toBoolean. |
| `toByte()` | `byte` | Executa a operação toByte. |
| `toChar()` | `char` | Executa a operação toChar. |
| `toShort()` | `short` | Executa a operação toShort. |
| `toInt()` | `int` | Executa a operação toInt. |
| `toLong()` | `long` | Executa a operação toLong. |
| `toFloat()` | `float` | Executa a operação toFloat. |
| `toDouble()` | `double` | Executa a operação toDouble. |
| `equals(Object)` | `boolean` | Compara esta instância com outra para igualdade/ordem. |
| `toString()` | `String` | Retorna a propriedade `representação em string`. |
| `hashCode()` | `int` | Retorna se code é verdadeiro. |
| `getObjectCritical()` | `LuaValue` | Retorna a propriedade `propriedade `object critical atual``. |
