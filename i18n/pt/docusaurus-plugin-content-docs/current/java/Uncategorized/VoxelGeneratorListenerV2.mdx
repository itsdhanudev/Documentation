import ApiDoc from '@site/src/components/ApiDoc';

# VoxelGeneratorListenerV2

Referência completa da API de `VoxelGeneratorListenerV2`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Uncategorized

**Componente:** não

---

```java
public interface VoxelGeneratorListenerV2
```

<ApiDoc
  methods={[
    {
      signature: "getGroundHeight(int, int)",
      returns: "int",
      description: <>Executa <code>getGroundHeight</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getBlockType(int, int, int, int)",
      returns: "int",
      description: <>Executa <code>getBlockType</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "storeChunk(OH3LevelIntArray, int, int)",
      returns: "void",
      description: <>Executa <code>storeChunk</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "loadChunk(int, int)",
      returns: "OH3LevelIntArray",
      description: <>Executa <code>loadChunk</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "onChunkCreated(SpatialObject, VoxelChunk)",
      returns: "void",
      description: <>Executa <code>onChunkCreated</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "onChunkBuilt(SpatialObject, VoxelChunk)",
      returns: "void",
      description: <>Executa <code>onChunkBuilt</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "drawFace(int, int, int, Point3)",
      returns: "boolean",
      description: <>Executa <code>drawFace</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "getTextureIndex(int, int, int)",
      returns: "int",
      description: <>Executa <code>getTextureIndex</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "onFaceBuilt(SpatialObject, VoxelChunk, int, int, int, Point3, int, int, int, int)",
      returns: "void",
      description: <>Executa <code>onFaceBuilt</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
