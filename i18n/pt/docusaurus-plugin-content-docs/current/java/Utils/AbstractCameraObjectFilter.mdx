# AbstractCameraObjectFilter

Referência completa da API de `AbstractCameraObjectFilter`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Utils

**Componente:** não

---

```java
public class AbstractCameraObjectFilter
```

## Métodos (10)

| Signature | Returns | Description |
| --- | --- | --- |
| `filterShadow()` | `boolean` | Filtra qual Sombra será renderizada. Retorne verdadeiro para renderizar e falso para ignorar. |
| `filterRenderer(ModelRenderer)` | `boolean` | Filtra qual ModelRenderer será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `filterRenderer(SkinnedModelRenderer)` | `boolean` | Filtra qual SkinnedModelRenderer será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `filterMaterialBake(Material, Vertex)` | `boolean` | Filtra qual Bake será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `filter3DText(Component)` | `boolean` | Filtra qual Texto 3D será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `renderFog()` | `boolean` | Define se o Fog será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `renderParticles()` | `boolean` | Define se as Partículas serão renderizadas. Retorne verdadeiro para renderizar e falso para ignorar. |
| `filterParticle(ParticleEmitter)` | `boolean` | Filtra qual Emissor de Partículas será renderizado. Retorne verdadeiro para renderizar e falso para ignorar. |
| `overrideAmbientLight()` | `Color` | Substitui a Cor da Luz Ambiente. Retorne a Cor que será substituída. |
| `overrideIndirectLight()` | `Color` | Substitui a Cor da Luz Indireta. Retorne a Cor que será substituída. |
