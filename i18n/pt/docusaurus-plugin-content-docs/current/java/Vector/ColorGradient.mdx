# ColorGradient

Referência completa da API de `ColorGradient`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Vector

**Componente:** não

---

```java
public class ColorGradient
```

## Construtores (2)

| Signature | Description |
| --- | --- |
| ColorGradient([Gradient]) | Executa `ColorGradient` e retorna quando a chamada do motor termina. |
| ColorGradient() | Executa `ColorGradient` e retorna quando a chamada do motor termina. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [Gradient] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (8)

| Signature | Returns | Description |
| --- | --- | --- |
| `addPoint(float, Color)` | [ColorGradient].[Point] | Adiciona point à coleção. |
| `removePointAt(int)` | `void` | Remove point at da coleção. |
| `pointAt(int)` | `Point` | Executa a operação pointAt usando os parâmetros informados (idx). |
| `clear()` | `void` | Limpa ou redefine o estado do objeto. |
| `pointsCount()` | `int` | Executa a operação pointsCount. |
| `evaluate(float)` | `Color` | Executa a operação evaluate usando os parâmetros informados (location). |
| `toJson()` | `String` | Executa a operação toJson. |
| `fromJson(String)` | `ColorGradient` | Executa a operação fromJson usando os parâmetros informados (json). |

## Métodos Estáticos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `genericInterface()` | `GenericClassInspectorEntry` | Executa `genericInterface` e retorna quando a chamada do motor termina. |
| `inspectorController()` | `ClassInspectorEntry` | Executa `inspectorController` e retorna quando a chamada do motor termina. |
