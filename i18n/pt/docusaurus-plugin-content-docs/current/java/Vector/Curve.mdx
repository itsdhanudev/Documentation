import ApiDoc from '@site/src/components/ApiDoc';

# Curve

Referência completa da API de `Curve`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Vector

**Componente:** não

---

```java
public class Curve
```

<ApiDoc
  constructors={[
    {
      signature: "Curve(Curve)",
      description: <>Executa <code>Curve</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "Curve()",
      description: <>Executa <code>Curve</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "instance",
      type: "Curve",
      description: <>Representa o valor de <code>instance</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
  methods={[
    {
      signature: "addPoint(float, float)",
      returns: "void",
      description: <>Adiciona point à coleção.</>
    },
    {
      signature: "setPoint(int, float, float)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>point``.</>
    },
    {
      signature: "pointAt(int)",
      returns: "Vector2",
      description: <>Executa a operação pointAt usando os parâmetros informados (idx).</>
    },
    {
      signature: "pointAt(int, Vector2)",
      returns: "Vector2",
      description: <>Executa a operação pointAt usando os parâmetros informados (idx, out).</>
    },
    {
      signature: "clear()",
      returns: "void",
      description: <>Limpa ou redefine o estado do objeto.</>
    },
    {
      signature: "pointsCount()",
      returns: "int",
      description: <>Executa a operação pointsCount.</>
    },
    {
      signature: "evaluate(float)",
      returns: "float",
      description: <>Executa a operação evaluate usando os parâmetros informados (x).</>
    },
    {
      signature: "apply()",
      returns: "void",
      description: <>Executa a operação apply.</>
    },
    {
      signature: "getWrapMode()",
      returns: "WrapMode",
      description: <>Retorna a propriedade <code>propriedade </code>wrap mode atual``.</>
    },
    {
      signature: "setWrapMode(WrapMode)",
      returns: "void",
      description: <>Define ou atualiza a propriedade <code>propriedade </code>wrap mode``.</>
    },
    {
      signature: "toJson()",
      returns: "String",
      description: <>Executa a operação toJson.</>
    },
    {
      signature: "fromJson(String)",
      returns: "Curve",
      description: <>Executa a operação fromJson usando os parâmetros informados (json).</>
    }
  ]}
  staticMethods={[
    {
      signature: "genericInterface()",
      returns: "GenericClassInspectorEntry",
      description: <>Executa <code>genericInterface</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "inspectorController()",
      returns: "ClassInspectorEntry",
      description: <>Executa <code>inspectorController</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
