# Point2

Referência completa da API de `Point2`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Vector

**Componente:** não

---

```java
public class Point2
```

## Construtores (3)

| Signature | Description |
| --- | --- |
| Point2([Vector2]) | Executa `Point2` e retorna quando a chamada do motor termina. |
| Point2() | Executa `Point2` e retorna quando a chamada do motor termina. |
| Point2([int], [int]) | Cria uma nova instância de Point2. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `vector2` | [Vector2] | Representa o valor de `vector2` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (2)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `x` | [int] | `get/set` | Atributo virtual exposto por `getX`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `y` | [int] | `get/set` | Atributo virtual exposto por `getY`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (41)

| Signature | Returns | Description |
| --- | --- | --- |
| `mul(Point2)` | `Point2` | Executa a operação mul usando os parâmetros informados (value). |
| `mul(int, int)` | `Point2` | Executa a operação mul usando os parâmetros informados (x, y). |
| `mul(int)` | `Point2` | Executa a operação mul usando os parâmetros informados (a). |
| `div(Point2)` | `Point2` | Executa a operação div usando os parâmetros informados (value). |
| `div(int, int)` | `Point2` | Executa a operação div usando os parâmetros informados (x, y). |
| `div(int)` | `Point2` | Executa a operação div usando os parâmetros informados (a). |
| `sum(Point2)` | `Point2` | Executa a operação sum usando os parâmetros informados (value). |
| `sum(int, int)` | `Point2` | Executa a operação sum usando os parâmetros informados (x, y). |
| `sum(int)` | `Point2` | Executa a operação sum usando os parâmetros informados (a). |
| `sub(Point2)` | `Point2` | Executa a operação sub usando os parâmetros informados (value). |
| `sub(int, int)` | `Point2` | Executa a operação sub usando os parâmetros informados (x, y). |
| `sub(int)` | `Point2` | Executa a operação sub usando os parâmetros informados (a). |
| `equals(Point2)` | `boolean` | Compara esta instância com outra para igualdade/ordem. |
| `equals(int, int)` | `boolean` | Compara esta instância com outra para igualdade/ordem. |
| `equals(int)` | `boolean` | Compara esta instância com outra para igualdade/ordem. |
| `set(Point2)` | `void` | Define ou atualiza the value. |
| `set(Vector2)` | `void` | Define ou atualiza the value. |
| `set(int, int)` | `void` | Define ou atualiza the value. |
| `set(int)` | `void` | Define ou atualiza the value. |
| `lerp(Point2, int)` | `void` | Executa a operação lerp usando os parâmetros informados (vector3, speed). |
| `lerp(int, int, int)` | `void` | Executa a operação lerp usando os parâmetros informados (x, y, speed). |
| `lerp(int, int)` | `void` | Executa a operação lerp usando os parâmetros informados (a, speed). |
| `lerpInSeconds(Point2, int)` | `void` | Executa a operação lerpInSeconds usando os parâmetros informados (vector3, speed). |
| `lerpInSeconds(int, int, int)` | `void` | Executa a operação lerpInSeconds usando os parâmetros informados (x, y, speed). |
| `lerpInSeconds(int, int)` | `void` | Executa a operação lerpInSeconds usando os parâmetros informados (a, speed). |
| `blend(Point2, int)` | `void` | Executa a operação blend usando os parâmetros informados (vector3, blend). |
| `blend(int, int, int)` | `void` | Executa a operação blend usando os parâmetros informados (x, y, blend). |
| `blend(int, int)` | `void` | Executa a operação blend usando os parâmetros informados (a, blend). |
| `normalize()` | `Point2` | Executa a operação normalize. |
| `normalizeLocal()` | `void` | Executa a operação normalizeLocal. |
| `length()` | `float` | Executa a operação length. |
| `sqrLength()` | `float` | Executa a operação sqrLength. |
| `distance(Point2)` | `float` | Executa a operação distance usando os parâmetros informados (vector3). |
| `sqrDistance(Point2)` | `float` | Executa a operação sqrDistance usando os parâmetros informados (vector3). |
| `dot(Point2)` | `float` | Executa a operação dot usando os parâmetros informados (vector3). |
| `dot(int, int)` | `float` | Executa a operação dot usando os parâmetros informados (x, y). |
| `dot(int)` | `float` | Executa a operação dot usando os parâmetros informados (a). |
| `cross(Point2)` | `Vector3` | Executa a operação cross usando os parâmetros informados (value). |
| `cross(int, int)` | `Vector3` | Executa a operação cross usando os parâmetros informados (x, y). |
| `copy()` | `Point2` | Retorna uma cópia da instância de Point2. |
| `toString()` | `String` | Retorna a propriedade `representação em string`. |

## Métodos Estáticos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `inspectorController()` | `ClassInspectorEntry` | Executa `inspectorController` e retorna quando a chamada do motor termina. |
| `zero()` | `Point2` | Executa a operação zero. |
