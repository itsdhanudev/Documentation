# Point3Buffer

Referência completa da API de `Point3Buffer`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Vector

**Componente:** não

---

```java
public class Point3Buffer
```

## Construtores (4)

| Signature | Description |
| --- | --- |
| Point3Buffer([Point3Buffer]) | Executa `Point3Buffer` e retorna quando a chamada do motor termina. |
| Point3Buffer([Engine]) | Executa `Point3Buffer` e retorna quando a chamada do motor termina. |
| Point3Buffer([int]) | Cria uma nova instância de Point3Buffer. |
| Point3Buffer([NativeIntBuffer]) | Cria uma nova instância de Point3Buffer. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `buffer` | [Point3Buffer] | Representa o valor de `buffer` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Métodos (12)

| Signature | Returns | Description |
| --- | --- | --- |
| `get(int)` | `Point3` | Retorna a propriedade `propriedade `valor value atual``. |
| `getX(int)` | `float` | Retorna a propriedade `propriedade `x atual``. |
| `getY(int)` | `float` | Retorna a propriedade `propriedade `y atual``. |
| `getZ(int)` | `float` | Retorna a propriedade `propriedade `z atual``. |
| `put(Point3)` | `void` | Define ou atualiza the value. |
| `put(int, int, int)` | `void` | Define ou atualiza the value. |
| `set(int, Point3)` | `void` | Define ou atualiza the value. |
| `set(int, int, int, int)` | `void` | Define ou atualiza the value. |
| `capacity()` | `int` | Executa a operação capacity. |
| `array()` | [Point3][] | Executa a operação array. |
| `position(int)` | `void` | Executa a operação position usando os parâmetros informados (idx). |
| `getFloatBuffer()` | [JAVARuntime].[NativeIntBuffer] | Retorna a propriedade `propriedade `float buffer atual``. |
