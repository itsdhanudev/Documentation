# Vector4

Referência completa da API de `Vector4`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Vector

**Componente:** não

---

```java
public class Vector4
```

## Construtores (22)

| Signature | Description |
| --- | --- |
| Vector4([Vector4]) | Executa `Vector4` e retorna quando a chamada do motor termina. |
| Vector4() | Executa `Vector4` e retorna quando a chamada do motor termina. |
| Vector4([float], [float], [float], [float]) | Cria uma nova instância de Vector4. |
| Vector4([float], [float], [float], [int]) | Cria uma nova instância de Vector4. |
| Vector4([float], [float], [int], [int]) | Cria uma nova instância de Vector4. |
| Vector4([float], [int], [int], [int]) | Cria uma nova instância de Vector4. |
| Vector4([int], [int], [int], [int]) | Cria uma nova instância de Vector4. |
| Vector4([int], [float], [int], [int]) | Cria uma nova instância de Vector4. |
| Vector4([int], [int], [float], [int]) | Cria uma nova instância de Vector4. |
| Vector4([int], [int], [int], [float]) | Cria uma nova instância de Vector4. |
| Vector4([float], [float], [float]) | Cria uma nova instância de Vector4. |
| Vector4([int], [float], [float]) | Cria uma nova instância de Vector4. |
| Vector4([float], [int], [float]) | Cria uma nova instância de Vector4. |
| Vector4([float], [float], [int]) | Cria uma nova instância de Vector4. |
| Vector4([int], [int], [float]) | Cria uma nova instância de Vector4. |
| Vector4([float], [int], [int]) | Cria uma nova instância de Vector4. |
| Vector4([float], [float]) | Cria uma nova instância de Vector4. |
| Vector4([int], [float]) | Cria uma nova instância de Vector4. |
| Vector4([float], [int]) | Cria uma nova instância de Vector4. |
| Vector4([int], [int]) | Cria uma nova instância de Vector4. |
| Vector4([float]) | Cria uma nova instância de Vector4. |
| Vector4([int]) | Cria uma nova instância de Vector4. |

## Atributos (1)

| Name | Type | Description |
| --- | --- | --- |
| `instance` | [Vector4] | Representa o valor de `instance` armazenado nesta instância. Leia ou atribua para atualizar a propriedade. |

## Atributos Virtuais (4)

| Name | Type | Access | Description |
| --- | --- | --- | --- |
| `w` | [float] | `get/set` | Atributo virtual exposto por `getW`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `x` | [float] | `get/set` | Atributo virtual exposto por `getX`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `y` | [float] | `get/set` | Atributo virtual exposto por `getY`. Acesse como um campo para ler ou escrever o valor subjacente. |
| `z` | [float] | `get/set` | Atributo virtual exposto por `getZ`. Acesse como um campo para ler ou escrever o valor subjacente. |

## Métodos (3)

| Signature | Returns | Description |
| --- | --- | --- |
| `set(Vector4)` | `void` | Define ou atualiza the value. |
| `set(float, float, float, float)` | `void` | Define ou atualiza the value. |
| `set(float)` | `void` | Define ou atualiza the value. |
