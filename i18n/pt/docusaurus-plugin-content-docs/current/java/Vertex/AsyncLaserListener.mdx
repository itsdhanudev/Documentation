import ApiDoc from '@site/src/components/ApiDoc';

# AsyncLaserListener

Referência completa da API de `AsyncLaserListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Vertex

**Componente:** não

---

```java
public interface AsyncLaserListener
```

<ApiDoc
  methods={[
    {
      signature: "onFinish(LaserHit)",
      returns: "void",
      description: <>Executa <code>onFinish</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
/>
