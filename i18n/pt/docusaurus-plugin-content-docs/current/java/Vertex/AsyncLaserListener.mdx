# AsyncLaserListener

Referência completa da API de `AsyncLaserListener`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** Vertex

**Componente:** não

---

```java
public interface AsyncLaserListener
```

## Métodos (2)

| Signature | Returns | Description |
| --- | --- | --- |
| `@MethodArgs([{"laserHit"}])` | `void` | Executa a operação @MethodArgs. |
| `onFinish(LaserHit)` | `void` | Executa `onFinish` e retorna quando a chamada do motor termina. |
