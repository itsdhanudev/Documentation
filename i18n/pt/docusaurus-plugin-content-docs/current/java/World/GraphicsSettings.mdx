import ApiDoc from '@site/src/components/ApiDoc';

# GraphicsSettings

Referência completa da API de `GraphicsSettings`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** World

**Componente:** não

---

```java
public class GraphicsSettings
```

<ApiDoc
  constructors={[
    {
      signature: "GraphicsSettings(GraphicsSettings)",
      description: <>Executa <code>GraphicsSettings</code> e retorna quando a chamada do motor termina.</>
    },
    {
      signature: "GraphicsSettings()",
      description: <>Executa <code>GraphicsSettings</code> e retorna quando a chamada do motor termina.</>
    }
  ]}
  attributes={[
    {
      name: "graphicsSettings",
      type: "GraphicsSettings",
      description: <>Representa o valor de <code>graphicsSettings</code> armazenado nesta instância. Leia ou atribua para atualizar a propriedade.</>
    }
  ]}
/>
