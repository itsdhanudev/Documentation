# WorldController

Referência completa da API de `WorldController`, incluindo construtores, atributos, atributos virtuais e métodos.

**Pacote:** `JAVARuntime`

**Categoria:** World

**Componente:** não

---

```java
public class WorldController
```

## Métodos Estáticos (24)

| Signature | Returns | Description |
| --- | --- | --- |
| `loadWorld(WorldFile)` | `void` | Carrega world no objeto. |
| `loadWorldAsync(WorldFile)` | `void` | Carrega world async no objeto. |
| `findObject(String)` | `SpatialObject` | Executa a operação findObject usando os parâmetros informados (name). |
| `getObjectList()` | [List]&[lt];[SpatialObject]&[gt]; | Retorna a propriedade `propriedade `object list atual``. |
| `getObjectsCount()` | `int` | Retorna a propriedade `propriedade `objects count atual``. |
| `getObject(int)` | `SpatialObject` | Retorna a propriedade `propriedade `object atual``. |
| `findFirstComponent(Class)` | &[lt];[T] [extends] [JAVARuntime].[Component]&[gt]; [T] | Executa a operação findFirstComponent usando os parâmetros informados (classReference). |
| `findFirstActiveComponent(Class)` | &[lt];[T] [extends] [JAVARuntime].[Component]&[gt]; [T] | Executa a operação findFirstActiveComponent usando os parâmetros informados (classReference). |
| `listAllComponents(String)` | [List]&[lt];[JAVARuntime].[Component]&[gt]; | Executa a operação listAllComponents usando os parâmetros informados (tittle). |
| `listAllComponents(Class)` | [List]&[lt];[JAVARuntime].[Component]&[gt]; | Executa a operação listAllComponents usando os parâmetros informados (classReference). |
| `getLoadPercent()` | `float` | Retorna a propriedade `propriedade `load percent atual``. |
| `getLoadProgress()` | `float` | Retorna a propriedade `propriedade `load progress atual``. |
| `getLoadedCount()` | `int` | Retorna a propriedade `propriedade `loaded count atual``. |
| `getLoadTotal()` | `int` | Retorna a propriedade `propriedade `load total atual``. |
| `getLoadMessage()` | `String` | Retorna a propriedade `propriedade `load message atual``. |
| `isLoaded()` | `boolean` | Retorna se loaded é verdadeiro. |
| `isLoading()` | `boolean` | Retorna se loading é verdadeiro. |
| `getGraphicsSettings()` | `GraphicsSettings` | Retorna a propriedade `propriedade `graphics settings atual``. |
| `getPhysicsSettings()` | `PhysicsSettings` | Retorna a propriedade `propriedade `physics settings atual``. |
| `findObjectWithTag(String)` | `SpatialObject` | Executa a operação findObjectWithTag usando os parâmetros informados (tagName). |
| `findObjectsWithTag(String)` | [List]&[lt];[SpatialObject]&[gt]; | Executa a operação findObjectsWithTag usando os parâmetros informados (tagName). |
| `findRandomObjectWithTag(String)` | `SpatialObject` | Executa a operação findRandomObjectWithTag usando os parâmetros informados (tagName). |
| `countObjectsWithTag(String)` | `int` | Executa a operação countObjectsWithTag usando os parâmetros informados (tagName). |
| `hasObjectsWithTag(String)` | `boolean` | Retorna se objects with tag é verdadeiro. |
