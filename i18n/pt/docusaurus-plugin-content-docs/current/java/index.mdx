# Java API

Todas as classes do runtime ficam em `JAVARuntime` e estão organizadas aqui por categoria.

- Se uma classe estende `Component`, ela aparece em `Java API/Components`, com a categoria indicada como `"<Categoria>"`.
- Todas as outras classes aparecem em `Java API`, com a categoria indicada como `"<Categoria>"`.
- Classes sem categoria ficam em `Uncategorized`.
