# Vetor3.sumLocal()

Método de instância em `Vector3`.

## Descrição

- Adds the specified Vector3 to this Vector3 and updates this Vector3. |
- Adds the specified Vector2 to this Vector3 and updates this Vector3. |
- Adds the specified X, Y and Z values to this Vector3 and updates this Vector3. |
- Adds the specified value to this Vector3 and updates this Vector3. |

## Method Signature

```java
sumLocal(Vector3)
sumLocal(Vector2)
sumLocal(float, float, float)
sumLocal(float)
```

## Mathematical Definition

`Component-wise or runtime-defined transformation based on overload.`

## Parameters

- `Vector3`: 3D vector input.
- `Vector2`: 2D vector used for XY/XZ/YZ component operations.
- `float`: scalar numeric input.

## Retorna

- `void`: returned by overloads of this method.

## Mutability

- Mutates o atual vector (in-place).

## Edge Cases

- Zero vectors can produce degenerate outputs for geometric methods.
- `NaN` and `Infinity` inputs propagate according to IEEE-754 floating-point behavior.
- Repeated operations may accumulate floating-point precision error.

## Usage Example

```java
Vector3 a = new Vector3(1f, 2f, 3f);
// Call the method according to your overload requirements.
// Example:
// a.sumLocal(...);
```

## Mathematical Example

Example input/output depends on overload; for vector arithmetic operations this is typically computed per-component.

## Visualization
